# High-risk audit: PR #314 Nova restore後のreload settle待ちとbounded capture不変条件diagnostics

> Status: accepted
> Audit date: 2026-09-14

- Auditor: 独立session（PR #314の実装を行っていないsessionによるaudit。solo保守のため、同一保守の別sessionとして実装経路に依存しない再実行・再確認を実施）
- PR: https://github.com/nunu1733/NunuLauncher/pull/314
- Head SHA: 8aebba1ce3fcee8c695cd16fa0aff8b906bffb52
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/34833668117
- Criteria: specs/299-nova-restore-capture-invalid/spec.md（frontmatter status: accepted。Re-audit (11)でChange history記述との整合を確認済み、`implemented` 再更新はdocs-onlyで予定）CI-AC-01, CI-AC-02, CI-AC-03, CI-AC-04, CI-AC-05, CI-AC-06, CI-AC-07, CI-AC-08
- Re-audit (1): 初回audit（head `0eb3355e0e`、CI run 34768482481）後、docs commit `483038d94f`（audit記録追加とspec statusの `implemented` への更新）が加わったため再監査。差分 `git diff 0eb3355e0e..483038d94fd2d378692e54dd5bf2d80adf11d635` は `docs/assessment/pr-314-nova-restore-capture.md`（追加）と `specs/299-nova-restore-capture-invalid/spec.md`（status行とChange history 1行のみ）の2ファイルで、production code・test・CI workflowへの変更は無い。受入条件（CI-AC-01..08）の定義内容は不変であり、初回auditの判定をそのまま承継する。
- Re-audit (2): commit `b669194b55`（「fix(299): harden capture-failure diagnostics and settle-wait lifecycle per review」、5ファイル +56/−19。監査対象code stateは `483038d94f..b669194b55` の差分6ファイル＝本audit記録の更新commit `f0c535ba00` を含む）が加わったため再監査。audit本人がdiffを直接reviewした結果:
  - **capture-failure diagnosticsのenum型化（CI-AC-08の強化、自由text穴の閉塞）**: `DiagnosticsLogger.logCaptureFailure` / `formatCaptureFailure` のinvariant引数が `String?` から `CaptureInvariantCategory?` へ変更され、定数名のrendering（`invariant.name`）はlogger内部に移動した。wiring（`LayoutApplicationModule`）とharness（`NovaRestoreCaptureTestBase`）、unit test（`DiagnosticsLoggerTest`）もenum直接渡しへ更新。これでinvariant fieldに入りうる値は閉じたenum定数名のみと型で強制され、初回auditで「自由文字列Parameterは不導入」と記述した境界が文字列型経由の迂回路を含めて真になる。出力形式（`phase=CAPTURE exceptionClass=IllegalArgumentException invariant=INVALID_WIDGET_ROW`）・redaction境界・journal語彙は不変。
  - **settle待ちlifecycleの強化（CI-AC-02の実装品質修正、意味論不変）**: `RestoreReloadSettleWait` にidempotentな `cleanup()`（`cleanedUp` / `stopped` flag、main handler上で `removeCallbacks(pollRunnable)` とobserverの `removeCallbacks` を1回だけ実行）を導入し、`convertAndRestore` 側は `awaitSettled()` をtry/finallyで覆い、`awaitSettled` 自体も `InterruptedException` を捕捉（interrupt flag復元・WARN log・次の完了reloadへ委譲）してfinallyでcleanupする。pollは匿名lambdaから名前付き `pollRunnable` へ変更され、cancelが実際に効く。これでsettle待ちの中断時・restore失敗時にobserver登録とpoll再scheduleが漏出するlifecycle leakが閉じた。timeout時fail-open・次の完了reloadへ委譲する意味論は不変であり、DB書込み・migration経路の追加は無い（handler callbackとmodel observerの登録解除のみ）。
  - 差分にlayout-write / migration pathの変更は無い（`NovaBackupConverter.kt` の変更は上記lifecycle管理のみでrestore系DB書込みは不変、`DiagnosticsLogger` / `LayoutApplicationModule` はdiagnostics read pathのみ、残り2ファイルはtest）。受入条件（CI-AC-01..08）の定義内容は不変であり、初回auditの判定を承継した上で、上記2点を検証済みの強化として記録する。
- Re-audit (3): review対応commit `c928ac66af`（「fix(299): restore reload completion barrier with generation identity; fail the restore when the reload never completes」、6ファイル +224/−127。監査対象code stateは `82fd75df1b..c928ac66af` の差分。`b669194b55..82fd75df1b` は本audit記録のdocs-only commitのみを事前確認済み）が加わったため再監査。audit本人がdiffを直接reviewした結果:
  - **LauncherModel / LoaderTaskのbridge（auditor review: 設計は正しい）**: `RestoreReloadRequest` token（requestId + completed/cancelled runnable）を導入し、`dispatchRestoreReload` はmLock下でtoken登録と `mModelLoaded=false` のみを行い、superseded tokenの `cancelled` はlockの外で実行。`completeRestoreReload` はmLock下のidentity照合（`mRestoreReloadRequest != token` はdrop）後に `completed` をlockの外で実行。`stopLoader` にも `cancelRestoreReload` を追加。`LoaderTask` は `notifyRestoreReloadComplete` flagを持つpublic 8引数constructorを追加（既存constructor chainは維持）し、transaction commit後のterminal boundaryで `MODEL_EXECUTOR.post(mLauncherBinder::notifyOrganizerReloadComplete)` がorganizer tokenと同一のpost-commit notify経路でrestore tokenを解決する（notifyはcommit後にpostされる）。tokenless reload（leaseToken 0）を維持するためloaderの修復sanitizeはそのまま走り、organizer lease意味論には非接触。token状態はすべてmLock下、runnablesはlock外実行、converter側runnablesはlatch + AtomicReferenceのみのlock-free実装のため、stopLoader経路でmLock保持下にrunnableが走っても再入・中断は起きない。各hunkにIssue #299の理由コメントがあり、#152 organizer tokenの既存patternを踏襲する最小bridge（AGENTS.mdのLauncher3 bridge規約の形に整合、設計記録はplan/assessment）。
  - **NovaBackupConverterの書換（heuristic barrierの撤回とfail-closed化）**: `RestoreReloadSettleWait`（BgDataModel.Callbacks観測 + isModelLoaded polling）は構造ごと削除され、registration leak経路は閉じた。tokenはlease内でdispatchし、当該generationのsuccessful completionのみを成功としてlease外でawaitする。絶対deadline（15秒、構築時に1回計算 — 再dispatchでは延長されない）超過時は `IllegalStateException` をthrowしてrestoreを失敗させる（fail-openな成功returnは廃止 — 初回audit Findingsの「timeout時fail-open」指摘はこの設計で解消）。モデル非活性時はbarrierを設けず `reloadAfterRestore` のbaseline fallbackのまま（次のactivation loadが修復generationで、authoritative captureは活性modelを介してのみ可能なため修復前captureに到達不能）。
  - **auditor発見の残存defect（re-dispatch経路のlatch再利用）**: `RestoreReloadBarrier` の `latch`（`CountDownLatch(1)`）と `outcome` は `dispatch()` で再初期化されない。最初のgenerationが一度でも `cancelled` になると（外部stopLoader・supersession・モデル非活性化）、以降のloopは既に0のlatchで即座に `await` が返り、staleな `outcome`（"cancelled"）を読んで再dispatchするため、新generationの完了を待てないまま `startLoader`/`stopLoader` のhot spinがdeadline（15秒）まで続き、restoreを失敗させる。結果はfail-safe（成功を偽装しない・破壊的書込み無し・15秒でbounded）だが、「cancelled generationをdeadline内で再dispatchし完了を待つ」設計記述はこの経路では機能せず、spin中はloader生成の連続start/stopが生じる。happy path（ cancelling無しで完了）は正しく機能しCI green。新barrier機構専用のunit/instrumentation testは本deltaに含まれない（既存laneのhappy path経由でのみ覆盖）。→ latch/outcomeのper-attempt再初期化（またはawait対象の同一generationへのbind）をreview follow-upとして要修正。このためspec statusは`accepted`へ戻されている（妥当な判断と判断する）。
  - 差分にlayout-write / migration pathの変更は無い（token bridgeはDB書込みを追加せず、`performRestore` / `applyConvertedGrid` 等のrestore系書込みは不変、LoaderTaskの修復sanitizeはtokenless維持により不変）。受入条件（CI-AC-01..08）の定義内容は不変。Cosmeticな残渣として、削除済みclassのKDocが `resolveIconPackLabel` 上に孤立残留、同一barrier comment blockの重複がある（動作影響なし）。
- Re-audit (4): Re-audit (3)で指摘したre-dispatch経路の残存defectに対する修正commit `a49dda3451`（「fix(299): reset barrier latch and outcome per re-dispatch attempt」、1ファイル +4行）とdocs commit `4040f102dc`（assessmentへのreview-cycle修正記録追加、1ファイル +11行。`c928ac66af..4040f102dc` の差分はこの2commitのみであることを `git log` / `--stat` で確認）が加わったため再監査。audit本人が修正diffを直接reviewした結果:
  - **latch/outcome再初期化は当該defectを正しく閉じる**: `RestoreReloadBarrier.dispatch()` 冒頭で `latch = CountDownLatch(1)` と `outcome.set("")` を行い、その後にtokenを登録する。新しいtokenのrunnablesは登録後にしか発火せず、旧tokenのrunnablesは発火済み（loopは発火済みのcancelledを観測した後にのみ再dispatchする。pending tokenは`cancelRestoreReload`/`completeRestoreReload`がmLock下でnull化済み）のため、resetとsignal喪失の競合は存在しない。これでcancelled → re-dispatch → 新generationのlatch待ち、の経路が機能し、Re-audit (3)で記録したhot spin（stale outcomeの連続再読み・loader generationの連続start/stop）は解消される。絶対deadline（構築時1回計算・再dispatchで不延長）とdeadline超過時のrestore失敗surfaceは不変。
  - 検証: audited head上のCI lane `organizer-instrumentation-issue299-tests`（run 34790445253、attempt 1で全14 job green）と、実施者によるemulator再検証（Control / WidgetWindow / UnknownProvider + cross-process A→force-stop→B、hasCallbacks-gated dispatch flow経由でlatch-reset pathを実行）がPASS。当該barrier loop自体の専用unit testは引き続き不在（初回Findingsの残余として維持）。Re-audit (3)の要修正事項は解決と判定する。
- Re-audit (5): Re-reviewの新規Major（callback lifecycle境界）への対応commit `fe04bfb960`（「fix(299): terminalize pending restore tokens on callback-lifecycle races」、4ファイル +90/−21。監査対象code stateは `4040f102dc..fe04bfb960` の差分 = 本audit記録の更新commit `2894d5004d` を含む）が加わったため再監査。audit本人がdiffを直接reviewした結果:
  - **in-dispatch terminalization（token無限pending経路の遮断）**: `dispatchRestoreReload` は `startLoader()` 後、mLock下で「tokenが現行（`mRestoreReloadRequest == token`）かつ `mLoaderTask == null`」を再確認し、該当時はtokenをnull化して `cancelled` をlockの外で発火する。callback消失でloader generationが生成されない経路（`startLoader` のfalse returnは成功と区別できない）でもtokenが無限pendingしない。identity照合により二重発火は不可能（並行 `stopLoader` が先に `cancelRestoreReload` でtokenをnull化した場合、identity照合が失敗し発火は起きない）。cancelled runnableはbarrier側でlatch + AtomicReferenceのみのlock-free実装のため、lock外発火で安全。
  - **barrierのcancelled分岐のhasCallbacks fallback（hot loopの構造的排除）**: `awaitCompletion` のcancelled経路で `model.hasCallbacks()` を再確認し、非活性ならactivation-load修復fallback（WARN + return）へ遷移 — 同期的に即cancelされるdispatchのbusy再dispatchループを構造的に排除した。非活性modelではreload自体が走らず、次のactivation loadが修復generationとなり、authoritative captureは活性modelを介してのみ可能なため修復前captureに到達不能（documented fallbackと同一意味論）。`hasCallbacks()` のlock外読みはracy snapshotだが、 fallback方向は保守的（活性化済みでもfallbackに落ちるだけで、修復はactivation loadが担う）、逆方向（活性→非活性化）は `dispatchRestoreReload` 内の即cancelled→loop再確認へ収束する。
  - **deadline経路のtoken clear（no lost-wakeup / no unbounded loop）**: deadline超過時はthrow前に `cancelRestoreReloadIfCurrent(currentRequestId)`（新設のpublic API、requestId identity照合の下でtokenをnull化し `cancelled` をlock外発火）でpending tokenをクリアする。これでrestore失敗後もstale tokenが後続generationに「完了」されることがなく、token lifetimeがbarrierの生存と厳密に一致する。barrier側は `currentRequestId` をattemptごとに追跡し、再dispatchでは既存どおり新latch/outcomeで待つためlost-wakeupはなく、loopは絶対deadline（不延長）でbounded。
  - **dedicated token-lifecycle testの断念は記録済み**: instrumentation processのlauncher auto-loadが非決定論的でtoken-state assertionが不安定になるため作成を断念し、barrier経路は既存family（callbacks=0: CrossProcess A/B fallback、callbacks有: barrier経由のControl/WidgetWindow/UnknownProvider + CI lane）で検証する方針をassessmentに記録（deviation記録あり）。**残余**: バリアloopのJVM unit test不在は引き続き推奨 follow-up。
  - 差分にlayout-write / migration pathの変更は無い（token lifecycle管理とbarrier制御のみ。DB書込み経路は不変）。受入条件（CI-AC-01..08）の定義内容は不変。
- Re-audit (6): inactive-model Major対応commit `922a5c03a4`（「fix(299): await the restore repair generation even without callbacks; drop the inactive-model fallback」、5ファイル +252/−34。`fe04bfb960..922a5c03a4` のchain = 本audit記録の更新commit `e53ba22cb3`（docs-only、+21/−14）+ このfix commit のみであることを `git log` / `--stat` で確認）が加わったため再監査。audit本人がdiffと新testを直接reviewした結果:
  - **no-callbacks dispatch経路（positive）**: `dispatchRestoreReload` のempty-callback即時cancelを削除し、`hasCallbacks()` false時は既存public API `startLoaderWithoutCallbacks()`（`startLoader(new Callbacks[0], true)` = #271でsettings-only processに確立された既存経路）へ流す。`allowEmptyCallbacks`でLoaderTask生成が保証され、`notifyRestoreReloadComplete = restoreToken != null` により#150 terminal boundary（commit後のMODEL_EXECUTOR post → binder lambda → identity照合付き `completeRestoreReload`）で解決される — 修复sanitizeを走らせるtokenless reloadのまま、UI束縛無しで修復generationが完了する。`ItemInstallQueue` の `FLAG_LOADER_RUNNING` pauseはstartLoader共通の既存副作用（KDoc記載どおり実Launcher bindingまでpaused、`ensureOrganizerStartupReconciliation` の既存precedentと同一意味論で新規side-effectは無い）。barrier必須化（`app != null` 時は常にdispatch+await。application欠如時のみ `reloadAfterRestore` baseline fallback）と、dispatch-timeのinactive-model正常return経路は閉じた。
  - **専用regression（positive）**: `NovaRestoreCaptureNoCallbacksTest`（committed、own `am instrument` process）が callbacks=0 preconditionを `check` で担保した上で、restore return時点のwidget行状態 (unbound=0, bound=1) と直後のproduction source captureのReadyをassert。issue299 CI laneへ組み込み済み（run 34801319508で実行・green）。
  - **残余defect（本auditで指摘）**: 「inactive-model fallback returnを削除」の主張に対し、`RestoreReloadBarrier.awaitCompletion` のcancelled分岐の `if (!model.hasCallbacks()) { WARN; return }` は**新headでも生存**（NovaBackupConverter.kt:337-341）。dispatch-time経路は閉じたが、待機中に外部stopLoaderでgenerationがcancelされ、かつloop再確認時点でcallbacks 0、という狭い競合では restore は自身の修復generationのcommitなしに成功returnしうる。spec観測契約（恒常化禁止・barrier後初回capture成功）は「captureは活性model必須＝最初のbindingまでに必ず修復commit済みgenerationが成立」という不変条件で維持されるが、**「model存在時の全return＝restore相関generation commit済み」という内部不変条件は成立的でない**。無条件re-dispatchでも（no-callbacks dispatchが修復generationを実行するため）progressが保証されるため、当該fallback branchは削除して設計記述と一致させるべき。あわせて、同一commitで追加されたassessmentの「fallback returnを削除し…cancelledは無条件re-dispatch」という記述とcodeが一致していない（docs/code不整合として記録）。
  - layout-write / migration path: `LauncherModel.java`（+15、dispatch分岐と既存API利用のみ）、`NovaBackupConverter.kt`（barrier制御のみ）、新test、ci.yml（lane +1行）、assessment。restore系DB書込みは不変。
- Re-audit (7・最終): Re-audit (6) Minor（cancelled分岐fallback残存・docs/code不整合）への対応commit `b12d8031fc`（「fix(299): remove the remaining cancelled-branch inactive-model fallback return」）が加わったため再監査。`git log 922a5c03a4..b12d8031fc` はこの1commitのみ（code変更は `NovaBackupConverter.kt` の−19/+12相当のみ。commitには前roundのaudit記録更新も含まれ、issue-299 assessmentは無変更 — その記述は本commitで初めてcodeと一致するようになった）。audit本人が最終code stateを直接reviewした結果:
  - **fallback return削除（Re-audit (6)指摘の解消）**: `awaitCompletion` のcancelled分岐から `if (!model.hasCallbacks()) { WARN; return }` が削除され、無条件re-dispatchのみが残った。barrierの終端は `observed == COMPLETED` からのreturn（=当該generation commit済み）とdeadline超過時の `cancelRestoreReloadIfCurrent` + throwのみ。これで **「app != null の `convertAndRestore` 正常return ⇒ restore相関の修復generationがcommit済み」は構造的に成立的**（dispatch-time・cancelled経路ともに `startLoader` / `startLoaderWithoutCallbacks` で実generationが必ず開始され、`mModelLoaded=false` によりLoaderTask生成が保証。in-dispatch terminalizationはdefensiveで、発火しても次のre-dispatchが実generationを開始する。loopはper-attempt latch/outcome resetでbusy-spinせず、絶対deadlineでbounded）。`NovaRestoreCaptureNoCallbacksTest` は削除前後でpresent。Re-audit (6)のMinor（docs/code不整合）は解消と判定する。
  - **observation（non-blocking・Minor）**: `dispatchRestoreReload` はNova restore worker threadから呼ばれ、`startLoaderWithoutCallbacks()` のKDoc precondition「Must be called on the UI thread」を満たさない呼び出し経路がある。実態として `startLoader` 内部はmLock保護・loader/MAIN executor postのみで完結し（`clearPendingBinds` もpost）、NoCallbacksTest・CI lane・emulator検証が実機で当該経路（empty callbacksでのrestore→repair commit→capture Ready）を回帰担保しているため動作上の支障は確認されていない。KDocと実呼び出しの整合（UI thread post化またはprecondition記述の更新）を将来のfollow-upに推奨する。merge blockerではない。
  - layout-write / migration path: 変更なし（削除されたのはbarrier制御のbranchのみ）。
  - **merge readiness判定**: Re-audit (1)-(7)で追跡したbarrier設計は、このheadで設計記述・code・committed test（no-callbacks regression含む）・CI merge gate（run 34804943219、attempt 1で全14 job green）が一致し、CI-AC-01..08の観測契約上残余は無い。残余は上記observationと、barrier loop分岐の直接JVM unit test不在（instrumentation regressionで代替済みと記録）、および元障害セッション由来の証拠boundary（CI-AC-01/06に記録済み・#298分離維持）のみ。本auditはこれらを条件付きではなく**承継記録として成立した独立エビデンス**とし、barrier designのmerge readinessは満たされていると判定する。
- Re-audit (8・最終): `b12d8031fc` 以降のコード・テスト・workflow差分（`ed8a4daac5`、`752b620d13`、`e3b9db3817`、`654b80e803`、`182db9cfba`、`33759767d5`）を独立Standards／Specサブエージェントが確認した。Standards、Specともに **No findings**。
  - `LauncherModel` はLoaderTaskの寿命ではなく `RestoreReloadRequest.loaderStarted` で生成を記録し、UI-thread dispatch契約を維持する。
  - `NovaRestoreCaptureNoCallbacksTest` はcallbackなし前提、bounded worker待機、`completed` 必須、重複terminal callback検出を検証する。cross-process Stage A/Bは復元後の `(unbound count) to (bound count) = 0 to 1` をprocess death前後でassertし、Stage Aがcompletion barrier後の状態であることをworkflowコメントにも反映した。
  - 最終候補HEADで `git diff --check`、`./gradlew spotlessCheck --console=plain`、`./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin --console=plain` が成功。CI run `34821703942` は `pull_request` event・PR #314・head `33759767d5` のattempt 2で全14 jobと `final-status` がsuccessであることを確認済み。
  - 追加差分にlayout write、migration、schema変更はなく、既知の#298 scope分離・元intermittent triggerの証拠boundaryも維持する。
  - Re-audit (9・証跡確定): `83e3f0d312` はRe-audit (8)で確認済みの最終コード・テスト・workflow（`33759767d5`）に対するaudit記録とspec status更新のみで、production code・test・workflowの追加変更はない。Re-audit (8)の独立Standards／Spec判定 **No findings** をこの最終証跡headへ承継し、CI merge gateは本headのrun `34824284489`（attempt 1、全14 jobおよび `final-status` success）で確認済み。
- Re-audit (10): deadline起算点Major対応commit `70284c36b9`（「fix(299): start the barrier deadline after the restore-family lease is released」）が加わったため再監査。`git log b12d8031fc..70284c36b9` の14 commit（Re-audit (8)/(9)が確認したUI-thread契約・loaderStarted追跡・cross-process assert等のcommit群 + 本fix）をaudit本人が直接reviewした結果:
  - **lazy deadline（deadline起算点Majorの解消）**: `RestoreReloadBarrier` の `deadlineUptimeMillis` はconstructor計算（−1L sentinel）から `awaitCompletion()` 冒頭の遅延初期化へ変更された。`awaitCompletion()` はrestore-family lease解放後にのみ呼ばれるため、lease内処理時間（grid書込・deep-shortcut pinning・executor scheduling）はcompletion budgetから構造的に除外される。一度のみ計算（`if (deadlineUptimeMillis < 0)`）でre-dispatchは延長せず、最初のawaitが新鮮に計算したdeadlineを使用するためlost-wakeupもない。15秒budgetは修復generationの実行時間に対して正しく見積もられるようになった。
  - **Attempt紐付けの強化（lost-signalの構造的排除）**: latch/outcomeを `Attempt` data classに束ね、各dispatchが新しいAttemptを生成してcallbacksにcaptureさせる。旧generationのstale callbackは自分のAttemptに書くため、現行attemptのlatchを誤って信号することは構造的に不可能（Re-audit (4)のreset方式より強い）。loopは各iteration冒頭で `currentAttempt` を読むため、await対象とcompletion通知のpairingが常に一致する。
  - **UI-thread契約の強制（Re-audit (7) observationの解消）**: `startLoaderWithoutCallbacks` に `Looper` main-thread guard（非UI threadからはISE）が付き、`dispatchRestoreReload` はloader起動を `MAIN_EXECUTOR.execute` へpostする。post内でtoken identityを再照合するため、deadline超過でtokenがclearされた後のdispatchは何も起動しない（ stray generation排除）。`RestoreReloadRequest.loaderStarted`（mLock下、LoaderTask生成位置で設定）が `mLoaderTask` の寿命ではなく生成事実を記録する — 旧proxy（`mLoaderTask == null`）は「正常完了taskがpost-commit通知前にmLoaderTaskをclearする」ため誤判定しうることが修正の動機であり、妥当。never-started時はcancelled → barrierがre-dispatch（収束）。
  - **InterruptedException処理の厳格化**: 待ちの中断はtoken clear + ISE（cause付き）でrestore失敗に — completion-only成功契約と一致。
  - **INVALID_CAPTURE_STATE（CI-AC-08の強化）**: 未型付け `IllegalArgumentException` のcapture失敗が汎用bounded category `INVALID_CAPTURE_STATE` を運ぶようになった（typed違反は具体category、他のRuntimeExceptionはfield無し — 従来どおり）。message等は型として渡らず、`OrganizationInputComposerTest` の新testがprivate layout detailを含むmessageでのnon-leakを検証。`docs/engineering/organizer-diagnostics.md` §7/§13同期済み。
  - **test強化**: CrossProcess Stage A/Bが `(unbound, bound) = (0, 1)` をprocess death前後でassert（barrierがcommitしたcapture-valid状態のdeterministic oracle化）、`NovaRestoreCaptureNoCallbacksTest` にworker-thread dispatch回帰（no hang・UI-thread直接呼出し無し・exactly-once terminal・completed必須）を追加。
  - **残余・不整合（Minor）**: specのfrontmatter status行は `accepted` のまま（Change historyの新規entryは「statusを implemented とした」と記載 — specファイル内部の不整合。docs-onlyで整合予定）。assessmentの新規entryが次bulletと行内で連結するformatting崩れ（cosmetic）。
  - layout-write / migration path: 変更なし（deadline計算timing・dispatch thread・diagnostics分類・test追加のみ。restore系DB書込みは不変）。受入条件（CI-AC-01..08）の定義内容は不変。
  - 検証: audited head上のCI run 34830689124（pull_request・PR #314関連付け・attempt 1で全14 jobおよび `final-status` success）と、audit本人の `--rerun` 強制再実行（下記）がgreen。
- Re-audit (11): Re-audit (10) Findingsの2件のdocs不整合を解消するcommit `8aebba1ce3`（「docs(299): align spec change-history wording with status and fix assessment formatting」。同一commitに前roundのaudit記録更新を含む）が加わったため再監査。`git diff 70284c36b9..8aebba1ce3` は `specs/299-nova-restore-capture-invalid/spec.md`（Change history 2行の言い換えのみ）、`docs/assessment/issue-299-nova-restore-capture-invalid.md`（formatting修正3行）、本audit記録（Re-audit (10)の記録）の3ファイルで、production code・test・CI workflowへの変更は無い（`--stat`で確認）。
  - **spec内部不整合の解消（Re-audit (10) Minorの解消）**: Change historyの新規entryが「statusを `implemented` とした」と主張しfrontmatter `accepted` と矛盾していた問題は、entryの言い換え（「一時的にstatusを `implemented` とした」+「後続reviewでdeadline起算点のMajor指摘を受け、修正中のためstatusを `accepted` へ戻した。修正の再監査・review通過後に `implemented` へ再更新する」）で解消。frontmatter（`accepted`）とChange historyが一致した。
  - **assessment formatting崩れの解消**: Re-audit (10)で指摘した行内連結（deadline起算点entryの末尾と次bulletの連結）が改行で修正された。
  - 検証: production code・testは `70284c36b9` から不変のため、Re-audit (10)の強制再実行結果（99 suite / 1095 tests / failures 0）とCI run 34830689124のlane結果をそのまま承継。audited head上の新CI merge gate run 34833668117の結果はExecuted test surface節に記録。なお同runのattempt 1は51分経過後にcancelledとなり（最終結論に至らないまま打ち切り。成否判定はattempt 2で実施 — Executed test surface節参照）、同一head上のHigh-risk gate run 34833668116は、本audit記録の更新前はHead SHAが `70284c36b9` でspecs deltaがnon-docs扱いになったためfail — 本Re-audit (11)の更新（docs-only）とCI re-run（attempt 2）により解消する経路。

## Scope

最終証跡監査対象は `8aebba1ce3fcee8c695cd16fa0aff8b906bffb52`（Re-audit (11)の監査対象。途中のhead `33759767d5` / `83e3f0d312` はRe-audit (8)/(9)で確認済み）。以下の履歴記録にある旧headは各re-audit時点の証跡である。最終CI証拠は上記CI runを使用する。

対象はPR #314（base `main`、head branch `issue-299-spec-plan`、audited head `8aebba1ce3fcee8c695cd16fa0aff8b906bffb52`）。`0eb3355e0e` で確定したcapture path・typed diagnosticsは以後不変で、以後の差分は（1）監査記録・spec status更新（`0eb3355e..483038d94f`、Re-audit (1)参照）、（2）review対応のdiagnostics enum型化とsettle待ちlifecycle強化（`483038d94f..b669194b55`、Re-audit (2)参照）、（3）generation-identity付きrestore reload completion barrierへの設計修正（`82fd75df1b..c928ac66af`、Re-audit (3)参照）、（4）Re-audit (3)指摘defectの修正とその記録（`c928ac66af..4040f102dc`、Re-audit (4)参照）、（5）callback-lifecycle raceへの対応（`4040f102dc..fe04bfb960`、Re-audit (5)参照）、（6）inactive-model dispatch-time対応と専用regression追加（`fe04bfb960..922a5c03a4`、Re-audit (6)参照）、（7）cancelled分岐fallback returnの削除（`922a5c03a4..b12d8031fc`、Re-audit (7)参照）、（8）UI-thread契約・loaderStarted追跡・cross-process assert強化とdeadline起算点の遅延初期化（`b12d8031fc..70284c36b9`、Re-audit (8)/(9)/(10)参照）、（9）spec Change history整合とassessment formatting修正（`70284c36b9..8aebba1ce3`、Re-audit (11)参照）である。(9)の差分はdocs/spec wordingのみ（production code・test・CI workflowは `70284c36b9` から不変）であり、layout-write / migration pathへの変更は無い。`git merge-base origin/main` は `c5274b5d0d`（#313 merge後の現行main）であり、code確定commit `0eb3355e0e` 自体は16ファイル、+428/−224で、残りはbranch上で先行commitされたspec（`specs/299-nova-restore-capture-invalid/spec.md`、`plan.md`）と調査assessment（`docs/assessment/issue-299-nova-restore-capture-invalid.md`）の追加と上記(1)-(9)である。task packetに記載のbase `37e3dd8feb` は現行merge-baseではなく（mainが#313で進行）、記録値「16ファイル +428/−224」はcode確定commit単体のdiffと一致する。本auditはGitHub APIとlocal gitで確認した値を正本とする。

この段落の `audited head b12d8031fc...` はRe-audit (7)時点の履歴値であり、現在の最終証跡監査対象はこの節冒頭に示した `8aebba1ce3fcee8c695cd16fa0aff8b906bffb52` である。

確認したdiff領域:

- `.github/workflows/ci.yml` — 新CI lane `organizer-instrumentation-issue299-tests`（scenario classごとの `connectedLawnWithQuickstepGithubDebugAndroidTest` 3実行 + manual `adb install -r` と `am instrument` によるcross-process pair（Stage A → force-stop → Stage B））。`final-status` のneedsへ追加。
- `lawnchair/src/app/lawnchair/backup/NovaBackupConverter.kt` — `convertAndRestore` のsettle barrier（`RestoreReloadSettleWait`）。`reloadAfterRestore` のdispatch前に `BgDataModel.Callbacks` をregisterし、restore-family leaseの解放後（`use` blockの外）で `finishBindingItems` 発火 + `LauncherModel.isModelLoaded` を上限15秒（`DEFAULT_SETTLE_TIMEOUT_MS = 15_000L`）で待ってからreturnする。timeout時はfail-open（WARN logを出しreturn）。
- `lawnchair/src/app/lawnchair/organizer/application/adapter/RowManifestCodec.kt` — widget不変条件の2箇所の `requireNotNull`（provider欠落 / appWidgetId欠落）を、`CaptureInvariantViolationException(CaptureInvariantCategory.INVALID_WIDGET_ROW, ...)` のthrowへ変更。captureのread pathのみ。
- `lawnchair/src/app/lawnchair/organizer/application/protocol/CaptureInvariant.kt` — 新規。閉じた `CaptureInvariantCategory` enum（初出 `INVALID_WIDGET_ROW`）と、`IllegalArgumentException` を継承する `CaptureInvariantViolationException`。
- `lawnchair/src/app/lawnchair/organizer/application/protocol/LayoutApplicationModule.kt` — `CaptureFailureObserver` 経由でcategoryを `DiagnosticsLogger` へ渡すwiring。
- `lawnchair/src/app/lawnchair/organizer/diagnostics/logger/DiagnosticsLogger.kt` — `logCaptureFailure` / `formatCaptureFailure` に閉じた定数名の `invariant=` fieldを追加（自由文字列Parameterは不導入）。
- `lawnchair/src/app/lawnchair/organizer/integration/OrganizationInputComposer.kt` — `CaptureFailureObserver` のsignature変更と、`LayoutWriterCanonicalCaptureSource` におけるtyped違反のclass identityを `IllegalArgumentException` へ正規化して返す処理。
- instrumentation harness — `tests/organizer-instrumentation/app/lawnchair/backup/` に `NovaRestoreCaptureTestBase` / `ControlTest` / `WidgetWindowTest` / `UnknownProviderTest` / `CrossProcessTest` を追加。head commitで `NovaRestoreCaptureInterruptedReloadTest.kt` を削除（中断注入scenarioはplan I-2契約どおり出荷しない）。JVM unit test `DiagnosticsLoggerTest` / `OrganizationInputComposerTest` を拡張。
- docs — `docs/engineering/organizer-diagnostics.md`（§7のredaction表へ `invariant=` fieldの行追加、§10の記述更新、change history）。上記のspec/plan/assessment追加。

runtime書き込み経路・migration対象の確認: PR labelは `risk: layout-data` であり、diffはhigh-risk path（`lawnchair/src/app/lawnchair/backup/`、`lawnchair/src/app/lawnchair/organizer/application/`）を含む。ただし本diffによる新しいlayout write・migrationは存在しない。settle待ちはcallbackのregister / `isModelLoaded` のpoll / callbackのremoveのみでDB writeを追加しない。restore自体の書込み（`performRestore`、`applyConvertedGrid` 等）は既存のBACKUP_RESTORE leaseとtransactionの内側から不変である。codec・`CaptureInvariant`・observer経路の変更はcapture（読み取り専用契約の維持）とdebug diagnostics出力のみに作用し、journal語彙（`CAPTURE_INVALID` 含む）・schema・`favorites` 書式・recovery stateは不変である。

## Criteria check

`specs/299-nova-restore-capture-invalid/spec.md`（accepted）の受入条件ごとの確認結果。調査証跡の正本は `docs/assessment/issue-299-nova-restore-capture-invalid.md`（以下「assessment」と記す）。

- **CI-AC-01（throw点の特定と記録）: 満たす（記録された限界付き）**。assessment I-2 additionsが、一時debug計測（commitせずrevert済み）によるthrow点の直接捕捉 `top=app.lawnchair.organizer.application.adapter.RowManifestCodec.toCanonical:297`（`requireNotNull(row.appWidgetId)`）と、同時観測（widget行 `appWidgetId=-1`、capture fail-closed）を記録。boundaryとして「synthetic issue-representative failure上での確定であり、元実機セッションとのthrow-site identityは未証明」が明記されている（下限Findings参照）。
- **CI-AC-02（barrier後の最初の権威的capture成功・恒常化しない）: 満たす（検証済みの経路上で。Re-audit (3)-(6)で設計・境界を確定）**。実装はRe-audit (3)で修正済み: `convertAndRestore` はlease内でgeneration-identity付きreload tokenをdispatchし、lease外でTHAT generationのsuccessful completionのみを成功としてawaitする（deadline 15秒超過時はrestore失敗をthrow — 初回実装の「timeout時fail-open成功return」はreviewで撤回され、私の初回Findings指摘は解消）。検証は `NovaRestoreCaptureControlTest` / `NovaRestoreCaptureWidgetWindowTest` / `NovaRestoreCaptureUnknownProviderTest`（barrier到達後のcapture Readyをassert）+ Re-audit (6)追加の専用regression `NovaRestoreCaptureNoCallbacksTest`（callbacks=0でreturn時点unbound行なし・直後のproduction capture Ready）がaudited head上のCI lane `organizer-instrumentation-issue299-tests`（run 34833668117）でPASS、実施者local（AVD `nunu_qpr2_api36_1`）でも再検証済み。assessment記録のre-restore実験（窓の再open → 修復generation完了後の再修復）も安定性を補強する。barrierはgeneration identityとterminal outcome（completed/cancelled）を持つようになり、restore API return / 固定待ち時間をcompletion扱いしないspecの定義、およびassessment「Completion barrier」節が求めていたgeneration相関oracleの方向に適合する。**boundary**: Re-audit (3)のlatch再利用defect（Re-audit (4)で修正）、callback消失時のtoken無限pending（Re-audit (5)でin-dispatch terminalization + deadline経路token clear）、inactive-model正常return（dispatch-time経路はRe-audit (6)でno-callbacks修復generation + 専用regressionにより閉鎖、cancelled分岐のfallback returnはRe-audit (7)の `b12d8031fc` で削除）、deadline起算点のlease内消費（Re-audit (10)のlazy deadlineでlease解放後に起算）— 全review指摘は解消済み。app存在時の正常returnはrestore相関修復generationのcommitを構造的に含意し、completion budgetからlease内時間が除外された（Re-audit (10)の invariant review参照）。残る境界はbarrier loop分岐の直接JVM unit test不在（worker-dispatch回帰とinstrumentation regression + CI laneで代替覆盖と記録）と、元障害セッション由来の証拠boundary（CI-AC-01/06参照）である。
- **CI-AC-03（fail-closedの維持）: 満たす**。readiness checkの弱化・`CAPTURE_INVALID` の握り潰しは無い。codecのthrow点は同一不変条件のtyped化であり、`LayoutWriterCanonicalCaptureSource` は引き続き `CanonicalCaptureReadResult.Invalid` を返す。`NovaRestoreCaptureUnknownProviderTest` はsettle点前のcapture Invalid（fail-closed）とsettle後のReady（削除修復）を両方assert。JVM unit testは私の再実行で99 suite / 1094 test、failure 0（下記）、CI `organizer-unit-tests` もsuccess。
- **CI-AC-04（#185 reserved-QSB保護の非回帰）: 満たす**。既存coverage（`OrganizationInputComposerTest` の#185 case、`organizer-instrumentation-shared-writer-tests` の `LoaderCursorOverlapAcceptanceContractTest` 等）がCI merge gate run 34833668117ですべてsuccess。assessmentも全runでreservation違反・`CAPTURE_RESERVED_OVERLAP` 未発生を記録。
- **CI-AC-05（自動regressionの追加）: 満たす**。committed instrumentation family + 新CI lane `organizer-instrumentation-issue299-tests` が `final-status` のmerge gateへ接続され、run 34833668117（audited head上）で実際に実行・成功している。deterministicなtest seamが作れたケースに該当し、代替device evidenceへの依存は不要。codec/loggerのtyped化はJVM unit test（`DiagnosticsLoggerTest` / `OrganizationInputComposerTest` 拡張、review対応でenum型化に追従、Re-audit (10)で `INVALID_CAPTURE_STATE` 分類test追加）でも覆盖される。Re-audit (6)ではinactive-model branchの専用regression `NovaRestoreCaptureNoCallbacksTest`（own process、CI lane組み込み、Re-audit (10)でworker-dispatch回帰test追加）が加わった。**boundary**: barrier loopのdeadline超過・連続cancel分岐への直接unit覆盖は引き続き不在（token lifecycle直接testはinstrumentation auto-load非決定性のため断念・assessmentに記録済み）。barrierのsettled経路は既存familyで覆盖される（Findings参照）。
- **CI-AC-06（emulatorでの繰り返しrestore → capture検証）: 満たす（記録された範囲で）**。assessmentのmatrixが同一環境での復帰・安定性（settle後Ready、中断注入→解除→回復、cross-processのprocess death跨ぎ持続 → 新processのsettleで修復、re-restoreで窓が再openしsettle後に再修復）を記録し、CI laneが同harnessを毎回実行する。ただし元障害セッションの「同一手順で揺れる」intermittent trigger自体の再現は未達で、assessmentがI-5 gate入力として明示している（後述）。
- **CI-AC-07（復旧挙動の確定とdecision gate記録）: 満たす**。assessment I-5 decision gate（2026-09-14記録、Re-audit (3)差分で修正履歴ごと更新）が「正規化/拒絶を採用しない」ことを記録し、根拠（settle到達reloadの後はwidget行がbind（有効id）またはdeleteで修復されることがI-2/I-4で決定的観測済み＝barrier後もinvalidが残る状態は不在）と、採用却下の理由（restore threadからのwidget bindという#298 hazard、未install providerでの行喪失）を示す。reviewでの設計修正（heuristic barrier + fail-open timeoutの撤回、generation-identity token + deadline時restore失敗への置換）もI-5記録とplan seam注記に同期済み。specの「不採用は別fixでCI-AC-02を満たせる場合のみ可」の条件を、その別fix（generation-identity付きcompletion barrier）の観測証跡とともに満たす。採用した振る舞い（barrier）はCI-AC-02の検証で被っている。
- **CI-AC-08（bounded category diagnostics・選択非依存の必須成果）: 満たす**。`CaptureInvariantCategory.INVALID_WIDGET_ROW`（閉じたenum）→ `phase=CAPTURE exceptionClass=IllegalArgumentException invariant=INVALID_WIDGET_ROW`（debug build・capture失敗時）。class identityはtyped違反でも `IllegalArgumentException` に正規化され、#172契約のidentity互換を維持。message / stack trace / layout由来textは型として渡らない（自由文字列Parameter不導入）。journal語彙は不変。`docs/engineering/organizer-diagnostics.md` の§7 redaction表・§13・change historyが同期されている。unit testでline formatとnon-containmentを検証（CI `organizer-unit-tests` success、私の再実行でもgreen）。Re-audit (2)でinvariant引数が `String?` から `CaptureInvariantCategory?` 型へ変更され、定数名以外が型として進入不可能になった（文字列経由の迂回路の閉塞。出力形式・redaction境界は不変、実施者によるemulator再検証ではinvariant category assertion付きでPASS）。Re-audit (10)で未型付け `IllegalArgumentException` に汎用bounded category `INVALID_CAPTURE_STATE` が導入され、IAE系capture失敗はすべてbounded categoryを運ぶように強化（他のRuntimeExceptionは従来どおりfield無し。private layout detailを含むmessageでのnon-leakを新unit testで検証）。

記録されたboundary（assessment I-5 gate入力として明示、本auditもそのまま承継する）: (1) 元実機セッションのthrow-site identityは未証明（shipped diagnosticsが例外classのみのため。確定はsynthetic issue-representative failure上）); (2) 元のintermittent triggerは未確定（controlled interruption matrixでI-3を履行しdeviation記録済み）; (3) #298のactual wrong-thread pathは未再現（#298 scope、本PRは同seamに触れない）。

## Executed test surface

audit本人による独立再実行（対象は既追跡分のみ・dirty fileなし）:

- 初回audit時（code state `0eb3355e0e`）、Re-audit (2)時（`b669194b55`、enum型化されたloggerとlifecycle強化を含む）、Re-audit (3)時（`c928ac66af`、generation-identity token barrierを含む）、Re-audit (4)時（`4040f102dc`、latch/outcome per-attempt resetを含む）、Re-audit (5)時（`fe04bfb960`、callback-lifecycle terminalizationを含む）、Re-audit (6)時（`922a5c03a4`、no-callbacks修復dispatchを含む）、Re-audit (7)時（`b12d8031fc`、cancelled分岐fallback削除を含む）、Re-audit (10)時（現audited head `70284c36b9`、lazy deadline・UI-thread契約・`INVALID_CAPTURE_STATE`を含む）の8回を実施。Re-audit (3)以降は `--rerun` でtest taskを強制再実行し（直前のrunがup-to-date判定だったため）、結果XMLのmtimeで再実行を確認した。いずれもorganizer unit test表面（`DiagnosticsLoggerTest`、`OrganizationInputComposerTest`、review attempt 1で環境timing flakeを起こした `ReadinessGateTest` を含む）を覆盖する。

```text
JAVA_HOME=/opt/homebrew/Cellar/openjdk@21/21.0.12/libexec/openjdk.jdk/Contents/Home \
ANDROID_HOME=$HOME/Library/Android/sdk \
./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'
→ [0eb3355e0e上] BUILD SUCCESSFUL in 27s（exit 0）。
→ [b669194b55上] BUILD SUCCESSFUL in 31s（exit 0）。
→ [c928ac66af上] ./gradlew ... --rerun → BUILD SUCCESSFUL in 4s（exit 0、test task強制再実行）。
→ [4040f102dc上] ./gradlew ... --rerun → BUILD SUCCESSFUL in 28s（exit 0、17 task再実行 — converter修正の再compileを含む）。
→ [fe04bfb960上] ./gradlew ... --rerun → BUILD SUCCESSFUL in 29s（exit 0、17 task再実行 — token lifecycle修正の再compileを含む）。
→ [922a5c03a4上] ./gradlew ... --rerun → BUILD SUCCESSFUL in 26s（exit 0、17 task再実行 — no-callbacks dispatch修正の再compileを含む）。
→ [b12d8031fc上] ./gradlew ... --rerun → BUILD SUCCESSFUL in 26s（exit 0、17 task再実行 — fallback削除の再compileを含む）。
→ [70284c36b9上] ./gradlew ... --rerun → BUILD SUCCESSFUL in 24s（exit 0、17 task再実行 — lazy deadline・UI-thread契約修正の再compileを含む）。
  初回〜Re-audit (7)のrun: 99 suite / 1094 tests / failures 0 / errors 0 / skipped 0。
  Re-audit (10)のrun（`INVALID_CAPTURE_STATE` 分類test追加により+1）: 99 suite / 1095 tests / failures 0 / errors 0 / skipped 0
  （いずれも build/test-results/testLawnWithQuickstepGithubDebugUnitTest のXML集計）
```

```text
python3 tools/repo-contract/test_validate_high_risk_evidence.py
→ Ran 51 tests in 0.015s / OK（exit 0。各re-audit時および初回時も同一51 testでOK）
```

```text
python3 tools/repo-contract/validate_high_risk_evidence.py \
  --repo nunu1733/NunuLauncher --pr-number 314 \
  --head-sha 8aebba1ce3fcee8c695cd16fa0aff8b906bffb52
→ PASS: audit docs/assessment/pr-314-nova-restore-capture.md covers
  8aebba1ce3fcee8c695cd16fa0aff8b906bffb52 with independent CI evidence
  (specs/299-nova-restore-capture-invalid/spec.md)（exit 0）
```

実施者が記録した実行（PR本文・assessment。CI側で機械検証済みの項目を含む。実施者環境: AVD `nunu_qpr2_api36_1` / API 36.1）:

```text
./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' → BUILD SUCCESSFUL
./gradlew spotlessCheck → PASS（CI job check-style と同一surface）
./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=app.lawnchair.backup.NovaRestoreCaptureControlTest      → PASS
./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=app.lawnchair.backup.NovaRestoreCaptureWidgetWindowTest → PASS
./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=app.lawnchair.backup.NovaRestoreCaptureUnknownProviderTest → PASS
adb shell am instrument -w -e class app.lawnchair.backup.NovaRestoreCaptureCrossProcessStageATest ... → PASS
adb shell am force-stop app.lawnchair.debug
adb shell am instrument -w -e class app.lawnchair.backup.NovaRestoreCaptureCrossProcessStageBTest ... → PASS
python3 tools/repo-contract/validate_repo_contract.py → PASS
./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=...ControlTest / ...WidgetWindowTest
  （Re-audit (2)のreview対応後の再検証。invariant category assertionと "Restore reload settled" log確認を含む） → PASS
./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.lawnchair.backup.NovaRestoreCaptureNoCallbacksTest
  + Control / WidgetWindow / UnknownProvider + cross-process A→force-stop→B（Re-audit (6)のhead `922a5c03a4` でのlocal再検証） → すべてPASS
```

CI merge gate（GitHub APIで直接確認。audit本人が `gh api` / `gh run view` で検証）:

- audited head `8aebba1ce3fcee8c695cd16fa0aff8b906bffb52` のmerge gateはrun 34833668117（attempt 2）: `event=pull_request`、`pull_requests=[314]`、`head_branch=issue-299-spec-plan`、`head_sha=8aebba1ce3fcee8c695cd16fa0aff8b906bffb52`、`path=.github/workflows/ci.yml`、`status=completed`、`conclusion=success`、`run_attempt=2`。
- 同runのjob別conclusion（per_page=100で取得。attempt 2）: `final-status: success` を含む全14 jobがsuccess。source job `organizer-unit-tests` / `check-style` / `build-debug-apk` は実行済みsuccess、新lane `organizer-instrumentation-issue299-tests` も実行済みsuccess（`NovaRestoreCaptureNoCallbacksTest` を含むper-class own-process実行）。attempt 1は51分経過後にissue53 laneの実行中にcancelled（それまでに完了したjobはすべてsuccess — 遅延laneの打ち切りであり品質問題ではない）のため、audit本人が `gh run rerun` で再実行を要求し、attempt 2で全job greenを確認した。なお同一commit上のHigh-risk gate workflow run 34833668116（attempt 1）は、本audit記録の更新前はHead SHAが `70284c36b9` でspecs deltaがnon-docs扱いになったためfail（「re-audit against the new head」） — 本Re-audit (11)のdocs-only更新と本記録により解消する。
- 参照run（prose記録、`CI run:` 行には新runのみ記載）: code commit `0eb3355e0e` 上のmerge gate run 34768482481、docs commit `483038d94f` 上のrun 34770025968、review-fix commit `b669194b55` 上のrun 34771951461（attempt 2。issue52 laneの環境flake — system launcher ANRによるwindow focus遮蔽 — をrerunで回収）、barrier設計修正commit `c928ac66af` 上のrun 34788278587（attempt 2。`ReadinessGateTest` のtiming flakeをrerunで回収、実施者local 3/3 green）、latch-reset修正commit `4040f102dc` 上のrun 34790445253（attempt 1で全job green）、callback-lifecycle修正commit `fe04bfb960` 上のrun 34794779554（attempt 2。`CategoryOverridePreferencesInstrumentationTest` のUI lane flakeをrerunで回収）、no-callbacks修正commit `922a5c03a4` 上のrun 34801319508（attempt 1で全job green）、fallback削除commit `b12d8031fc` 上のrun 34804943219（attempt 1で全job green）、deadline起算点修正commit `70284c36b9` 上のrun 34830689124（attempt 1で全job green）、最終コード候補 `33759767d5` 上のrun 34821703942（attempt 2）と証跡確定head `83e3f0d312` 上のrun 34824284489（attempt 1、Re-audit (8)/(9)の記録対象）も、同一条件（pull_request / PR #314関連付け / completed / success / 全job green）をaudit本人が確認済み。各head上のlane実行がその都度緑であることを上記run列が示す。

- 最終コード候補 `33759767d5` と証跡確定head `83e3f0d312` のローカル再検証: `git diff --check`、`./gradlew spotlessCheck --console=plain`、`./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin --console=plain` → すべて成功。deadline起算点修正head `70284c36b9` のCI merge gateはrun `34830689124`（attempt 1）で全14 jobおよび `final-status` successを確認済み（先行head `83e3f0d312` はrun `34824284489`）。docs整合head `8aebba1ce3`（本audited head）のCI merge gateは冒頭記載のrun `34833668117` attempt 2。

## Findings

- **【解消済み → 設計修正の記録】初回実装のsettle待ちtimeout fail-open**: 初回実装（`0eb3355e0e`）は15秒のsettle heuristic超過時もfail-openでreturnしていたため、restore return後も「次に完了するreload」までの残存窓でOrganizer要求がfail-closedを続け、かつbarrier signalがgeneration identityを持たなかった。Re-audit (3)の `c928ac66af` でこの設計はreviewにより撤回され、generation-identity付きtoken barrier（当該generationのsuccessful completionのみ成功・deadline超過時はrestore失敗をthrow）に置換された。本指摘は解消。**ただし後述のre-dispatch経路defectが新たな残余として記録される**。
- **【解消済み（Re-audit (4)で確認）】Re-audit (3) barrierのre-dispatch経路のlatch再利用defect**: `RestoreReloadBarrier` が `latch`（`CountDownLatch(1)`）と `outcome` を `dispatch()` ごとに再初期化しないため、最初のgenerationが一度でも `cancelled` になると（外部stopLoader・supersession・モデル非活性化で到達しうる）loopが既に0のlatchで即座にawaitから返りstaleな `outcome`（"cancelled"）を読み、新generationの完了を待てないまま `startLoader`/`stopLoader` のhot spinが絶対deadline（15秒）まで続きrestoreを失敗させていた。`a49dda3451` が `dispatch()` 冒頭での `latch = CountDownLatch(1)` + `outcome.set("")`（token登録前）で修正 — 旧tokenのrunnablesは発火済みで後から新pairに書く者はいないためsignal喪失の競合はなく、audit本人のdiff reviewで当該defectの解消を確認した。deadline超過時のrestore失敗surfaceは不変。テストカバー面では、Re-audit (5)で「token lifecycle直接testはinstrumentation processのlauncher auto-load非決定性のため断念」が記録されたが、Re-audit (6)でno-callbacks branchの専用regression `NovaRestoreCaptureNoCallbacksTest`（own process・CI lane組み込み）が追加され、inactive-model contract pathの自動回帰は確保された。残余はbarrier loopのcancelled/re-dispatch/deadline分岐への直接JVM unit覆盖（今後のfollow-upに推奨）。
- **【解消済み（Re-audit (7)で確認）】cancelled分岐のinactive-model fallback returnの残存**: Re-audit (6)で指摘した `RestoreReloadBarrier.awaitCompletion` の `if (!model.hasCallbacks()) { WARN; return }`（当時NovaBackupConverter.kt:337-341）は、`b12d8031fc` で削除された。barrierの終端はCOMPLETEDからのreturn（=当該修復generation commit済み）とdeadline超過時の `cancelRestoreReloadIfCurrent` + throwのみとなり、cancelledは無条件re-dispatch（no-callbacksでも実generationが開始されるためprogress保証）。これにより「app != null時の正常return ⇒ restore相関generation commit済み」が構造的に成立的となり、Re-audit (6)で記録したdocs/code不整合もcode側の実現によって解消した（assessment記述は当該commitで無変更だが、記述がcodeと一致する状態になった）。
- **【Cosmetic】削除済みclassの孤立KDoc**: `RestoreReloadSettleWait` 削除時に旧KDocが `resolveIconPackLabel` 上に孤立残留し、同一barrierのcomment blockも重複している（動作影響なし。docs-only commitで整理可能）。
- **CI-AC-01/CI-AC-06の証拠boundaryは記録済みのまま**: 元実機セッションのthrow-site identityは未証明、元のintermittent triggerは未確定、#298 actual wrong-thread pathは未再現。いずれもassessmentがI-5 gate入力として明示しており、本auditはspec要件がその記述と整合して満たされていると判定するが、元障害の完全な因果確定は这两点の将来追跡に依存する。後続追跡は#298（scope分離維持）とassessment記録の再開条件に従う。
- **#298のactual pathは本PRでは意図的に扱わない**: specのNon-goalどおり、#298と同一seam（restore/reload窓）への追加介入は避けられている。completion barrier（およびそのreload）が#298の症状を部分的に緩和しうるかに見えても、その判断は#298側の証拠で行うべきであり、本auditは緩和主張を認めない。
- **cross-process検証surfaceの手動性**: Gradle connected testはtest APK再install時にapp dataを消去するため、cross-process pairはCI lane内でもmanual `adb install -r` + `am instrument` + `force-stop` の手順に依存する（lane commentとassessmentに文書化済み）。動作はCIで毎回検証されるが、emulator runner側の環境変化に対する脆弱性は残る。
- **task packetのdiff記述と実測の差異**: packet記載のbase `37e3dd8feb`（16ファイル +428/−224）は現行merge-baseではなくcode確定commit `0eb3355e0e` 単体のdiffに一致する。実際のPR diff（vs `c5274b5d0d`）はGitHub API取得時点で22ファイル +3022/−33（初回audit時18ファイルから、audit記録追加と各review対応で増加）。本auditの判定は実測値に基づく。
- **Re-audit (8) current result**: 最新の独立Standards監査およびSpec監査はいずれも **No findings**。上記の過去Findingは各re-auditで解消を確認済みであり、`33759767d5` のworkflowコメント差分も実装・テスト手順と一致する。
- **【解消済み（Re-audit (10)で確認）】deadline起算点のlease内消費**: Re-reviewが指摘したMajor — barrierのabsolute deadlineがconstructor（lease解放前）で起算されるため、lease内処理時間が15秒completion budgetを消費し、低速deviceで修復generationに実行機会を与えないまま偽restore failureが成立しうる — は、`70284c36b9` の遅延初期化（`awaitCompletion()` 開始時・lease解放後に一度だけ計算、re-dispatchで不延長）で解消。audit本人のdiff reviewで確認（Re-audit (10)参照）。NoCallbacksTestが修正版でpassし（return時点(0,1)+capture Ready）、lease内budget消費が起きていないことの実行証拠となる。
- **【解消済み（Re-audit (11)で確認）】specファイル内部のstatus不整合**: `70284c36b9` で追加されたspec Change history entryは「statusを `implemented` とした」と記載するが、frontmatter `status:` 行は `accepted` のままという不整合は、`8aebba1ce3` の言い換え（`implemented` flipを一時的と明記、`accepted` への帰還と再更新予定を正確に記述）で解消した。あわせてassessment新規entryと次bulletの行内連結formatting崩れも同commitで修正済み。validatorはfrontmatter（accepted）を検証するため動作上の支障は無く、`implemented` 再更新はdocs-onlyで行われる。
- **再監査の成立条件（Re-audit (1)〜(11)）**: 初回audit（head `0eb3355e0e`）以降、docs commit `483038d94f`（audit記録追加・spec status更新。差分はdocs/specのみ）、review対応commit `b669194b55`（diagnostics enum型化とsettle lifecycle強化）、review対応commit `c928ac66af`（generation-identity barrierへの設計修正）、修正commit `a49dda3451`（latch/outcome reset）、docs commits `4040f102dc` / `2894d5004d` / `e53ba22cb3`（assessment・audit記録の更新。すべてdocs-onlyのみであることを`--stat`で確認済み）、修正commit `fe04bfb960`（callback-lifecycle race対応）、修正commit `922a5c03a4`（inactive-model dispatch-time対応 + 専用regression追加。cancelled分岐fallbackの残存をRe-audit (6)で指摘）、修正commit `b12d8031fc`（同fallback削除。同じcommitに前roundのaudit記録更新を含む）、中間commit群 `ec3b569914` / `f98917d65b` / `dbfb28c65b` / `fd696e6699` / `ed8a4daac5` / `752b620d13` / `e3b9db3817` / `654b80e803` / `182db9cfba` / `33759767d5`（UI-thread契約・loaderStarted追跡・`INVALID_CAPTURE_STATE`・test強化。Re-audit (8)/(9)で独立確認）とdocs commits `83e3f0d312` / `4eced43c3a`、修正commit `70284c36b9`（deadline起算点の遅延初期化。Re-audit (10)で解消確認）、docs commit `8aebba1ce3`（spec history言い換え・assessment formatting修正・audit記録更新。docs-only — production codeは `70284c36b9` から不変、Re-audit (11)で確認）が加わった。各差分をaudit本人が直接reviewし（Re-audit (1)-(11)参照）、audited headを `8aebba1ce3` へ更新し、当該head上のmerge gate run 34833668117をCI証拠とする。本audit記録自体の更新（およびspec `implemented` 再更新）はdocs-only commitとしてheadに積まれ、gateが許容する経路である。head以降にさらにcode変更が入った場合は本auditを無効とし再auditを要する（docs-only commitのみ監査有効性を維持）。
