# High-risk audit: PR #222 STABLE_PAGE_TIDY_V1 — page-local lift-then-place compaction strategy (child 4)

> Status: accepted — GO (round 3; lineage: NO-GO at `c38205bfa2` → Blocking fixed, GO at `371a4f5649` → owner review Medium×2/Low×1 → test-only fix, GO at `67ae85bd92` — see Findings 1)
> Audit date: 2026-09-05

- Auditor: Independent subagent audit session with no involvement in the implementation session (solo-maintenance independent re-read + re-run; same role and criteria as the PR #207 / #221 audits; author of this PR's first-pass NO-GO and second-pass GO records)
- PR: https://github.com/nunu1733/NunuLauncher/pull/222
- Head SHA: 67ae85bd92cc07ddaf64044bf2b88d5c820907b0
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33944119954 (`pull_request` `ci.yml` run on the audited head, associated with PR #222 — completed success, all 13 jobs green including `final-status`, `organizer-unit-tests`, `check-style`, `build-debug-apk`, and all seven `organizer-instrumentation-*` lanes; no flaky re-run needed. Prior rounds' runs 33940036245 (`c38205bfa2`) and 33940761147 (`371a4f5649`) were likewise fully green.)
- Criteria: specs/182-layout-strategy-catalog/spec.md — FR-016, AC-4, AC-15 (AC-6, AC-9b and the §STABLE_PAGE_TIDY_V1 normative rules are cited for human review; the machine gate tokenizes the remaining IDs), docs/adr/0012-versioned-layout-strategy-catalog.md — ADR-0012

## Scope

Cumulative PR diff `git diff c50ec0cea1..67ae85bd92` = the feature commit `c38205bfa2` (four production files + one new test file, all planning/rules-internal) + the audit-fix commit `371a4f5649` (bundle semantic version bump to `organization-policy-v2.1`, one test assertion, one unused-local removal, one DEEP_SHORTCUT test) + the owner-review fix commit `67ae85bd92` (**test-only**: three test files; production code is byte-identical to the second-pass GO head).

- `c38205bfa2` (first-pass audited): `FullRunExecution.kt` — `execute` dispatches on `strategy.unitOrder` (`CANONICAL_TIE_BREAK` → `executeCanonicalPageCompact`, the former body; `CAPTURED_VISUAL_PAGE_LOCAL` → new `executeStablePageTidy`; `CAPTURED_VISUAL_GLOBAL` → loud `IllegalStateException`); trailing preservation loop extracted into shared `appendPreservedPlacements`. `PlacementAllocator.kt` — `PageScope.CAPTURED_PAGE_ONLY` + `Allocator.allocateOnPageOnly(span, page)` (single-page first-fit, `null` on no fit, never touches `allocateOnNewPages`/`newPages`). `LayoutStrategyRegistry.kt` — registers `STABLE_PAGE_TIDY_V1` (`createsFolders=false`, filter = kind `APPLICATION`/`DEEP_SHORTCUT` and workspace span `1×1`, `CAPTURED_VISUAL_PAGE_LOCAL`, `CAPTURED_PAGE_ONLY`, `TOP_LEFT_ROW_MAJOR`). `BuiltInOrganizerPolicyBundleSource.kt` — `runtimeSupported` = {`CANONICAL_PAGE_COMPACT_V1`, `STABLE_PAGE_TIDY_V1`}, default unchanged. `StablePageTidyStrategyTest.kt` (new) — tests through `OrganizationPlanner.plan` only.
- `371a4f5649` (second-pass audited): `PolicyModels.kt` — `POLICY_BUNDLE_VERSION` → `organization-policy-v2.1` with child-2 → child-4 progression KDoc; `BuiltInOrganizerPolicyBundleSourceTest.kt:18` assertion updated; unused local removed; `deepShortcutsAreEligibleTidyUnitsToo` added.
- `67ae85bd92` (this round): `OrganizationInputComposerTest.kt` — `twoValidSelectionsComposeEndToEndWithDistinctRulesIdentities` (AC-6 end-to-end: same capture composed under two content-consistent valid selection snapshots); `EffectiveRulesIdentityTest.kt` — synthetic `FUTURE_TIDY_V1` replaced with the real `STABLE_PAGE_TIDY_V1` and the stale child-2-only comment corrected; `StablePageTidyStrategyTest.kt` — `tidyFormsNoFoldersEvenWhenCanonicalGroupingWouldApply` (grouping-eligible fixture with a canonical control run that must form exactly one folder).

No file under `organizer/application/**`, Launcher3 provider/model/writer, backup, deck, or UI paths is changed; no runtime layout-write path, DB schema, or public planner shape is touched. The golden corpus resources, `PlacementAllocator.kt`, `LayoutStrategyRegistry.kt`, `BuiltInOrganizerPolicyBundleSource.kt`, and all production planning code are untouched by both fix commits, so the first-pass strategy-behavior verification carries over unchanged.

## Criteria check

- **Spec §STABLE_PAGE_TIDY_V1 normative rules — PASS** (verified in full at the first pass; no production code has changed since):
  - *Eligible set*: `movableItems` is exactly `determinePreservation == null` (PlanningPlacement.kt:67–69) — available, unlocked, top-level, non-reservation, non-dock/widget/app-pair/legacy, role-`Movable` items of kinds `APPLICATION`/`DEEP_SHORTCUT`/`FOLDER`; the registry filter further requires `APPLICATION`/`DEEP_SHORTCUT` and workspace span `GridSpan(1,1)`. The `1×1` condition is present and correct; DEEP_SHORTCUT eligibility has a dedicated test.
  - *Strategy-fixed reporting*: `strategyFixed = movableItems.filterNot(eligibleUnitFilter)` (non-`1×1` apps/deep shortcuts + existing folders) are `markOccupied` on their captured page and reported `Disposition.Preserved(STRATEGY_PRESERVED)` — never `ALREADY_CANONICAL`/`NON_TARGET`; genuinely preserved kinds keep higher-precedence reasons (disjoint sets; `appendPreservedPlacements` re-derives `determinePreservation` unchanged; widget → `WIDGET`, folder child → `STRUCTURAL` asserted).
  - *Visual order*: `compareBy(cell.y, cell.x, id)` per page — exactly `(cell.y, cell.x, ItemId)`, total order.
  - *Page-locality*: units grouped by captured page, allocated only via `allocateOnPageOnly(span, PageRef(capturedPageId))`, which never calls `allocateOnNewPages`; `newPages` is empty by construction (tests assert). The canonical flow's `CAPTURED_PAGE_ONLY` branch throws — no page-creating path exists for the tidy strategy. `null` → `error(...)` loud invariant failure, matching the existing canonical convention and the spec's "fails loudly, never a silent partial plan".
  - *Lift-then-place / placeability*: eligible units are unmarked before their turn (shared materialization marks only preserved items + reservations), so the i-th unit in visual order always finds the i-th free row-major cell — every admitted unit is placeable.
  - *Idempotence*: placement maps visual order onto sorted free cells, so after materialization captured visual order equals target traversal order and a replan returns each unit to its own cell (`ALREADY_CANONICAL`, zero `Moved`, no new pages/folders) — verified constructively and by the materialize-then-replan test (with a 2×2 fixed obstacle) and the already-tidy empty-diff test. Matches §Idempotence arguments.
  - *No new folders / folder children unchanged*: `createsFolders=false`, `newFolders = emptyList()`, members stay `STRUCTURAL` at captured ranks. **Strengthened this round**: `tidyFormsNoFoldersEvenWhenCanonicalGroupingWouldApply` uses two same-profile, same-non-fallback-category (S1/GAMES) apps that satisfy `minGroupSize = 2`, and asserts the canonical control run on the identical input forms exactly one folder (`newFolders.size == 1`) before asserting tidy forms none, emits no `FOLDER_MEMBER` disposition, and keeps all three apps as workspace placements — the contrast eliminates the "passes only because grouping never applies" false positive. Independently recomputed: the control expectation is real (P-04/P-05 grouping applies to the two GAMES apps; the unsignaled third app falls back to OTHER as a singleton).
  - *Representative fixture*: `(0,0),(2,0),(3,0)` → `(0,0),(1,0),(2,0)`; second-page apps never move to page 1.
- **AC-4 (single seam, purity) — PASS.** `OrganizationPlanner.plan` remains the sole external seam; the branch is reachable only through a registered `StrategyDefinition` behind V-20. No test references internal helpers. `PurityGuardTest` walks every `.kt` under `organizer/planning` and passes fresh; production files add no imports and no platform/IO/Clock/Random references.
- **AC-6 (selection participates in provenance) — strengthened, PASS.** `twoValidSelectionsComposeEndToEndWithDistinctRulesIdentities` exercises the production composer end-to-end with two valid runtime-supported selections (`CANONICAL_PAGE_COMPACT_V1`, `STABLE_PAGE_TIDY_V1`) over the same capture and asserts: both `Ready`; identical `provenance.policyBundle` (selection never enters the bundle identity); effective `rules.organizationStrategy` differs as expected; `provenance.rules` differs (identity-content invariant on the effective-`RuleSemantics` digest); and each `provenance.layoutStrategySelection` equals its own snapshot identity. The test's snapshot digests are content-consistent with the store's publication format — independently verified against `LayoutStrategySelectionStore.kt`: `canonicalSelection(selection) = selection?.value.orEmpty()`, digest `sha256Canonical(canonical)` (`:75–76`, `:118–127`, `:196`), `versionOrGeneration = "schema-$SCHEMA_V1-generation-$generation"` with `SCHEMA_V1 = 1` — matching the test's `sha256Canonical(selection.value)` / `"schema-1-generation-N"`. `EffectiveRulesIdentityTest` now names the real strategy instead of a synthetic future ID.
- **AC-9b / ADR-0007 §8 catalog coherence and bundle versioning — PASS (first-pass Blocking resolved at `371a4f5649`).** `acceptedIds == definitions.keys` makes the coherence test (exact set equality `runtimeSupported == implemented`, `default ∈ runtimeSupported`, `default == rules.organizationStrategy`) follow the registry automatically; it passes unchanged with the expanded set, default still `CANONICAL_PAGE_COMPACT_V1`. The bundle ships under the new semantic version `organization-policy-v2.1` plus new digest — exactly the spec 182 requirement and the model ADR-0012 accepted (its rejected alternative — same version, digest-only — no longer ships). `semanticVersion` participates in `canonicalRepresentation()`, so the digest covers the new version string; `validate()` remains self-consistent against the constant.
- **AC-5 / golden oracle — PASS.** Golden corpus resources and `tests/unit/resources/planner-golden-corpus/sha256.txt` (`0c98c7b9110a8cfdaa1319513d541569bd416b60fa94bc31f17fa17a02edb4a9`) are untouched by all three commits, and `GoldenOracleCorpusTest` passes fresh at `67ae85bd92` (inside the 900-test run and via the explicit filter). `executeCanonicalPageCompact` is the former `execute` body statement-for-statement; the extracted `appendPreservedPlacements` iterates the same list with the same arguments and appends the same placements in the same order; the canonical registry definition is unchanged. No pre-existing test assertion was modified except the bundle-version assertion the version bump legitimately required.
- **FR-016 / AC-15 — see Criteria line and CI evidence.** FR-016's selection/preview UI surface is child 8 (#218); this child's bundle expansion is the required policy-side step.

## Executed test surface

Independent inspection: full `git diff 371a4f5649..67ae85bd92` read (three test files + the committed second-pass record); `LayoutStrategySelectionStore.kt:60–198` + `canonicalSelection` read to verify the test's digest/versionOrGeneration format claims; composer selection-validation/provenance path grepped (`OrganizationInputComposer.kt:190–270`, `:510`); canonical-control folder-formation expectation independently recomputed against `formFolderGroups` semantics; `git diff --name-only` scope checks confirming production code and golden corpus untouched by both fix commits; first- and second-pass full-diff inspections (all production files, `PlanningPlacement.place`/`determinePreservation`, `PolicyModels.kt`, bundle source, registry, allocator, tests, ADR-0007 §8, ADR-0012, spec 182, plan.md, Issue #214) carried forward. Issue #219 body read: the AC-9/AC-11 cross-strategy harness/property runner is now explicitly in its scope ("PR #222 owner review より本 Issue の scope へ明示移管").

Local runs at the audited head `67ae85bd92` (working tree clean apart from this docs-only audit-record update; JDK 21.0.12, Gradle 9.3.0):

- `./gradlew spotlessCheck --rerun-tasks` → **BUILD SUCCESSFUL** (5 tasks executed, forced fresh). Note: combining `spotlessCheck` with test tasks in one invocation trips a pre-existing Gradle implicit-dependency validation error (`:spotlessJava` vs `:compatLib:compileDebugAidl` outputs) unrelated to this PR; CI runs `check-style` separately and it passes.
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --rerun` → **BUILD SUCCESSFUL**; test task executed (not cached); **80 suites, 900 tests, 0 failures, 0 errors, 0 skipped**, including `StablePageTidyStrategyTest` (9), `GoldenOracleCorpusTest` (1), `EffectiveRulesIdentityTest` (6), `OrganizationInputComposerTest` (18), `PurityGuardTest` (1), `BuiltInOrganizerPolicyBundleSourceTest` (6).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests '*StablePageTidyStrategyTest*' --tests '*GoldenOracleCorpusTest*' --tests '*EffectiveRulesIdentityTest*' --tests '*OrganizationInputComposerTest*'` → **BUILD SUCCESSFUL** (explicit run of the round-3-owned suites).
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` → **BUILD SUCCESSFUL**.
- `python3 tools/repo-contract/validate_repo_contract.py` → **OK**; `python3 tools/repo-contract/validate_diagnostics_contract.py` → **PASS**.
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL**.
- `gh pr checks 222 --repo nunu1733/NunuLauncher` and `gh run view 33944119954 --repo nunu1733/NunuLauncher` → run `33944119954` is the `pull_request` `ci.yml` run on `head_sha = 67ae85bd92cc07ddaf64044bf2b88d5c820907b0`, **completed, conclusion success, all 13 jobs green** (`changes`, `validate-repo-contract`, `check-style`, `organizer-unit-tests`, `build-debug-apk`, seven `organizer-instrumentation-*` lanes, `final-status`). The `high-risk-evidence` gate run 33944119974 failed only because the committed record still pointed at the superseded head `371a4f5649` with non-docs changes after it — correctly detected by the gate, resolved by this update.
- `python3 tools/repo-contract/validate_high_risk_evidence.py --repo nunu1733/NunuLauncher --pr-number 222 --head-sha 67ae85bd92cc07ddaf64044bf2b88d5c820907b0` → **PASS** (recorded below after this file was written).

## Findings

1. **Lineage.** Round 1 (`c38205bfa2`): NO-GO — bundle published under the digest-only model ADR-0012 explicitly rejects. Round 2 (`371a4f5649`): Blocking resolved via `organization-policy-v2.1` bump; GO. Owner review on the GO head: Blocking 0 / Medium 2 / Low 1 (composer AC-6 end-to-end coverage for two valid selections; folder-formation contrast test; cross-strategy runner ownership transfer). Round 3 (`67ae85bd92`): both Mediums addressed with test-only additions independently verified above; the Low is resolved by the explicit scope transfer to Issue #219. No production code changed in round 3.
2. **Medium (tracked, non-blocking) — shared cross-strategy property/harness coverage and performance samples.** `PlannerContractHarnessTest`/`PlannerGeneratedPropertyTest`/`PlannerBenchmarkTest` remain pinned to `CANONICAL_PAGE_COMPACT_V1`; spec AC-9/AC-11/AC-12 execution for `STABLE_PAGE_TIDY_V1` is now explicitly owned by Issue #219 (child 9 evidence matrix), whose body names the cross-strategy runner transfer. Tracked; not a merge blocker for this child.
3. **Low — stale ADR prose (optional).** ADR-0012's decision/consequences text still describes the active bundle as `organization-policy-v2` (written at acceptance, before any enablement); the spec's enablement rule already generalizes this ("e.g. `organization-policy-v2.1`"), and the code KDoc records the progression. A one-line ADR prose touch-up is the owner's call; not a merge blocker.
4. **Low — fixture grid note.** The hole-closing test uses a 4×4 device where the spec scenario names 4×5; the asserted cells are identical for the fixture, so this is not a normative deviation.

**Verdict: GO** at `67ae85bd92`. The strategy behavior, seam, purity, bundle versioning/coherence, AC-6 provenance contract (now end-to-end for two valid selections), folder-formation contrast, default stability, and golden-oracle evidence all pass independent verification at the new head; CI `final-status` is green on this SHA; and the high-risk evidence validator passes with this record. PR #222 has sufficient independent high-risk evidence to merge once this updated record is pushed and the `high-risk-evidence` check re-runs green.
