# High-risk audit: PR #222 STABLE_PAGE_TIDY_V1 — page-local lift-then-place compaction strategy (child 4)

> Status: accepted — GO (re-audit; the first pass at `c38205bfa2` was NO-GO on one Blocking bundle-versioning finding, fixed in `371a4f5649` — see Findings 1)
> Audit date: 2026-09-05

- Auditor: Independent subagent audit session with no involvement in the implementation session (solo-maintenance independent re-read + re-run; same role and criteria as the PR #207 / #221 audits; re-auditor of this PR's own first-pass NO-GO record)
- PR: https://github.com/nunu1733/NunuLauncher/pull/222
- Head SHA: 371a4f56494a7e0d0ebc88914fa1b0ba8371e954
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33940761147 (`pull_request` `ci.yml` run on the audited head, associated with PR #222 — completed success, all 13 jobs green including `final-status`, `organizer-unit-tests`, `check-style`, `build-debug-apk`, and all seven `organizer-instrumentation-*` lanes; no flaky re-run was needed on this head. The first-pass CI run 33940036245 on `c38205bfa2` was likewise fully green.)
- Criteria: specs/182-layout-strategy-catalog/spec.md — FR-016, AC-4, AC-15 (AC-9b and the §STABLE_PAGE_TIDY_V1 normative rules are cited for human review; the machine gate tokenizes the remaining IDs), docs/adr/0012-versioned-layout-strategy-catalog.md — ADR-0012

## Scope

Cumulative PR diff `git diff c50ec0cea1..371a4f5649` = the audited feature commit `c38205bfa2` (four production files + one new test file, all planning/rules-internal) plus the fix commit `371a4f5649` (bundle semantic version bump, one test assertion, one unused-local removal, one new test case, and — via the implementer's `git add -A` — this audit record itself, which is retained as lineage and updated in place here).

- `c38205bfa2` (first-pass audited): `FullRunExecution.kt` — `execute` dispatches on `strategy.unitOrder` (`CANONICAL_TIE_BREAK` → `executeCanonicalPageCompact`, the former body; `CAPTURED_VISUAL_PAGE_LOCAL` → new `executeStablePageTidy`; `CAPTURED_VISUAL_GLOBAL` → loud `IllegalStateException`); trailing preservation loop extracted into shared `appendPreservedPlacements`. `PlacementAllocator.kt` — `PageScope.CAPTURED_PAGE_ONLY` + `Allocator.allocateOnPageOnly(span, page)` (single-page first-fit, `null` on no fit, never touches `allocateOnNewPages`/`newPages`). `LayoutStrategyRegistry.kt` — registers `STABLE_PAGE_TIDY_V1` (`createsFolders=false`, filter = kind `APPLICATION`/`DEEP_SHORTCUT` and workspace span `1×1`, `CAPTURED_VISUAL_PAGE_LOCAL`, `CAPTURED_PAGE_ONLY`, `TOP_LEFT_ROW_MAJOR`). `BuiltInOrganizerPolicyBundleSource.kt` — `runtimeSupported` = {`CANONICAL_PAGE_COMPACT_V1`, `STABLE_PAGE_TIDY_V1`}, default unchanged. `StablePageTidyStrategyTest.kt` (new) — 7 tests through `OrganizationPlanner.plan` only.
- `371a4f5649` (this re-audit's delta): `PolicyModels.kt` — `POLICY_BUNDLE_VERSION` `organization-policy-v2` → `organization-policy-v2.1` with a KDoc recording the child-2 (`-v2`) → child-4 (`-v2.1`) progression; `BuiltInOrganizerPolicyBundleSourceTest.kt:18` version assertion updated to match; `FullRunExecution.kt` unused local `val input` removed (no behavior change); `StablePageTidyStrategyTest.kt` gains `deepShortcutsAreEligibleTidyUnitsToo` (a `DEEP_SHORTCUT` at `(3,3)` tidies to `(3,0)` behind three apps, asserting `Moved(SINGLE_PLACEMENT)` — expected value independently recomputed and correct).

No file under `organizer/application/**`, Launcher3 provider/model/writer, backup, deck, integration, diagnostics, or UI paths is changed; no runtime layout-write path, DB schema, or public planner shape is touched. The golden corpus resources, `PlacementAllocator.kt`, `LayoutStrategyRegistry.kt`, and the bundle content file are untouched by the fix commit (`git diff c38205bfa2..371a4f5649 --name-only` over those paths is empty), so the first-pass strategy-behavior verification carries over unchanged.

## Criteria check

- **Spec §STABLE_PAGE_TIDY_V1 normative rules — PASS** (verified in full at the first pass; the fix commit changes none of the verified behavior):
  - *Eligible set*: `movableItems` is exactly `determinePreservation == null` (PlanningPlacement.kt:67–69) — available, unlocked, top-level, non-reservation, non-dock/widget/app-pair/legacy, role-`Movable` items of kinds `APPLICATION`/`DEEP_SHORTCUT`/`FOLDER`; the registry filter further requires `APPLICATION`/`DEEP_SHORTCUT` and workspace span `GridSpan(1,1)`. The `1×1` condition is present and correct; DEEP_SHORTCUT eligibility now has a dedicated test.
  - *Strategy-fixed reporting*: `strategyFixed = movableItems.filterNot(eligibleUnitFilter)` (non-`1×1` apps/deep shortcuts + existing folders) are `markOccupied` on their captured page and reported `Disposition.Preserved(STRATEGY_PRESERVED)` — never `ALREADY_CANONICAL`/`NON_TARGET`; genuinely preserved kinds keep higher-precedence reasons (disjoint sets; `appendPreservedPlacements` re-derives `determinePreservation` unchanged; widget → `WIDGET`, folder child → `STRUCTURAL` asserted).
  - *Visual order*: `compareBy(cell.y, cell.x, id)` per page — exactly `(cell.y, cell.x, ItemId)`, total order.
  - *Page-locality*: units grouped by captured page, allocated only via `allocateOnPageOnly(span, PageRef(capturedPageId))`, which never calls `allocateOnNewPages`; `newPages` is empty by construction (tests assert). The canonical flow's `CAPTURED_PAGE_ONLY` branch throws — no page-creating path exists for the tidy strategy. `null` → `error(...)` loud invariant failure, matching the existing canonical convention and the spec's "fails loudly, never a silent partial plan".
  - *Lift-then-place / placeability*: eligible units are unmarked before their turn (shared materialization marks only preserved items + reservations), so the i-th unit in visual order always finds the i-th free row-major cell — every admitted unit is placeable.
  - *Idempotence*: placement maps visual order onto sorted free cells, so after materialization captured visual order equals target traversal order and a replan returns each unit to its own cell (`ALREADY_CANONICAL`, zero `Moved`, no new pages/folders) — verified constructively and by the materialize-then-replan test (with a 2×2 fixed obstacle) and the already-tidy empty-diff test. Matches §Idempotence arguments.
  - *No new folders / folder children unchanged*: `createsFolders=false`, `newFolders = emptyList()`, members stay `STRUCTURAL` at captured ranks.
  - *Representative fixture*: `(0,0),(2,0),(3,0)` → `(0,0),(1,0),(2,0)`; second-page apps never move to page 1.
- **AC-4 (single seam, purity) — PASS.** `OrganizationPlanner.plan` remains the sole external seam; the branch is reachable only through a registered `StrategyDefinition` behind V-20. No test references internal helpers (`FullRunExecution`, `Allocator`, registry). `PurityGuardTest` walks every `.kt` under `organizer/planning` and passes fresh; the new/changed files add no imports and no platform/IO/Clock/Random references.
- **AC-9b / ADR-0007 §8 catalog coherence and bundle versioning — PASS (first-pass Blocking resolved).** `acceptedIds == definitions.keys` makes the coherence test (exact set equality `runtimeSupported == implemented`, `default ∈ runtimeSupported`, `default == rules.organizationStrategy`) follow the registry automatically; it passes unchanged with the expanded set, default still `CANONICAL_PAGE_COMPACT_V1`. The fix bumps the bundle semantic version to `organization-policy-v2.1` — exactly the spec 182 §"Spec-level catalog vs runtime-supported set" requirement ("new bundle semantic version/generation and digest … e.g. `organization-policy-v2.1`") and the model ADR-0012 accepted (its rejected alternative — same version, digest-only — is no longer what ships). `semanticVersion` participates in `canonicalRepresentation()`, so the digest covers the new version string; `validate()` remains self-consistent against the constant; the only other `"organization-policy-v2"` literals in the tree are synthetic fixture identities in `EffectiveRulesIdentityTest` (unrelated to the active bundle).
- **AC-5 / golden oracle — PASS.** Golden corpus resources and `tests/unit/resources/planner-golden-corpus/sha256.txt` (`0c98c7b9110a8cfdaa1319513d541569bd416b60fa94bc31f17fa17a02edb4a9`) are untouched by both commits, and `GoldenOracleCorpusTest` passes fresh at `371a4f5649` (inside the 898-test run and via the explicit filter). `executeCanonicalPageCompact` is the former `execute` body statement-for-statement; the extracted `appendPreservedPlacements` iterates the same list with the same arguments and appends the same placements in the same order; the canonical registry definition is unchanged. No pre-existing test assertion was modified except the bundle-version assertion the fix legitimately required.
- **FR-016 / AC-15 — see Criteria line and CI evidence.** FR-016's selection/preview UI surface is child 8 (#218); this child's bundle expansion is the required policy-side step.

## Executed test surface

Independent inspection: full `git diff c38205bfa2..371a4f5649` read (fix delta, all five files including the committed prior audit record); re-read of `PolicyModels.kt` catalog/digest/validate against the new constant; DEEP_SHORTCUT test expectation independently recomputed; `git diff c38205bfa2..371a4f5649 --name-only` scope check confirming allocator/registry/bundle-content/golden-corpus files untouched; first-pass full-diff inspection of `c50ec0cea1..c38205bfa2` (all five files, complete `FullRunExecution.kt`/`PlacementAllocator.kt`/`LayoutStrategyRegistry.kt`/`PlanningPlacement.place`/`determinePreservation`/`BuiltInOrganizerPolicyBundleSourceTest.kt`/`StablePageTidyStrategyTest.kt`/`PurityGuardTest`, ADR-0007 §8, ADR-0012, spec 182, plan.md, Issue #214) carried forward.

Local runs at the audited head `371a4f5649` (working tree clean apart from this docs-only audit-record update; JDK 21.0.12, Gradle 9.3.0):

- `./gradlew spotlessCheck --rerun-tasks` → **BUILD SUCCESSFUL** (5 tasks executed, forced fresh). Note: combining `spotlessCheck` with test tasks in one invocation trips a pre-existing Gradle implicit-dependency validation error (`:spotlessJava` vs `:compatLib:compileDebugAidl` outputs) unrelated to this PR; CI runs `check-style` separately and it passes.
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --rerun` → **BUILD SUCCESSFUL**; test task executed (not cached); **80 suites, 898 tests, 0 failures, 0 errors, 0 skipped**, including `StablePageTidyStrategyTest` (8), `GoldenOracleCorpusTest` (1), `PurityGuardTest` (1), `BuiltInOrganizerPolicyBundleSourceTest` (6), `ContractShapeTest` (41), `PlannerContractHarnessTest` (42), `PlannerGeneratedPropertyTest` (7).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests '*StablePageTidyStrategyTest*' --tests '*GoldenOracleCorpusTest*'` → **BUILD SUCCESSFUL** (explicit run of the PR-owned suites).
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` → **BUILD SUCCESSFUL**.
- `python3 tools/repo-contract/validate_repo_contract.py` → **OK**; `python3 tools/repo-contract/validate_diagnostics_contract.py` → **PASS**.
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL**.
- `gh pr checks 222 --repo nunu1733/NunuLauncher` and `gh run view 33940761147 --repo nunu1733/NunuLauncher` → run `33940761147` is the `pull_request` `ci.yml` run on `head_sha = 371a4f56494a7e0d0ebc88914fa1b0ba8371e954`, **completed, conclusion success, all 13 jobs green** (`changes`, `validate-repo-contract`, `check-style`, `organizer-unit-tests`, `build-debug-apk`, seven `organizer-instrumentation-*` lanes, `final-status`). The `high-risk-evidence` gate run 33940761082 failed only because the committed record still pointed at the superseded head — correctly detected by the gate, resolved by this update.
- `python3 tools/repo-contract/validate_high_risk_evidence.py --repo nunu1733/NunuLauncher --pr-number 222 --head-sha 371a4f56494a7e0d0ebc88914fa1b0ba8371e954` → **PASS** (recorded below after this file was written).

## Findings

1. **Blocking (first pass) — RESOLVED at `371a4f5649`.** The first-pass audit (record committed inside the fix commit, originally NO-GO at `c38205bfa2`) found the bundle published under the model ADR-0012 explicitly rejects: same `POLICY_BUNDLE_VERSION` string `organization-policy-v2`, digest-only expansion. The fix bumps the constant to `organization-policy-v2.1` (matching the spec's own example), updates the single version assertion, and records the child-2 → child-4 version progression in the KDoc. Re-verified fresh at the new head (bundle test 6/6, digest self-consistency, `validate()` path). The NO-GO→fix→GO lineage is preserved in this file's history and in the committed first-pass copy.
2. **Medium (tracked, non-blocking) — shared cross-strategy property/harness coverage and performance samples not yet extended to the new strategy.** `PlannerContractHarnessTest`/`PlannerGeneratedPropertyTest`/`PlannerBenchmarkTest` remain pinned to `CANONICAL_PAGE_COMPACT_V1`, so spec AC-9 (shared invariants per strategy through the public seam) and AC-12 (strategy × matrix performance samples, named in Issue #214's acceptance with device evidence shareable with child 9) are not yet exercised for `STABLE_PAGE_TIDY_V1`. The plan assigns the cross-strategy runner and performance matrix to the evidence child (#219, child 9); confirm #219's scope covers tidy strategy rows so this does not fall between #214 and #219.
3. **Low — stale ADR prose (optional).** ADR-0012's decision/consequences text still describes the active bundle as `organization-policy-v2` (written at acceptance, before any enablement); the spec's enablement rule already generalizes this ("e.g. `organization-policy-v2.1`"), and the code KDoc now records the progression. A one-line ADR prose touch-up (or leaving it as the historical acceptance-time statement) is the owner's call; not a merge blocker.
4. **Low — fixture grid note.** The hole-closing test uses a 4×4 device where the spec scenario names 4×5; the asserted cells are identical for the fixture, so this is not a normative deviation.

**Verdict: GO** at `371a4f5649`. The first-pass Blocking bundle-versioning finding is resolved exactly as the accepted spec/ADR require; the strategy behavior, seam, purity, coherence-test auto-follow, default stability, and golden-oracle evidence all pass independent verification at the new head; CI `final-status` is green on this SHA; and the high-risk evidence validator passes with this record. PR #222 has sufficient independent high-risk evidence to merge once this updated record is pushed and the `high-risk-evidence` check re-runs green.
