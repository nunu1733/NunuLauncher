# High-risk audit: PR #322 AI personalization Context / PersonalizedIntent exchange contract

> Status: accepted
> Audit date: 2026-09-15

- Auditor: independent audit session (ZCode subagent session, distinct from the implementation session; solo保守のための独立session再実行・再確認)。実装sessionとは別のsessionとして、実装コードの変更は行わず、読み取り・検証・本監査記録の作成のみを実施した。
- PR: https://github.com/nunu1733/NunuLauncher/pull/322
- Head SHA: fc331290f67ba2364c7cf8f926b33fe06c071b76
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/34962577307
- Criteria: specs/204-ai-personalization-context-intent-contract/spec.md — AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9, AC-10, AC-11, AC-12, AC-13, AC-14, FR-017
- Criteria: docs/adr/0007-authoritative-organization-policy-sources.md — ADR-0007
- Criteria: docs/adr/0012-versioned-layout-strategy-catalog.md — ADR-0012
- Criteria: docs/adr/0010-qsb-row-item-overlap-interop.md — ADR-0010

> 監査履歴: (1) 初回audit (head `428d45a2387f6afb7459a6448facfd5ccb672eed`、CI run [34953107141](https://github.com/nunu1733/NunuLauncher/actions/runs/34953107141))。(2) 再audit (head `4302fe039b3bab381f3d96e4ced7ec5998a2aac4`、audit commit `b4e37a2784`、CI run [34956918427](https://github.com/nunu1733/NunuLauncher/actions/runs/34956918427)) — 前回P2-1/P3-1対応の検証。(3) **本再audit (第3回, head `fc331290f67ba2364c7cf8f926b33fe06c071b76`)** — ChatGPT PR review "Request changes" (P1×3/P2×1) の対応commitによるコード変更後の再audit。
>
> 第3回再audit対象の変更 (`b4e37a2784..fc331290f6`): (1) planner preference consumption (`FullRunContext.preferences` 追加、canonical executorでのimportance ordering rank `unitPreferenceRank` と `pageAffinity` preferred-page bias `biasedPreferredPage`。intentなしrunはbyte-identical)、(2) `ExportSessionStore` seamのtyped expiry (`load` が期限切れmatching recordも返す、`save` がBoolean返し)、(3) `ContextExportCodec` 新規 (export envelope codec、encode時256KiB上限fail-closed)、(4) `IntentValidator` のpageAffinity範囲検証 (0 <= p < pageCount、範囲外 `INVALID_ENUM`)、(5) test新規/修正 (`IntentPreferenceConsumptionTest`, `ContextExportCodecTest`, `PlannerMobilityBindingTest` fixture修正, `AndroidExportSessionStoreTest` typed expiry e2e)、(6) plan.md review response記録。

## Scope

対象diff (`origin/main` = `9ea2ba0eb4d9ef61bd96ef2b480bbd20ed055edd` → 第3回再audit head `fc331290f67ba2364c7cf8f926b33fe06c071b76`、受入commit `e6dfe8fa4b` とPR review対応commit `4302fe039b`/`fc331290f6` を含む):

- 新規純粋package `lawnchair/src/app/lawnchair/organizer/personalization/` への追加 (spec 204の契約module): `ContextExportModels.kt`, `ContextExportBuilder.kt`, `RandomIdAllocator.kt`, `SourceContextIdentity.kt`, `IntentModels.kt`, `IntentCodec.kt`, `IntentValidator.kt`, `IntentIdentity.kt`, `IntentPlannerAdapter.kt`, `ExportSessionStore.kt` (seam interfaceのみ)。既存 #203 fileへの変更なし。
- Android境界1点: `lawnchair/src/app/lawnchair/organizer/integration/AndroidExportSessionStore.kt` (新規) + `SecureRandomIdAllocator`。
- Planning seamへのadditive変更: `OrganizationInput.intentPreferences` (optional, 既存runはnull), `PolicySourceKind.PERSONALIZED_INTENT` + `InputProvenance.personalizedIntent` 第7input (sentinel default), `PlanningResult.personalizedIntentDigest` echo, `PlanningResultCanonicalization` のecho伝播, `rules/PolicyModels.kt` のenum追加。
- docs: spec.md/plan.md (status accepted), requirements.md (FR-017、FR-014境界、D-011), CONTEXT.md (用語4件), DESIGN.md (module行/gate行), organizer-diagnostics.md (intent identity許容行)。
- 新規test 10 file (`IntentPreferenceConsumptionTest`, `ContextExportCodecTest` は第3回で追加)。`main` 由来の既存test fileは無修正 (本PR自身が導入した `AndroidExportSessionStoreTest` はseam変更に合わせてPR内で更新 — main由来testの無修正通過は回帰基準として維持)。

確認したruntime書き込み経路とmigration対象:

- 唯一の新規永続化は `AndroidExportSessionStore` による `noBackupFilesDir` 配下の単一JSON (`organizer_personalization_export_session_v1.json`, AtomicFile, schema version 1)。Launcher favorites DBへの書込み・DB schema migrationは存在しない。backup対象外位置。
- validatorは純粋関数でzero-write構造。**第3回でplanner consumptionが実装された** (`FullRunContext.preferences` → canonical executor)。consumptionはsingleton orderingへのimportance rank (no-preference時は定数rankで既存順序と等価) と `preferredPage` のsoft biasのみで、`determinePreservation`・allocator capacity制約・`TargetSet`・run mode・strategy宣言semanticsには触れない (実装読解で確認)。composerへのaccepted intent読取りは引き続き#205/#206接続時 (plan.md記録済み)。
- `risk: layout-data` の実質: planner seamへのadditive optional field追加のみで、既存runはsentinel identityとnull projectionで既存挙動・provenance不変 (下記Criteria check参照)。

## Criteria check

spec 204 (status: accepted) の受入条件と、ADR-0007/0012/0010のauthority境界について、head `428d45a` の実装を独立に確認した (コード読解 + ローカルtest実行 + CI結果の照合)。

- **AC-1 (FR-017)**: 満たす。requirements.mdへFR-017 (FR-014との境界備考、D-011のFR-017言及) が追加済み (受入commit `e6dfe8fa4b`)。
- **AC-2 (versioned schema)**: 満たす。`personalization-context-v1` / `personalized-intent-v1` の固定schemaVersion定数、unknown versionのfail-closed拒否 (`IntentCodecTest.unknownSchemaVersionIsRejectedFailClosed`、export側は `PersonalizationContextExportV1` のinit requireと `ContextExportCodec.decode` のschemaVersion照合)、immutable semantic version規則の明文化 (spec「Compatibility / migration」節)。**第3回再audit追記**: export envelopeのJSON表現が新規 `ContextExportCodec` に一意に固定された — field名 (`schemaVersion`/`exportId`/`tier`/`gridContext`/`items[].{ref,kind,category,groupSemantic,label,pageAffinity,regionAffinity,mobility,fixReason,usage}`/`preservedConstraints.{reservedRegions,preservedCounts}`/`capabilities.{intentSchemaVersion,functions}`/`usageSignals`) はspec Contract 1のfield名と一致することをencode/decode双方向で確認、optional fieldはnullとして出力せず省略 (`optionalFieldsAreOmittedNotEmittedAsNull`)、encode時に256KiB上限を実測して `Oversize` fail-closed。
- **AC-3 (provider非依存)**: 満たす。codec/validatorはtransport非依存のcanonical JSONのみを扱い、test double (`SequentialIdAllocator` とJSON fixture) が同一seamで検証される。production consumer不在はspec範囲外 (#205/#206) であり契約上の問題ではない。
- **AC-4 (planner authority / FORBIDDEN_CONTENT)**: 満たす。intent schemaにraw座標・span・reservation・DB mutation・scriptの表現は存在せず、schema外authority keyは `FORBIDDEN_CONTENT` でreject (`IntentCodec.FORBIDDEN_KEYS`, `decodeEnvelope`/`decodeItemIntents`)。validator/adapter/plannerは保持判断 (`determinePreservation`) を変更しない。ADR-0007のauthority model (bundle ≠ dynamic state) と整合 — intentは `OrganizationInput.intentPreferences` 経由のordering/preference biasのみ。**第3回再audit追記**: planner側の実消費が実装された (`FullRunContext.preferences`、`unitPreferenceRank`、`biasedPreferredPage`)。spec「Planner接続」節の消費範囲 (importance順序、pageAffinity soft ordering hint) に限定されており、保持判断・constraints・run mode・`TargetSet` 意味論を弱めない。widget親和の実効化は引き続きplanner側のwidget対応strategy判断に委ねられる。ADR-0012の「intentはcatalogへの新strategy追加ではなくseam入力の1つ」との整合も維持。
- **AC-5 (typed fail-closed zero-write)**: 満たす。`SCHEMA_MISMATCH`/`EXPORT_MISMATCH`/`SESSION_EXPIRED`/`CONTEXT_STALE`/`OVERSIZE`/`UNKNOWN_REF`/`DUPLICATE_REF`/`INCOMPLETE_COVERAGE`/`INVALID_ENUM`/`FORBIDDEN_CONTENT`/`MOBILITY_CONTRADICTION` がtyped sealed resultで実装され、各classにcontract testが存在 (`IntentCodecTest`, `IntentValidatorTest`)。`CAPABILITY_UNSUPPORTED` はV1でreserved/unreachable (spec固定どおり、V1必須failure test対象外)。coverage partition (itemIntents refs ∪ unresolvedRefs == 全exported refs、互いに素) をvalidatorで確認。**第2回再auditで解消 (P3-1)**: `unresolvedRefs` 内部重複は `INCOMPLETE_COVERAGE` に分類され、specのcoverage partition節の「両集合の重複」定義と整合 (`itemIntents` 内重複は `DUPLICATE_REF` のまま)。**第3回再audit追記**: intent `pageAffinity` がexport grid page範囲外 (負または `pageCount` 以上) の場合 `INVALID_ENUM` でrejectされる検証が追加された (前回P3-2の実質対応)。分類はspecの `INVALID_ENUM` 行 (closed domain値の範囲外) の拡張適用であり、review決定としてplan.mdに記録済み。
- **AC-6 (intent identity / sentinel)**: 満たす。accepted intentはcanonical representationのSHA-256 content digest付きidentity (`IntentIdentityCalculator`)。no-intent sentinelは `versionOrGeneration="none"`、`sha256 = SHA-256("personalized-intent:none")` で、`PolicyInputIdentity` の型不変条件 (非空version、64hex) を満たす (`UnlinkabilityAndIdentityContractTest.noIntentSentinelIsAValidPolicyInputIdentity`)。#203の `sha256Canonical` と同一の計算であることをコード照合で確認。
- **AC-7 (prompt injection脅威モデル)**: 満たす。injection label fixture test (`externalRedactedTierExcludesTheWholeFreeTextClass` は "Ignore previous instructions" labelを使用)、allow-list/closed enum reject、`rationale` のauthorityなしを確認。
- **AC-8 (privacy tier)**: 満たす。export modelはpackage名・profile identity・raw ms・DB row ID・内部ItemIdのfieldを構造的に持たない。`EXTERNAL_REDACTED` は自由文class (label) 全体を除外しsurrogateも生成しない (builder単一点制御 + test)。`LOCAL_FULL`/`EXTERNAL_WITH_LABELS` のみlabelを含む。
- **AC-9 (preview path再利用)**: 満たす。新preview truthは作らず、accepted intentは `OrganizationInput.intentPreferences` → 既存 `OrganizationPlanner.plan` → `PlanningResult` の既存pathのみを通る。adapterの差分はechoとprojectionのみ。**第3回再audit追記**: 消費test (`IntentPreferenceConsumptionTest`) が同一accepted intent + 同一inputsからの決定的再現 (NFR-003) と、preference差がplacementに決定的に反映されることを確認。
- **AC-10 (test計画)**: 満たす。plan.mdのVerification表にAC対応のtest表面が記載されている。
- **AC-11 (process death耐性)**: 満たす。`AndroidExportSessionStore` はAtomicFileによるdurable保持。store再生成 (process death模擬) を跨ぐ取り込み成功test、失効 `SESSION_EXPIRED`、不明session不在読出し、破損storageのfail-closed ("no session") をtestで確認 (`AndroidExportSessionStoreTest`)。**第3回再audit追記**: seamの `load` が期限切れmatching recordも返すようになり (`active` は引き続き未失効のみ)、validatorが `SESSION_EXPIRED` (matching + expired) と `EXPORT_MISMATCH` (unknown/old) をtypedに区別できるようになった。`processDeathAfterExpiryDistinguishesSessionExpiredFromExportMismatch` がprocess death + 失効後のend-to-end typed区別を検証する。`save` はdurable書込み成否をBooleanで返し、書込み失敗時にexport発行をfail-closed抑止できる契約になった (seam注釈、testで `assertTrue(store.save(session))` を確認)。
- **AC-12 (source context binding / signal分離)**: 満たす。`SourceContextIdentity` のdigest入力はcaptured snapshot (kind/lock/availability/placement) + TargetSet role + 解決済み分類の構造的canonical rowsのみで、#203 signal snapshot・envelope・exportId/ref・tier・capabilityは含まない (実装読解で確認)。envelope走査test (`theExportDocumentCarriesNoStateFingerprintOrInternalIdentity`)、signal変化でdigest不変 (`signalOnlyChangesNeverMoveTheDigest`)、signal変化のみのimport成功 (`signalOnlyChangesNeverRejectTheImport`)、signal provenanceのsession記録と非照合 (`ContextExportBuilderTest` lines 290–300、validatorはsignalを入力に取らない) を確認。**指示検証項目「structural sourceContextDigestが#203 signal snapshotを含まない」: 確認済み。signal変化でCONTEXT_STALEにならない。**
- **AC-13 (kind + mobility projection matrix)**: 満たす。kind matrix: `APP_OR_SHORTCUT`/`FOLDER`/`WIDGET` のみexportされ、`APP_PAIR`/`SHORTCUT_LEGACY`/`Unknown`/unsupported containerはconstraint-only集計 (`preservedCounts`) で、それらへの参照は `UNKNOWN_REF`。mobility matrix: `projectMobility` の先勝ち順位 (RESERVED_REGION → LOCKED → UNAVAILABLE → DOCK → widget CONDITIONAL → APP_PAIR_MEMBER → FOLDER_MEMBER → MOVABLE) は `determinePreservation` (`planning/PlanningPlacement.kt:379-420`, ADR-0010のreservation最優先を含む) と独立照合で一致。locked itemのsemantic fieldはFIXED/LOCKEDへの矛盾として `MOBILITY_CONTRADICTION` に分類され、`FORBIDDEN_CONTENT` には分類されない (`lockedItemMovementPreferenceIsNotForbiddenContent`, `fixedRefContradictionsAreMobilityContradictions`)。widget (`CONDITIONAL`) の `desiredGroup`/`groupSemantic` rejectと、FIXED refの `preserve` のみ許可を確認。**planner binding test (前回P2-1 → 解消、第3回でP3-4のfixture限界も解消)**: `PlannerMobilityBindingTest` (3 tests) が全fixture caseでのexport mobility判定と `determinePreservation` 移動可否の束縛、role Preserved (full-target `NON_TARGET`) caseのunderlying cause境界、builder出力と `projectMobility` 述語の一致を検証する。第2回で指摘したMOVABLE/CONDITIONAL分岐未到達問題はfixture修正で解消した: workspace itemをreservation (row 0) と重複しない行 (y=2) へ配置し、`reservedOverlap` caseのみrow 0に残すことで `MOVABLE → plannerReason==null` と `CONDITIONAL` (widget可搬性、`relocateWidgets` 有無でのplanner判定反転) の両分岐が実際に到達する。非addressable kind (`APP_PAIR`/`SHORTCUT_LEGACY`) はmobility述語の束縛対象外であることが明示的にコメントされ、skipの理由が説明可能になった。
- **AC-14 (cross-export unlinkability)**: 満たす。`exportId` と `items[].ref` は同一注入seam (`RandomIdAllocator`) の新鮮な乱数 (counter/timestamp/決定的導出なし。同一canonical状態の再exportが識別子を共有しないtest、degenerate allocatorのexport scope検出test、`SecureRandomIdAllocator` の16byte crypto-strength entropy)。export文書に `sourceContextDigest` 相当のstate fingerprint・signal identityが現れない走査test。ref↔ItemId mapはsessionのみ (`AndroidExportSessionStore`)。semantic contentからの確率的linkageが保証対象外である旨はspec脅威modelに明記済み。
- **ADR-0007 (authority境界)**: 整合。intent identityはADR-0007 §9の「optional dynamic input, always-present identity」規律を第7inputとして踏襲する。intent identityはimmutable bundle identityやmandatory dynamic cutにjoinしない。planner保持判断はplanner正本のまま。
- **ADR-0012 (strategy catalog)**: 整合。intentはcatalogへの新strategy追加ではなく、`OrganizationPlanner.plan` seamへの入力の1つ。strategy semanticsを弱めない。
- **ADR-0010 (reservation authority)**: 整合。reservation重複itemはexport時点で最優先の `FIXED/RESERVED_REGION` として投影され、intentでは動かせない (`MOBILITY_CONTRADICTION`)。preservedConstraintsにreservation列挙を含む。

既存run (intentなし) の挙動・provenance不変: `intentPreferences`/`personalizedIntent`/`personalizedIntentDigest` はすべてdefault付きadditive field (null / sentinel / null)。**第3回再auditでのplanner consumption追加後も、intentなしrunは `FullRunContext.preferences == null` のため `unitPreferenceRank` が定数rankを返し (既存comparatorと等価)、`biasedPreferredPage` がcaptured pageをそのまま返すため、ordering/placementはbyte-identical** (実装読解で確認)。`main` 由来の既存test fileは無修正で全organizer test (1190件) が通過。sentinel identityは既存plan/preview/applyのbyte挙動に影響しない (identityはprovenance rowの追加のみ)。

## Executed test surface

第3回再audit (対象commit `fc331290f67ba2364c7cf8f926b33fe06c071b76` のcheckout上):

- `git status` / `git log b4e37a2784..fc331290f6` — 前回audit後の変更 (planner consumption 2 file、store seam 2 file、codec新規、validator、plan.md、test 4 file) のみを確認、他の変更なし。
- `./gradlew spotlessCheck` → **PASS** (exit 0)。
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'` → **BUILD SUCCESSFUL**。organizer全体で **1190 tests, 0 failures/errors** (test結果XMLより集計)。第3回で新規/更新されたtestの実行確認: `IntentPreferenceConsumptionTest` (planning) 3 tests / 0 failures、`ContextExportCodecTest` 4 tests / 0 failures、`PlannerMobilityBindingTest` (fixture修正後) 3 tests / 0 failures、`AndroidExportSessionStoreTest` (typed expiry e2e追加) 0 failures。
- CI (GitHub Actions, `pull_request` event, PR #322, head `fc331290f67ba2364c7cf8f926b33fe06c071b76`): run [34962577307](https://github.com/nunu1733/NunuLauncher/actions/runs/34962577307)。**1回目のattemptで全job成功** (issue52含む。12m5s)。merge gate `final-status` は **success** (job: https://github.com/nunu1733/NunuLauncher/actions/runs/34962577307/job/104363021781)。`organizer-unit-tests` / `check-style` / `build-debug-apk` / instrumentation 7件すべてsuccess。run metadata照合 (`gh api`): `head_sha=fc331290f67ba2364c7cf8f926b33fe06c071b76`, `event=pull_request`, `pull_requests=[322]`, `conclusion=success`。

監査履歴の実行記録 (参考):

- 初回audit (head `428d45a2387f6afb7459a6448facfd5ccb672eed`): `./gradlew spotlessCheck` PASS、organizer 1179 tests / 0 failures、CI run [34953107141](https://github.com/nunu1733/NunuLauncher/actions/runs/34953107141) — 1回目attemptの `organizer-instrumentation-issue52-tests` UI timeout (`verifiedSuccessKeepsAppliedWordingAfterRecoveryPreviewCancel`, `ComposeTimeoutException`) を `gh run rerun --failed` で再実行しPASS、`final-status` success (job: https://github.com/nunu1733/NunuLauncher/actions/runs/34953107141/job/104336508558)。
- 第2回再audit (head `4302fe039b3bab381f3d96e4ced7ec5998a2aac4`): `./gradlew spotlessCheck` PASS、organizer 1182 tests / 0 failures、CI run [34956918427](https://github.com/nunu1733/NunuLauncher/actions/runs/34956918427) — 1回目attemptのissue52 UI timeout (`unresolvedDurableStatusRestoresFocusToStartAction`) をrerunでPASS、`final-status` success (job: https://github.com/nunu1733/NunuLauncher/actions/runs/34956918427/job/104349600094)。

## Findings

問題の分類と記録。**mergeを阻害するP1は存在しない。**

### 第3回再audit (head `fc331290f67ba2364c7cf8f926b33fe06c071b76`) での解消確認

- **前回P2-1 (AC-13のplanner一致property test欠落) → 解消 (第2回)**、**P3-4 (binding testのMOVABLE/CONDITIONAL分岐未到達) → 解消 (第3回)**: `PlannerMobilityBindingTest` のfixture修正により、workspace itemをreservation (row 0) と重複しない行 (y=2) へ配置し `reservedOverlap` caseのみrow 0に残すことで、`MOVABLE → plannerReason==null` と `CONDITIONAL` (widget可搬性、`relocateWidgets` 有無でplanner判定が反転すること) の両分岐が実際に到達するようになった。非addressable kindのskip理由も明示された。
- **前回P3-2 (intent `pageAffinity` 範囲検証なし) → 実装上は解消 (第3回)**: `IntentValidator` がpageAffinityをexport grid page範囲 (`0 <= p < pageCount`) で検証し、範囲外を `INVALID_ENUM` でrejectする。ただし **この新検証への専用unit testは本headに存在しない** (既存testは範囲内の値のみ使用) — 回帰防止の観点からtest追加を #323 で追跡することを推奨する (下記参照)。
- **P3-3 / P3-5 → フォローアップIssue [#323](https://github.com/nunu1733/NunuLauncher/issues/323) に分離済み** (P3-3: export側degenerate入力のuntyped例外、P3-5: `unresolvedRefs` 内重複の新分類への個別test不在)。本PRのmergeを阻害しない。#323はOPENで、P3-2〜P3-5のcheckboxを追跡する (P3-2の実装が本headで行われたため、#323側のcheckbox更新はowner判断で行うこと)。
- **ChatGPT PR review (P1×3/P2×1) 対応の検証**: (1) planner preference consumptionはspec「Planner接続」節 (受入gate Q1) の範囲 (importance ordering rank、pageAffinity preferred-page bias) に限定され、`determinePreservation`・allocator constraints・`TargetSet`・run modeを弱めない (`intentNeverWeakensPreservationConstraints` でlocked itemのdisposition/pageがintent preferenceで変化しないことを検証)。intentなしrunはbyte-identical (第3回の1190 test無修正通過で回帰確認)。(2) export session storeのtyped expiry区別はend-to-endで検証済み (AC-11追記参照)。(3) `ContextExportCodec` はspec Contract 1のfield名/schemaVersion/optional omissionと一致し、encode時の256KiB fail-closedを4 testで検証。

### 現在のFindings

- **P3-6: pageAffinity範囲検証への専用test不在 (第3回で新規確認)** — `IntentValidator` のpageAffinity範囲検証 (負、`pageCount` 以上) に対する直接のcontract testが存在しない (既存testは範囲内の値のみ使用)。class自体 (`INVALID_ENUM`) は検証されるため契約上の欠落ではないが、回帰防止の観点で #323 への追記を推奨する。
- **P3-3: export側のdegenerate入力はuntyped例外** (維持、#323で追跡): items > 512、ref衝突、空labelの場合、builderはtyped failureではなく `IllegalStateException` でfail-loudする。純粋builder内のfail-fastであり失敗時zero-writeだが、typed failure経路の外であることを記録する。
- **P3-5: `unresolvedRefs` 内重複の新分類への個別test不在** (維持、#323で追跡): 分類変更 (`DUPLICATE_REF` → `INCOMPLETE_COVERAGE`) に対する直接のcontract testは追加されていない。`INCOMPLETE_COVERAGE` class自体は既存testで検証される。
- **参考 (flake記録)**: 初回/第2回再audit時のCI 1回目attemptで `organizer-instrumentation-issue52-tests` のUI待ちtimeout (`ComposeTimeoutException` 5s、別test名) が発生し、監査sessionによる失敗job再実行でPASSした。第3回 (本head) のCIは1回目のattemptで全job成功。本PR diffと無関係なUI instrumentationの不安定さであり、#323に追跡候補として記載済み。
- **留意 (未確認範囲)**: (1) composer/producer接続 (#205/#206) までの実runでの `EXPORT_MISMATCH` (不明session宛intent) 経路は本PRでは未接続だが、第3回でstore不在読出し → validator typed区別のend-to-end testが追加された。(2) emulator instrumentation test群は本監査ではローカル実行せず、CIの成功で確認 (API 35/36系7job + db migration + issue155/299/52/53/99 + shared-writer)。(3) 再生成時のpreview無効化 (spec「再生成とstale preview」シナリオ) の実装はcomposer統合と同時に#205で到達可能になり、本PRでの直接testは存在しない (plan.mdに記録済みの意図的defer)。

結論: spec 204の受入条件 AC-1〜AC-14 は、第3回再audit head `fc331290f67ba2364c7cf8f926b33fe06c071b76` で検証済み。ChatGPT review (P1×3/P2×1) 対応はspec契約 (Planner接続、fail-closed、process death耐性、Contract 1表現) と整合し、planner preference consumptionによって既存runのbyte-identical性とdeterminism/idempotenceが維持されている。残存する指摘はP3のみ (P3-6は新規、P3-3/P3-5は[#323](https://github.com/nunu1733/NunuLauncher/issues/323)で追跡) で、merge gate (`final-status`) は対象commit上でsuccess。P3群はplanner正本契約とfail-closed構造によりlayout破壊につながる経路を生まない。
