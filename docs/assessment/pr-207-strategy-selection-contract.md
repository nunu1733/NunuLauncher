# High-risk audit: PR #207 Versioned layout-strategy selection contract (child 2)

> Status: accepted — GO
> Audit date: 2026-09-04
> Audit scope note: re-audit at head `e38c92aed4`; supersedes this record's NO-GO version at head `498ae8c0e1`, committed as `9278ee3444` — see Findings for lineage

- Auditor: Independent subagent audit session with no involvement in the implementation or review-fix sessions (solo-maintenance independent re-read + re-run; same session lineage as the first audit, re-executed against the new head)
- PR: https://github.com/nunu1733/NunuLauncher/pull/207
- Head SHA: e38c92aed40c642a54c66b9ed26f67bcced30631
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33884482040 (`pull_request` `ci.yml` run on the audited head, associated with PR #207 — **completed success**, all 14 jobs green including `final-status`, `organizer-unit-tests`, `check-style`, `build-debug-apk`, and all seven `organizer-instrumentation-*` lanes)
- Criteria: specs/182-layout-strategy-catalog/spec.md — FR-016, AC-3, AC-3b, AC-4, AC-5, AC-6, AC-7, AC-9b (AC-3b / AC-9b are cited for human review; the machine gate tokenizes the remaining IDs). The accepted ADR governing this work (ADR 0012, versioned layout-strategy catalog, status accepted) is referenced throughout the body below; its path and ID token are kept off the machine-checked line because the ADR file does not define its own ID token.

## Scope

Cumulative audited change set: `git diff main...e38c92aed4` — the child-2 vertical of spec 182: `StrategyId`/`RuleSemantics.organizationStrategy` public-shape change (`rule-v2`, bundle `organization-policy-v2`), internal `LayoutStrategyRegistry` + minimal `CANONICAL_PAGE_COMPACT_V1` adapter dispatch, `LayoutStrategySelectionStore` (Rule-Management read source + validated write command), composer fifth-policy-input cut + effective-`RuleSemantics` rules identity, V-20 catalog rejection, diagnostics `strategyVersion` echo, `PreserveReason.STRATEGY_PRESERVED` + en/ja wording, spec/ADR/requirements/CONTEXT/DESIGN updates, and the pinned pre-#182 golden oracle. The branch point (`merge-base main HEAD`) is `5fdab48082e271a5489ff344a6d3c8c17ceffea8`, exactly the baseline spec 182 pins as the oracle source. No production file under `organizer/application/**`, Launcher3 provider/model/writer, backup, or deck paths is changed; no runtime layout-write path is touched.

This is the second pass of the same independent audit session. Lineage: the first audit at `498ae8c0e1` returned **NO-GO** because (1) the `tests/organizer-instrumentation` source set did not compile (13 Kotlin errors), failing all seven instrumentation lanes and `final-status` in CI run 33874039993, and (2) the PR carried no risk labels so the mechanical gate classified it low-risk; it additionally recorded a defense-in-depth concern (the diagnostics echo redaction was vacuous at its only production call site). The fix round is two commits: `9278ee3444` (audit findings — androidTest compile fixes, registry-based echo, risk labels) and `e38c92aed4` (stabilize two-panel orientation test row selection). The fix-round diff was read in full: the only production change is `ManualOrganizationRun.kt` (the redaction call site); everything else is `tests/organizer-instrumentation/**` plus this audit record. All planning/rules/integration/diagnostics production files and all unit tests are byte-identical to the first-pass audited head, so the first pass's source-level verification carries over unchanged.

Fix-round verification (independent):

- **Finding 1 (androidTest compile)** — `ProductionOrganizationInputInstrumentationTest.kt` adds `layoutStrategySelections = EmptyLayoutStrategySelectionSource()` at all six composer call sites; the new fake returns a `Ready` snapshot with `schemaVersion=1, generation=0, selection=null` and identity `("schema-1-generation-0", "c"×64)` — matching the store's defined first-run empty state, so the fakes exercise the composer's default-selection path, not a bypass. `ManualOrganizationPreferencesInstrumentationTest.kt` and `OrganizerDiagnosticsRouteInstrumentationTest.kt` replace `OrderingPolicy.CANONICAL_V1` with `StrategyId("CANONICAL_PAGE_COMPACT_V1")`, bump the rules fixture to `RuleVersion("v2")`, and add the `organizationStrategy` echo / `layoutStrategySelection` provenance row. `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` now passes locally (it failed with exactly the 13 reported errors at `498ae8c0e1`).
- **Finding 3 (echo redaction)** — `ManualOrganizationRun.kt:248–257` now passes `runtimeStrategyIds = LayoutStrategyRegistry.acceptedIds.map { it.value }.toSet()`, i.e. the planner's actual runtime-enabled set, so the redaction is no longer self-derived and the defense-in-depth contract in `organizer-diagnostics.md` §D-02 holds at the production call site. The misleading comment was replaced.
- **Finding 2 (labels)** — `gh pr view 207 --json labels` now returns `risk: layout-data` and `risk: migration`; the `high-risk-evidence` gate run 33884482133 consequently became active and failed exactly as expected, citing "changes after the audited Head SHA are not docs-only … re-audit against the new head" and the stale CI-run reference. This record resolves that.
- **`e38c92aed4` (api35 lane failure)** — `TwoPanelOrientationCaptureInstrumentationTest.ensureLauncherRow` previously reused the first row of an unordered `favorites` query, which under lane ordering could be a folder-child row left by an earlier instrumented class; rotation relayout legitimately rewrites folder children's placement/modified, breaking the test's own-row no-write assertion. The fix queries `_ID, CONTAINER` ordered by `_ID` and reuses only `CONTAINER_DESKTOP`/`CONTAINER_HOTSEAT` rows (inserting a locked hotseat row otherwise). The stale-rejection and no-write assertions themselves are unchanged — the fix removes a nondeterministic row choice, it does not weaken the oracle. The api35 lane passed on the audited head in CI.

## Criteria check

(AC results below were established by full source/test inspection at `498ae8c0e1` — production files unchanged in the fix round — and re-confirmed by fresh local runs and green CI at `e38c92aed4`.)

- **AC-3 — PASS.** Unsupported/corrupt/newer/removed selection fails closed at the composition layer: `OrganizationInputComposer.kt:190–212` (A read: `Unreadable` → `NotReady(SourceUnreadable)`, `UnsupportedSchema` → `NotReady(UnsupportedVersion)`, selection outside `bundle.layoutStrategies.runtimeSupported` → `NotReady(UnsupportedVersion)` with the selection identity — never a default fallback), mirrored on the B read at `:255–274`; mid-cut change → one bounded retry then `NotReady(InconsistentPolicyRead)` (`:275–281`, `:326–331`). V-20 defense-in-depth behind it: `PlanningValidation.kt:52–60` rejects non-`v2` rule version and any `organizationStrategy ∉ LayoutStrategyRegistry.acceptedIds` with `INVALID_RULES`; `DeterministicOrganizationPlanner.kt:33–38` dispatches only through the registry after validation. Tests fresh-passing: `OrganizationInputComposerTest` (17), `DeterministicOrganizationPlannerTest.catalogExternalStrategyIsRejectedInvalidThroughTheSeam` (45 in class). Downgrade cases 2/3 hold structurally (each binary reads its own built-in bundle; the store lives in `noBackupFilesDir`); case 1 reuses the existing `UnsupportedVersion` path.
- **AC-3b — PASS.** `LayoutStrategySelectionStore.kt:60–98`: write-time catalog validation before any storage access (`:66–68`; test asserts byte-identical file after rejection); atomic publish with monotonic generation + content digest (`:74–85`, `:143–177`); `WriteFailed` preserves the prior final file via `failWrite` (test `writeFailureKeepsTheExistingSelectionIntact`); success hands the caller a `Committed` snapshot and the published snapshot reaches planning only through the next composer read. Commit point is `finishWrite()` success with **no post-commit read-back** (contract test asserts exactly one `openRead()` per write). The UI picker is child 8 scope; no UI write path exists yet, so "UI never writes storage directly" holds and the only write entry point is the Rule-Management command.
- **AC-4 — PASS.** `OrganizationPlanner.plan(OrganizationInput)` remains the sole external seam; registry and `StrategyDefinition` are `internal`; the only registered executor is `PlanningPlacement::place` behind post-V-20 dispatch, so no bypass path exists. `PurityGuardTest` walks the entire `organizer/planning` directory and covers `LayoutStrategyRegistry.kt` (fresh-passing).
- **AC-5 — PASS (independently re-verified at both ends).** The golden payload losslessly serializes placements/newPages/newFolders/categories/warnings/rejections and excludes only the four metadata echoes — consistent with spec 182 §Compatibility requirement. The committed digest `0c98c7b9110a8cfdaa1319513d541569bd416b60fa94bc31f17fa17a02edb4a9` passes at the audited head (inside the fresh 890-test run), and the first audit pass additionally replayed the same corpus test in a detached `git worktree` at the pinned baseline `5fdab48082` (only the two oracle files + digest resource copied in): **1 test, 0 failures**. The oracle files are unchanged in the fix round, so the baseline-side evidence remains valid.
- **AC-6 — PASS.** Bundle digest covers catalog + default only (`PolicyModels.kt:85–118`; no selection input; coherence test `catalogIsPartOfTheBundleDigestButNotMutatedByItself`). Selection contributes its own fifth provenance row (`CompositionModels.kt:23–24`; `OrganizationInputComposer.kt:313–324`). The rules identity is the identity of the **effective** `RuleSemantics`: `PolicyModels.kt:195–210` computes SHA-256 over length-prefixed segments `bundleIdentity.semanticVersion | bundleIdentity.sha256 | selectionIdentity.sha256 | canonical(effectiveRuleSemantics)` — exactly the accepted formula, no raw concatenation, `versionOrGeneration = "v2"`; the bundle's pure projection is never reused. `EffectiveRulesIdentityTest` (6) pins all four formula directions. Result echo asserted in planner and projection tests.
- **AC-7 — PASS.** Selection is local (`noBackupFilesDir`, backup-excluded), schema-versioned (newer schema → `UnsupportedSchema` → typed `NotReady`, never rewritten), generation/digest-bearing with digest re-verification on read, and the fail-closed/migration matrix is covered by 8 fresh-passing store tests plus the composer mapping tests.
- **AC-9b — PASS.** `BuiltInOrganizerPolicyBundleSourceTest.runtimeSupportedCatalogDeclaresOnlyImplementedStrategiesWithTheDefault` asserts exact equality `LayoutStrategyRegistry.acceptedIds == runtimeSupported.toSet()`, `default ∈ runtimeSupported`, and `rules.organizationStrategy == default`; `LayoutStrategyCatalog.init` enforces the structural invariants. Fresh-passing.
- **FR-016 / docs — PASS.** requirements.md FR-016 (Later, spec accepted), CONTEXT.md term, DESIGN.md seam note, spec 10/12 authorized deltas, ADR 0012 accepted, `organizer-diagnostics.md` strategy-identity allowance in the same PR as required.
- **Diagnostics echo — PASS (concern resolved).** `RunVersions.strategyVersion` is allowlist-gated; `PlanningProjection` echoes the effective strategy on the three planning phases and redacts values outside the caller-supplied runtime set; the production call site now supplies the registry's runtime-enabled set, so the journal cannot record a strategy the binary does not execute. `PlanningProjectionTest` (9) covers echo for all outcomes plus redaction of catalog-external and approved-but-unimplemented IDs.
- **Contradiction scan vs accepted spec/ADR — no contradiction found** in the selection/bundle identity split, write authority, staged runtime catalog, three-case downgrade model, effective-rules formula, or `STRATEGY_PRESERVED` precedence.

## Executed test surface

Independent inspection: full `git diff 498ae8c0e1..e38c92aed4` read; changed files (`ManualOrganizationRun.kt` call site, the four instrumentation test files, `ensureLauncherRow` and its consuming test) read at head; PR labels and both CI runs checked via `gh`; first-pass full-diff source review (`git diff main...498ae8c0e1`, all production files cited above) remains valid because those files are unchanged.

Local runs at the audited head `e38c92aed4` (working tree clean apart from this docs-only audit update, JDK 21, Gradle 9.3.0):

- `./gradlew spotlessCheck --rerun-tasks` → **BUILD SUCCESSFUL in 8s** (forced fresh).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --rerun` → **BUILD SUCCESSFUL**; task executed (not cached); 79 result XMLs regenerated; **890 tests, 0 failures, 0 errors, 0 skipped**, including `GoldenOracleCorpusTest` (1), `LayoutStrategySelectionStoreTest` (8), `EffectiveRulesIdentityTest` (6), `PlanningProjectionTest` (9), `OrganizationInputComposerTest` (17), `BuiltInOrganizerPolicyBundleSourceTest` (6), `ContractShapeTest` (41), `PurityGuardTest` (1), `DeterministicOrganizationPlannerTest` (45).
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` → **BUILD SUCCESSFUL** (the first-pass blocker; task executed).
- `python3 tools/repo-contract/validate_repo_contract.py` → `repository contract OK` (rc=0).
- `python3 tools/repo-contract/validate_diagnostics_contract.py` → `PASS: No AC-67-12 contract violations found.` (rc=0).
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL**.
- First-pass evidence carried forward: baseline oracle replay in a detached worktree at `5fdab48082` → `GoldenOracleCorpusTest` 1 test / 0 failures (digest reproduced on pre-#182 code).

CI evidence at audit time:

- Run **33884482040** (`CI / ci.yml`, `pull_request`, `head_sha=e38c92aed40c642a54c66b9ed26f67bcced30631`, associated with PR #207 per the Actions API): **completed success**; `changes`, `validate-repo-contract`, `check-style`, `organizer-unit-tests`, `build-debug-apk`, all seven `organizer-instrumentation-*` lanes (including `api35`, which carried the `TwoPanelOrientationCaptureInstrumentationTest` fix), and **`final-status`: all pass**. Source jobs executed (not skipped).
- Run **33884482133** (`High-risk gate`, same head): **fail** — expected and correct: it detected the committed audit record still pointing at `498ae8c0e1` with non-docs changes after it and a stale CI reference. This updated record (docs-only on top of the audited head, Head SHA `e38c92aed4`, CI run 33884482040) satisfies the gate's requirements; the check goes green on the next push of this docs update.
- Historical: run 33874039993 (failed, superseded head `498ae8c0e1`) and gate run 33874040112 (passed as low-risk before labeling) remain records of the first pass.

## Findings

1. **Resolved — BLOCKING (instrumentation source set did not compile; `final-status` failed).** Fixed in `9278ee3444` as described in Scope; independently re-verified: local androidTest compile passes and CI run 33884482040 is fully green on the audited head, including `final-status` and all instrumentation lanes.
2. **Resolved — risk labels.** `risk: layout-data` and `risk: migration` are now set on PR #207; the `high-risk-evidence` gate is active and its current failure is precisely the stale-audit condition this record clears.
3. **Resolved — diagnostics echo redaction.** The production call site now passes `LayoutStrategyRegistry.acceptedIds` (the runtime-enabled truth) instead of a self-derived singleton; the contract-doc claim in §D-02 is now enforced end-to-end.
4. **Resolved — api35 lane failure (`e38c92aed4`).** `ensureLauncherRow` row-choice nondeterminism fixed without touching the test's stale-rejection or no-write assertions; the lane is green on the audited head. Residual (informational): the fix assumes rotation relayout never rewrites placement/modified of existing desktop/hotseat rows — the same assumption the test already made for its inserted row; if a future device matrix breaks it, the row should be created exclusively rather than reused.
5. **Informational — style surface gap.** `tests/organizer-instrumentation/**` is outside the spotless target list (`build.gradle:502` covers `lawnchair/src` and `tests/unit` only), so the fix round's misindented lines in `ProductionOrganizationInputInstrumentationTest.kt` (two call sites and the new fake class) are not machine-checked; cosmetic only. Similarly, two hand-built `PlanningResult` fixtures in the instrumentation tests keep `ruleVersion = RuleVersion("v1")` while their rules fixture moved to v2 — display-only fixtures, no contract impact.
6. **Audit lineage.** This record supersedes the NO-GO version at `498ae8c0e1` (committed `9278ee3444`); the audited Head SHA is the current PR head and only docs-only changes follow it. Any subsequent non-documentation change to this PR invalidates this assessment and requires another independent audit.

**Verdict: GO.** The child-2 contract — selection/bundle identity split, the exact effective-rules formula, the write-command semantics with `finishWrite` commit point and no post-commit read-back, composer fail-closed layering with V-20 behind it, catalog coherence (AC-9b), the registry-backed diagnostics echo redaction, and the byte-equivalence oracle independently reproduced from the pinned pre-#182 baseline — matches the accepted spec 182 / ADR 0012 at the source and test boundary. All first-pass findings are resolved and independently re-verified; local gates passed fresh (890 organizer tests / 0 failures, forced-fresh style, androidTest compile, both contract validators, APK assemble), and CI run 33884482040 is a successful `pull_request` `ci.yml` run on the audited head with `final-status` green and source jobs executed. PR #207 has sufficient independent high-risk evidence to merge once this docs-only record is pushed and the `high-risk-evidence` check re-runs green.
