# High-risk audit: PR #202 Semantic naming for Organizer-generated folders

> Status: ready — GO
> Audit date: 2026-09-04

- Auditor: Independent re-audit session separate from the implementation/review-fix sessions (solo-maintenance audit)
- PR: https://github.com/nunu1733/NunuLauncher/pull/202
- Head SHA: 6b44c5578836d6ac99954e2a8405aeb0c8fbf2ed
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33831906490
- Criteria: specs/201-generated-folder-semantic-naming/spec.md — FN-AC-01, FN-AC-02, FN-AC-03, FN-AC-04, FN-AC-05, FN-AC-06, FN-AC-07, FN-AC-08, FN-AC-09, FN-AC-10, FN-AC-11, FN-AC-12, FN-AC-13, FN-AC-14, FN-AC-15

## Scope

This is the second (re-audit) pass of the same independent session. The first audit covered the Issue #201 implementation diff `c83ead1d4b..3af33ca222` and returned **NO-GO** (recorded at audit commit `09136c639f`): the PR's own FN-AC-15 instrumentation change deterministically broke `ManualOrganizationPreferencesInstrumentationTest.previewRemainsReadableAtTwoHundredPercentFontScale`, failing CI run `33830673434` in `organizer-instrumentation-issue52-tests` and therefore `final-status`. The fix commit `6b44c55788` ("fix(201): align 200% font-scale test with concrete preview count contract") is now the audited head.

The cumulative diff `3af33ca222..6b44c55788` was independently verified to contain exactly three files: this audit record (docs-only, added by `09136c639f`), one plan.md line correcting the audit filename reference to `pr-202-generated-folder-semantic-naming.md`, and the single test-method change in `ManualOrganizationPreferencesInstrumentationTest.kt`. **No production code changed after the first audit**, so the first audit's source-level verification of the naming seam carries over in full.

The audited change set (41 files at `3af33ca222` plus the test fix) touches the persisted-layout write surface only through the content of `Favorites.TITLE` for newly inserted folders (`risk: layout-data` label confirmed on the PR): planner types (`PlanningResult.kt`, `PlanningPlacement.kt`), the application materializer/preview/protocol layer (`OrganizationPlanMaterializer.kt`, `PlanPreviewProjector.kt`, `PlanPreview.kt`, `LayoutApplicationModule.kt`, `PlanPreviewProtocol.kt`, new `FolderTitleResolver.kt`), the UI-layer production resolver (`GeneratedFolderTitles.kt`, `CategoryOverridePresentation.kt`, `OrganizationPreviewContent.kt`), outer composition (`LawnchairApp.kt`), en/ja strings, tests, and the superseded spec-194/195 contract lines plus `CONTEXT.md` / `DESIGN.md`. No schema, row codec, write-set format, recovery manifest, policy bundle, taxonomy version, or diagnostics contract is changed. No production code, test, or workflow file is modified by this audit; this record is the audit-session repository change.

## Criteria check

- **FN-AC-01 — PASS.** `spec.md` §Generated-folder naming contract and `plan.md` §Design define `NewFolder.naming: FolderNaming` as the authoritative semantic identity, `FolderTitleResolver` + materializer as the single resolution point, and preview/writer as plan readers only.
- **FN-AC-02 — PASS (contract-test side).** `OrganizationPlanMaterializer.materialize` takes `titleResolver` and calls `resolve` exactly once per planned folder (`OrganizationPlanMaterializer.kt:94-101`); the former double `placeholderFolder` construction is resolved (`newFolder` builds `base` once, then `base.copy(placement = ...)`); the resolved value is frozen as `title = OptionalText.Present(resolvedTitle)` on the `Insert` intended state (`:270`), replacing the hardcoded `"Folder"`. `GeneratedFolderNamingMaterializerTest.resolvesEachPlannedFolderExactlyOnceAndFreezesTitleIntoInsert` asserts N folders → exactly N `resolve` calls via `RecordingFolderTitleResolver` and title propagation. Re-run locally at `3af33ca222`: pass (861 organizer tests, 0 failures). The oracle's real-device Organizer run evidence is still to be recorded in the PR (see Findings 4).
- **FN-AC-03 — PASS.** `CategoryOverrideCategoryPresentations.findForCategory` is a total lookup returning `null` for unknown categories (`CategoryOverridePresentation.kt:65`); `GeneratedFolderTitles.resolver` maps `null` to `organizer_generated_folder_fallback_name` via an exhaustive `when` with no exception catching and never returns a raw ID. `GeneratedFolderTitlesTest` (fake `StringProvider`) verifies known category → label resource, every bundle category never reaching the fallback, unknown `CategoryId` → fallback resource without raw-ID leak. Blank resolver output fails closed: materializer `Result.Invalid` (`OrganizationPlanMaterializer.kt:99`) and projector `Result.Invalid` for `Absent`/blank intended title (`PlanPreviewProjector.kt:78-83`), each with a dedicated test.
- **FN-AC-04 — PASS.** `formFolderGroups` groups by `(profile, category)` and skips the fallback category (`PlanningPlacement.kt:460-472`, unchanged convention); the corpus property test asserts per-folder `naming.category` equals the members' grouping category and `assertNotEquals(fallback, naming.category)` across all 64 generated fixtures (generator uses 2 categories and small folder capacities, so same-category splits occur); the title is a pure function of `naming`, so split folders share one deterministic title with no ordinal/suffix (also asserted by the `titlesByName` map in the materializer test).
- **FN-AC-05 — PASS.** `Preserve` carries the captured source state unchanged and `Update`'s intended state is `original.copy(placement = ...)` only (`OrganizationPlanMaterializer.kt:88-92,140-144`); title is set only on planned-folder `Insert`s. `GeneratedFolderNamingMaterializerTest.preserveAndUpdateActionsNeverAlterTitles` asserts `expected.title == intended.title` for every `Update`.
- **FN-AC-06 — PASS.** `NewFolderChange.name` is built from `insert.intended.title` (`PlanPreviewProjector.kt:78-91`); `Absent` or blank fails closed to `Result.Invalid` (→ `MATERIALIZATION_INVALID` in the preview protocol). `PlanPreviewProjectorTest` verifies name ↔ intended-title equality (`通信`) and both fail-closed cases. No category→name mapping exists outside `GeneratedFolderTitles` (grep: `resolve` is called only in the materializer).
- **FN-AC-07 — PASS.** `PlannerGeneratedPropertyTest.generatedCorpusFolderNamingMatchesGroupingCategory` exists and passes; the existing determinism/metamorphic/idempotence suite is untouched by the diff and passed in the local re-run.
- **FN-AC-08 — PASS.** Existing application protocol/verification tests (e.g. `NewFolderCanonicalOrderProtocolTest`) are not in the diff and passed unchanged in the local re-run; the DB-side exact verification already covers TITLE via the existing row codec (plan §Current evidence trace).
- **FN-AC-09 — PASS.** `OrganizationPreviewContentTest.newFolderRowListsPlacementAndMembers` updated to the named format (`Create new folder “Communication” at …`); group order / row order / count truth / truncation tests are unchanged apart from the fixture `name` field. The instrumentation concrete-list test (`previewDetailsRenderConcreteChangeListMatchingPreviewCounts`) was updated to the named row and passed in the CI lane.
- **FN-AC-10 — PASS.** `FolderNaming` is a closed sealed interface (v1 `FromCategory` only) on `NewFolder` and therefore on `ValidatedLayoutPlan.newFolders` with the same type (read back from `plan.newFolders` in the materializer test); resolver `when` exhaustiveness is compile-enforced in `GeneratedFolderTitles` and the test fake. Note: the `ContractShapeTest` extension is fixture-level (naming added to constructed `NewFolder`s); the shape guarantee is realized through compile-time exhaustiveness plus the materializer contract test rather than a dedicated new shape method.
- **FN-AC-11 — PASS.** The only new user-facing source string is `organizer_generated_folder_fallback_name` (en "Folder" / ja "フォルダ"); `manual_organization_preview_new_folder_row` was reworded in both `values/` and `values-ja/`.
- **FN-AC-12 — PASS.** The superseded contract lines in `specs/194-plan-preview-seam/spec.md` and `specs/195-organizer-confirmation-change-list/spec.md` are updated with Issue #201 markers; `CONTEXT.md` gains the 生成フォルダ名 term; `DESIGN.md` §4.1/§4.2 mention the naming seam and the outer-composition injection.
- **FN-AC-13 — PASS.** The resolved title is frozen into the plan at materialize time; the unchanged coordinator test `ManualOrganizationRunTest.previewedPlanIsAppliedDirectlyWithoutConfirmTimeMaterialization` asserts `lastAppliedPlan === previewedPlan`, and combined with the resolve-once counter this establishes that a locale change between preview and confirm neither re-materializes nor re-resolves. No re-resolution path exists in projector/writer code.
- **FN-AC-14 — PASS.** `grep -rn "import app.lawnchair.organizer.ui" lawnchair/src/app/lawnchair/organizer/application/` returns nothing; `LawnchairApp.kt:114` (outer composition) builds `GeneratedFolderTitles.resolver(this)` and injects it into `LayoutApplicationModule.production(this, resolver)`; `production(...)` has no resolver default, and all eight `production(` call sites (production + instrumentation) pass it explicitly.
- **FN-AC-15 — PASS (fixed at `6b44c55788`).** The first audit found `previewRemainsReadableAtTwoHundredPercentFontScale` deterministically failing because it injected the concrete change list while still asserting the Summary-derived count-only row. The fix replaces that assertion with the concrete-mode contract: `manual_organization_moved_count(2)` (the `PreviewCounts` header) displayed and `onAllNodesWithText(moved_single_placement(1)).assertCountEquals(0)` — exactly mirroring the sibling `previewDetailsRenderConcreteChangeListMatchingPreviewCounts` per spec 195 §D2 ("Summary-derived change-count lines never mix with PreviewCounts truth") — while keeping the named new-folder row ("Communication") + confirm/cancel displayed assertions at `Density(1f, fontScale = 2f)`. The other new test `generatedFolderTitlesResolveActualResourcesAndFallback` verifies ja/en actual-resource resolution via `createConfigurationContext` and unknown `CategoryId` → generic fallback without raw-ID leak. CI run `33831906490` `organizer-instrumentation-issue52-tests` passed with all 27 tests in the class completing (0 failures), so the FN-AC-15 evidence lane is green on the audited head.

## Executed test surface

Independent inspection: full `git diff c83ead1d4b..3af33ca222` and `git diff 3af33ca222..6b44c55788` read; source files `PlanningResult.kt`, `PlanningPlacement.kt`, `OrganizationPlanMaterializer.kt`, `PlanPreviewProjector.kt`, `PlanPreview.kt`, `LayoutApplicationModule.kt`, `PlanPreviewProtocol.kt`, `FolderTitleResolver.kt`, `CategoryOverridePresentation.kt`, `GeneratedFolderTitles.kt`, `OrganizationPreviewContent.kt`, `LawnchairApp.kt`, both strings files, and the new/updated test files read at the audited heads; import-direction grep as cited above.

Local runs at the first audited head `3af33ca222` (JDK 21.0.12, Gradle 9.3.0):

- `./gradlew spotlessKotlinCheck spotlessJavaCheck --rerun-tasks` → **BUILD SUCCESSFUL** (5 actionable tasks: 5 executed; forced fresh execution).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --rerun` → **BUILD SUCCESSFUL**; all 75 result XMLs regenerated; **861 tests, 0 failures, 0 errors, 0 skipped**.
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL**.

Local runs at the re-audited head `6b44c55788` (working tree clean):

- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` → **BUILD SUCCESSFUL** (387 actionable tasks: 14 executed, 3 from cache, 370 up-to-date) — the fixed instrumentation test compiles.
- `./gradlew spotlessKotlinCheck spotlessJavaCheck --rerun-tasks` → **BUILD SUCCESSFUL** (5 actionable tasks: 5 executed; forced fresh).

Qualifying CI evidence for the audited head — run `33831906490` (`.github/workflows/ci.yml`, `pull_request`, PR #202, branch `issue-201-generated-folder-semantic-naming`, head `6b44c5578836d6ac99954e2a8405aeb0c8fbf2ed`), completed **success**:

- `changes`, `validate-repo-contract`, `check-style`, `organizer-unit-tests`, `build-debug-apk`: **success**;
- `organizer-instrumentation-issue52-tests`: **success** (27/27 tests in `ManualOrganizationPreferencesInstrumentationTest`, including the fixed font-scale test and the new actual-resource/fallback test);
- `organizer-instrumentation-api35-tests`, `-shared-writer-tests`, `-db-migration-tests`, `-issue53-tests`, `-issue99-tests`, `-issue155-tests`: **success**;
- `final-status`: **success**.

Lineage note: the first attempt of run `33831906490` failed only in `organizer-instrumentation-issue53-tests` (5 `OnboardingOrganizationProposalInstrumentationTest` real-touch tests with `launcherWindowFocus=false`, `events=[]`, and an emulator console startup error — an environmental emulator window-focus failure in a class this PR does not touch; the same lane passed on the preceding commit). The auditor re-ran the failed jobs on the identical SHA; the re-run completed with every job green. Run `33830673434` (first audited SHA `3af33ca222`) remains the failed run that motivated the fix and is superseded by this record.

## Findings

1. **Resolved Blocking — FN-AC-15 font-scale instrumentation test (first-audit finding).** The stale count-only summary-row assertion in concrete-preview mode was replaced with the spec 195 §D2-consistent contract (header count displayed, summary line absent, named row + actions displayed); the issue52 lane is green on the audited head.
2. **Resolved Blocking — qualifying CI evidence (first-audit finding).** CI run `33831906490` is a completed successful pull_request-event `ci.yml` run on audited SHA `6b44c5578836d6ac99954e2a8405aeb0c8fbf2ed` with `final-status` green and all source jobs executed. Only this docs-only audit commit follows the audited head.
3. **Informational — issue53 lane environmental flake.** `OnboardingOrganizationProposalInstrumentationTest` real-touch tests failed once on emulator window-focus (`launcherWindowFocus=false`, zero injected events) and passed unchanged on a same-commit re-run. Not a PR #202 defect; worth tracking as a connected-test stability observation if it recurs.
4. **Informational — real-device evidence still open.** The PR's manual-verification checkbox (real-device Organizer run for generated folder names / TalkBack, FN-AC-02 oracle) is unchecked; it must be recorded in the PR before merge per the PR's own checklist.
5. **Informational — audit filename lineage.** `plan.md` now references `docs/assessment/pr-202-generated-folder-semantic-naming.md` (PR-number-keyed, matching `validate_high_risk_evidence.py`); the first audit's note about the stale `pr-201-*` reference is closed.

**Verdict: GO.** The production naming seam (planner semantic identity, single materializer resolution, fail-closed blank/unknown handling, preview/apply title equality, dependency direction, docs/supersession updates) verified clean at the source/test boundary and every locally re-runnable gate passed fresh; the first audit's blocking FN-AC-15 defect is fixed with a test-only change, and CI run `33831906490` is green on the audited head including `organizer-instrumentation-issue52-tests` and `final-status`. PR #202 has sufficient independent high-risk evidence to merge once the PR's own real-device evidence item (Finding 4) is recorded. Any subsequent non-documentation change to this PR invalidates this assessment and requires another independent audit.
