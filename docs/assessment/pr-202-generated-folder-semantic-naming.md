# High-risk audit: PR #202 Semantic naming for Organizer-generated folders

> Status: ready — GO
> Audit date: 2026-09-04

- Auditor: Independent re-audit session separate from the implementation/review-fix sessions (solo-maintenance audit)
- PR: https://github.com/nunu1733/NunuLauncher/pull/202
- Head SHA: a5ac70d94a2aae74411211ef8d860fed3f40b372
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33839475496
- Criteria: specs/201-generated-folder-semantic-naming/spec.md — FN-AC-01, FN-AC-02, FN-AC-03, FN-AC-04, FN-AC-05, FN-AC-06, FN-AC-07, FN-AC-08, FN-AC-09, FN-AC-10, FN-AC-11, FN-AC-12, FN-AC-13, FN-AC-14, FN-AC-15

## Scope

This is the third pass of the same independent audit session. Lineage: the first audit at `3af33ca222` returned NO-GO (audit commit `09136c639f`, failing CI run `33830673434`) because the PR's own FN-AC-15 font-scale instrumentation test was deterministically broken; the fix `6b44c55788` was re-audited GO (audit commit `6b2f4100a6`, green CI run `33831906490`). A subsequent implementation review then requested changes (PR comment thread), which are now the audited head `a5ac70d94a` ("fix(201): render planned-folder destinations by resolved name; split-title end-to-end test"). Because production code changed in this round, the previous GO record is superseded by this one.

The review-round diff `6b44c55788..a5ac70d94a` was independently verified to contain: `PreviewFolderRef.Planned(ordinal, name: PreviewLabel)` (`PlanPreview.kt`); a projector-side `NewFolderOrdinal → resolved PreviewLabel` join built from the Insert intended titles with fail-closed join misses (new `plannedFolderNames` function, `PositionContext` parameter, `PlanPreviewProjector.kt`); planned-folder destination rendering by name in `OrganizationPreviewContent.kt`; `manual_organization_preview_position_folder_planned` switched `%1$d`→`%1$s` in `values/` and `values-ja/`; `GeneratedFolderTitles.resolver(Context)` now resolving against the given context as-is (production path exercised by instrumentation with locale contexts); the new `SplitFolderNamingEndToEndTest`; projector/content/instrumentation test updates; and spec 201 §Preview integration + FN-AC-04/FN-AC-15 oracle amendments plus the spec 194 `PreviewFolderRef` block update. No planner, materializer, protocol, wiring, schema, policy-bundle, or diagnostics code changed in this round.

The cumulative audited change set (`c83ead1d4b..a5ac70d94a`) touches the persisted-layout write surface only through the content of `Favorites.TITLE` for newly inserted folders (`risk: layout-data` label confirmed on the PR). No production code, test, or workflow file is modified by this audit; this record is the audit-session repository change.

## Criteria check

- **FN-AC-01 — PASS.** `spec.md` §Generated-folder naming contract and §Preview integration (now including the `PreviewFolderRef.Planned(ordinal, name)` clause) and `plan.md` §Design define the authoritative source, single-resolution rule, and read-only preview/writer structure.
- **FN-AC-02 — PASS (contract test + real-device evidence).** `OrganizationPlanMaterializer.materialize` resolves exactly once per planned folder (`OrganizationPlanMaterializer.kt:94-101`), freezes the title into the `Insert` intended state, and the double-construction is eliminated; `GeneratedFolderNamingMaterializerTest` asserts N folders → N `resolve` calls and title propagation (re-run locally fresh: pass). The previously open real-device evidence is now recorded in the PR comment thread: Pixel 9a (ja locale) Organizer run showing named preview rows (`新規フォルダ「仕事効率化」…`, `新規フォルダ「ソーシャル」…`), successful apply with internal verification, named home-screen folders, and unchanged existing 「Google」 folder; the PR body checkbox is ticked.
- **FN-AC-03 — PASS (strengthened).** Total lookup `findForCategory` (`null` for unknown, no exception catching), deterministic `null` → `organizer_generated_folder_fallback_name` mapping, and blank fail-closed at materializer and projector are unchanged and re-verified. Raw-identifier non-exposure is now stronger: planned-folder move destinations carry the resolved `PreviewLabel` instead of the planner ordinal, and no `%1$d` folder-position string remains in either locale (grep clean).
- **FN-AC-04 — PASS (strengthened).** `SplitFolderNamingEndToEndTest.capacityOverflowSplitsOneCategoryIntoFoldersWithIdenticalTitles` drives the real `DeterministicOrganizationPlanner` with folder capacity 2×2 and nine same-category (COMMUNICATION) apps, asserts `newFolders.size >= 2` (a genuine split), then materializes through the real `OrganizationPlanMaterializer` and asserts: exactly one `resolve` per split folder, all `Insert` titles identical, non-blank, and containing no digits (no ordinal/suffix), and every split folder's `naming == FromCategory(COMMUNICATION)`. The companion `dispositionIsFolderMemberForAllSplitMembers` guards that the fixture really routes members through the folder-member path. Both pass in the fresh local run. The fallback-category skip convention remains asserted by the corpus property test.
- **FN-AC-05 — PASS.** `Preserve` carries the captured source state and `Update`'s intended state is `original.copy(placement = ...)` only; `GeneratedFolderNamingMaterializerTest.preserveAndUpdateActionsNeverAlterTitles` asserts title equality. Unchanged this round; re-passed locally.
- **FN-AC-06 — PASS (strengthened).** `NewFolderChange.name` and the new `PreviewFolderRef.Planned.name` are both sourced from the single `plannedFolderNames` join over the Insert intended titles; missing Insert / `Absent` / blank title / join miss all fail closed to `Result.Invalid` (`PlanPreviewProjector.kt:54-95,172-195,254-260`; destination null propagates through `itemRows` → `?: return Result.Invalid`). `PlanPreviewProjectorTest.moveIntoPlannedFolderRendersTheResolvedFolderName` verifies a move-into-planned-folder destination equals the folder row's resolved name (`ソーシャル`); the Absent/blank fail-closed tests remain. No category→name re-derivation exists outside `GeneratedFolderTitles`.
- **FN-AC-07 — PASS.** `generatedCorpusFolderNamingMatchesGroupingCategory` plus the untouched determinism/metamorphic/idempotence suite passed in the fresh local re-run (864 organizer tests, 0 failures).
- **FN-AC-08 — PASS.** Existing application protocol/verification tests are not in the diff and passed unchanged in the local re-run; DB-side exact verification covers TITLE via the existing row codec.
- **FN-AC-09 — PASS.** `OrganizationPreviewContentTest` covers the named new-folder row and now the named planned-folder destination (`dockPositionsAreOneBasedAndPlannedFoldersRenderByName`); group order / row order / count truth / truncation invariants are unchanged. The instrumentation concrete-list and font-scale tests passed in the CI lane.
- **FN-AC-10 — PASS.** `FolderNaming` remains a closed sealed interface with compile-enforced exhaustive `when` in production and test resolvers; the same type propagates onto `ValidatedLayoutPlan.newFolders` (read back in the materializer and split tests).
- **FN-AC-11 — PASS.** Still exactly one new user-facing source string (`organizer_generated_folder_fallback_name`, en/ja); this round only reworded the existing `folder_planned` position string in both locales (no new source).
- **FN-AC-12 — PASS.** spec 194 / spec 195 supersession lines, `CONTEXT.md` term, and `DESIGN.md` seam notes are present; this round additionally amended spec 194's `PreviewFolderRef` block and spec 201's §Preview integration to document the named-destination contract with the review-round change history entry.
- **FN-AC-13 — PASS.** Title remains frozen into the plan at materialize time; `ManualOrganizationRunTest.previewedPlanIsAppliedDirectlyWithoutConfirmTimeMaterialization` (same plan instance applied) plus the resolve-once counter establish no re-resolution on locale change. Unchanged this round; re-passed locally.
- **FN-AC-14 — PASS.** Re-verified at the new head: `grep -rn "import app.lawnchair.organizer.ui" lawnchair/src/app/lawnchair/organizer/application/` returns nothing; `LawnchairApp.kt` still injects `GeneratedFolderTitles.resolver(this)` into `LayoutApplicationModule.production(this, resolver)` (the `resolver(Context)` simplification keeps production behavior identical — the Application's resources carry the process locale).
- **FN-AC-15 — PASS (strengthened).** The instrumentation `generatedFolderTitlesResolveActualResourcesAndFallback` now exercises the production `GeneratedFolderTitles.resolver(Context)` adapter itself with `createConfigurationContext` ja/en contexts (asserting 「通信」/“Communication” and unknown-category fallback without raw-ID leak), and the 200% font-scale concrete-preview test asserts the named new-folder row + confirm/cancel displayed with the spec 195 §D2 count contract. CI run `33839475496` `organizer-instrumentation-issue52-tests` passed with all 27 tests in the class completing (0 failures) on the audited head.

## Executed test surface

Independent inspection: `git diff 6b44c55788..a5ac70d94a` read in full; changed production files (`PlanPreview.kt`, `PlanPreviewProjector.kt`, `OrganizationPreviewContent.kt`, `GeneratedFolderTitles.kt`, both strings files), the new `SplitFolderNamingEndToEndTest`, updated projector/content/instrumentation tests, and the spec 194/201 amendments read at head `a5ac70d94a`; ordinal/`%1$d` greps and the application→ui import grep as cited above; PR comment thread (review + device re-verification + TalkBack evidence) read.

Local runs at the audited head `a5ac70d94a` (working tree clean, JDK 21.0.12, Gradle 9.3.0):

- `./gradlew spotlessKotlinCheck spotlessJavaCheck --rerun-tasks` → **BUILD SUCCESSFUL** (5 actionable tasks: 5 executed; forced fresh).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --rerun` → **BUILD SUCCESSFUL**; all 76 result XMLs regenerated (05:15:47Z); **864 tests, 0 failures, 0 errors, 0 skipped**, including `SplitFolderNamingEndToEndTest` (2 tests, 0 failures).
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL**.
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` → **BUILD SUCCESSFUL**.

Qualifying CI evidence for the audited head — run `33839475496` (`.github/workflows/ci.yml`, `pull_request`, PR #202, branch `issue-201-generated-folder-semantic-naming`, head `a5ac70d94a2aae74411211ef8d860fed3f40b372`), completed **success**, all 13 jobs green:

- `changes`, `validate-repo-contract`, `check-style`, `organizer-unit-tests`, `build-debug-apk`: **success**;
- `organizer-instrumentation-issue52-tests`: **success** (27/27 tests, including the production `resolver(Context)` path and the font-scale named-row test);
- `organizer-instrumentation-api35-tests`, `-shared-writer-tests`, `-db-migration-tests`, `-issue53-tests`, `-issue99-tests`, `-issue155-tests`: **success**;
- `final-status`: **success**.

Lineage note: the first attempt of run `33839475496` failed only in `organizer-instrumentation-issue52-tests` — `changeListTraversalReachesExpandAndReviewActions` with `AssertionError: Failed to assert the following: (Focused = 'true')`. This is an unchanged DPAD-focus traversal test whose fixture contains six plain workspace moves (`newFolderCount = 0`, no planned-folder rows), so the review-round row-text change cannot affect it; the same test passed on both previously audited heads. The signature matches the emulator window-focus flake observed in the issue53 lane during the second audit. The auditor re-ran the failed jobs on the identical SHA and the lane completed 27/27 green. Prior runs `33830673434` (failed, superseded by fix `6b44c55788`) and `33831906490` (success at the superseded head `6b44c55788`) remain historical records.

## Findings

1. **Resolved — review Major 1 (planned-folder destination ordinal).** `PreviewFolderRef.Planned` now carries `(ordinal, name)`; the projector builds the name join from Insert intended titles with fail-closed misses; the UI renders the resolved name; `%1$d` folder-position strings are gone in en and ja; grep confirms no planner-ordinal reaches user-facing rendering (remaining `ordinal` uses are internal correlation/row ordering and the distinct spec 195 §D5 grid row-ordinal note). Verified by `moveIntoPlannedFolderRendersTheResolvedFolderName` and the device re-verification comment showing `→ 新規フォルダ「ソーシャル」` move rows.
2. **Resolved — review Major 2 (split-title end-to-end evidence).** `SplitFolderNamingEndToEndTest` forces a real capacity split through the real planner and asserts identical, digit-free Insert titles end-to-end; passes locally and is included in the green CI organizer-unit-tests job.
3. **Resolved — review Minor (production resolver path).** `GeneratedFolderTitles.resolver(Context)` resolves against the given context as-is and the instrumentation now exercises that exact production path with locale-aware contexts; production wiring behavior is unchanged.
4. **Resolved — device + TalkBack evidence.** The FN-AC-02/FN-AC-15 manual evidence (Pixel 9a ja run at `a5ac70d94a`, named destinations, apply verification, TalkBack readback of folder titles) is recorded in the PR comment thread and the PR body checkbox is ticked.
5. **Informational — connected-test focus flakiness across two lanes.** Emulator window/focus instability has now produced one-off failures in the issue53 real-touch lane (second audit) and the issue52 DPAD-traversal lane (this audit), each passing on a same-commit re-run. Not a PR #202 defect; worth a separate tracking issue if the pattern recurs.
6. **Informational — audit lineage.** This record supersedes `09136c639f` (NO-GO at `3af33ca222`) and `6b2f4100a6` (GO at `6b44c55788`); the audited Head SHA is the current PR head and only this docs-only audit commit follows it.

**Verdict: GO.** All three review requirements are implemented and independently verified at the source/test boundary; the naming seam, fail-closed joins, ordinal-free user-facing rendering, split-title determinism end-to-end, dependency direction, and spec supersession lineage all check out; local gates passed fresh (864 organizer tests / 0 failures, style, build, androidTest compile), and CI run `33839475496` is green on the audited head including `organizer-instrumentation-issue52-tests` and `final-status`, with the single traversal-lane flake and its same-commit re-run documented truthfully. PR #202 has sufficient independent high-risk evidence to merge. Any subsequent non-documentation change to this PR invalidates this assessment and requires another independent audit.
