# High-risk audit: PR #191 #181 audit follow-ups — codec KDoc confinement/waiver notes, #152 spec lock-state amendment

> Status: accepted
> Audit date: 2026-09-02

- Auditor: ZCode independent audit session (separate agent from the implementation session); the auditor made no production, test, or spec changes — the only artifact produced by this session is this audit record
- PR: https://github.com/nunu1733/NunuLauncher/pull/191
- Head SHA: 905ed162c8c9f7f6629d863aa4fb39b5b7325d81
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33591788714 (verified via the GitHub API as a completed, successful `pull_request` run of `.github/workflows/ci.yml` associated with PR #191, on the exact audited head SHA `905ed162c8c9f7f6629d863aa4fb39b5b7325d81` and head ref `issue-181-audit-followups`; `final-status` passed with `organizer-unit-tests`, `check-style`, `build-debug-apk`, `validate-repo-contract`, and all `organizer-instrumentation-*` lanes executed and green — none skipped. This is attempt 2: attempt 1 failed only in the `organizer-instrumentation-api35-tests` lane — one orientation-capture test outside the #181 scope (`TwoPanelOrientationCaptureInstrumentationTest.orientationChangeRejectsPreChangePlanAsStaleWithoutDbWrite`) asserted unequal row state amid emulator-infrastructure errors in the job log (`Unable to connect to adb daemon on port: 5037`; `Failed to start Emulator console for 5554`) — and the failed job was re-run successfully on the same head; the identical flake signature is documented in the merged PR-180 record. No #181-scope test was involved in the flake.)
- Criteria: docs/adr/0004-organizer-lock-persistence.md ADR-0004

Criteria-citation note: the owning issue #181 carries no `AC-NN` requirement IDs, and the affected spec `specs/152-reload-model-snapshot-verification/spec.md` numbers its criteria `AC-152-01`…`AC-152-06` (verified in-repo at spec.md L240–L269). The gate's requirement-ID grammar in `tools/repo-contract/validate_high_risk_evidence.py` (`_REQUIREMENT_ID_RE`, L94–L96) extracts only whole-token `FR-<n>` / `NFR-<n>` / `AC-<n>` / `ADR-<nnnn>` IDs from `Criteria:` lines — the hyphen-suffixed `AC-152-NN` family is invisible to it — so ADR-0004 is the machine-checked line: the governing accepted decision that lock truth lives in `favorites.organizerLockState`, directly relevant to the spec lock-state amendment verified below. Issue #181's three acceptance items and the #152 spec amendment are verified individually in "Criteria check". Same accepted format as the merged PR-180 record; this is a gate-format limitation, not an evidence gap.

## Scope

PR #191 resolves Issue #181 — the three non-blocking residuals from the independent audit of PR #180 (#152 model-snapshot verification). `git rev-parse HEAD` = `905ed162c8c9f7f6629d863aa4fb39b5b7325d81`, equal to `gh pr view 191 --json headRefOid`; `git status --porcelain` empty before this record was created; branch `issue-181-audit-followups`, base `main`, state OPEN. `gh pr view 191 --json labels` returns `[]` — the PR carries no `risk:` label; the gate classified it high-risk purely by path: `lawnchair/src/app/lawnchair/organizer/application/adapter/ModelProjectionCodec.kt` falls under `HIGH_RISK_PATH_PREFIXES[0]` (`lawnchair/src/app/lawnchair/organizer/application/`, validator L45–L50). An audit record is therefore required even for a KDoc-only delta.

The full diff (`gh pr diff 191`, two files, +41/−6) was read and verified line-by-line against the checked-out tree at head:

- `lawnchair/src/app/lawnchair/organizer/application/adapter/ModelProjectionCodec.kt` (+33/−2, single hunk `@@ -69,8 +69,39 @@`): the deleted lines are two KDoc prose lines ("Called from `LauncherModel`'s completion path…"); the 33 added lines are all ` * ` KDoc prose inside the object's block comment (checked-out file L64–L105): the corrected caller description (L72–L75), the thread-confinement note (L77–L86), and the test-coverage waiver note (L88–L104). No executable statement, no `import`, no annotation, no signature, and no string literal consumed at runtime changed — every changed line is inside the `/** … */` comment.
- `specs/152-reload-model-snapshot-verification/spec.md` (+8/−4, single hunk in the Scope bullet): "lock placement occupancy" is qualified (checked-out L80–L84: placements of locked items are verified via the placement and structure fields; the per-item organizer lock flag is not model-representable per ADR-0004 and is excluded from the model leg; the DB leg retains it), and the exclusion list gains "organizer lock state" and "raw folder ranks" (checked-out L88–L90). No other spec content changed.

Non-comment changes found in the delta: **none**. The compiled behavior surface is identical to the merged PR #180 head: `captureModelSnapshot`/`capture`/`projectItem` bodies (L113–L145, L147–L221) are byte-identical to the pre-PR versions (the diff touches no line outside the comment block), so no behavioral re-verification of the verification seam is required beyond regression execution.

## Criteria check

- **(a) Issue #181 item 1 — model-leg test matrix: waived on the issue with accurate capability claims — met.** The issue body (verified via `gh issue view 181 --json body`) item 1 allows "a Robolectric- or instrumentation-level per-kind matrix for the capture codec, **or a documented waiver**". The waiver is recorded on the issue in the "Waiver record correction (re-review) — Item 1" comment (2026-09-02T03:53:55Z), and its capability claims were cross-checked against the actual test files: `RealAdapterRowMatrixInstrumentationTest` exercises `RowManifestCodec` DB round-trips only (all its tests call `RowManifestCodec.capture`/`values`; the file never references `ModelProjectionCodec`); `ProductionPublicSeamInstrumentationTest` has exactly 4 tests (`contextMismatchIsRejectedByRealAdapterBeforeAnyWrite` L93, `noChangeApplyAndMissingRecoveryUseProductionAdapters` L107, `rowlessLogicalFirstPageAllocatesPlannedPageAfterFirstScreenId` L139, `committedCreatingBoundariesReconcileThroughPublicApplyAndRestartSeams` L182) — none asserts per-kind projection equality (they assert write-set preparation, `NoChanges`/`NotRestorable`, planned-page identity mapping, and checkpoint/restart reconciliation; the only snapshot-adjacent assertion is DB-manifest equality via `recaptureDb().manifest` L223–L227); `OrganizerReloadSupersessionTest.completedOutcomeCarriesSnapshotAndSupersededNeverDoes` (L458) covers snapshot transport/generation binding only (asserts non-empty delivered snapshot for the completed request, null for the superseded one — L480–L493). `grep -rn "ModelProjectionCodec\|captureModelSnapshot" tests/` finds no test usage, and a repo-wide file grep finds the symbol only in `ModelProjectionCodec.kt` itself and `LauncherModel.java` — so "no JVM unit test and no deterministic per-kind fixture" is accurate. The KDoc waiver text (L88–L104) states the same weaker claims and explicitly accepts the residual risk; it is not overstated.
- **(b) Issue #181 item 2 — spec amendment consistency: met.** The amended spec wording matches the pre-existing implementation: `lawnchair/src/app/lawnchair/organizer/application/protocol/ModelProjection.kt` KDoc L29–L32 already named "reserved regions, organizer lock state, raw folder ranks" among fields verified solely by the DB leg, and this PR does not touch that file (diff stat confirms only the two files above). ADR-0004 (`docs/adr/0004-organizer-lock-persistence.md`, `status: accepted`) stores lock truth in the dedicated `favorites.organizerLockState` column (L9–L17); the column constant is defined at `src/com/android/launcher3/LauncherSettings.java:357`. Grep over `src/` and `lawnchair/src/` for `ORGANIZER_LOCK_STATE`/`organizerLockState`: the only AOSP-side readers are schema/migration/statistics (`DatabaseHelper.java:288–290` upgrade, `GridSizeMigrationUtil.java:200,213` grid migration, `ModelDbController.java:917` count query) — no loader path (`LoaderTask`/`WorkspaceItemProcessor`/`LoaderCursor`/`ItemInfo`/`BgDataModel`) reads it, so the flag is genuinely not model-representable and the model-leg exclusion is fail-safe (a weaker model leg cannot produce false success; the DB leg keeps full canonical equality and the recovery manifest carries it — `RowManifestCodec.kt:212,244,362`, `RecoveryRecordCodec.kt:247,313,338`).
- **(c) Issue #181 item 3 — KDoc confinement statement matches actual code: met.** Each claim was verified against the code: (i) the capture runs on MODEL_EXECUTOR inside the #150 terminal boundary — `LoaderTask.java:429–437` posts `mLauncherBinder::notifyOrganizerReloadComplete` to `MODEL_EXECUTOR` only after `transactionCommitted`, and `BaseLauncherBinder.java:113–115` runs the lambda, which is the `() -> completeOrganizerReload(organizerToken, … captureModelSnapshot(mBgDataModel, mApp.getContext()))` built in `LauncherModel.startLoader` (`LauncherModel.java:421–426`); (ii) `synchronized(bgDataModel)` covers the `workspaceItems`/`appWidgets`/`collections` copies — `ModelProjectionCodec.kt:124–131`; (iii) `bgDataModel.collections` is read again outside the block at L135–L138 and `bgDataModel.lastLoadId` at L144, exactly as the KDoc states ("subsequent reads … are safe under this confinement"); (iv) `catch (Throwable)` returning null is fail-closed for capture exceptions — L114–L119. The KDoc correctly presents the `synchronized` block as defense-in-depth and states the off-executor-caller obligation; it does not claim `catch` substitutes for confinement.
- **(d) KDoc symbol accuracy: met.** `grep -rn verifyOrganizerWorkspaceReload` over the repo returns no matches (exit 1) — the nonexistent symbol from the earlier review round is gone; the KDoc names the real call path (`LauncherModel.startLoader(...)` organizer completion lambda → `captureModelSnapshot` → `completeOrganizerReload(...)`), matching `LauncherModel.java:415–426` and `:511`. The confinement note cites "Issue #181 item 3" and the waiver cites "item 1" — correct per the issue body (item 1 = model-leg test matrix, item 3 = capture confinement).
- **(e) No behavior change on a high-risk path file: met.** All changed codec lines are inside the KDoc block comment (Scope §); the executable surface of `ModelProjectionCodec.kt` is unchanged, and the full unit suite re-executed green on this head (below).
- **Issue record (acceptance hygiene): met.** The "Spec/Plan Decision — Issue #181" comment (2026-09-02T03:36:48Z) records Type: maintenance/chore, Requirement impact: none, Risk: low, the no-dedicated-spec reason, and the verification plan, as required by the workflow for an issue without a `specs/181-*` directory. Minor note in Findings §1: that decision comment's original Item 1 waiver table and Item 3 caller reference were superseded by the 03:53:55Z correction comment; the final head and the corrected comment are consistent.

## Executed test surface

Independent local execution on macOS arm64 at the clean checkout `905ed162c8` (`git status` clean before writing this record; all prior `build/test-results` purged at 13:48 JST so every cited XML is freshly generated by the runs below):

- `git rev-parse HEAD` — `905ed162c8c9f7f6629d863aa4fb39b5b7325d81`, equal to `gh pr view 191 --json headRefOid`; `gh pr view 191 --json state,labels` — OPEN, `[]`.
- `gh pr diff 191 --repo nunu1733/NunuLauncher` — full diff read; every changed line re-verified against the checked-out tree.
- `gh issue view 181 --json body,comments` — item numbering and waiver/decision records verified as above.
- `./gradlew spotlessCheck --rerun-tasks` — `BUILD SUCCESSFUL in 8s`; all five tasks (`spotlessJava`, `spotlessJavaCheck`, `spotlessKotlin`, `spotlessKotlinCheck`, `spotlessCheck`) genuinely executed (an initial plain run reported UP-TO-DATE, so execution was forced for independent evidence).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests "app.lawnchair.organizer.application.protocol.*" --rerun` — `BUILD SUCCESSFUL in 21s`; JUnit XML totals across the 10 `app.lawnchair.organizer.application.protocol.*` result files: **167 tests, 0 failures, 0 errors, 0 skipped** (ApplyProtocolTest 65, RecoveryProtocolTest 42, RestartReconcilerTest 16, RecoveryPreviewProtocolTest 14, ReadinessGateTest 13, ModelProjectionTest 5, LayoutApplicationModuleRecoveryEntryTest 4, NewFolderCanonicalOrderProtocolTest 2, RecoveryStoreReconciliationSessionTest 3, RunMutexRestoreSuspensionTest 3).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --rerun` (full unit suite) — `BUILD SUCCESSFUL`; JUnit XML totals across all 73 result files: **828 tests, 0 failures, 0 errors, 0 skipped**; all XMLs regenerated at 13:50 by this run (the 10 protocol files re-confirmed at 167/0/0/0 in the fresh set).
- CI: run `33591788714` (`pull_request`, `.github/workflows/ci.yml`) verified green on the exact head `905ed162c8…` after this record's audit pass — attempt 2, all 13 jobs success including `final-status`, `organizer-unit-tests`, `check-style`, `build-debug-apk`, `validate-repo-contract`, and every `organizer-instrumentation-*` lane (api35, issue52, issue53, issue99, issue155, shared-writer, db-migration). Attempt 1 had failed only in the api35 lane on the unrelated orientation-capture infrastructure flake recorded in the `CI run:` line above; the failed job was re-run successfully on the same head.

## Findings

No blocking findings. The delta is comments/docs-only on a high-risk path file, every KDoc and spec claim checked against the code is accurate, the issue record satisfies Issue #181's acceptance ("resolved or explicitly waived with a reason recorded on this issue"), and the full unit suite plus style check are green on the audited head.

### 1. Superseded text remains in the Issue #181 decision comment (non-blocking)

The 03:36:48Z "Spec/Plan Decision" comment still contains the original overstated Item 1 coverage table and names `LauncherModel.verifyOrganizerWorkspaceReload()` in its Item 3 section — both corrected at the 03:51:41Z re-review and superseded by the 03:53:55Z "Waiver record correction" comment, which the final head follows. GitHub comments are immutable history and the correction explicitly says the earlier claim was overstated, so this is acceptable; noted so a future reader weights the corrected comment.

### 2. Gate-format limitation persists

Same condition recorded in the PR-175 and PR-180 audits: `AC-152-NN` cannot be machine-cited by `_REQUIREMENT_ID_RE`, so this record carries ADR-0004 on the `Criteria:` line. The existing recommendation (extend the grammar or rename spec IDs) stands; out of scope for #181 per its Non-goals.

### Independence

This audit was performed in a ZCode session separate from the implementation session, after the head `905ed162c8` was finalized (three commits: `79b66f64ec` initial, `ee0a571335` review round 1, `905ed162c8` review round 2 corrections). The auditor changed no production, test, or spec file; the only artifact is this record, added in a docs-only commit after the audited SHA, which the gate's lineage rule permits.
