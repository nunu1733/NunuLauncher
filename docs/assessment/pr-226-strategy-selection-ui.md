# High-risk audit: PR #226 strategy selection and preview-explanation UI (child 8)

> Status: accepted — GO (re-audit at `41fbb5d9c7`; lineage: round 1 NO-GO at `fefc98dda5` → round 2 GO at `55c1d012c8` → owner review → round 3 NO-GO at `79816984c9` → round 4 GO, see Lineage below)
> Audit date: 2026-09-05

- Auditor: Independent subagent audit session with no involvement in the implementation session (solo-maintenance independent re-read + re-run; same role and criteria as the PR #207 / #221 / #222 / #223 / #224 / #225 audits; this is the fourth round of this PR's audit, conducted as a fresh independent session with no shared state with rounds 1–3)
- PR: https://github.com/nunu1733/NunuLauncher/pull/226
- Head SHA: 41fbb5d9c78514a282743ded080f22274363b323
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/33968778792 (`pull_request` `ci.yml` run with `head_sha = 41fbb5d9c78514a282743ded080f22274363b323`, associated with PR #226 on head ref `issue-218-strategy-selection-ui` — completed, conclusion success, all 13 jobs green including `final-status`, `organizer-unit-tests`, `check-style`, `build-debug-apk`, and all seven `organizer-instrumentation-*` lanes; no flaky re-runs were needed on this head. The issue52 lane (`organizer-instrumentation-issue52-tests`, job 101313502695) ran the three configured classes (`ManualOrganizationProductionE2EInstrumentationTest`, `ManualOrganizationPreferencesInstrumentationTest`, `StrategyPickerInstrumentationTest`) with `Starting 33 tests on emulator-5554` / `Finished 33 tests` / `BUILD SUCCESSFUL` in its log — the 27 pre-existing MFO-15/spec-195 cases plus all 6 `StrategyPickerInstrumentationTest` cases, including the four round-3 additions (`firstRunShowsTheBundleDefaultAsTheEffectiveSelection`, `strategyRowsExposeRadioSemanticsInsideASelectableGroup`, `failedReadShowsNoActiveSelection`, `strategyPickerIsWrappedInASelectableGroup`). The separate `high-risk-gate` run 33968778785 failed solely because the committed audit record still pinned the previous head `79816984c9` while code changed after it — the correct stale-record detection, resolved by this update.)
- Criteria: specs/182-layout-strategy-catalog/spec.md — FR-016, AC-3, AC-7, AC-8, AC-13, AC-15 (the §Selection contract write-authority items, §Preview integration, §Failure layering, and §Accessibility and localization paragraphs are cited for human review; the machine gate tokenizes the remaining IDs), specs/194-plan-preview-seam/spec.md — PP-AC-07, PP-AC-12, docs/adr/0012-versioned-layout-strategy-catalog.md — ADR-0012

## Lineage

Round 1 audited `fefc98dda5` → **NO-GO** (picker placement broke 12 spec 52 MFO-15 / spec 195 instrumentation tests; `contentDescription` suppressed the strategy name in TalkBack; new UI test unwired from CI). Round 2 audited `55c1d012c8` → **GO** (picker moved to the list end, `contentDescription` removed, test wired into the issue52 lane; committed as `ad6c3aeb52`). Owner review then requested changes (Blocking 0 / Medium 2 / Low 1): first-run must display the bundle default as the effective selection while a failed read stays hidden; the five rows should form one mutually-exclusive radio group (`selectableGroup`); re-tapping the selected strategy should be a no-op. Round 3 audited the owner-review fix `79816984c9` → **NO-GO**: the first-run fix conflated a failed read with first-run absent (elvis fall-through displayed the default on read failure — fail-closed regression), `selectableGroup()` on the section header was a provable no-op in Compose 1.10.1, and `strategyPickerItems` gained an unused `default` parameter. Fix commits `a1f20694db` (tri-state + single-item selectableGroup + corrupt-store regression test), `1ecd0f38b6` (unmerged-tree assertion), and `41fbb5d9c7` (testTag restored on the picker container) address all three; this round independently re-audits the full PR at the new head.

## Scope

`git diff b16390f5a7..41fbb5d9c7` = three code commits for the feature plus its fixes (`fefc98dda5`, `55c1d012c8`, `79816984c9`, `a1f20694db`, `1ecd0f38b6`, `41fbb5d9c7`) plus docs-only audit-record commits. The round-4 delta `ad6c3aeb..41fbb5d9c7` touches three files: `ManualOrganizationPreferences.kt` (+92/−33 net across the owner-review and round-3 fix commits), `StrategyPickerInstrumentationTest.kt` (+85), and the audit record itself. No file under `organizer/application/**`, `organizer/integration/**`, `organizer/rules/**` (store/composer/bundle), Launcher3 provider/model/writer, backup, deck, DB schema, or the policy catalog is changed at any point in the PR; the only persisted-state write the UI issues remains the existing Rule Management `select()` command. `ci.yml` is unchanged after the round-1 lane wiring.

## Criteria check

- **Spec 182 §Selection contract write authority items 1–4 / AC-3 — PASS.** The only storage-write call site in the UI remains `LayoutStrategySelectionModule.store(context).select(id)` (verified by re-grep at `41fbb5d9c7`); UI state after success comes from the returned published snapshot (`write.snapshot.selection`), never the request. Store write-time validation, atomic publication, and failure-preserves-selection live unchanged in `LayoutStrategySelectionStore.kt` (child 2). Non-`Committed` results are ignored by the UI: previous selection stays displayed, no run restarts. The re-tap early-return (`if (id == selectedStrategy) return`) is a correct radio no-op — no new store generation, no restart for the already-effective strategy, including the first-run default case.
- **Spec 182 §Failure layering / fail-closed picker display — PASS (round-3 Blocking 1 resolved).** `readSelectedStrategy(context)` now returns the full `LayoutStrategySelectionSnapshot?` (read success → snapshot, including `selection = null` for first-run absent; read failure → null), and the picker state is the explicit tri-state `if (snapshot == null) null else snapshot.selection ?: strategyCatalog?.default`. A failed read can no longer fall through to the default: the elvis is guarded by the snapshot-null branch. The regression is pinned by the new instrumentation test `failedReadShowsNoActiveSelection`, which writes a corrupt `selection-v1` file and asserts `assertIsNotSelected()` on both the canonical and tidy rows — exactly the test whose absence let the round-3 regression through. The composer independently returns typed `NotReady` (`SourceUnreadable`/`UnsupportedVersion`), so the fail-closed double defense is coherent again.
- **Spec 182 §Accessibility / AC-13 (radio group) — PASS (round-3 Finding 2 resolved).** The picker is now a single `item(key = "strategy-picker")` containing `Column(Modifier.fillMaxWidth().testTag("manual-organization-strategy-picker").semantics { selectableGroup() })` with the header and all five rows as children: the marked node is now the semantics parent of the selectable rows, which is the position where Compose's `SelectableGroup` → `CollectionInfo`/`CollectionItemInfo` mapping is effective (the exact fix round 3 prescribed). Per-row semantics are unchanged (`selectable(selected, Role.RadioButton)` over merged name+description text — the round-2 TalkBack announcement contract holds; no `contentDescription` override exists). The group is asserted for real by `strategyPickerIsWrappedInASelectableGroup`: it fetches the unmerged-tree node by `testTag` and asserts `SemanticsProperties.SelectableGroup` via `config.contains(...)` — an assertion that fails if the modifier or tag regresses (the tag was indeed lost and restored in commits `1ecd0f38b6`/`41fbb5d9c7`, and the final form is the one verified here). Virtualization loss for 6 rows is negligible, as accepted in round 3.
- **Round-3 Finding 3 (unused `default` parameter) — RESOLVED.** `strategyPickerItems` again takes exactly `(catalog, selected, onSelect)`; the caller passes `catalog = strategyCatalog?.runtimeSupported` and the bundle default is consumed only in the selection-state computation, where it belongs.
- **Spec 182 §Preview integration "fresh compose/plan cycle" / spec 52 §Coordinator (no snapshot reuse) — PASS (unchanged).** A committed change while a run is active issues `dismiss()` then `start(trigger)`; `dismiss()` during `ApplicationInProgress`/`Applying` returns without state change and `start()` returns `Busy`, so an in-flight application is never restarted; `start()` always runs a fresh capture and the composer reads the store itself.
- **Spec 182 §Preview integration counts / spec 194 PP-AC-07, PP-AC-12 one-truth — PASS (unchanged).** `crossPageMovedCount` and `preservedByStrategyCount` are computed inside `PlanPreviewProjector.project` from the same `changes` list published as the rows (`PlanPreviewProjector.kt:129`); header and rows cannot diverge; `Summary`/`PreviewCounts` split untouched.
- **Spec 182 §Diagnostics strategy identity — PASS (unchanged).** `Summary.organizationStrategy` is coordinator UI state only; the sole diagnostics-side reference is the child-2 `PlanningProjection.kt:57` journal echo from `PlanningProjection`'s own result type (version-identifier allowance), not the UI state. No new journal fields.
- **Canonical executor aliasing — PASS.** `LayoutStrategyRegistry` maps `CATEGORY_CONTIGUOUS_V1` directly to `UnitOrdering.CATEGORY_CONTIGUOUS_PAGE_LOCAL` and `definition(id)` returns null for unknown IDs; `FullRunExecution`'s dispatch retains `else -> throw IllegalStateException(...)` (line 410) — no aliasing or silent fallback introduced anywhere in the PR.
- **Spec 182 §Preview integration "only supported strategies offered" / catalog coherence — PASS.** Rows come from `runtimeSupported` only; the displayed default is a member of that set (AC-9b), so first-run display adds no catalog-external row.
- **AC-15 / AGENTS.md high-risk evidence gate — MET.** `final-status` succeeded on the audited head `41fbb5d9c7` in the PR's own `pull_request` merge-gate run 33968778792, with source jobs executed; this record is the independent audit artifact.

## Executed test surface

Independent inspection (fresh session, no shared state with the implementation or prior audit rounds): full cumulative `git diff ad6c3aeb..41fbb5d9c7` read line-by-line (round-3/4 deltas), with the feature contracts re-verified by targeted re-reads of the current head rather than trusting earlier-round records: `ManualOrganizationPreferences.kt` tri-state initialization, `select()` call sites, `strategyPickerItems` signature and semantics chain re-read; `LayoutStrategyRegistry.kt` strategy definitions and `FullRunExecution.kt` dispatch re-read for aliasing; diagnostics consumption of `organizationStrategy` re-grepped under `organizer/diagnostics/`; `PlanPreviewProjector.kt` count derivation re-checked; all six `StrategyPickerInstrumentationTest` cases read against the behaviors they claim; `gh pr checks 226` and CI run 33968778792 verified via `gh run view --json` (head SHA `41fbb5d9c78514a282743ded080f22274363b323`, completed success) and the issue52 job log (`Starting 33 tests` / `Finished 33 tests` / `BUILD SUCCESSFUL`).

Local runs at the audited head `41fbb5d9c7` (working tree clean apart from this then-uncommitted updated audit record; JDK 21, Gradle 9.3.0):

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL** (also green as CI `check-style` on the exact head).
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'` → **BUILD SUCCESSFUL**; 0 failures (unit surface unchanged by the round-3/4 fixes, which are instrumentation-only; also green as CI `organizer-unit-tests` on the exact head).
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin` → **BUILD SUCCESSFUL**.
- `python3 tools/repo-contract/validate_repo_contract.py` → **OK**; `python3 tools/repo-contract/validate_diagnostics_contract.py` → **PASS** (also green as CI `validate-repo-contract` on the exact head).
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL** (also green as CI `build-debug-apk` on the exact head).
- No local emulator/AVD was available for `connectedLawnWithQuickstepGithubDebugAndroidTest`; substituted with CI evidence: the `organizer-instrumentation-issue52-tests` lane on run 33968778792 executed the full class list including `StrategyPickerInstrumentationTest`, 33/33 tests finished, job success — the connected surface of this exact head is green (as are all six other instrumentation lanes).
- `gh pr checks 226` at audit time → run 33968778792: 13/13 jobs pass including `final-status`; the only red check was `high-risk-evidence` (run 33968778785), failing solely on the stale audited-head record, resolved by this update.

## Findings

1. **Resolved (round-3 Blocking 1) — read-failure default display.** The tri-state branch (`snapshot == null → no selection`, `selection == null → bundle default`, else persisted selection) is implemented exactly as round 3 prescribed, and `failedReadShowsNoActiveSelection` pins the corrupt-store case (canonical and tidy rows both `assertIsNotSelected`).
2. **Resolved (round-3 Finding 2) — effective selectableGroup.** The picker is a single LazyColumn item whose `Column` carries `testTag` + `selectableGroup()` and is the semantics parent of all five rows; the group is asserted on the unmerged tree via `SemanticsProperties.SelectableGroup` in `strategyPickerIsWrappedInASelectableGroup`.
3. **Resolved (round-3 Finding 3) — unused `default` parameter** removed from `strategyPickerItems`.
4. **Carried from round 2 (unchanged, tracked, non-blocking):** write failures remain silent at the UI (spec-satisfying, follow-up live-region announcement recommended); `crossPageMovedCount` counts Workspace→Workspace moves only (`FolderMember` carries no page — recommend a spec/CONTEXT definition note); main-thread store read in `remember` (deviates from the `Dispatchers.IO` preference-read pattern; small file); `dismiss()`-during-`Recovering` entry point (pre-existing `dismiss()` semantics, DB safety preserved by application-layer gates); "Custom strategy" fallback copy nit; ja strings indentation nit.

**Verdict: GO** at `41fbb5d9c7`. All round-1 and round-3 findings are fixed and independently re-verified: the write-authority contract holds end-to-end (UI issues only the validated `select()` command, never touches storage or an in-run composed input), the fail-closed read-failure display is restored with a corrupt-store regression test, the radio-group semantics is now effective and asserted on the unmerged tree, the fresh dismiss+start cycle respects `ApplicationInProgress`, the PreviewCounts one-truth split and diagnostics isolation are untouched, and the canonical executor retains its `else -> throw` dispatch. The full PR's own merge-gate run 33968778792 is green on this head (`final-status` success, 13/13 jobs, issue52 lane 33/33 including the four new StrategyPicker cases), and all local style/unit/compile/validator/assembly checks pass fresh. Committing this record resolves the `high-risk-evidence` gate on re-run, matching the PR #222–#225 pattern.
