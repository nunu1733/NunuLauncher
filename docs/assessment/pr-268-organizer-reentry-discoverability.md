# Independent assessment: PR #268 (issue 232 re-entry discoverability)

- Date: 2026-09-10 (UTC)
- Auditor: independent session (general-purpose subagent dispatched by the owner workflow), not the implementing worker session
- Audited implementation Head SHA: `f3b40fb062c117894c056dcb9fcb31414b83ccb9` (PR #268 head at audit time)
- Follow-up audit-fix commit (post-audit, test/screenshot only): `adbdefc195` — extends the 200% font-scale test to exercise the hint and adds evidence 08. Executed by the worker, not re-audited line-by-line; its only content is the auditor's finding 1 remediation.
- Base SHA: `2c80f2142c2f9de5bd5c6c98db67b9d2a13cbf75` (main after spec PR #267 merge)
- Spec acceptance criteria referenced: [specs/232-organizer-reentry-discoverability/spec.md](../../specs/232-organizer-reentry-discoverability/spec.md) AC-1..AC-5 (accepted @ `86b356a8a3`, PR #267)
- Diff inspected: full. 12 files, +452/-7 at the audited head: `lawnchair/src/app/lawnchair/organizer/ui/OrganizationOnboardingProposal.kt`, `lawnchair/src/app/lawnchair/ui/preferences/destinations/HomeScreenPreferences.kt`, `lawnchair/res/values/strings.xml`, `lawnchair/res/values-ja/strings.xml`, `tests/organizer-instrumentation/.../OnboardingOrganizationProposalInstrumentationTest.kt`, 7 evidence PNGs. `git diff base..head` contains zero changes under `src/com/android/launcher3/` — upstream `AbstractFloatingView.java` is untouched.
- High-risk gate applicability: NOT APPLICABLE per [github-workflow.md](../project/github-workflow.md) §高リスクPRへの独立エビデンス要求/適用条件: no `risk: layout-data` / `risk: migration` label (verified via API), and no listed high-risk path (`organizer/application/**`, `LauncherProvider.java`, `ModelWriter.java`, `deck/**`, …) is touched. The change is presenter/UI + resources + tests + evidence. The `high-risk-evidence` run judged it low-risk and passed. This assessment was nonetheless produced per the user's explicit request, in the gate's format.
- Executed test surface (re-executed by the auditor on head `f3b40fb062`, JDK 21.0.12):
  - `./gradlew spotlessCheck --console=plain` → BUILD SUCCESSFUL
  - `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --console=plain` → BUILD SUCCESSFUL
  - `python3 tools/repo-contract/validate_repo_contract.py` → repository contract OK
  - `python3 tools/repo-contract/validate_diagnostics_contract.py` → PASS
  - `python3 tools/localization/verify_nunu_ja_resources.py --baseline 505dbc40e6154c05158b5d0271c45f6a885a411b` → exit 1 with 10 findings, all pre-existing `manual_organization_*` placeholder-contract mismatches identical on base; none involve `organization_onboarding_reentry_*`. No new findings introduced.
  - Connected instrumentation: the worker ran the full `OnboardingOrganizationProposalInstrumentationTest` suite 19/19 PASS locally on API 36 emulator `issue142_api36` (nunu_qpr2_api36_1 was excluded because another session owns it); the auditor read the five new tests and confirmed they assert the claimed behaviors. CI instrumentation jobs re-execute them independently.
- Worker post-audit execution (finding 1 remediation, head `adbdefc195`): `./gradlew spotlessCheck` → PASS; connected run of `realLauncherFloatingHostKeepsAllActionsWithinViewportAtTwoHundredPercentFontScale` (now including hint viewport/safe-area assertions) → PASS on `issue142_api36`; 200% screenshot captured as evidence 08.
- CI runs observed at audit time (head `f3b40fb062`, pull_request events):
  - CI run 34428785030: pass = build-debug-apk, check-style, organizer-unit-tests, validate-repo-contract, changes; instrumentation jobs pending at audit time. https://github.com/nunu1733/NunuLauncher/actions/runs/34428785030 — `final-status` is confirmed green by the merge operator before merge (it re-runs on the head incl. the audit-fix commit).
  - High-risk gate run 34428785066: completed/success. https://github.com/nunu1733/NunuLauncher/actions/runs/34428785066
- Evidence images verified: `docs/assessment/evidence/issue-232/01..08 .png` exist (145 KB–1.4 MB, sequential timestamps). The auditor read 02-reentry-hint.png (bottom-sheet hint "You can organize anytime" / "Long-press the Home screen → Home settings → Home screen → Organize home layout.") and 06-home-screen-general.png ("Organize home layout" row with unchanged subtitle inside the "General" section, above the fold). Evidence 08 shows the same hint at 200% font scale fully inside the viewport.
- Criteria mapping:
  - AC-1: PASS — `onLater` calls `controller.defer()` then `close(false)` then `showReentryHint()` (defer strictly before hint, OrganizationOnboardingProposal.kt); the hint writes no persistence and adds no run-journal event; `laterTapShowsTheReentryHintAndPreservesTheDeferOutcome` asserts DEFERRED survives hint show and Back close.
  - AC-2: PASS — Back (`canHandleBack` → upstream `close()` → `handleClose`), outside touch (`onControllerInterceptTouchEvent` closes on DOWN outside bounds and returns false so the gesture falls through), and timeout (`postDelayed` 6s) all funnel to `handleClose`, which only removes the view and restores focus; verified by `reentryHintDismissesOnOutsideTouchWithoutTouchingTheProposalOutcome` and evidence 02→03.
  - AC-3: PASS — `manual_organization_title` row moved from the `Layout` group to the end of the `General` group with byte-identical label/destination/subtitle; `homeScreenSettingsShowsTheOrganizerEntryInGeneralAboveTheFold` verifies placement via the accessibility tree; evidence 06.
  - AC-4: PASS — focus restore verified by `reentryHintRestoresFocusToThePreHintTargetWhenClosed`; DPAD/TalkBack reachability via the focusable title, `getAccessibilityInitialFocusView`, and `announceAccessibilityChanges`. The auditor's 200%-font-scale gap (finding 1) was closed post-audit by the extended 200% viewport test (hint bounds within viewport and safe area, both guidance lines laid out) plus evidence 08.
  - AC-5: PASS — `showOrganizationReentryHint` wraps display in `runCatching` with a `Log.w` handler; `hintDisplayFailureNeverUndoesTheDeferOutcomeOrCrashes` injects a factory failure through the `createHint` seam and asserts the store stays DEFERRED with no crash and no hint.
- Findings:
  1. Medium, resolved post-audit: AC-4's 200% font-scale reflow was not exercised for the hint at audit time. Closed by commit `adbdefc195` (extended 200% test + evidence 08).
  2. Info: the failure injection covers the `createHint` seam (construction failure). A hypothetical exception inside `show()` after `dragLayer.addView` would leave a partially attached view since onFailure only logs; no realistic throw point exists there today.
  3. Info: the ja localization script exits 1 on this head with the same 10 pre-existing findings as base/main; not introduced by this PR.
  4. Info (PR body only): the body's "[spec 161]" link points at the quality-strategy doc; cosmetic.
- Verdict: **PASS**. The implementation on the audited head conforms to accepted spec 232 (AC-1..AC-5): defer-then-hint ordering, persistence-free hint lifecycle with Back/outside/timeout routed through `handleClose`, runCatching-guarded best-effort display, General-section promotion with identical row semantics, dual-locale strings under the `organization_onboarding_` prefix, and no upstream or persistence-layer changes. The single substantive gap (200% evidence) was closed by `adbdefc195`. CI `final-status` on the final head must be green before merge per the normal merge gate.
