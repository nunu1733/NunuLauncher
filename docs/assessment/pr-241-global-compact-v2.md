# High-risk audit: PR #241 GLOBAL_COMPACT_V2 — existing 1×1 folders compact across pages (spec 237)

> Status: accepted — round-3 re-anchor GO, verdicts unchanged. Round 1 audited `eba38d573dce45fe3c10c778eb8813f7cce78547` (AC-10 open); round 2 audited `ccdb4c338946115276acefac43eadda108aa066f` (AC-10 closed with on-device evidence); this round-3 re-audit re-anchors to the current head after the docs-only follow-ups `100a521c22` and `bcc70c6138` — no source file changed since `ccdb4c3389`
> Audit date: 2026-09-07

- Auditor: Independent audit session (general-purpose subagent), not the implementing session — the same independent session performed round 1 (head `eba38d573dce45fe3c10c778eb8813f7cce78547`), round 2 (head `ccdb4c338946115276acefac43eadda108aa066f`), and this round-3 re-anchor; no involvement in the implementation
- PR: https://github.com/nunu1733/NunuLauncher/pull/241
- Head SHA: bcc70c61385c4a5c06fd93b025868b0a7540b217
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/34116566434 (`pull_request` `ci.yml` run verified via `gh run view`: completed, conclusion `success`, `head_sha = bcc70c61385c4a5c06fd93b025868b0a7540b217`, head branch `agent/issue-237-global-compact-v2-spec`; `final-status` green, and the source jobs `organizer-unit-tests`, `check-style`, `build-debug-apk`, `validate-repo-contract`, plus all seven `organizer-instrumentation-*` lanes executed and passed — see `gh pr checks 241` output in Executed test surface)
- Criteria: specs/237-global-compact-v2-folder-relocation/spec.md AC-1 AC-2 AC-3 AC-4 AC-5 AC-6 AC-7 AC-8 AC-9 AC-10 AC-11 AC-12
- Criteria: specs/182-layout-strategy-catalog/spec.md FR-016 AC-4 AC-5 AC-9 AC-15
- Criteria: docs/adr/0012-versioned-layout-strategy-catalog.md ADR-0012
- Criteria: docs/adr/0007-authoritative-organization-policy-sources.md ADR-0007

## Scope

The PR now spans seven commits; this record audits the head `bcc70c61385c4a5c06fd93b025868b0a7540b217` (local checkout verified at exactly this commit, clean tree).

Round-3 delta under audit (verified with `git diff ccdb4c3389..bcc70c6138`): `100a521c22` (committed the round-2 audit record update; replaced `237-after-page2.png` with a distinct re-capture) and `bcc70c6138` (plan.md documentation-consistency sync requested by the owner's docs review; audit-record sync). Changed files: `docs/assessment/pr-241-global-compact-v2.md`, `docs/assessment/ac14-device-evidence/237-after-page2.png`, `specs/237-global-compact-v2-folder-relocation/plan.md` — **no file under `lawnchair/` or `tests/` changed** (`git diff ccdb4c3389..bcc70c6138 -- lawnchair/ tests/` is empty), so every round-1/round-2 source conclusion applies byte-identically to this head. The plan.md sync was reviewed for accuracy and matches the verified implementation: Data-flow step 5 and the AC-4 row now state the settled member-recapture semantics (production `Preserved{NON_TARGET}` via `FullTargetSetMaterializer` + spec 10 precedence; direct-seam `Preserved{STRUCTURAL}`), the duplicated Oracle/`PlannerContractHarnessTest` change-set rows were deduplicated, the change set now records the `RunEvent.kt` allowlist fix and the anti-drift contract test from `ccdb4c3389`, and the documentation/checklist boxes reflect reality (954-test count matches this session's re-run). No discrepancy found in the sync.

Gate note: the evidence gate's `non_docs_files_after` backstop exempts only `docs/`, so the `specs/plan.md` sync commit tripped the high-risk check even though the delta contains no code — a mechanical classification, resolved by this re-anchor (per the gate's rule: any non-`docs/` change after the audited head requires re-audit against the new head).

Round-1 scope (audited at `eba38d573d`, 3 commits `f998da7dae` → `fbc5f3fca7` → `eba38d573d`; full detail in the round-1 record history below): the versioned successor strategy `GLOBAL_COMPACT_V2` (`LayoutStrategyRegistry` + shared `executeGlobalCompact`), bundle `organization-policy-v2.5`, truthful V1/V2 picker copy (en/ja), harness alignment to production recapture semantics (`PostPlanMaterializer`, `Oracle.checkIdempotence`, `CrossStrategyCorpusTest` derived formation-idempotence fixtures), picker instrumentation row, spec 237 + plan, `CONTEXT.md`/`requirements.md`. No file under `organizer/application/`, Launcher3 provider/model/writer, backup, or deck changed; golden corpus resources byte-identical; `determinePreservation` precedence unchanged.

Round-2 delta under audit (verified with `git show`):

- `47090e0f4d` — docs-only: committed the round-1 audit record (`docs/assessment/pr-241-global-compact-v2.md`). No source change.
- `ccdb4c3389` — code + evidence, three parts:
  1. **Diagnostics allowlist fix** (`lawnchair/src/app/lawnchair/organizer/diagnostics/model/RunEvent.kt`): `RunVersions.APPROVED_VERSIONS` gains `GLOBAL_COMPACT_V2`. Root cause verified by reading the code: `PlanningProjection.project` (`diagnostics/projection/PlanningProjection.kt:56`) constructs `RunVersions.create(strategyVersion = …)`, and `RunEvent.kt` `require(value in APPROVED_VERSIONS)` throws `IllegalArgumentException` for unapproved IDs — so every on-device `GLOBAL_COMPACT_V2` run aborted right after capture (run coroutine → `State.Cancelled`) while all planner/picker unit tests passed, because only the runtime diagnostics path constructs `RunVersions`. The one-line fix is exactly the ADR-0012 rule (a new strategy ID extends the approved-identifier set).
  2. **Anti-drift contract test** (`tests/unit/app/lawnchair/organizer/diagnostics/model/RunEventSerializationTest.kt`): new `everyRegisteredStrategyIsAnApprovedVersionIdentifier` iterates `LayoutStrategyRegistry.acceptedIds` and requires `RunVersions.create(strategyVersion = id.value)` to succeed for each, pinning allowlist ⊇ registry so this drift cannot recur (class 19/19 green).
  3. **AC-10 device evidence** (`docs/assessment/ac14-device-evidence/237-*.png`, six files): before pages 1–3 (2026-09-06 case rebuilt on a Pixel 9a: sparse front page, folder-dominated pages 2–3), V2 preview confirmation, applied result pages.

Runtime write-path audit of the delta: `RunEvent.kt` is diagnostics-only (no planner/writer/DB change); the PNGs are documentation artifacts. All round-1 conclusions (V1 byte-equivalence, idempotence proof, bundle coherence, harness/production alignment) apply verbatim to this head because the source delta since `eba38d573d` is confined to the diagnostics allowlist, one test file, docs, and PNGs (`git diff eba38d573d..ccdb4c3389 -- lawnchair/src tests/unit` touches only `diagnostics/model/RunEvent.kt` and `diagnostics/model/RunEventSerializationTest.kt`).

## Criteria check

Spec 237 (`specs/237-global-compact-v2-folder-relocation/spec.md`):

- **AC-1 — PASS.** Spec accepted; versioned-successor decision recorded with rationale (round-1 finding, unchanged at this head).
- **AC-2 — PASS.** `GlobalCompactStrategyTest.v2ExistingFolderJoinsCrossPageCompaction` and `v2ThreePageFolderHeavyLayoutFillsEarlierPagesFirst` run through the public seam `OrganizationPlanner.plan`; green at this head (suite 954/954). Additionally, the round-2 on-device run exercised the full user path end-to-end (capture → preview → apply) after the allowlist fix, which round 1 could not demonstrate.
- **AC-3 — PASS.** The no-gaps-behind property is pinned by the same fixtures plus the non-1×1 boundary tests; `CrossStrategyCorpusTest` exercises V2 over the full shared corpus via the registry.
- **AC-4 — PASS.** Folder identity/members/rank/profile assertions at plan level; no writer/application change in the entire PR diff, so folder relocation rides the existing workspace-unit write path (folder row cell/screen update semantics unchanged; member rows and container references structurally untouched).
- **AC-5 — PASS.** `v2FormationThenApplyThenRecaptureThenReplanIsAnEmptyDiff` pins formation → apply → recapture → replan as an empty diff with production recapture roles (formed folder `ALREADY_CANONICAL`, member `NON_TARGET`); corpus-derived `.idempotence` fixtures cover every strategy; the spec's 4-step fixed-point proof matches the shipped executor. The on-device preview's 新しいフォルダ: 1個 confirms formation occurs in practice and the suite owns its replan stability.
- **AC-6 — PASS.** Compositional: V2 emits `Moved{FOLDER_UNIT}` (plan-level tests); the strategy-agnostic projector's cross-page counting and the FOLDER_UNIT → `moveReasonFolderUnit` wording are pre-existing and tested. The device preview screenshot confirms the user-visible rows: four folders listed as 「フォルダ単位で移動」 with ページをまたいで移動: 5件.
- **AC-7 — PASS.** Golden corpus `tests/unit/resources/planner-golden-corpus/` (incl. `sha256.txt`) untouched since `main` (verified again on the round-2 delta diff); `GoldenOracleCorpusTest` green; the five pre-existing V1 test bodies unmodified; the shared-executor deltas remain provably no-ops for V1.
- **AC-8 — PASS.** `BuiltInOrganizerPolicyBundleSourceTest` asserts `organization-policy-v2.5` and `runtimeSupported == LayoutStrategyRegistry.acceptedIds` set equality with `default ∈ runtimeSupported`; green.
- **AC-9 — PASS.** Truthful localized V1/V2 copy (en/ja); `StrategyPickerInstrumentationTest` V2 row executed in CI's `organizer-instrumentation-issue52-tests` lane (green on both round-1 and round-2 heads).
- **AC-10 — PASS (closed; evidence caveat resolved).** The 2026-09-06 case was rebuilt on a physical device (Pixel 9a, 1080×2424 screenshots committed at `docs/assessment/ac14-device-evidence/`): before pages 1–3 (16:10; sparse page 1: 設定/Play ストア/Gmail/フォト; page 2: folders SNS/買い物/ツール; page 3: folder その他 + YouTube), preview (19:10; strategy ページ間でコンパクト, 整理対象 29件/3ページ, 移動 9件/保持 20件, 新しいフォルダ 1個, 新しいページ 0枚, ページをまたいで移動 5件, with SNS/買い物/ツール/その他 listed as フォルダ単位で移動 from pages 2–3 into page 1), after (19:12; front page filled with the four relocated folders, the new folder 仕事効… holding Play ストア and Gmail, plus YouTube). The result matches what the strategy name predicts. The round-2 evidence caveat (`237-after-page2.png` duplicating `237-after-page1.png`) was resolved at `100a521c2`: the file was replaced with the true post-apply state, and the device DB was queried directly — `favorites` contains exactly one `screen` (0) with the 8 top-level units, so the launcher holds a single workspace page after apply and no movable 1×1 top-level unit remains behind. This DB observation is the authoritative page-2 evidence, recorded in [Issue #237's AC-10 comment](https://github.com/nunu1733/NunuLauncher/issues/237#issuecomment-5569381592); no separate empty-page screenshot exists to take because page 2 no longer exists.
- **AC-11 — PASS.** Selection-layer fail-closed covered by the strategy-generic tests (`LayoutStrategySelectionStoreTest.unsupportedStrategyIsRejectedAtWriteTimeWithoutTouchingStorage`, `OrganizationInputComposerTest` unsupported-selection → `NotReady`), all green at this head.
- **AC-12 — PASS.** `risk: layout-data` label present; the verified successful `final-status` merge-gate run 34116566434 on this exact head; this independent audit record covers the head including the round-2 code change and the round-3 docs sync (the gate correctly failed after round 1 — a code commit followed the round-1 audited SHA — and after round 2 — the `specs/` plan sync counts as non-docs to the gate's backstop; re-audit/re-anchor performed each time, as required).

Spec 182 (`specs/182-layout-strategy-catalog/spec.md`): **FR-016, AC-4, AC-5, AC-9, AC-15 — PASS** (round-1 findings carry over: sole external planning seam intact; pinned golden-corpus byte-equivalence; every catalog strategy incl. V2 runs the shared contract/property suite through the public seam; successful `final-status` CI plus this independent record).

ADR-0012 (`docs/adr/0012-versioned-layout-strategy-catalog.md`): **PASS.** New ID, no rename/removal of V1, 1×1-only cross-page movers. Round 2 adds a direct application of ADR-0012's identity rule: the successor's ID extends the diagnostics `APPROVED_VERSIONS` set, and the new contract test enforces the registry↔allowlist coupling so future strategy IDs cannot drift.

ADR-0007 (`docs/adr/0007-authoritative-organization-policy-sources.md`) §8: **PASS.** Bundle published as `organization-policy-v2.5` with recomputed digest, no in-place migration, `rule-v2`/selection-store schema unchanged; downgrade safety remains selection-layer `NotReady`.

## Executed test surface

Round-3 commands (repo root at head `bcc70c61385c4a5c06fd93b025868b0a7540b217`, clean tree, `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`; the delta since `ccdb4c3389` is docs/PNG-only, so the gradle tasks were largely up-to-date cache hits — the commands still executed and passed against the exact audited tree):

- `git log --oneline -4 FETCH_HEAD` / `git rev-parse HEAD` → HEAD = `bcc70c61385c4a5c06fd93b025868b0a7540b217` (docs(237): sync plan and audit record with settled semantics and evidence).
- `git diff ccdb4c3389..bcc70c6138 --name-status` → exactly `docs/assessment/pr-241-global-compact-v2.md`, `docs/assessment/ac14-device-evidence/237-after-page2.png`, `specs/237-global-compact-v2-folder-relocation/plan.md`; `git diff ccdb4c3389..bcc70c6138 -- lawnchair/ tests/` → empty.
- `JAVA_HOME=… ./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'` → BUILD SUCCESSFUL, exit 0. JUnit XML totals: 88 test classes, **954 tests, 0 failures, 0 errors, 0 skipped**. Key classes: `RunEventSerializationTest` 19/19 (incl. `everyRegisteredStrategyIsAnApprovedVersionIdentifier`), `GlobalCompactStrategyTest` 11/11, `CrossStrategyCorpusTest` 3/3, `GoldenOracleCorpusTest` 1/1, `BuiltInOrganizerPolicyBundleSourceTest` 6/6, `LayoutStrategySelectionStoreTest` 8/8, `OrganizationInputComposerTest` 18/18.
- `JAVA_HOME=… ./gradlew spotlessCheck` → BUILD SUCCESSFUL, exit 0.
- `JAVA_HOME=… ./gradlew assembleLawnWithQuickstepGithubDebug` → BUILD SUCCESSFUL, exit 0.
- `python3 tools/repo-contract/validate_repo_contract.py` → `repository contract OK`, exit 0.
- `gh run view 34116566434 --repo nunu1733/NunuLauncher --json …` → `status: completed`, `conclusion: success`, `event: pull_request`, `headSha: bcc70c61385c4a5c06fd93b025868b0a7540b217`, workflow `CI`, head branch `agent/issue-237-global-compact-v2-spec`.
- `gh pr checks 241 --repo nunu1733/NunuLauncher` → all jobs of run 34116566434 pass: `organizer-unit-tests`, `check-style`, `build-debug-apk`, `validate-repo-contract`, `final-status`, and `organizer-instrumentation-{shared-writer,db-migration,api35,issue52,issue155,issue99,issue53}-tests`. The separate `high-risk-evidence` (High-risk gate) check fails at re-anchor time because the committed round-2 record pinned `ccdb4c3389…` and the `specs/plan.md` sync commit counts as non-docs to the gate's backstop — this updated record is the resolution.
- `shasum -a 256 docs/assessment/ac14-device-evidence/237-*.png` → all six committed PNGs have distinct SHA-256 (`237-after-page2.png` re-captured at `100a521c2`, SHA `3eaabc0d…`; visually verified as a distinct 19:30 capture of the single remaining home screen — consistent with the device-DB single-page observation; adb is not available in the audit environment, so the DB query itself is evidenced via the Issue #237 AC-10 comment, not independently re-executed).

Round-2 surface (executed at `ccdb4c338946115276acefac43eadda108aa066f`; merge-gate run of that round: run 34110363181, fully green): the same four gradle/python commands plus CI verification — valid for every source path, unchanged since.

Round-1 surface (executed at `eba38d573dce45fe3c10c778eb8813f7cce78547`; merge-gate run of that round: run 34088182723, also fully green): the same four gradle/python commands plus the first CI verification.

## Findings

1. **Round-1 open item AC-10 is closed** by the committed device evidence; verdicts are now PASS on all twelve spec 237 acceptance criteria. The interim evidence-quality caveat (`237-after-page2.png` duplicating `237-after-page1.png`) was resolved at `100a521c2` — the re-captured PNG was re-verified in round 3 (distinct SHA-256 `3eaabc0d…`; visual inspection shows the single remaining home screen at 19:30), and the device DB query recorded in the Issue #237 AC-10 comment shows `favorites` has exactly one screen after apply (no page 2 exists), which is the authoritative empty-later-pages evidence. No remaining caveat.
2. **Device crash root cause and fix (verified, in scope of this re-audit):** during the round-1→round-2 evidence run, every on-device `GLOBAL_COMPACT_V2` run aborted right after capture with `IllegalArgumentException: strategyVersion must be an approved version identifier, got 'GLOBAL_COMPACT_V2'` — `PlanningProjection.project` constructs `RunVersions`, whose `APPROVED_VERSIONS` allowlist lacked the successor ID (the planner and picker never construct `RunVersions`, which is why 953 unit tests missed it). Fixed in `ccdb4c3389` by adding the ID (ADR-0012 extension of the approved set) and pinned by the new registry-sweep contract test. Lesson recorded for future strategy enablements: enabling a strategy must extend the diagnostics allowlist in the same change — now enforced by test.
3. **Separate pre-existing app bug (follow-up issue, not a blocker):** during device triage, `LawnchairBugReporter` failed to persist crash logs — the timestamped crash-report filename contains `/`, and the save throws `IOException`, so crash reports were silently not written. This masked the V2 crash's persistence path during triage and is unrelated to spec 237; it deserves its own issue and fix (sanitize the filename before the timestamp is embedded).
4. **Gate state at re-anchor time:** the `high-risk-evidence` check fails on the branch because the committed round-2 record pinned `ccdb4c3389…` and the follow-up commit touched `specs/` — which the gate's docs-only exemption (`non_docs_files_after`) does not cover, even though no code changed. This is the gate working as designed (its backstop exempts only `docs/`); this updated record (Head SHA `bcc70c6138…`) is the resolution, and the commit carrying it must touch only `docs/assessment/` so the audited head remains the last non-`docs/` change. Round-1 (34088182723), round-2 (34110363181), and round-3 (34116566434) merge-gate runs are all fully green on their respective heads.
5. **Carried-over non-blocking observations from round 1:** AC-11 downgrade evidence is generic (no V2-on-v2.4 fixture); AC-6 evidence is compositional (no V2-specific preview-row test). The round-1 stale test-count observation (PR body said 952) was resolved by the implementing session — the PR body now reports 954 and updates the AC-9/AC-10 evidence rows.
6. **V1 invariance and harness/production faithfulness are unchanged** at this head: the round-2 delta touched no planning, bundle, or UI source, and the round-3 delta touched no source at all; golden corpus digest file untouched; `determinePreservation` precedence untouched; `PostPlanMaterializer` mirrors `FullTargetSetMaterializer` (workspace `FOLDER` → `Movable`, `FolderMember` → `Preserved`); the round-3 plan.md sync matches these verified semantics exactly (no discrepancy found).
