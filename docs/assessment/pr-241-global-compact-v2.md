# High-risk audit: PR #241 GLOBAL_COMPACT_V2 — existing 1×1 folders compact across pages (spec 237)

> Status: accepted — GO for merge on this record, with AC-10 (device evidence) recorded as an open item in Findings
> Audit date: 2026-09-07

- Auditor: Independent audit session (general-purpose subagent), not the implementing session — no involvement in the implementation; independent re-read of the diff and independent re-execution of the verification surface
- PR: https://github.com/nunu1733/NunuLauncher/pull/241
- Head SHA: eba38d573dce45fe3c10c778eb8813f7cce78547
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/34088182723 (`pull_request` `ci.yml` run verified via `gh run view`: completed, conclusion `success`, `head_sha = eba38d573dce45fe3c10c778eb8813f7cce78547`, head branch `agent/issue-237-global-compact-v2-spec`; `final-status` green, and the source jobs `organizer-unit-tests`, `check-style`, `build-debug-apk`, `validate-repo-contract`, plus all seven `organizer-instrumentation-*` lanes executed and passed — see `gh pr checks 241` output in Executed test surface)
- Criteria: specs/237-global-compact-v2-folder-relocation/spec.md AC-1 AC-2 AC-3 AC-4 AC-5 AC-6 AC-7 AC-8 AC-9 AC-11 AC-12
- Criteria: specs/182-layout-strategy-catalog/spec.md FR-016 AC-4 AC-5 AC-9 AC-15
- Criteria: docs/adr/0012-versioned-layout-strategy-catalog.md ADR-0012
- Criteria: docs/adr/0007-authoritative-organization-policy-sources.md ADR-0007

## Scope

Audited the PR head commit `eba38d573dce45fe3c10c778eb8813f7cce78547` (branch `agent/issue-237-global-compact-v2-spec`, three commits `f998da7dae` → `fbc5f3fca7` → `eba38d573d`; local checkout was at exactly this commit with a clean working tree; verified via `git status` and `git log`). The diff (`gh pr diff 241`) touches 18 files:

- Production planning (`lawnchair/src/app/lawnchair/organizer/planning/`): `LayoutStrategyRegistry.kt` (new `GLOBAL_COMPACT_V2` `StrategyId` + `StrategyDefinition` — eligible = 1×1 `APPLICATION`/`DEEP_SHORTCUT`/top-level `FOLDER` workspace units, `CAPTURED_VISUAL_GLOBAL`, `CAPTURED_THEN_NEW`, `TOP_LEFT_ROW_MAJOR`; new `StrategyDefinition.strategyFixes(item)` predicate = `unitOrder != CANONICAL_TIE_BREAK && !eligibleUnitFilter(item)`); `FullRunExecution.kt` (V1/V2 share one `executeGlobalCompact`; delta = formation candidates now `filter { it.kind != ItemKind.FOLDER }` and moved workspace units report `PlacementCode.FOLDER_UNIT` when `kind == FOLDER`).
- Bundle/rules: `PolicyModels.kt` (`POLICY_BUNDLE_VERSION` `organization-policy-v2.4` → `organization-policy-v2.5`), `BuiltInOrganizerPolicyBundleSource.kt` (`runtimeSupported` gains `GLOBAL_COMPACT_V2`; V1 kept; default `CANONICAL_PAGE_COMPACT_V1` unchanged).
- UI: `ManualOrganizationPreferences.kt` (V2 name/description mapping) and `lawnchair/res/values{,-ja}/strings.xml` (new `organization_strategy_global_v2_*`; V1 copy updated to "apps only" / 既存のフォルダは移動しません).
- Tests: `GlobalCompactStrategyTest.kt` (6 new V2 cases + additive helper refactors with default parameters; pre-existing V1 case bodies unmodified), `CrossStrategyCorpusTest.kt` (derives `.idempotence` fixtures from every formation fixture with `ContractCheck.IDEMPOTENCE` and pins their existence via `formationFixturesCarryTheIdempotenceCheck`), harness `PostPlanMaterializer.kt` (materialized synthetic folder → `ExistingRole.Movable`; materialized `FolderMember` items → `ExistingRole.Preserved`), harness `Oracle.kt` (`checkIdempotence.expectedReason` gains a `strategyFixes` → `STRATEGY_PRESERVED` branch after the `NON_TARGET`/`STRUCTURAL` branches), `PlannerContractHarnessTest.kt` (`materializationPreservesOriginalRoles` expects `Movable` for the synthetic folder), `BuiltInOrganizerPolicyBundleSourceTest.kt` (v2.5), `StrategyPickerInstrumentationTest.kt` (V2 row in display and click-action assertions).
- Docs: `CONTEXT.md` (strategy-fixed unit term), `docs/product/requirements.md` (FR-016 traceability), new `specs/237-global-compact-v2-folder-relocation/spec.md` + `plan.md`.

Runtime write-path audit: no file under `organizer/application/`, Launcher3 `provider/`/`model/`/writer, backup, or deck changed; no DB schema, recovery, or writer-path change. Folder relocation is emitted as an ordinary workspace `WorkspaceTarget` through the existing writer admission path (kind-agnostic), so member rows and folder `favorites` container references are structurally untouched by the diff. Selection store schema stays v1. `git diff` over `tests/unit/resources/` is empty — the golden corpus directory `tests/unit/resources/planner-golden-corpus/` including `sha256.txt` is byte-identical to `main`. `determinePreservation` in `PlanningPlacement.kt` (spec 10 precedence `NON_TARGET` > `STRUCTURAL`) is not in the diff and is unchanged, as is production `integration/FullTargetSetMaterializer.kt` (workspace `FOLDER` → `Movable`, `FolderMember` → `Preserved`, verified by reading the source).

## Criteria check

Spec 237 (`specs/237-global-compact-v2-folder-relocation/spec.md`):

- **AC-1 — PASS.** The spec is accepted (frontmatter `status: accepted`), and the Versioning decision section records the versioned-successor choice with the spec 182 / ADR-0012 rationale.
- **AC-2 — PASS.** `GlobalCompactStrategyTest.v2ExistingFolderJoinsCrossPageCompaction` (2-page: existing 1×1 folder on the later page relocates into the earlier page's free cell as `Moved{FOLDER_UNIT}`, members/rank/profile/naming asserted unchanged) and `v2ThreePageFolderHeavyLayoutFillsEarlierPagesFirst` (3-page device-shape fixture: folders f1/f2 fill page 1 first, later-page app follows, `crossPageMoves == 3`, no page deleted) both run through the public seam `OrganizationPlanner.plan`; both green (11/11 in the class, 0 failures).
- **AC-3 — PASS.** The same fixtures assert no movable 1×1 top-level unit remains behind while an earlier placeable cell exists; `v2NonSquareMovableUnitsStayStrategyPreserved` and `v2NonUnitSpanFolderIsStrategyPreserved` pin the eligible-set boundary; `CrossStrategyCorpusTest` exercises V2 over the full shared corpus via the registry.
- **AC-4 — PASS.** Folder identity, member list/rank, and profile isolation are asserted at plan level (`v2ExistingFolderJoinsCrossPageCompaction`: member keeps `FolderMember(FolderRef, rank)` target and its preserve reason; folder id/naming/members unchanged). The diff contains no writer/application change, so the "folder move = folder row cell/screen update only through the existing writer path" property holds by construction of the change set (apply code is kind-agnostic for workspace units); the apply-module suite ran green as part of the 953-test run.
- **AC-5 — PASS.** `v2FormationThenApplyThenRecaptureThenReplanIsAnEmptyDiff` directly pins the formation → apply → recapture → replan transition with production recapture roles (materialized folder `Movable`, members `Preserved`), asserts the replan has zero `Moved`, no new folders, the formed folder reclaims its cell as `Preserved{ALREADY_CANONICAL}` (not `STRATEGY_PRESERVED`), and the member reports `Preserved{NON_TARGET}`. `CrossStrategyCorpusTest` derives `.idempotence` fixtures from every formation fixture for every strategy and pins their existence; determinism/idempotence property suite green (3/3, incl. `formationFixturesCarryTheIdempotenceCheck`). The spec's 4-step fixed-point proof matches the shipped code: (1) fixed set is re-marked occupied at captured cells every run (`strategyFixed` loop), (2) `allocateCapturedThenNew` first-fit in global captured visual order restores the consumption order after materialization, (3) formation candidates exclude `FOLDER` and recaptured members are `Preserved`/non-candidates, so replan forms no folder.
- **AC-6 — PASS.** Preview projection is strategy-agnostic: V2 emits `Disposition.Moved(PlacementCode.FOLDER_UNIT)` for relocated folders (asserted in the V2 plan tests), the projector's cross-page counting is covered by `PlanPreviewProjectorTest.strategyConsequenceCountsSplitCrossPageMovesAndStrategyPreservedRows`, and the `FOLDER_UNIT` → `moveReasonFolderUnit` wording path is pre-existing and tested (`OrganizationPreviewContentTest`). No preview code changed.
- **AC-7 — PASS.** `git diff` over `tests/unit/resources/planner-golden-corpus/` is empty (`sha256.txt` unmodified); `GoldenOracleCorpusTest` ran unmodified and green (1/1, digest-verified) on the audited commit; the 5 pre-existing V1 test cases in `GlobalCompactStrategyTest` are body-unmodified and green; the shared-executor deltas are provably no-ops for V1 (V1's filter already excludes `FOLDER` from both the formation-candidate map and the mover stream, so the new `kind != FOLDER` filter and the `FOLDER_UNIT` code selection are unreachable under V1).
- **AC-8 — PASS.** `BuiltInOrganizerPolicyBundleSourceTest` asserts `organization-policy-v2.5` and `runtimeSupportedCatalogDeclaresOnlyImplementedStrategiesWithTheDefault` enforces `runtimeSupported == LayoutStrategyRegistry.acceptedIds` (set equality, not just subset) and `default ∈ runtimeSupported` — 6/6 green.
- **AC-9 — PASS.** Localized copy added in `values/` and `values-ja/` (V1 name now "Compact across screens (apps only)" / ページ間でコンパクト（アプリのみ） with description "Existing folders stay where they are." / 既存のフォルダは移動しません — truthful to V1 behavior; V2 "Moves apps and folders from later screens into earlier free spaces."). `StrategyPickerInstrumentationTest` gained the V2 row in both its displayed and click-action assertions, and CI executed it on the audited commit: the `organizer-instrumentation-issue52-tests` job's gradle invocation names `StrategyPickerInstrumentationTest` explicitly and the job log shows "Finished 41 tests on emulator-5554" with the job green.
- **AC-10 — OPEN (not met yet).** Device evidence (before/preview/after screenshots of the 2026-09-06 case) is not recorded in the Issue or PR; the PR body itself marks it 未記録 (要 follow-up). The corresponding 3-page fixture exists and passes (`v2ThreePageFolderHeavyLayoutFillsEarlierPagesFirst`), but the screenshot evidence itself must not be fabricated — tracked as the one open acceptance item.
- **AC-11 — PASS.** The downgrade/fail-closed path is covered by the existing strategy-generic selection-layer tests, all green: `LayoutStrategySelectionStoreTest.unsupportedStrategyIsRejectedAtWriteTimeWithoutTouchingStorage` (8/8 class green) and `OrganizationInputComposerTest` unsupported-selection → `OrganizationInputComposition.NotReady` (18/18 green). A V2-selected device downgraded to a v2.4 binary is an instance of exactly this "selection outside the active bundle's runtime-supported set" path; there is no V2-specific v2.4-binary fixture (noted in Findings).
- **AC-12 — PASS** (by this record). The PR carries `risk: layout-data`, the verified successful `final-status` CI run on the audited head, and — with the file you are reading — the independent audit record in `docs/assessment/`.

Spec 182 (`specs/182-layout-strategy-catalog/spec.md`):

- **FR-016 — PASS** (traceability updated in `docs/product/requirements.md`; selection/preview contracts extended without changing their shape).
- **AC-4 — PASS.** `OrganizationPlanner.plan(OrganizationInput)` remains the sole external planning seam; the diff changes only planning-internal code, bundle content, and UI copy — no new seam.
- **AC-5 — PASS.** The pinned golden corpus discipline is honored: corpus untouched, `GoldenOracleCorpusTest` green, so `CANONICAL_PAGE_COMPACT_V1` output remains byte-equivalent to the pinned pre-#182 oracle.
- **AC-9 — PASS.** Every catalog strategy (V2 included, via `LayoutStrategyRegistry.acceptedIds`) runs the shared conservation/bounds/overlap/lock/profile-isolation/determinism/idempotence contract and property tests through the public seam; suite green.
- **AC-15 — PASS.** Successful `final-status` CI on the audited head verified through the GitHub API, plus this independent audit record.

ADR-0012 (`docs/adr/0012-versioned-layout-strategy-catalog.md`): the folder-compacting variant ships as a new ID `GLOBAL_COMPACT_V2` rather than silently widening `GLOBAL_COMPACT_V1`; V1 is neither renamed nor removed; cross-page movers remain 1×1-only (non-1×1 units `STRATEGY_PRESERVED`), so the heterogeneous-span constraint and its idempotence-argument requirement are respected — the V2 proof is stated in the spec and pinned by tests (AC-5 above).

ADR-0007 (`docs/adr/0007-authoritative-organization-policy-sources.md`) §8: strategy enablement published as a new bundle semantic version `organization-policy-v2.5` with recomputed digest, no in-place migration, `rule-v2` and selection-store schema v1 unchanged; each binary reads its own bundle, and downgrade safety comes from the selection layer (`NotReady`), consistent with §8.

## Executed test surface

All commands run from the repo root at the audited commit (clean tree, detached-equivalent head `eba38d573d`, branch `agent/issue-237-global-compact-v2-spec`), with `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`:

- `git status` / `git log --oneline -3` → working tree clean; HEAD = `eba38d573d fix(237): run formation recapture through the shared idempotence oracle` (matches PR head OID from `gh pr view 241`).
- `JAVA_HOME=… ./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'` → BUILD SUCCESSFUL in 22s, exit 0. JUnit XML totals over `build/test-results/testLawnWithQuickstepGithubDebugUnitTest/`: 88 test classes, **953 tests, 0 failures, 0 errors, 0 skipped**. Key classes: `GlobalCompactStrategyTest` 11/11 (5 pre-existing V1 cases unmodified + 6 new V2 cases), `CrossStrategyCorpusTest` 3/3, `GoldenOracleCorpusTest` 1/1, `PlannerContractHarnessTest` 42/42, `BuiltInOrganizerPolicyBundleSourceTest` 6/6, `LayoutStrategySelectionStoreTest` 8/8, `OrganizationInputComposerTest` 18/18.
- `JAVA_HOME=… ./gradlew spotlessCheck` → BUILD SUCCESSFUL, exit 0.
- `JAVA_HOME=… ./gradlew assembleLawnWithQuickstepGithubDebug` → BUILD SUCCESSFUL, exit 0.
- `python3 tools/repo-contract/validate_repo_contract.py` → `repository contract OK (/Users/nunu/Documents/work/NunuLauncher)`, exit 0.
- `gh run view 34088182723 --repo nunu1733/NunuLauncher --json …` → `status: completed`, `conclusion: success`, `event: pull_request`, `headSha: eba38d573dce45fe3c10c778eb8813f7cce78547`, workflow `CI`.
- `gh pr checks 241 --repo nunu1733/NunuLauncher` → all jobs of run 34088182723 pass (`organizer-unit-tests`, `check-style`, `build-debug-apk`, `validate-repo-contract`, `final-status`, and `organizer-instrumentation-{shared-writer,db-migration,api35,issue52,issue155,issue99,issue53}-tests`); the separate `high-risk-evidence` check (High-risk gate workflow) fails at audit time with `FAIL: no docs/assessment/pr-241-<slug>.md audit record for this PR` — expected before this record exists (see Findings).
- `gh run view --job 101636215029 --repo nunu1733/NunuLauncher --log | grep StrategyPickerInstrumentationTest` → the issue52 instrumentation job's script explicitly runs `app.lawnchair.organizer.ui.StrategyPickerInstrumentationTest`; log shows `Finished 41 tests on emulator-5554 - 16`, job conclusion success (AC-9 instrumented coverage executed on the audited commit).

## Findings

1. **AC-10 is the one open acceptance item.** Device evidence (2026-09-06 before/preview/after screenshots in Issue #237 or this PR) is not recorded; the PR body discloses this honestly. The reproducing 3-page fixture and its assertions exist and pass, so the remaining work is evidence capture, not code. This record does not fabricate it. Recommend tracking to closure before or immediately after merge per the spec's test oracle.
2. **Expected gate failure at audit time:** the separate High-risk gate workflow run (run id 34088182894, `high-risk-evidence` job) failed with "no docs/assessment/pr-241-<slug>.md audit record for this PR" because this audit record did not yet exist on the branch. This record resolves it; since it lands as a docs-only commit after the audited head, the gate's docs-only exemption (`non_docs_files_after`) keeps the audited `Head SHA` valid without a re-audit.
3. **AC-11 evidence is generic, not V2-specific.** Downgrade fail-closed is covered by the pre-existing unsupported-selection tests (write-time rejection + composer `NotReady`), which is the same code path a V2 selection hits on a v2.4 binary. No dedicated "V2 selected on v2.4 bundle" fixture exists. Acceptable per plan.md's verification table, but a cheap targeted fixture would make the downgrade scenario self-documenting (follow-up candidate, not blocking).
4. **AC-6 evidence is compositional.** No V2-specific preview-row test was added; correctness rests on V2 emitting `Moved{FOLDER_UNIT}` (plan-level tests) plus the pre-existing strategy-agnostic projector and FOLDER_UNIT wording tests. The composition is sound and all parts ran green; a one-case V2 preview test would close the seam explicitly (follow-up candidate, not blocking).
5. **V1 invariance is structurally proven and empirically pinned:** the two shared-executor deltas are unreachable under V1 (its eligibility filter already excludes `FOLDER`), V1 test bodies are unmodified, and the golden corpus digest file is untouched with `GoldenOracleCorpusTest` green — byte-equivalence holds on the accepted corpus.
6. **Harness/production faithfulness verified:** `PostPlanMaterializer` now mirrors `FullTargetSetMaterializer` (unlocked/available workspace `FOLDER` → `Movable`; `FolderMember` → `Preserved`), and the new `Oracle` branch preserves production precedence (`NON_TARGET` > `STRUCTURAL`, then `strategyFixes` → `STRATEGY_PRESERVED` before `ALREADY_CANONICAL`). `determinePreservation` itself is unchanged, so no strategy's public output changed via the harness edits (tests/documents only, as the spec's change history records).
7. **Count note:** the local suite reports 953 organizer tests vs the PR body's "952" — consistent with the review-follow-up commits adding the `formationFixturesCarryTheIdempotenceCheck` contract test after the body was drafted; CI's `organizer-unit-tests` is green on the audited SHA, so this is a stale-prose artifact, not a verification gap.
