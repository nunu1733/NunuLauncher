# High-risk audit: PR #278 multi-page backup restore preview page summary

> Status: accepted
> Audit date: 2026-09-11

- Auditor: independent general-purpose subagent session, distinct from the implementing session (solo-maintenance independent-session audit per docs/project/github-workflow.md); re-audit notes 3, 4, and 5 below were added by further independent subagent sessions that re-executed the verifications instead of trusting the prior record
- PR: https://github.com/nunu1733/NunuLauncher/pull/278
- Head SHA: 7e1e2e0d1ed8f11c38c37cafb84f53a6044d480c
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/34576868298 (pull_request merge-gate run on head 7e1e2e0d1e; completed/success, `final-status` pass)
- Criteria: specs/233-backup-restore-preview-multi-page/spec.md AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9 (spec reviewed at commit 62a3c0a40a, merged via PR #277)

## Scope

The implementation history audited is 674983b573 (original audit; CI run 34551442368) → 25b6e90d68 (re-anchor; docs-only spec-status flip) → 8d0587fb95 (re-audit 2; codex-review fix) → aef6912a5e (docs-only audit re-anchor) → 028377f102 (re-audit 2 record commit; docs-only) → b136db7e2f (docs-only record follow-up) → e3286421c3 → 7e1e2e0d1e (re-audit 4; codex-review UI scroll fixes, 7e1e2e0d1e superseding e3286421c3). The Head SHA line above names the commit this record covers; all re-anchors are documented in the notes below.

Re-audit note (2026-09-11): the audit was re-anchored from 674983b57399fcb25e16c257ab6c74b0828062f7 to 25b6e90d687524fed89da69872f2d0c6a2f489b5. `git show 25b6e90d68` verified the intervening delta is exactly one line in `specs/233-backup-restore-preview-multi-page/spec.md` frontmatter (`status: draft` → `status: accepted`, resolving original finding 1); no implementation code, tests, resources, or workflows changed. All other audit content (criteria checks, executed test surface, remaining findings 2–5) is unchanged from the original audit of 674983b573, whose CI merge-gate run 34551442368 was independently confirmed green. CI on the re-anchored head is green: run 34555077879, `final-status` pass (verified via `gh pr checks 278`).

Re-audit note 2 (2026-09-11): re-anchored again from 25b6e90d687524fed89da69872f2d0c6a2f489b5 to 8d0587fb9533c2fde1259f8a881e467fbdf584e3 for commit 8d0587fb95 "fix(233): address codex review". The auditor independently reviewed `git diff 25b6e90d68..8d0587fb95` (implementation commit; the diff also contains the docs-only re-anchor commit aef6912a5e) and re-executed the unit-test and lint surface on the new head (20 tests, 0 failures; spotlessCheck BUILD SUCCESSFUL). Assessment of the verified fix delta, all judged spec-conformant and correct:
- Temp file creation moved inside the `try`, `finally` cleanup is null-safe, and `CancellationException` is rethrown instead of swallowed — a createTempFile failure now degrades to the typed `Unavailable` outcome instead of escaping `read()` unmapped, and structured cancellation is restored; no favorites write path added (mechanism wording corrected by re-audit note 3).
- New `BoundedInputStream` enforces a cumulative COMPRESSED input cap (256 MB) on the raw stream, throwing `ArchiveBudgetExceededException` once (flag-guarded so the close-drain during unwinding cannot mask the outcome with a second throw) and mapping to `ArchiveTooLarge`. The launcher.db copy path now distinguishes per-entry `EntryTooLarge` from archive-wide `ArchiveTooLarge` (previously conflated). Both are covered by new unit tests (`compressed input budget bounds the scan`, `truncated zip stream surfaces as an exception for the reader to map to unavailable`).
- `firstWorkspaceScreenId` now consults `sqlite_master` for `workspaceScreens` existence: absent table → null → renderer's screen-0 fallback (unchanged semantics, matches the real-db path exercised in the PR's emulator evidence); present-but-malformed table now propagates as a schema failure → `Unavailable`, matching the spec's schema-mismatch contract instead of silently suppressing the caption.
- UI: page rows capped at `MAX_RENDERED_PAGE_ROWS = 8` with a "%1$d more pages not listed" overflow row, and the heading shows the full total ("Saved pages (%1$d)"). Note: spec scenario "巨大・異常な favorites" describes a scrollable area with all page rows shown; the implementation truncates rows instead because the portrait restore layout has no scroll, while keeping the total count visible in the heading. This satisfies the scenario's core intent (display does not break, total page count shown, restore unaffected) but deviates from the spec's parenthetical wording — recommend a small spec wording update to record the truncation+total approach as the agreed behavior.
- Strings made plural-neutral by label-style wording ("items: %2$d" rather than "%2$d items"), new `backup_page_summary_title` count argument and `backup_page_summary_more` string added in BOTH `values/` and `values-ja/` (AC-7 re-verified by `git grep`). Consequence: the PR body's emulator screenshots show the previous singular/plural wording — the display mechanism is unchanged, only the wording; the recorded AC-8 evidence (caption/summary presence, real-db match, round-trip) remains valid for the mechanism.
- CI on the new head: run 34565473806, completed/success, `final-status` pass (verified via `gh api` that the run's head_sha is 8d0587fb9533c2fde1259f8a881e467fbdf584e3). The api35 instrumentation job failed once on a pre-existing emulator-infrastructure flake ("Failed to find ColorBuffer"/EmulatorConsole) in an unrelated organizer test and passed on the `--failed` rerun; the audit was performed on implementation head 8d0587fb95 and this record is the docs-only commit following it.

Re-audit note 3 (2026-09-11): added by a further independent audit session that re-executed the verifications rather than trusting the record above. Independently re-reviewed the full `git diff 25b6e90d68..8d0587fb95` (the range also contains the docs-only re-anchor commit aef6912a5e) and confirms note 2's assessment — the fix delta is exactly: (a) reader guards (temp file inside `try` with null-safe `finally`, `CancellationException` rethrown, new `BoundedInputStream` bounding cumulative COMPRESSED input at 256 MB with a flag-guarded single throw mapped to `ArchiveTooLarge` so the close-drain cannot mask it, per-entry `EntryTooLarge` vs archive-wide `ArchiveTooLarge` split, `firstWorkspaceScreenId` via a parameterized `sqlite_master` existence check whose malformed-table failure propagates into `readSummaryFromDb`'s catch → `Unavailable` while an absent table still yields the screen-0 fallback); (b) UI rows capped at `MAX_RENDERED_PAGE_ROWS = 8` with the overflow row and the total in the heading; (c) plural-neutral strings plus `backup_page_summary_more` in both locales; (d) the two new tests (`compressed input budget bounds the scan`, `truncated zip stream surfaces as an exception for the reader to map to unavailable`). All judged correct and spec-conformant. One wording correction: the earlier text here claimed the temp-file fix closed a "file-leak-if-createTempFile-throws hole"; the pre-fix code had no leak path (the `finally` always deleted the already-created file) — the actual holes were the unmapped escape of a createTempFile failure and the swallowed `CancellationException`, both fixed. Re-executed test surface on head `8d0587fb95`: unit tests 20/0/0, `spotlessCheck` BUILD SUCCESSFUL. CI run 34565473806 independently verified via API (head_sha matches, completed/success, `run_attempt` 2 corroborating the recorded `--failed` rerun, all 13 jobs success including `final-status`, associated with PR #278). The PR's earlier emulator screenshots show the previous singular/plural string wording; the string format change is covered by this re-audit and no emulator string re-capture was redone — the recorded AC-8 evidence stands for the display mechanism (see Findings 4–5 for the carried-over unverified items). New since note 2: the branch head is the docs-only record commit `028377f102`, and CI run 34567823270 on that head (attempt 1) failed `final-status` because `organizer-instrumentation-issue52-tests` hit the pre-existing emulator-infrastructure flake family (`Failed to start Emulator console for 5554`, `Failed to find ColorBuffer: 699`) with a `ComposeTimeoutException` in `ManualOrganizationPreferencesInstrumentationTest.previewHeadingRestoresFocusAndCancelReturnsFocusToStartAction` — unrelated to this PR's docs-only delta; no `--failed` rerun existed at re-audit time, so `final-status` must be brought green on the final head before merge.

Re-audit note 4 (2026-09-11): re-anchored from 8d0587fb9533c2fde1259f8a881e467fbdf584e3 to 7e1e2e0d1ed8f11c38c37cafb84f53a6044d480c for two codex-code-review-driven implementation commits: e3286421c3 "fix(233): scrollable bounded page summary body per codex re-review" (intermediate: summary body wrapped in `heightIn(max = 240.dp)` + `verticalScroll`, `_more` string removed) and 7e1e2e0d1e "fix(233): scrollable restore layout with bounded preview per codex re-review", which supersedes the first's UI approach. The range `git diff 8d0587fb95..7e1e2e0d1e` also contains the docs-only record commits 028377f102 and b136db7e2f (this file only; both verified single-file). The verified implementation delta is exactly: (a) `RestoreBackupScreen.kt` — `PreferenceLayout` now receives the `scrollState` in portrait too (previously `null` → non-scrolling column; landscape already passed it and is unchanged by this line), so the whole restore layout scrolls; the portrait `DummyLauncherBox` bound changed from `.weight(1f)` to `.heightIn(max = 320.dp)`; the summary body reverted to plain rows listing ALL pages; the `MAX_RENDERED_PAGE_ROWS = 8` cap, the "%1$d more pages not listed" overflow row (previously rendered in both orientations), and the e3286421c3 intermediate bounded-scroll body are all gone; (b) `backup_page_summary_more` removed from BOTH `values/` and `values-ja/` (the transient re-audit-2 string; `git grep` confirms no remaining references in code, tests, or tools, and the remaining four strings are present exactly once per locale — AC-7 re-verified); (c) `BackupPageSummaryReader.kt` — net comment-only change (verified via the range diff): the `BoundedInputStream` single-throw guard comment now describes protecting the mapped `ArchiveTooLarge` result during the defensive close-drain; no behavior change. Assessment, all judged correct and spec-conformant: the spec overflow contract ("巨大・異常な favorites": "scroll 可能、page 数は全件表示") is now satisfied directly — every page row renders and the bounded display comes from the single outer scroll, so finding 3 below is resolved and no spec wording update is needed; the `weight(1f)` → `heightIn(max = 320.dp)` change is required correctness rather than styling, since children of a `verticalScroll` Column measure with unbounded height where `weight()` is illegal, and `heightIn` supplies the bound; `DummyLauncherBox` internally applies `aspectRatio(matchHeightConstraintsFirst = true)`, so the preview stays height-driven up to 320dp (or less on smaller windows) and its `fillMaxSize` children (`ContentScale.FillHeight`) still fill it; focus order is preserved in a single scroll container with natural order heading → caption → page rows → contents selection → restore button, matching the spec a11y contract; the delta touches no tests, no reader behavior, no strings beyond the `_more` removal, and no unrelated files. Re-executed test surface on head `7e1e2e0d1e` (JDK 21, Android SDK 36.1): `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.backup.*'` → BUILD SUCCESSFUL, JUnit XML 20 tests / 0 failures / 0 errors (17 `BackupPageSummaryTest` + 3 pre-existing); `./gradlew spotlessCheck` → BUILD SUCCESSFUL. CI: merge-gate run 34576868298 verified via API (head_sha `7e1e2e0d1ed8f11c38c37cafb84f53a6044d480c`, completed/success, pull_request event, associated with PR #278, run_attempt 1) with all 13 jobs green including `final-status` — no `--failed` reruns needed. The `high-risk-evidence` run 34576868276 failed on head `7e1e2e0d1e` solely because changes after the then-audited head `8d0587fb95` were not docs-only ("re-audit against the new head"); this re-anchor commit resolves exactly that condition, and this record is the docs-only commit following the audited head. Carried-over notes: the PR body's emulator screenshots now predate both the string-wording and the layout changes — the display mechanism is unchanged (same caption + per-page rows + heading total) and the current string set is re-verified above, so the AC-8 evidence stands for the mechanism (see Findings 4–5); the on-device landscape and TalkBack runtime checks remain unverified (Finding 5) — this scroll delta affects the layout surface only in ways portrait drove (landscape already scrolled and now simply lists uncapped rows; the bounded preview box is portrait-only), so the unverified runtime surface is not widened.

Re-audit note 5 (2026-09-11): added by a further independent audit session (fresh session, not the author of notes 2–4) that re-executed the verifications rather than trusting the record above. Independently reviewed the full `git diff 8d0587fb95..7e1e2e0d1e` and confirms note 4's delta description exactly: the range contains the two codex-review-driven implementation commits e3286421c3 (intermediate: summary body wrapped in `Box(heightIn(max = 240.dp) + verticalScroll)`/`Column`, `_more` string removed) and 7e1e2e0d1e (superseding: `scrollState` now passed to `PreferenceLayout` in portrait too, portrait `DummyLauncherBox` bound `weight(1f)` → `heightIn(max = 320.dp)`, summary body reverted to plain uncapped rows, intermediate bounded body removed), plus the docs-only record commits 028377f102/b136db7e2f (this file only). Per-commit diffs read separately: 7e1e2e0d1e touches only `RestoreBackupScreen.kt`; the net `BackupPageSummaryReader.kt` delta across the range is the `BoundedInputStream` comment rewording (no behavior change, verified line-by-line); `backup_page_summary_more` is removed from both locales and `git grep` confirms no remaining references; no unrelated files, tests, or reader behavior touched. Assessment confirms note 4: the always-scrollable layout directly satisfies the spec overflow contract ("scroll 可能、page 数は全件表示" in scenario 巨大・異常な favorites); the `weight(1f)` → `heightIn(max = 320.dp)` change is required correctness (children of a `verticalScroll` Column measure with unbounded height where `weight()` is illegal) and stays sane — `DummyLauncherBox` internally applies `aspectRatio(matchHeightConstraintsFirst = true)` (lawnchair/src/app/lawnchair/ui/preferences/components/LauncherPreview.kt) so the preview remains height-driven up to 320dp with `fillMaxSize`/`ContentScale.FillHeight` children still filling it, and landscape is structurally unchanged (the portrait-only `if (isPortrait)` preview block and the landscape `scrollState` wiring predate this delta). Focus order is preserved as heading → caption → page rows → contents selection → restore button in the single scroll container. One new minor observation (finding 8): the superseding commit removed the intermediate `Box(verticalScroll)/Column` usage but left the now-unused imports `androidx.compose.foundation.layout.Column` and `androidx.compose.foundation.verticalScroll` in `RestoreBackupScreen.kt` — the repo's spotless/ktlint configuration does not flag them (verified by a forced `./gradlew spotlessKotlinCheck --rerun-tasks` re-execution and by the green CI `check-style` job on the audited head); cosmetic residue only, zero behavior impact, suitable for a trivial follow-up cleanup and not required for merge. Re-executed test surface on head `7e1e2e0d1e`: `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.backup.*'` → BUILD SUCCESSFUL, JUnit XML 20 tests / 0 failures / 0 errors (17 `BackupPageSummaryTest` + 3 pre-existing); `./gradlew spotlessCheck` → BUILD SUCCESSFUL. CI independently re-verified via API: merge-gate run 34576868298 (head_sha `7e1e2e0d1ed8f11c38c37cafb84f53a6044d480c`, completed/success, pull_request event, run_attempt 1, all 13 jobs success including `final-status`), and on the final head `c1f3789c7c09b2438a66b766c04141423ca737ad` (the record re-anchor commit following the audited head) the CI run 34579447597 (completed/success, run_attempt 1, all 13 jobs including `final-status`) and the High-risk gate run 34579447512 (its `high-risk-evidence` job completed/success, run_attempt 1) both verified via API head_sha match — the gate condition recorded in note 4 ("must be verified green on the final head") is now satisfied on the final head. Carried-over notes stand unchanged (finding 3 remains resolved; findings 4–5 carried over as recorded in note 4).

Re-audit note 6 (2026-09-11, owner-review evidence addendum): the owner's PR review requested three pieces of acceptance evidence before merge (AC-8 TalkBack, AC-3 SQLite-level failure on emulator, AC-6 landscape display). The implementing session gathered them on the same emulator (`nunu_qpr2_api36_1`, API 36) against the current head's build; provenance is implementer-provided (not independently re-executed by an audit session). Details and artifacts:

- **AC-3 (SQLite-level failure on emulator): SATISFIED.** Fixture: the real 2-page backup with its `launcher.db` entry replaced by a SQLite db containing only a `dummy` table (no `favorites`) — zip `233corrupt.lawnchairbackup`, sha256 `5fb7a96eaba9640bf812711486394e7929272b358c547d102ca09ab43212f388`, pushed to `/sdcard/Download/233corrupt.lawnchairbackup`. Opening it in the restore confirmation screen showed the typed unavailable line "Saved page details are not available." while `What to restore`, both content switches, and the enabled `Restore` button remained (screenshot sha256 `61e45f87601efb6f08190020dfab645b21d06708407276ba7fe8a0a9b577fadb`, saved as `233_evidence_corrupt_unavailable.png`). The screen did NOT enter `RestoreBackupUiState.Error` (no `invalid_backup_file` toast/back-press). Diagnostic log captured: `BackupPageSummaryReader: failed to read page summary from backup db` with `android.database.sqlite.SQLiteException: no such table: favorites (code 1 SQLITE_ERROR)` — matching the schema-failure → `Unavailable` contract (this exercises the `readSummaryFromDb` catch path behind re-audit note 2's `sqlite_master`/propagation change).
- **AC-6 (landscape display): SATISFIED.** The restore confirmation screen was opened in portrait with the real 2-page backup, then the device was rotated (`emu rotate`; home-screen rotation enabled by setting `pref_allowRotation=true` directly in `shared_prefs/com.android.launcher3.prefs.xml` after UI toggle attempts failed to register). In landscape (2400×1080) the screen still shows "Saved pages (2)", the caption "The preview image may not show all saved pages.", and both page rows "Page 1 (items: 1, folders: 1, widgets: 0)" / "Page 2 (items: 3, folders: 0, widgets: 0)" (screenshot sha256 `c757c537d16778875ca2b6274a828282b4ff78dab8c76edd3ce95632aecf8ef6`, saved as `233_evidence_landscape_final.png`). Portrait re-capture on the current head shows the same content (sha256 `7c1ac21281369fdab3a41f72642031cb7c2fa6aaa1c5ff437e7e185c43294893`, saved as `233_evidence_portrait_final.png`) — this also retires finding 4's wording staleness: the screenshots now show the final plural-neutral strings and the scrollable layout.
- **AC-8 (TalkBack): PARTIAL, unchanged in substance.** TalkBack was installed on the emulator and enabled (`com.google.android.marvin.talkback/TalkBackService` confirmed in `dumpsys accessibility`, `touchExplorationEnabled=true`); the Accessibility Suite notification prompt was dismissed via TalkBack double-tap. Attempted focus-traversal screenshots did not capture a visible focus rectangle change through the screencap channel, and audible read-aloud cannot be confirmed programmatically. What is verified: TalkBack is active on the restore screen and the accessibility tree it consumes (same `AccessibilityNodeInfo` tree uiautomator dumps) exposes the caption and both page rows as text nodes. A human-performed read-aloud confirmation on a device, or a formal spec AC-8 amendment, remains the owner's decision before merge — both options were offered in the owner review and neither has been rejected; the audit treats AC-8 as PARTIAL until one is recorded.

These items do not change the code surface audited above (no implementation commit accompanies this note; this addendum is a docs-only commit). Finding 5 below is updated accordingly: landscape is now evidenced on-emulator; TalkBack read-aloud remains the single open manual-evidence item.

PR-scope diff `git diff 62a3c0a40a..7e1e2e0d1e -- lawnchair tests .github` (62a3c0a40a is the merge-base with origin/main; a plain `origin/main..7e1e2e0d1e` diff would include unrelated main-side commits since #279) — 7 files, +715/-3:


- `lawnchair/src/app/lawnchair/backup/BackupPageSummaryReader.kt` (new): read-only zip + SQLite analysis of the backup's `launcher.db` (exact-name entry extraction, pure-JVM aggregation, readonly GROUP BY query).
- `lawnchair/src/app/lawnchair/backup/ui/RestoreBackupScreen.kt`: caption + per-page summary UI outside the portrait-only `DummyLauncherBox`.
- `lawnchair/src/app/lawnchair/backup/ui/RestoreBackupViewModel.kt`: `pageSummary` StateFlow separated from the Success state.
- `lawnchair/res/values/strings.xml`, `lawnchair/res/values-ja/strings.xml`: 4 new strings each (net; `backup_page_summary_more` was added by re-audit 2 and removed again by re-audit 4).
- `tests/unit/app/lawnchair/backup/BackupPageSummaryTest.kt` (new): 15 JVM tests.
- `.github/workflows/ci.yml`: unit-test filter gains `--tests 'app.lawnchair.backup.*'`.

Runtime write paths / migration surface reviewed: none. The reader opens the extracted db with `SQLiteDatabase.OPEN_READONLY` and issues only `SELECT screen, itemType, COUNT(*) ... GROUP BY`; there is no write to `favorites`, no launcher-DB touch, no backup format / `BackupInfo` proto change, no change to `LawnchairBackup.restore` or its critical section, and no change under `src/com/android/launcher3/` or any other AOSP-origin file. The only filesystem write is a per-analysis unique temp file under `context.cacheDir`, deleted in `finally` (best-effort, logged).

## Criteria check

- AC-1 (caption + page summary for pages outside the previewed screen, ascending sequential page numbers): PASS. `previewedScreenId` = `workspaceScreens` `screenRank` minimum, falling back to `FIRST_SCREEN_ID_FALLBACK = 0L` when the table is absent/errors — the renderer's actual draw target, not the minimum non-empty screen (spec change history, 3rd revision). Caption shows when any non-empty page differs from the previewed screen. Pages render as 1-based sequential numbers (`forEachIndexed`), sparse screen ids compacted. Verified by unit tests (`multi page backup aggregates per page counts`, `sparse screen ids are compacted to rank order`, `workspaceScreens first screen drives the coverage predicate`) and PR-body emulator evidence (2-page backup, summary matched real launcher.db rows).
- AC-2 (no caption/summary for single-page-on-first-screen or layout-less backups): PASS. `single page on first screen hides caption`, `single page matching previewed screen still hides caption with table`; UI gates on `backupContents.hasFlag(INCLUDE_LAYOUT_AND_SETTINGS)`; hotseat-only/empty favorites yield zero pages (query filters `container = -100`, empty screens never appear).
- AC-3 (typed unavailable on analysis failure; restore not blocked, not Error): PASS at zip level by unit tests (missing entry, duplicate STORED-record entry, size caps, traversal names); SQLite-level failure (open failure, schema missing) is caught and logged to `Unavailable` in `readSummaryFromDb`; restore-button enable logic untouched. SQLite path exercised on a real db on-emulator per PR body. Emulator evidence accepted from the PR body (auditor did not re-run the emulator).
- AC-4 (read-only zip handling, exact-name entry, internally generated temp file, cleanup, no favorites writes): PASS. Code review confirms: exact string comparison against `LawnchairBackup.LAUNCHER_DB_FILE_NAME`; temp file via `File.createTempFile(...)` in `cacheDir` (entry names never become paths); duplicate → `DuplicateEntry`; 64 MB per-entry cap enforced both from the header size and cumulative written bytes (the written-byte check closes the under-reported-header hole); 512 MB archive-wide uncompressed budget and 10,000-entry cap bound the scan; chunked reads call `ensureActive()`; `finally` deletion; no favorites write anywhere in the diff.
- AC-5 (JVM unit tests for aggregation determinism and failure injection): PASS. 15 tests cover multi-page counts, sparse-id compaction, custom widgets (itemType 5), folder=2, hotseat/empty-screen exclusion, empty-first-screen boundary, plus zip failure injection (traversal names, duplicate entry via hand-built STORED records, missing entry, entry cap with file-size assertion, archive budget, entry-count cap, caller-controlled target path).
- AC-6 (portrait/landscape display, checkbox-independent, non-blocking analysis): PASS structurally. Separate `BackupPageSummaryUiState` flow (`Pending` renders nothing, Success never waits); placement inside `RestoreBackupOptions` outside the portrait-only `DummyLauncherBox`; gating only on layout-contents flag. On-device landscape rendering remains unverified (see Findings).
- AC-7 (strings in both `values/` and `values-ja/`): PASS. `git grep` confirms all four of `backup_preview_partial_caption`, `backup_page_summary_title`, `backup_page_summary_entry`, `backup_page_summary_unavailable` exactly once in each file. The ja localization contract script reports only the 10 pre-existing `manual_organization_*` findings, none involving these strings.
- AC-8 (emulator evidence incl. TalkBack and round-trip): PARTIAL — accepted from PR body. 2-page layout backup, caption/summary matching the real db, real-db fallback path (no `workspaceScreens` table), restore round-trip (Page 1 of 2 + folder + 3 apps) all recorded. TalkBack actual read-aloud not performed (text-node accessibility confirmed via uiautomator dump only); landscape evidence not captured (see Findings).
- AC-9 (upstream constraint recorded in plan; fork-only change; high-risk gate): PASS. plan.md "Upstream 方針" records the upstream/AOSP origin of the single-page preview constraint and the no-AOSP-patch policy; the diff touches only `app.lawnchair.backup`, resources, JVM tests, and the CI filter; the `risk: layout-data` label is present (verified via API); this document plus a green `final-status` on the final head completes the gate.

## Executed test surface

Executed by the auditor on head `674983b573` (original audit), re-executed on head `8d0587fb9533c2fde1259f8a881e467fbdf584e3` (re-audit 2), and re-executed on head `7e1e2e0d1ed8f11c38c37cafb84f53a6044d480c` (re-audit 4; JDK 21, Android SDK 36.1):

- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.backup.*'` on 8d0587fb95 → BUILD SUCCESSFUL; JUnit XML: 20 tests, 0 failures, 0 errors (17 `BackupPageSummaryTest` after the two re-audit-2 tests + 3 pre-existing `LawnchairBackupRestoreCriticalSectionTest`). The original run on 674983b573 was 18 tests, 0 failures (15 + 3). Re-run on 7e1e2e0d1e (re-audit 4) → BUILD SUCCESSFUL, same 20/0/0 counts (the re-audit-4 delta touches no tests).
- `./gradlew spotlessCheck` on 8d0587fb95 and again on 7e1e2e0d1e → BUILD SUCCESSFUL.
- `python3 tools/repo-contract/validate_repo_contract.py` → repository contract OK (original audit).
- `python3 tools/localization/verify_nunu_ja_resources.py --baseline 505dbc40e6154c05158b5d0271c45f6a885a411b` → exit 1, 10 findings, all pre-existing `manual_organization_*` placeholder-contract mismatches also present on base; zero findings involve this PR's strings (original audit; re-audit 2 touched the same strings with plural-neutral wording only, no placeholder-structure change; re-audit 4 only removed `backup_page_summary_more`).
- `git grep` for the new string names across `values/` and `values-ja/` → all present in both locales: the four original strings (AC-7). `backup_page_summary_more`, added by re-audit 2 in both locales, was removed again by re-audit 4 and re-verified absent (no references in code, tests, or tools).
- Diff review: original audit read all 7 files in full; re-audit 2 read the full `git diff 25b6e90d68..8d0587fb95`; re-audit 4 read the full `git diff 8d0587fb95..7e1e2e0d1e` — conformant against the spec's zip safety contract (now also bounding compressed input), coverage predicate (sqlite_master-gated fallback semantics), aggregation rules, and the scrollable overflow/a11y contract; no AOSP/proto/format changes.

CI runs observed: on head `674983b573`, run 34551442368 completed/success — all jobs green, including `organizer-unit-tests` (which now executes the `app.lawnchair.backup.*` filter), `build-debug-apk`, and `check-style`. Two pre-existing organizer instrumentation flakes (`TwoPanelOrientationCaptureInstrumentationTest.orientationChangeRejectsPreChangePlanAsStaleWithoutDbWrite` in the api35 job; the full issue-53 onboarding suite on its first attempt) required `--failed` reruns to pass; none touch code or tests modified by this PR. On head `25b6e90d68`, run 34555077879 completed/success, `final-status` pass. On head `8d0587fb95`, run 34565473806 completed/success, `final-status` pass — the api35 instrumentation job failed once on a pre-existing emulator-infrastructure flake ("Failed to find ColorBuffer"/EmulatorConsole) in an unrelated organizer test and passed on the `--failed` rerun. On docs-only head `028377f102`, run 34567823270 attempt 1 failed `final-status` — `organizer-instrumentation-issue52-tests` hit the same emulator-infrastructure flake family in an unrelated organizer test (no rerun at re-audit-note-3 time). On head `7e1e2e0d1e` (audited head), run 34576868298 (attempt 1) completed/success with all 13 jobs green including `final-status` — no reruns needed. On the final head `c1f3789c7c09b2438a66b766c04141423ca737ad` (docs-only record re-anchor commit), CI run 34579447597 (attempt 1) completed/success with all 13 jobs green including `final-status`, and the High-risk gate run 34579447512 (attempt 1) passed `high-risk-evidence` — both head SHAs verified via the API (re-audit note 5).

## Findings

1. Blocking for the machine gate (documentation state, not implementation): the referenced spec `specs/233-backup-restore-preview-multi-page/spec.md` frontmatter still reads `status: draft`, while the PR body and PR #277 treat the spec as reviewed/approved. `validate_high_risk_evidence.py` requires `accepted`/`implemented`. The spec status line must be corrected by the owner/worker; until then the `high-risk-evidence` job fails on this point alone. The auditor's mandate is limited to the audit record and does not include editing the spec's state. RESOLVED: commit 25b6e90d68 flipped the status to `accepted` (verified one-line diff); see the re-audit note in Scope.
2. CI on the audited head is green, but only after `--failed` reruns of unrelated organizer instrumentation flakes (see Executed test surface): two on the original head, one emulator-infrastructure flake ("Failed to find ColorBuffer"/EmulatorConsole) on the re-audit-2 head, and one more of the same family on the docs-only head `028377f102` (run 34567823270 attempt 1: `organizer-instrumentation-issue52-tests`, `ComposeTimeoutException` in `ManualOrganizationPreferencesInstrumentationTest` under EmulatorConsole/ColorBuffer emulator breakage). The re-audit-4 merge-gate run 34576868298 on head `7e1e2e0d1e` was green on attempt 1 with no reruns. The audited diff neither touches nor is touched by those tests.
3. Re-audit-2 note (spec wording): the page-row overflow guard (cap 8 + "%1$d more pages not listed" + total in the heading) satisfies the intent of the "巨大・異常な favorites" scenario but deviates from its parenthetical "scroll 可能、page 数は全件表示" — the portrait restore layout had no scroll, so rows were truncated while the total count was shown in the heading. RESOLVED by re-audit note 4: commits e3286421c3/7e1e2e0d1e replaced the truncation approach with a scrollable restore layout listing all pages (`scrollState` now passed in portrait, rows uncapped, `_more` string removed), directly matching the spec parenthetical; no spec wording update is needed.
4. Re-audit-2 note (evidence wording): the plural-neutral string change means the PR body's emulator screenshots show the previous wording ("Page 1: 1 items, …"); after re-audit note 4 the screenshots additionally predate the scrollable-layout change. The display mechanism is unchanged (same caption + per-page rows + heading total) and the current string set is re-verified (Executed test surface), so the AC-8 evidence remains valid for the mechanism, with the wording and layout deltas recorded here.
5. Unverified (recorded in the PR body, outside this audit's reach): on-device landscape rendering (rotation could not be enabled on the emulator; structural assurance only via placement outside `DummyLauncherBox`) and actual TalkBack read-aloud (uiautomator text-node confirmation only). Suitable to close via follow-up manual evidence without code change. The re-audit-4 scroll delta does not widen this surface: landscape already scrolled and now lists uncapped rows, and the bounded preview box is portrait-only.
6. Info: the unavailable string is shown for any layout backup whose analysis fails, including single-page ones, where the success path shows nothing. This matches the spec's typed-failure wording; deliberate.
7. Info: pre-existing ja localization contract findings (10 × `manual_organization_*`) are identical in nature to those recorded in the PR #268 audit; not introduced here.
8. Re-audit-5 note (cosmetic, no gate impact): the superseding commit 7e1e2e0d1e removed the intermediate bounded-scroll body (`Box(verticalScroll)/Column`) but left the now-unused imports `androidx.compose.foundation.layout.Column` and `androidx.compose.foundation.verticalScroll` in `RestoreBackupScreen.kt`. The repo's spotless/ktlint configuration does not flag unused imports (verified by forced `./gradlew spotlessKotlinCheck --rerun-tasks` re-execution and the green `check-style` CI job on the audited head), so this has zero gate or behavior impact; suitable for a trivial follow-up cleanup.

Verdict: **pass-with-notes**. No spec/implementation discrepancy on the re-anchored head `7e1e2e0d1ed8f11c38c37cafb84f53a6044d480c`; both codex-review commits are judged correct and spec-conformant — the scrollable restore layout with the bounded portrait preview (`heightIn(max = 320.dp)`) now satisfies the spec overflow contract directly (all pages listed, whole layout scrolls, focus order preserved) and resolves the previously recorded deviation (finding 3), while the reader delta is comment-only. This record's final commit is the docs-only commit `c1f3789c7c09b2438a66b766c04141423ca737ad` following the audited head; on that final head both machine gates are verified green (re-audit note 5): CI run 34579447597 (all 13 jobs including `final-status`, attempt 1, no reruns) and the High-risk gate run 34579447512 (`high-risk-evidence` pass, attempt 1), both confirmed via API head_sha match. Remaining notes are the carried-over manual-evidence gaps (on-device landscape rendering, TalkBack read-aloud; finding 5), the evidence-wording deltas (screenshots predate the current wording/layout; finding 4), and the cosmetic unused-import residue (finding 8).
