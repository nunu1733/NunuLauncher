# High-risk audit: PR #467 freeze AI consultation / external agent exchange (Issue #443, FR-017)

> Status: accepted
> Audit date: 2026-09-26

- Auditor: independent general-purpose subagent session (ZCode); implemented by a separate session; solo-maintenance independence noted. The auditor executed the listed verification commands and CI inspections itself and did not rely on implementer claims.
- PR: https://github.com/nunu1733/NunuLauncher/pull/467
- Head SHA: d3a3d3088b7e93621ddb4344248d995448f82e85
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/36238470611
- Criteria: specs/443-freeze-ai-exchange/spec.md (AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8; FR-017 Frozen, D-016, D-04/D-17 freeze notes) + plan.md Verification table

## Scope

Pre-check: `gh repo view -R nunu1733/NunuLauncher --json nameWithOwner,defaultBranchRef` → `nameWithOwner: nunu1733/NunuLauncher`, default branch `main`. `gh pr view 467` → headRefOid `d3a3d3088b7e93621ddb4344248d995448f82e85` = local HEAD of branch `issue-443-freeze-ai-exchange`, base `main`, MERGEABLE. Working tree clean except the untracked local `refocus-drafts/` directory (not part of this PR; excluded from this assessment, including its pre-existing broken-link findings from `validate_repo_contract.py`).

Diff (`git diff origin/main...HEAD --stat`), 16 files, +715/−5, 9 commits, merge base `42037b609782cb022fc0f8b7ae3969ed586a781b`:
- `lawnchair/src/app/lawnchair/preferences2/PreferenceManager2.kt`: new boolean preference `exchangeAiConsultationEnabled`, key `exchange_ai_consultation_enabled`, default from `R.bool.config_default_exchange_ai_consultation`.
- `lawnchair/res/values/config.xml`: `config_default_exchange_ai_consultation` = `false` (translatable="false").
- `lawnchair/res/values/strings.xml`: toggle label/description strings.
- `lawnchair/src/app/lawnchair/ui/preferences/destinations/ExperimentalFeaturesPreferences.kt`: `SwitchPreference` row bound to `prefs2.exchangeAiConsultationEnabled.getAdapter()`.
- `lawnchair/src/app/lawnchair/ui/preferences/destinations/ManualOrganizationPreferences.kt`: (a) live read of the preference via `asState()` in the compose layer only; (b) hoisted `LaunchedEffect(scopeConfirmedState?.runId, exchangeAiConsultationEnabled)` OUTSIDE `PreferenceLazyColumn` that auto-calls `coordinator.planWithConfirmedScope()` under `withContext(Dispatchers.IO)` when state is `ScopeConfirmed` and the toggle is OFF (same threading discipline as the `execute` helper, runId+toggle key against re-run, coordinator's own state guard as fail-safe); (c) the `State.ScopeConfirmed` lazy-list branch emits nothing when OFF (`if (exchangeAiConsultationEnabled)` guard over the whole method-choice face: headline, both arms, `exchangeFlowItems` hosting, rejection/discard-failure rows).
- `build.gradle`: property-gated `filter { excludeTestsMatching("app.lawnchair.organizer.ui.exchange.*") }` inside `tasks.withType(Test).matching { it.name == "testLawnWithQuickstepGithubDebugUnitTest" }` behind `providers.gradleProperty("nunu.excludeAiExchangeUnitTests").isPresent()`.
- `.github/workflows/ci.yml`: `organizer-unit-tests` job adds `-Pnunu.excludeAiExchangeUnitTests=true`.
- `tests/unit/app/lawnchair/organizer/ui/ExchangeAiConsultationDefaultTest.kt` (new, 3 tests).
- `tests/organizer-instrumentation/.../ManualOrganizationPreferencesInstrumentationTest.kt`: 4 existing ON-path oracles gained `enableAiConsultationForTest()`; 4 new oracles (OFF non-empty cut, OFF empty cut, ON both arms, real-DataStore round-trip); real-DataStore seam `setAiConsultationForTest` + `@After resetAiConsultationToDefaultOff`.
- `tests/organizer-instrumentation/.../MissingAppSelectionInstrumentationTest.kt`: 3 existing oracles gained the ON seam + `@After` reset.
- `tests/organizer-instrumentation/.../UsageAccessJitInstrumentationTest.kt`: cross-origin oracle sets ON + `@After` reset.
- `tools/repo-contract/ci_portfolio_map.yml`, `docs/engineering/ci-test-portfolio.md`, `docs/product/organizer-to-be-ux.md` (D-04/D-17 freeze notes), `specs/443-freeze-ai-exchange/spec.md` (Revision 4, accepted) + `plan.md`.

Checked runtime surface: no layout-DB write path added or changed (the compose branch changes only the pre-apply face selection); no migration, backup/restore contract change (new preference intentionally not in the backup contract; missing key reads as default OFF), no permission/communication/secret change. `risk: layout-data` / `risk: migration` labels are not the trigger here — the PR is audited against its spec ACs per the tasking.

## Criteria check

- **AC-1: PASS (device screenshot portion → pending, see AC-7).** Diff verified: preference added with the specified key and `config.xml` bool default `false`; toggle row added to `ExperimentalFeaturesPreferences`. Executed locally at the audited head: `ExchangeAiConsultationDefaultTest` (3 tests, run inside the gated unit command) green — asserts `config_default_exchange_ai_consultation` exists with `>false<` and `translatable="false"`, and that the compiled `PreferenceManager2.class` contains the DataStore key `exchange_ai_consultation_enabled` (indirect source/bytecode oracle; it fails hard via `assertNotNull`/`assertTrue` when artifacts are missing — it does not skip). Persistence through the REAL DataStore is verified on device: `aiConsultationToggleRoundTripsThroughTheRealDataStore` (OFF→ON→OFF via `PreferenceManager2.exchangeAiConsultationEnabled.set/get`) passed in the manual-organization-ui lane XML. The toggle-row screenshot (spec oracle table) is pending device evidence — no artifact shows the Experimental Features screen.
- **AC-2: PASS (device instrumentation).** Code path verified at the audited head: the `ScopeConfirmed` branch is entirely behind `if (exchangeAiConsultationEnabled)`, and the hoisted effect auto-advances OFF runs. Device evidence: manual-organization-ui lane XML (run 36238470611) contains `aiConsultationOffSkipsTheMethodChoiceFaceAndReachesThePreview` (non-empty cut: asserts `manual_organization_method_title`, `manual_organization_method_plain`, `exchange_method_consult` counts = 0, preview reached, `applyCalls == 0`) and `aiConsultationOffEmptyCutSkipsTheMethodChoiceFace` (empty cut) — both ran on the emulator (timestamps in the combined 151-test invocation XML) with no failure entries; the lane was fully green on attempt 2.
- **AC-3: PASS (device instrumentation).** `aiConsultationOnKeepsTheMethodChoiceFaceWithBothArms` (both arms displayed with click actions, plain arm reaches preview) passed on device. The 4 pre-existing AI-arm oracles in `ManualOrganizationPreferencesInstrumentationTest`, the 3 ON-seam oracles in `MissingAppSelectionInstrumentationTest`, and `UsageAccessJitInstrumentationTest.crossOriginExchangePresentationPausesTheRunUntilResolution` gained the explicit ON seam (real DataStore `set(true)` with a verified read-back, `@After` reset OFF) and all passed in the same lane XML — the ON contract is unchanged.
- **AC-4: PASS (maintained oracle, OFF-condition re-execution).** `OrganizerHubPreferencesInstrumentationTest` contains no toggle manipulation, so in the same lane invocation it ran at default OFF (and after each ON-seam test's `@After` reset) — hub oracles including `hubProposalRowOpensTheRunFaceWithTheImportReviewPreOpened` and `hubStatusRowsPrecedeTheActionsInReadingOrder` are present in the lane XML with no failures. The production-route durable oracle `OrganizerDiagnosticsRouteInstrumentationTest.issue372ConsultationSessionSurvivesARealMaterialsWriteViaTheProductionRoute` flaked on attempt 1 (`ComposeTimeoutException` after 5000 ms — the #418 environment signature, also present as the same failure on pre-PR main run 36082413664) and passed on the attempt-2 rerun; per the spec disposition table this oracle is kept and environment tracking is owned by #418/#304. TTL-expiry behavior is unchanged code (no diff in hub/session code) and covered by the unchanged oracles.
- **AC-5: PASS (independent diff inspection).** All 16 files match the spec's allowed-diff list: test setup/ON-condition seam, new OFF-path oracles, preference test addition, CI filter change, preference/toggle/compose-branch production change. Forbidden diffs are absent: zero changes under `lawnchair/src/app/lawnchair/organizer/ui/exchange/` (production) and zero changes/deletions to existing exchange test files (`ExchangeFlowStateHolderTest` etc. byte-identical, still 6 files in `tests/unit/app/lawnchair/organizer/ui/exchange/`); no contract change to `integration.exchange`/`personalization.exchange` code; the 5 deleted diff lines are replaced lines in ci.yml (run line), ManualOrganizationPreferences.kt (branch line), and 3 doc lines. Run coordinator (`ManualOrganizationRun.kt`) untouched; no DB write path; no permission change.
- **AC-6: PASS (XML evidence + portfolio docs).** Locally executed at the audited head with `-Pnunu.excludeAiExchangeUnitTests=true`: `build/test-results/testLawnWithQuickstepGithubDebugUnitTest/` contains 160 XML files / 1697 tests, **0** `TEST-app.lawnchair.organizer.ui.exchange.*` files, `TEST-...ExchangeFlowStateHolderTest.xml` absent (the #352 oracle left the gate), 0 failures / 0 errors / 0 skipped, while all 16 pipeline/contract XMLs are present and green (3 `integration.exchange` classes, 13 `personalization.exchange` classes). The new `ExchangeAiConsultationDefaultTest` (not in `ui.exchange`) ran inside the gate. CI: the `organizer-unit-tests` job (attempt 2, job 108397253021) log shows the gradle command with `-Pnunu.excludeAiExchangeUnitTests=true` and BUILD SUCCESSFUL. The 6 frozen test files are retained in the repo. `ci_portfolio_map.yml` (note above `permanent_gates`) and `ci-test-portfolio.md` (organizer-unit-tests row) updated in the same PR; `validate_ci_portfolio.py` locally `CI portfolio validation OK`. Property-gating verified by code reading: without the property the filter block is skipped (full suite; plan records the measured 1644↔1783 switch on this machine setup).
- **AC-7: PENDING (device evidence artifact not yet produced).** The two OFF-path behaviors are device-verified functionally by the instrumentation oracles above (both executed on the emulator in run 36238470611 and green), but the spec oracle requires an emulator screenshot/recording artifact, linked in the PR, showing (a) the toggle OFF hiding the AI arm / method-choice face and (b) the run going straight to the plain organize path. No such artifact exists on run 36238470611 (artifacts: lane reports ×2, failure-time emulator evidence ×2 from attempt 1, debug APK, and `manual-organization-ui-evidence` = Issue #52 state screenshots `start/preparation/preview-confirm/success/failure/stale/recovery-failure` written by oracle tests — not AC-7-framed evidence and no Experimental Features toggle-row screenshot). **Marked pending device evidence; not counted as satisfied by this audit.**
- **AC-8: PASS (device instrumentation).** The OFF oracle `aiConsultationOffSkipsTheMethodChoiceFaceAndReachesThePreview` additionally asserts the focus contract: after the OFF auto-advance it waits (≤5 s) for `manual_organization_preview` to satisfy `assertIsFocused()` — i.e., after the omitted method-choice face the preview face's first node is the focus target. Device XML: passed. TalkBack device confirmation is explicitly not required by spec AC-8 (judgment recorded in the spec).

## Executed test surface

All commands executed by the auditor at head `d3a3d3088b7e93621ddb4344248d995448f82e85`, branch `issue-443-freeze-ai-exchange`, clean tree (untracked `refocus-drafts/` untouched):

- `./gradlew spotlessCheck` → exit 0.
- `./gradlew assembleLawnWithQuickstepGithubDebug` → exit 0.
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest -Pnunu.excludeAiExchangeUnitTests=true --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*' --tests 'app.lawnchair.migration.*' --tests 'app.lawnchair.DeviceProfileOverridesPresetResolutionTest'` → BUILD SUCCESSFUL; XML inspection: 160 files / 1697 tests, failures 0, errors 0, skipped 0; **0** `ui.exchange` XMLs; `ExchangeFlowStateHolderTest` XML absent; 16 `integration.exchange`/`personalization.exchange` XMLs present and green; `ExchangeAiConsultationDefaultTest` 3 tests green.
- `/tmp/venv-yaml/bin/python tools/repo-contract/validate_ci_portfolio.py` → `CI portfolio validation OK` (exit 0).
- Static verification: full `git diff origin/main...HEAD` read against the spec allowed/forbidden diff list; hoisted-effect placement, IO dispatch and runId keying confirmed against `ManualOrganizationPreferences.kt:204,573-575`; property gating confirmed in `build.gradle`.
- GitHub API (all explicit to `nunu1733/NunuLauncher`): `gh pr view 467` (head SHA, base, MERGEABLE); run 36238470611 → workflow CI, event pull_request, headSha matches, conclusion `success`, `run_attempt: 2`; attempts/1/jobs → failures exactly `organizer-instrumentation-manual-organization-ui-tests`, `organizer-instrumentation-category-override-tests` (+ `final-status`); both jobs' step logs show the attempt-1 failures are `OrganizerDiagnosticsRouteInstrumentationTest.issue372ConsultationSessionSurvivesARealMaterialsWriteViaTheProductionRoute` and `CustomCategoryPreferencesInstrumentationTest.renameKeepsTheEntryPresentedAsTheSameCategory`, both `ComposeTimeoutException: Condition still not satisfied after 5000 ms` (environment-flaky signature classified on Issue #418 — comment 2026-09-26T11:39:10Z records the classification before the rerun; the issue372 signature equals the pre-PR main run 36082413664 failure); attempt-2 jobs all success including `final-status`. `organizer-unit-tests` job log shows the exclusion property in the gradle command. High-risk gate run 36238470603 → `High-risk gate`, conclusion `success`, same headSha. Artifacts of run 36238470611 downloaded and inspected: `organizer-instrumentation-manual-organization-ui-reports` (attempt-1 XML: 151 tests, exactly 1 failure = the #418-signature test; all 8 new/seamed #443 oracles and the hub oracle classes present without failure) and `organizer-instrumentation-category-override-reports` (attempt-1 XML: 1 failure = the #418-signature ComposeTimeout). Not rerun locally: emulator instrumentation lanes themselves (CI lane XML + job success is the evidence per spec oracle table); no connected-device run was available to this audit session, which is also why AC-7's artifact is judged pending rather than produced here.

## Findings

- Nothing blocking merge found. All machine-checkable claims were reproduced independently at the audited head; the diff matches the spec's allowed-diff contract exactly.
- **AC-7 remains open (non-blocking for code, blocking for Issue #443 closure):** the OFF-path behaviors are device-verified by green instrumentation oracles, but the spec's evidence artifact (emulator screenshot/recording linked in the PR, including the toggle-row view for AC-1) has not been produced. Issue #443 must not be closed until this artifact exists.
- Rerun disclosure: run 36238470611 is attempt 2. Attempt 1 failed 2 lanes (`category-override`, `manual-organization-ui`) plus `final-status`, both with the 5000 ms `ComposeTimeoutException` environment signature; both were classified on Issue #418 (comment 2026-09-26T11:39:10Z) before the rerun, consistent with the pre-PR main run 36082413664 signature, and both lanes were green on rerun. This is the classified-rerun policy working as designed; the underlying #418/#304 tracking remains open.
- The frozen suite (6 classes / 139 tests, `ui.exchange`) is now intentionally unobserved by automated CI (main/weekly/scheduled share the same `organizer-unit-tests` job). Per spec Revision 3 this is the accepted disposition: observation is local/manual only, and any freeze-exception change must run the affected classes locally within its PR. Residual risk is accepted by the spec, not silently hidden.
- The JVM default oracle (`ExchangeAiConsultationDefaultTest`) pins config.xml and compiled bytecode rather than the runtime default itself; the runtime persistence contract is covered on device by the real-DataStore round-trip oracle. Indirection is acceptable given the JVM-only seam, and the device oracle covers the live path.
- Untracked local `refocus-drafts/` pre-existing broken-link findings from `validate_repo_contract.py` are excluded from this assessment (not part of this PR's tree).


## Addendum 2026-09-26: AC-7 device evidence

- Device evidence captured on local emulator (API 36 family, `issue142_api36`) running the audited build (`Lawnchair.15.Dev.(d3a3d30).github.debug.apk`):
  - `docs/assessment/evidence/pr-467/443-ac1-toggle-row-default-off.png` — Experimental features screen with the AI consultation toggle present and OFF (AC-1 row presence).
  - `docs/assessment/evidence/pr-467/443-ac7-off-direct-preview.png` — after scope confirm on a non-empty cut, the run reached the preview face directly; no method-choice headline or AI arm rendered (AC-7 (a)+(b)).
  - `docs/assessment/evidence/pr-467/443-ac7-off-path.mp4` — screen recording of the same OFF run path.
- AC-7 is therefore resolved; no code changed after the audited head (`d3a3d3088b`) — this addendum and the evidence files are docs-only.
