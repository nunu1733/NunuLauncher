# High-risk audit: PR #350 #327 interview-first Exchange (AC-1〜AC-6 独立監査)

> Status: accepted
> Audit date: 2026-09-18

- Auditor: 独立audit session (general-purpose subagent、実装sessionと別)。実装者への確認は行わず、repository内の証拠と本sessionの再実行結果のみを根拠とする
- PR: https://github.com/nunu1733/NunuLauncher/pull/350
- Head SHA: 5f735526b369686a668ea4568928cf6d6c069e16
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35276653532
- Criteria: specs/327-agent-exchange-interview-first/spec.md (FR-017, AC-1, AC-2, AC-3, AC-4, AC-5, AC-6)

## Scope

監査対象は base `8fd05a40d51abd24b40a7b93579bb9b76d046f75` (origin/main、#348 merge後) → head `5f735526b369686a668ea4568928cf6d6c069e16` の差分7コミット (2f1e0f58d0 → 5f735526b3)。`git diff --stat` による変更範囲は以下の11ファイル (+884 / -13)。

- 実装 (7ファイル): `lawnchair/src/app/lawnchair/organizer/personalization/exchange/ExchangePackageComposer.kt` (interview-first INSTRUCTION_OPEN・canonical template定数・footer phase gate)、`lawnchair/src/app/lawnchair/organizer/ui/exchange/ExchangeFlowUi.kt` (idle/scoped entry rowへの `ExchangeCapabilityNotes` mount・testTag)、`lawnchair/res/values/strings.xml` / `values-ja/strings.xml` (capability説明7 key + transport success更新)、`tests/unit/.../exchange/ExchangePackageComposerTest.kt` (回帰+新test拡張)、`tests/unit/.../exchange/Issue327InterviewFirstContractTest.kt` (新規、blocking oracle)、`tests/unit/.../ui/exchange/ExchangeCapabilityCopyTest.kt` (新規、strings/copy oracle)、`tests/organizer-instrumentation/.../ExchangeImportSurfaceInstrumentationTest.kt` (rendered-UI oracle 2 test追加)
- 文書 (4ファイル): `specs/327-agent-exchange-interview-first/spec.md` / `plan.md` (accepted → implemented)、`CONTEXT.md` (整理方針確認の用語追加)

runtime書き込み経路・migration対象: なし。DB書き込み、`favorites` 操作、schema migration、permission/network追加は差分に存在しない (新規永続化なし、instruction/copy/strings/testのみの変更)。`compose(exportJson)` signature・`parsePackageStructure`・`ExchangeContract`・`IntentWireContract`・`IntentCodec`・`ExchangeImportPipeline`・import入力UI (#332) は無変更。回帰条件の `tests/unit/.../Issue348AiFacingContractSyncTest.kt` は `git diff 8fd05a40..5f735526 -- tests/unit/app/lawnchair/organizer/personalization/exchange/Issue348AiFacingContractSyncTest.kt` が空 (無変更) であることを確認。`ExchangePackageComposer.kt` の `outputContractSection()` / `youMustSection()` 関数本体はdiff hunkに +/- として一切現れず無変更 (削除された「You may」clarifying questions行は accepted spec AC-1のpinning対象外であり、`Issue348AiFacingContractSyncTest` がpinする ask-before-final文は不変更の You must 側に残存)。

確認した外部証拠の受入連鎖: Issue #327 の ChatGPT spec review **Approve** (head `c7f3b7000e920cfdfd26ab661d0ebf74234691f9`、[comment](https://github.com/nunu1733/NunuLauncher/issues/327#issuecomment-5720837963)) と Phase2実装 re-review **Approve** (head `f0c176f57152cf0b3784fa5f87df7654fcab246b`、[comment](https://github.com/nunu1733/NunuLauncher/issues/327#issuecomment-5721349944))、および CI run 35276653532 (head = 本audit Head SHA と完全一致、`final-status` success)。

## Criteria check

- **AC-1 (interview-first instruction + #348回帰): 充足。** `INSTRUCTION_OPEN` の Phase 1節が (a)「Your first response must NOT contain the final JSON artifact — no fenced code block, no intent JSON」(初回artifact禁止)、(b)「Interview first even if the CONTEXT data alone looks sufficient」(exportだけで判断可能でも原則ヒアリング)、(c)「Ask 2 to 4 short questions that are easy to answer on a phone」+ 質問主题の例 (2〜4問bound)、(d)「briefly summarize the organization policy... ask the user to confirm」「Produce the final JSON artifact only after the user confirms」(要約と了承後のみ生成) を含む。footer先頭に phase gate文を追加、self-check・Response format文は無変更で保持。`ExchangePackageComposerTest` (11 tests、interview/skip/template/partial authoring/section順序/`parsePackageStructure` 往復assertを含む) が本session再実行で全成功。`Issue348AiFacingContractSyncTest` (16 tests) は無変更のまま全成功し、descriptor派生Output contract・You must production-enforced規則・partial authoring・ask-before-final・finalize前self-check・canonical authoring form要求・repair導線不在を維持。
- **AC-2 (canonical template semantic-value-neutral + pipeline oracle): 充足。** `CANONICAL_INTENT_TEMPLATE` は `schemaVersion` のみ descriptor由来の正規literal (`IntentWireContract.field("schemaVersion").exactValue` = `ContextExportContract.INTENT_SCHEMA_VERSION = "personalized-intent-v3"`、AC-2(e)) で、`exportId`・`ref`・`unresolvedRefs`・`rationale` は全大文字placeholder、`importance` は `enumClaims` の許容値列挙placeholder (HIGH/NORMAL/LOW)、判断を伴うoptional field (desiredGroup/groupSemantic/pageAffinity/regionAffinity/preserve/globalPreference/confidence) は不掲載 (AC-2(a))。template行はfence・INTENT marker非含有で、前後行もfenceでないことを `ExchangePackageComposerTest` がpin (AC-2(b))。`Issue327InterviewFirstContractTest.templateSubstitutionReachesValidationThroughTheProductionPipeline` は合成export/session fixture (MOVABLE 3件・2ページ) へのplaceholder置換 → 単一fenced `json` block → `ExchangeImportPipeline.import` → `Validated` をblocking oracleとし、未言及refのcanonical unresolved completion (#330 partial authoring回帰: authored 1 / unresolved-authored 1 / omitted 1) までassert (AC-2(c))。`IntentCodec.decode` 閉schema・descriptor key包含は補助oracleとして `Issue348AiFacingContractSyncTest` (codec keyset test) に確保。placeholderと実export値の不一致 test (AC-2(a)の誤認防止) も同test fileに存在。両test全成功を再実行で確認。
- **AC-3 (skip宣言=確認stepの代替・要約必須): 充足。**「If the user explicitly says the questions are not needed and asks you to generate right away, treat that as the confirmation: reply with your brief policy summary and the final artifact together in that same reply — never skip the summary」がinstructionに存在し、`ExchangePackageComposerTest.skipDeclarationContractIsPinned` が各断片をpin。footer gate文も「or explicitly skipped the questions」でskipを確認stepの代替として明示。再実行で成功。
- **AC-4 (capability具体例・直接変更しない明示、idle/scoped両entry): 充足。** `ExchangeCapabilityNotes` が `ExchangeEntryRow` (testTag `exchange-entry-capability`) と `ExchangeScopedEntryRow` (testTag `exchange-scoped-entry-capability`) の両方にmountされ、5項目の具体例 (よく使う/まとめる/動かしたくない維持/前ページに寄せる/今の配置を崩さない) +「AIがホーム画面を直接変更することはありません。再整理に使う『あなたの希望・傾向』を作ります。」(en: "The AI never changes your home screen directly...") を schema用語なしで表示。ja/en両localeのkey存在と構造は `ExchangeCapabilityCopyTest` (unit、再実行PASS) がpin。実Compose UI render oracle 2 test (idle: capability tag+全実resource text、scoped: mount+主要契約文) は `tests/organizer-instrumentation/` の `ExchangeImportSurfaceInstrumentationTest` にあり、CI run 35276653532 の `organizer-instrumentation-issue332-tests` job (ci.yml のclass filterで当該testクラスを明示実行) success を確認。
- **AC-5 (会話flow・1往復の説明): 充足。** `exchange_capability_flow` が「依頼文は1回送り、AIからの質問 (2〜4問) に答えて整理方針を確認すると、最終案を1回だけ受け取れます。会話の内容がNunuLauncherに送信されることはありません。」(en相当も同旨) でflowと1往復+privacyを説明。送信完了status `exchange_transport_success` も「コピー・共有・保存しました」に始まり「質問 (2〜4問) に答えて整理方針を確認すると、最終案を1回だけ受け取れます」へ更新済みで、transport中立 (送信済みの誤示なし) を `ExchangeCapabilityCopyTest.transportSuccessCopyStaysTransportNeutral` がpin。flow文の「1回」2回出現assertもunit testに存在。rendered UI側のflow表示assertはAC-4と同一instrumentation oracle。再実行でunit全成功。
- **AC-6 (provider中立性): 充足。** 変更後 `ExchangePackageComposer.kt` 全文を `function call|tool call|system prompt|system role|api key|memory|automat|browser|gemini|openai|claude` で走査し該当なし (instructionは平文multi-turn chatのみで完結、web検索許可はbaseからの既存 You may)。UI copyの「ChatGPT等 / such as ChatGPT」はspecが許容する具体providerの例示であり、必須化する文言は含まない。
- **AC-7 (representative provider device evidence): 未完了 (計画的分離)。** 実デバイス+ownerログインを要するため [Issue #351](https://github.com/nunu1733/NunuLauncher/issues/351)「[Evidence]: #327 interview-firstのAC-7/AC-8 device evidence取得」(OPEN) へ分離済み。本PRのコード・テスト・CIは AC-1〜AC-6 を覆っており、spec/plan/PRの Status 記載 (AC-7/AC-8のみ後続evidence pass) と Issue #351 の実在をhead上およびGitHub APIで確認した。
- **AC-8 (a11y evidence + ja/en strings両方): strings部分は充足、a11y evidenceは未完了 (計画的分離)。** capability説明・CTAのja/en keyは両localeに存在しunit testでpin済み (再実行PASS)。TalkBack/large fontの手動evidenceは AC-7 とともに Issue #351 へ分離。capability notesは静的Text (live region不要) で読み上げ可能な箇条書き構造。

## Executed test surface

すべて `/Users/nunu/Documents/work2/NunuLauncher` (checkout HEAD、clean tree) 上で本audit sessionが再実行。環境は JDK 21.0.12 / Gradle 9.3.0。

| Command | 結果 |
|---|---|
| `git rev-parse HEAD` | `5f735526b369686a668ea4568928cf6d6c069e16` (監査対象Head SHAと一致、`git status` clean) |
| `git log --oneline 8fd05a40d51abd24b40a7b93579bb9b76d046f75..5f735526b369686a668ea4568928cf6d6c069e16` | 7 commits (2f1e0f58d0..5f735526b3) — PR headのcommit listと一致 |
| `git diff 8fd05a40..5f735526 -- tests/unit/app/lawnchair/organizer/personalization/exchange/Issue348AiFacingContractSyncTest.kt` | 空出力 (無変更確認) |
| `git diff 8fd05a40..5f735526 --stat` | 11 files、+884 / -13 (Scope列挙と一致) |
| `./gradlew spotlessCheck --rerun-tasks` | **PASS** (BUILD SUCCESSFUL、5 actionable tasks: 5 executed — キャッシュ経由でない強制再実行) |
| `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.personalization.exchange.*' --tests 'app.lawnchair.organizer.ui.exchange.*'` | **PASS** (BUILD SUCCESSFUL、fresh result XML: 129 tests / 0 failures / 0 skipped / 0 errors) |

内訳 (test-results XML、実行時刻 2026-09-18 07:02 本地): `ExchangePackageComposerTest` 11 / `Issue327InterviewFirstContractTest` 2 / `Issue348AiFacingContractSyncTest` 16 / `ExchangeImportPipelineTest` 23 / `ImportNormalizerTest` 22 / `IntentImportParserTest` 16 / `SessionExportReconstructorTest` 5 / `ExchangeGenerationGateTest` 4 (personalization.exchange計99) + `ExchangeCapabilityCopyTest` 4 / `ExchangeDisclosureStateTest` 7 / `ExchangeFlowStateHolderTest` 19 (ui.exchange計30)。

assembleとinstrumentationはlocal再実行しない旨を明示し、CI run の証拠を参照する: https://github.com/nunu1733/NunuLauncher/actions/runs/35276653532 は head SHA `5f735526b369686a668ea4568928cf6d6c069e16` (本audit対象と完全一致) で完走・`final-status` success。内 `build-debug-apk` success (assemble相当)、`check-style` success、`organizer-unit-tests` success、`organizer-instrumentation-issue332-tests` success (ci.yml の class filter `app.lawnchair.organizer.ui.exchange.ExchangeImportSurfaceInstrumentationTest` で実行され、今回追加のrendered-UI oracle 2 testを含む)。`gh run view 35276653532` / `gh pr view 350` / `gh issue view 327` / `gh issue view 351` / `gh repo view nunu1733/NunuLauncher` をread-onlyで実行し、PR base/head SHA・Issue review連鎖・#351 の分離内容を確認した。

## Findings

- **blocking findingなし。** AC-1〜AC-6はhead上のコード・テスト (本session再実行) とCI証拠で充足。#348回帰 (sync test無変更+16 tests成功、Output contract/You must/self-check/canonical form/repair導線禁止の無変更)、exchange framing・import pipeline・DBへの非接触 (`IntentImportParser` / `ExchangeImportPipeline` 等無変更、plan「変更しないもの」list遵守) も確認。高リスクhome layout安全規約の対象 (favorites全削除・遅延依存・手動backupのみをundoとみなす実装) はいずれも差分に存在しない。
- **AC-7/AC-8の後続evidence分離 (想定どおり・要追跡):** AC-7 (representative provider device evidence) と AC-8のa11y evidence (TalkBack/large font、ja/en) は Issue #351 (OPEN、title `[Evidence]: #327 interview-firstのAC-7/AC-8 device evidence取得`、Refs #327/#350) へ分離済み。#351側で契約不適合 (例: instruction遵守率、skip文言の誘発、capability説明の読み上げ品質) が判明した場合はコード変更を伴う後続Issueが必要になる。spec frontmatterのstatusは `implemented` だが、Status記述はAC-7/AC-8未完了を明記しており、Issue #327 の close は #351 のevidence完了を待ってから行うべき (中间PRの`Refs`運用と整合)。
- **軽微 (non-blocking) 観察 1:** templateの `importance` placeholderは実装で `REPLACE_WITH_HIGH_NORMAL_LOW` (descriptor列挙を `joinToString("_")` で生成) となり、spec/planの例示 `REPLACE_WITH_HIGH_NORMAL_OR_LOW` ("OR"付き) と文言が異なる。spec側は「例:」表記であり、placeholderは「許容値を列挙する」ことを満たす上、descriptor由来の自動同期は例示固定より望ましい。testは生成値側でpinしており contract test の置換も実値追従なので回帰穴はない。
- **軽微 (non-blocking) 観察 2:** footerのtemplate指示文は "Replace every \"REPLACE_WITH_...\" value" と置換規則をplaceholder文字列で参照しており、`Issue327InterviewFirstContractTest` の `!payload.contains("REPLACE_WITH_")` assert と同一語彙で整合している。templateが1行のみ (count==1) assert・CONTEXT data行との区別 assert もあり、`parsePackageStructure` の footer/data分離表面は維持されている。
- 独立性の注記: 本auditは実装session・ChatGPT review session (Approve連鎖は前提証拠として参照したが自分の再実行で独立検証済み) とは別のZCodeサブエージェントが、head SHAチェックアウトのclean treeで全commandを再実行して記録した。
