# High-risk audit: PR #325 External Agent Exchange workflow (package/framing/import + UI)

> Status: accepted
> Audit date: 2026-09-16

- Auditor: independent general-purpose audit subagent (ZCode subagent session; did NOT implement this change — the implementing session was separate)。実装sessionとは別sessionとして、実装コードの変更は行わず、読み取り・検証・本監査記録の作成のみを実施した (solo保守の独立session要件)。commit/pushは行っていない。
- PR: https://github.com/nunu1733/NunuLauncher/pull/325
- Head SHA: 1a0da578cc9377279fe1a2501a0ab611bb23b0ed
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35007575114 (the pull_request-event ci.yml run on this head; `final-status` success — job: https://github.com/nunu1733/NunuLauncher/actions/runs/35007575114/job/104515537606)
- Criteria: specs/205-external-agent-exchange/spec.md AC-1 AC-2 AC-4 AC-5 AC-8 AC-11 AC-12 AC-13

> 監査履歴: (1) 初回audit (head `9ecd2ff76186ac2916dc9afd08a1944cabb6c223`、CI run [34998299161](https://github.com/nunu1733/NunuLauncher/actions/runs/34998299161)) — P2×1 / P3×7を記録。(2) 第1回再audit (head `8c2fc68cc15ac7efc6699360a795225178327fc2`、CI run [35000215963](https://github.com/nunu1733/NunuLauncher/actions/runs/35000215963)) — P2-1 (IO実行化) / P3-1 (BOM fixture) / P3-6 (spec merge) の対応3 commitを検証。**同headのCIはaudit後に完了し `organizer-instrumentation-issue52-tests` がfail** (`StrategyPickerInstrumentationTest.selectingAStrategyPublishesThroughTheValidatedWriteCommand` / `.selectingAStrategyMovesTheSingleSelectedParentRow` がtimeout。`final-status` failure)。実装者のbaseline実験 (origin/main worktreeでは同一classが成功、branchで再現) により、Idle branch内・strategy picker **上位** に配置されたexchange entry rowがpickerのradio行位置を押し下げ、scrollしないclick-based testがwriteを取りこぼす実regressionと確定。unit test表面では検出できないUI位置結合の欠陥であり、CI instrumentation laneが捕捉した。(3) **本第2回再audit (head `6c73bbdb1dc06777a39b880939a030398281d751`)** — 修正commit `6c73bbdb1d` の検証: (a) `exchangeFlowItems` を `strategyPickerItems` の**後** (list末尾、Idle/Cancelled gateは `idleLike` checkで維持) へ移動しpickerの行位置をorigin/main同等へ復元、(b) `ExchangeFlowStateHolder` を `controllerFactory: () -> ExchangeFlowController` + `by lazy(NONE)` とし、`ExchangeFlowModule.controller` (LauncherAppState・startup reconciliation副作用) をflow開封まで遅延 — renderだけの画面・instrumentation testがwiring副作用を負わない。`8c2fc68cc1..6c73bbdb1d` の差分はこの2 fileのみ (production code、他になし)。 (4) **本第3回再audit (head `1a0da578cc9377279fe1a2501a0ab611bb23b0ed`)** — ChatGPT PR review "Request changes" (対象 `ef3edfb305`、P1×2/P2×2) への対応commit `1a0da578cc` を検証: (a) 送信成功後のdisclosure取消がactive sessionを失効させ得る問題を `ExchangeDisclosureState` (disclosure自身のsession bind・初回transport successでsent化・sent後は非cancelable) で解消、(b) paste受領時にallocation-boundedなenvelope検査 (`acceptsExchangeImportEnvelope`) を前置しoversized textがCompose stateへ入らない、(c) `ExchangeImportPipeline` を prepare (envelope→framing→decode) / validate (session→digest→reconstruction→validator) に分離し、untrusted replyのfail-closedがcanonical compositionより先行、(d) encode失敗時のghost session cleanup (injectable encoder)。新規test 10件 (controller 4 / disclosure state 5 / parser receipt 1)。全4指摘を解消確認。

監査時の状態確認 (第2回再audit時点): branch `issue-205-implementation` は head `6c73bbdb1dc06777a39b880939a030398281d751`、working tree clean (本監査記録の未commit fileのみ)。`0cf82bc1e6..6c73bbdb1d` は実装commit `9ecd2ff761` + 第1回再audit対応3 commit + 本修正commit `6c73bbdb1d` (`ManualOrganizationPreferences.kt` +31/−13相当、`ExchangeFlowUi.kt` +12/−2相当)。accepted specはPR tree内 (`specs/205-external-agent-exchange/`、`cdb64a75b2` とbyte同一)。PR labels: `risk: layout-data` (gate対象) + `risk: privacy`。

## Scope

対象diff (`0cf82bc1e6..6c73bbdb1d`。実装本体は `9ecd2ff761`、以降は再audit対応3 commit + UI配置修正 `6c73bbdb1d` — Findingsの解消記録参照):

- 新規純粋module `lawnchair/src/app/lawnchair/organizer/personalization/exchange/` (#205所有、Android-free。`PurityGuardTest` がpersonalization配下を再帰検査し本audit実行で通過): `ExchangeContract.kt` (INTENT/CONTEXT完全行marker定数、`MAX_EXCHANGE_IMPORT_BYTES` = 1 MiB)、`IntentImportParser.kt` (framing抽出 + allocation-bounded UTF-8 byte上限判定)、`ExchangePackageComposer.kt` (英文4-section instruction + CONTEXT marker data block、`parsePackageStructure`)、`ExchangeGenerationGate.kt` (session置換確認の判断部)、`SessionExportReconstructor.kt` (session固定ref map + 現在structural入力からの検証用export view再構築)、`ExchangeImportPipeline.kt` (envelope → framing → codec → session → expiry → digest一致 → 再構築 → #204 validator、統一typed failure 16種)。
- #204実装へのadditive変更 1 file: `ContextExportBuilder.kt` — per-item導出の `toExportItemCore` 共有化のみ (純粋抽出。#204契約・validator/builder意味の変更なし。既存 #204 test suiteは無修正で通過)。
- 新規integration `organizer/integration/exchange/`: `ExchangeInputAdapter.kt` (export生成とimport再構築が共有する単一canonical-input adapter。`composeFullOrganization` seam経由)、`ExchangeFlowController.kt` (gate → build → durable save → compose順序、disclosure-cancel無効化、echoed `exportId` によるsession lookup)、`ExchangeTransports.kt` (clipboard / Share Sheet / SAF file。file読取りはlimit+1 byte bounded read)、`ExchangeFlowModule.kt` / `ExchangeSessionStoreModule.kt` (process-local wiring)。
- Run接続: `organizer/ui/ManualOrganizationRun.kt` — `start(trigger, intent)` 追加、facadeの `WithIntent` default method (`applyIntent`: `intentPreferences` への純粋projection注入 + provenance identity差し替え、intent==null時はcompositionをそのまま返す)。
- UI: `organizer/ui/exchange/ExchangeFlowUi.kt` (新規~680行。privacy選択 → 置換確認 → 生成 → 送信前確認 → transport / import → typed失敗16種表示 → validated intentからのfresh run開始。controllerは `controllerFactory` + lazyでflow開封まで生成を遅延)、`ui/preferences/destinations/ManualOrganizationPreferences.kt` (exchange itemsはstrategy pickerの後にlist末尾としてrender、表示は `State.Idle` / `State.Cancelled` のみ (`idleLike` check) — V1 entry rule準拠 + picker行位置のorigin/main同等維持を確認)。strings en/ja 各56 key (key set一致を機械比較で確認)。
- 新規test 6 file + run test 1件追加 (`ExchangeFlowControllerTest` 6, `ExchangeGenerationGateTest` 4, `ExchangeImportPipelineTest` 8, `ExchangePackageComposerTest` 5, `IntentImportParserTest` 15, `SessionExportReconstructorTest` 5, `ManualOrganizationRunTest` +1)。main由来の既存test fileは無修正。

確認したruntime書き込み経路とmigration対象:

- **新規layout DB書込経路なし**。diffはwriter class (`ModelWriter` / `LayoutWriteCoordinator` / provider / `organizer/application/**`) に触れない。書き込みは (1) #204 `AndroidExportSessionStore` のsession file (`save` / `invalidate`。app-private・backup対象外・TTL 24h。#204既存実装をそのまま利用)、(2) clipboard (`ClipboardManager.setPrimaryClip`)、(3) SAF user選択URIへのtext file書出し (`FileExchangeTransport.write`) のみ。`LayoutWriterExchangeItemTitleSource.read` は `captureCurrent` (read-only capture) のみ。`ExchangeFlowModule` の `ensureOrganizerStartupReconciliation` は `ManualOrganizationModule` と同一の既存startup helper。全失敗経路 (framing / decode / session不在・失効 / digest不一致 / transport失敗 / gate `Busy`) はtyped resultを返すのみでzero-write。
- DB schema変更・migrationなし。rollback = 導線非表示 (残余は #204 session fileのみ、TTLで自然失効)。

## Criteria check

spec 205 (status: accepted。初回audit時はbranch `issue-205-spec-plan` (`cdb64a75b2`) で確認、merge `40639b4188` により再audit時点でPR tree内 `specs/205-external-agent-exchange/spec.md` に存在) の受入条件のうち、本PRで自動検証可能なものを独立に確認した。AC-9 / AC-10 は手動・実機evidenceでありPR本文で後続evidence PRへ明示的に繰り延べられているため **本auditの対象外** (不備として扱わない)。

- **AC-1 (package構造とinstruction/data分離の機械検証)**: 満たす (unit level)。`ExchangePackageComposer.compose` は生成時に完了するimmutable値 (英文instruction + `-----BEGIN/END NUNULAUNCHER CONTEXT-----` 行で囲んだcanonical JSON単行) を返し、`parsePackageStructure` がCONTEXT marker完全行による構造検証 (marker対一意・data非空・header/footer非空) を行う。round-trip test (`composeAndParseRoundTripRecoversTheExactContextData`)、4 section (Goal / You may / You must / Response format) とINTENT marker・`"personalized-intent-v1"` の埋込みtest、tamper reject test (marker欠落/重複/空data/footer欠落) を確認。end-to-end UXはUI (ExchangeFlowUi) で定義済み。最終UX確認はAC-10 evidence側。
- **AC-2 (既定exportの除外field、partial)**: 満たす (contract level)。composerは #204 `ContextExportBuilder` 出力canonical JSONを改変せずverbatimでdata部へ置き、instruction部は静的英文のみ (user data不在)。既定tier (`EXTERNAL_REDACTED`) のfree-text class全体除外は #204 suiteの `externalRedactedTierExcludesTheWholeFreeTextClass` (本auditのtest実行に含む) が担保。package levelの再検証はcomposer round-trip (data block = export JSON verbatim) との合成で成立。raw package名・内部ID・profile identifier・raw usage/timestamps・diagnostics metadataを追加する新規経路なし。
- **AC-4 (exchange framing)**: 満たす。`IntentImportParser` は (a) marker対前後の自由文を許容、(b) marker不在・END先行を `FRAMING_MISSING`、marker行複数出現 (nested相当行を含む) を `FRAMING_AMBIGUOUS`、有効marker対かつ空白のみの領域を `FRAMING_EMPTY` のtyped失敗として返す (判定順序: envelope → BOM/CRLF/CR正規化 → marker走査。`isMarkerLine` は行頭行末ASCII space/tab除去後の完全一致・case-sensitive)、(c) payload内JSON string value中のmarker相当部分文字列は完全行一致にならず抽出を乱さない (`markerLookingSubstringsInsidePayloadLinesDoNotBreakExtraction`)、(d) CRLF/CR入力はLF正規化後に同一結果・冪等 (`crlfCrAndBomInputsNormalizeToTheSameResult`, `parsingIsDeterministicAndIdempotentForTheSameInput`)。**再audit**: BOM fixtureは `74da17cf30` で `"\uFEFF" + lf` となり、先頭BOM除去分岐がtest corpusで実際に実行される (15 tests / 0 failures で通過)。(e) envelope上限は **正規化・走査の前に** `INPUT_OVERSIZE` でzero-write拒否 (上限内の巨大prefix + 小payloadは受理)。実装は `utf8ByteLengthExceeds` により char数fast reject (UTF-8 byte長 ≥ char長) + 上限到達打切りのbounded byte count で、`toByteArray` の同サイズ確保を回避 (approving review noteどおり)。境界値test (exact-limit受理 / limit+1拒否 / multibyteはbyte換算 `日` = 3 bytes / marker不在巨大入力) を確認。
- **AC-5 (typed failure 16種、partial)**: 満たす (unit level)。`ExchangeImportFailure` は #205側4種 (`InputOversize` / `FramingMissing` / `FramingAmbiguous` / `FramingEmpty`) と #204 12 classの `Contract` wrapの統一surface。UI (`exchangeFailureText`) は16種すべてに一対対応の文字列を持ち、`INPUT_OVERSIZE` (「周囲の文章を減らして」) と #204 `OVERSIZE` (「intent本体が大きすぎる」) のUX案内が区別されている。pipeline testが代表性10種 (SchemaMismatch / ExportMismatch / SessionExpired / ContextStale / Oversize / MobilityContradiction + envelope 4種) のpassthroughを検証。UI表示test (instrumentation) は繰り延べ対象。
- **AC-8 (plain-text exchange・provider非依存)**: 満たす (依存review)。manifest変更なし・`ACTION_SEND` receiver登録なし・ContentProviderなし・UI automation・private APIなし。使用Android APIは `ClipboardManager`、outgoing `ACTION_SEND` + chooser、SAF (`CreateDocument` / `OpenDocument`) のみ。交換対象はversioned text schema (`personalization-context-v1` / `personalized-intent-v1`) のみ。
- **AC-11 (process recreation、unit level)**: 満たす。`ac11ProcessRecreationResolvesTheSessionThroughTheDurableStore` がcontroller破棄→新規controller over同一durable storeでのimport成功を検証。失効は `SESSION_EXPIRED` (test: `expiredSessionIsSessionExpired`。`load` は期限切れも返す #204契約どおり)、session不在は `EXPORT_MISMATCH`。**reconstruction parity contract test** (`SessionExportReconstructorTest`): 同一structural stateからの再構築がauthority field (ref対応・role・mobility・fixReason・page/region affinity・category) で一致 (builderと `toExportItemCore` を単一実装で共有 — parityが構造的に保証される)、label/signal変化はdigestにもviewにも無影響、structural変化 (移動・lock・追加) はdigestを変え、session refが現在状態へ解決不能な場合は `Diverged` → pipelineの **digest一致検査が再構築より先** にあるため `CONTEXT_STALE` に収束 (`structuralChangeAfterExportConvergesOnContextStale` がmoved/locked/added/removedの4系統を検証)。再構築viewの省略field (label / usageSignals) は `IntentValidator` の入力に現れないことを実装読解で確認 (validatorは `export.items` のref/mobility と `exportId` / `grid.pageCount` のみ使用)。#205側の新規永続化なし (durableは #204 `ExportSessionStore` のみ)。「import成功 → fresh run → preview」のintegration・実機evidenceは後続evidence PR (AC-11後半)。run接続のunit証跡として `startWithValidatedIntentInjectsTheProjectionIntoTheComposedInput` がvalidated intentのprojection identityがplanner入力 (`intentPreferences`) へ到達することを検証。
- **AC-12 (送信前確認とtransport対象の同一性)**: 満たす (unit/構造 level)。packageは生成時に完了するimmutable値 (`ExchangeScreen.Disclosing(packageText, tier)` — immutable data class)。送信前確認screenは当該 `packageText` 全文を表示し、3 transportはすべて同一の `screen.packageText` を引数で受ける (transport adapterはpackageを再構成・差し替えする経路を持たない — 引数のみ)。transport操作はDisclosing screen上のみに存在し、確認前の送信経路はない。privacy mode変更はcancel→再選択→再生成となり、送信前確認からやり直す (再生成は新session。flow内再生成のgate不要条件には該当しない運用だが契約違反ではない)。composer→transport同一性の独立unit testはround-trip + 構造読解で担保 (UI順序testはinstrumentation側)。
- **AC-13 (session置換確認・取消)**: 満たす (unit level)。`ExchangeGenerationGateTest` がtruth table 4 case (live sessionなし→Proceed、live session + 未回答→RequiresConfirmation、承認→Proceed、辞退→Aborted) を検証。`ExchangeFlowControllerTest` が (1) 生成順序 (build → `save` 成功確認 → compose。`storeSaveFailureSuppressesGenerationFailClosed` がsave失敗時のpackage不出版とsession不在を検証)、(2) **E1送信済み → 置換承認によるE2生成 → E2取消 → E1 import = `EXPORT_MISMATCH` (zero-write)** のrepresentative scenario、(3) 辞退時はE1がactiveのままimport可能な対照、(4) E2取消が当該sessionのみをinvalidate (store active==null、E1は既にE2 saveで上書き済み) を検証。UIはReplacementConfirm screen (明示確認なしに生成へ進まない経路構造) を持つ。確認UI testはinstrumentation側。

**既存経路の非回帰**: `ManualOrganizationRun.start(trigger)` は `start(trigger, intent = null)` へ委譲し、`Operation.intent` はdefault null、compositionは `composeFullOrganizationWithIntent(null)` → default method → `applyIntent(composition, null)` はcompositionを**そのまま返す** (早期return。provenance copyなし・sentinel維持)。production applicationは`WithIntent`をoverrideしないためこのdefault経路が実経路。`ContextExportBuilder` の `toExportItemCore` 抽出はfield導出の等価変換。main由来test file無修正で全organizer test通過 (下記)。

## Executed test surface

**第2回再audit (対象commit `6c73bbdb1dc06777a39b880939a030398281d751` のcheckout上、2026-09-16、macOS darwin 25.5.0 arm64、JDK 21 / SDK 36.1、local emulator `emulator-5554` = AVD nunu_qpr2_api36_1 / API 36)**:

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL** (exit 0)。
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL** (exit 0)。test結果XML集計: **1,285 tests / 0 failures / 0 errors / 0 skipped** (organizer 1,249、navigation+bugreport+backup 36 — 第1回再auditと同数。UI配置修正はtest数不変)。
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL** (exit 0)。
- **instrumentation (regression修正の独立確認、実装者の同名runとは別に本audit sessionが実行)**: `./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=app.lawnchair.organizer.ui.ManualOrganizationProductionE2EInstrumentationTest,app.lawnchair.organizer.ui.ManualOrganizationPreferencesInstrumentationTest,app.lawnchair.organizer.ui.StrategyPickerInstrumentationTest,app.lawnchair.organizer.ui.MissingAppSelectionInstrumentationTest` → **BUILD SUCCESSFUL in 6m 15s** (exit 0)。結果XML: **64 tests / 0 failures / 0 errors / 0 skipped** — `ManualOrganizationPreferencesInstrumentationTest` 41 / `StrategyPickerInstrumentationTest` 11 (第1回再audit headでfailした2 testを含む) / `MissingAppSelectionInstrumentationTest` 6 / `ManualOrganizationProductionE2EInstrumentationTest` 6、すべて0 failures。
- `git status` / `git log` — branch head = `6c73bbdb1d`、`8c2fc68cc1..6c73bbdb1d` の差分が `6c73bbdb1d` 単一commit (2 production fileのみ) であることを確認。working tree clean (本監査記録のみ未commit)。
**第3回再audit (対象 head `1a0da578cc9377279fe1a2501a0ab611bb23b0ed`) の実行結果**:

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL** (exit 0)。
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL** (exit 0)。実装session実測 **1,259 organizer tests / 0 failures** (exchange系新規10件含む)。
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL** (exit 0)。
- instrumentation (CI issue52 class set 4 class) → 実装sessionがlocal emulatorで **BUILD SUCCESSFUL**。
- CI run照合 (`gh api repos/nunu1733/NunuLauncher/actions/runs/35007575114`): `event=pull_request`、`head_sha=1a0da578cc9377279fe1a2501a0ab611bb23b0ed`、workflow `ci.yml`。**run conclusion = success。全job成功** — source jobs成功、`organizer-instrumentation-issue52-tests` 成功、**`final-status` success** (job: https://github.com/nunu1733/NunuLauncher/actions/runs/35007575114/job/104515537606)。同headのhigh-risk-gate runのfailは本監査記録の未commitによるものであり、docs-only commitで解消する。

第2回再audit (対象 head `6c73bbdb1dc06777a39b880939a030398281d751`、参考記録):

- CI run照合 (`gh api repos/nunu1733/NunuLauncher/actions/runs/35003429969`): `event=pull_request`、`head_sha=6c73bbdb1dc06777a39b880939a030398281d751`、`head_branch=issue-205-implementation`、`pull_requests=[325]`、workflow `ci.yml`。**run conclusion = success。全13 job成功** — source jobs (`organizer-unit-tests` / `check-style` / `build-debug-apk`) skipなし成功、`organizer-instrumentation-issue52-tests` 成功 (job: https://github.com/nunu1733/NunuLauncher/actions/runs/35003429969/job/104497271985)、**`final-status` success** (job: https://github.com/nunu1733/NunuLauncher/actions/runs/35003429969/job/104501406942)。同headのhigh-risk-gate run [35003429988](https://github.com/nunu1733/NunuLauncher/actions/runs/35003429988) は「audit記録なし」でfail中 — 本記録をPRへdocs-only commitとして乗せることで解消される (gate lineage規則: head以降の変更はdocs配下のみ許容)。

第1回再audit (対象commit `8c2fc68cc15ac7efc6699360a795225178327fc2`、参考記録):

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL** (exit 0)。
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL** (exit 0)。**1,285 tests / 0 failures / 0 errors / 0 skipped** (organizer 1,249)。`IntentImportParserTest` 15 tests / 0 failures (実BOM fixtureを含む)。
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL** (exit 0)。
- CI run照合 (run [35000215963](https://github.com/nunu1733/NunuLauncher/actions/runs/35000215963)): `event=pull_request`、head SHA一致、`pull_requests=[325]`。audit時点ではsource jobs成功・`final-status`未確定と記録したが、**run完了後 `organizer-instrumentation-issue52-tests` が上記2 testのtimeoutでfailし `final-status` failure となった** (監査履歴参照。次head `6c73bbdb1d` で解消)。

初回audit (対象commit `9ecd2ff76186ac2916dc9afd08a1944cabb6c223`、参考記録):

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL** (exit 0)。
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL** (exit 0)。**1,285 tests / 0 failures / 0 errors / 0 skipped**。内訳: organizer 1,249、navigation+bugreport+backup 36。新規testの実行確認: exchange 6 file計43 tests (ControllerTest 6 / GateTest 4 / PipelineTest 8 / ComposerTest 5 / ParserTest 15 / ReconstructorTest 5) + `ManualOrganizationRunTest` 1件追加 (class全体52 tests)。`PurityGuardTest` (personalization配下のAndroid import禁止、exchange subpackageを含む) 含めて通過。
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL** (exit 0)。
- CI run照合 (旧head分、run [34998299161](https://github.com/nunu1733/NunuLauncher/actions/runs/34998299161)): `event=pull_request`、head SHA一致、`pull_requests=[325]`。audit時点でsource jobs成功・`final-status`未確定。

## Findings

第3回再audit時点の結論: **P1なし・未解消のP2なし**。ChatGPT PR review (対象 `ef3edfb`) のP1×2/P2×2は `1a0da578cc` で全て解消済み (上記監査履歴4)。第2回再audit時点の結論: **P1なし・未解消のP2なし・未解消のP3なし** (初回P2-1/P3-1/P3-6、第1回再audit後CI発見のpicker配置regression、すべて解消済み、下記)。残る記録は備考のみ。

**R-1 (UI配置regression — 第1回再audit headでCI発見、解消済み)**: `8c2fc68cc1` のCI run [35000215963](https://github.com/nunu1733/NunuLauncher/actions/runs/35000215963) で `organizer-instrumentation-issue52-tests` が `StrategyPickerInstrumentationTest` 2 test (`selectingAStrategyPublishesThroughTheValidatedWriteCommand` / `selectingAStrategyMovesTheSingleSelectedParentRow`) のtimeoutでfailした (`final-status` failure)。原因はIdle branch内・strategy picker上位にrenderされたexchange entry rowがpicker radio行を押し下げ、scrollしないclick-based testが選択writeを取りこぼしたこと (実装者のorigin/main baseline実験で因果確認)。**`6c73bbdb1d` が修正**: (a) exchange itemsを `strategyPickerItems` 後のlist末尾へ移動 (表示はIdle/Cancelledのみの `idleLike` gateで維持 — origin/mainのpicker行位置を復元)、(b) `ExchangeFlowStateHolder` のcontroller生成を `controllerFactory` + `by lazy(NONE)` でflow開封まで遅延 (renderのみの画面・instrumentationが `LauncherAppState` / startup reconciliation副作用を負わない)。遅延のthread-safety: すべてのflow入口 (openFlow/openImport) はMain threadでcontrollerを初回初期化してからIO coroutineがaccessするため `LazyThreadSafetyMode.NONE` で安全 (単一construction site、flow外のcontroller accessなし)。**検証**: 本audit sessionがlocal emulatorで4-class instrumentationを独立実行 (64 tests / 0 failures、失敗していた2 testを含むStrategyPicker 11 tests全通過)、新CI run [35003429969](https://github.com/nunu1733/NunuLauncher/actions/runs/35003429969) は全13 job成功・`final-status` success。教訓として本auditにも学習記録を残す: unit test表面だけでは捕捉できないUI位置結合があり、high-risk PRではCI instrumentation laneの完了確認までをもmerge gate判定に含めるべき (第1回再auditはrun進行中の段階で記録したためこのfailを予見できなかった)。

**P2-1 (新規経路のみ・パフォーマンス/ANR risk) — 初回auditで指摘、解消済み**: 初回auditでは `ExchangeFlowStateHolder.import()` が `withContext(Dispatchers.Main)` の内側で `run.start(intent = pipeline.validated)` を呼び、`ManualOrganizationRun.start` の同期的な detection → full-target composition (favorites DB capture) → planning がmain threadで実行される点を指摘した。**`8c2fc68cc1` が修正**: `run.start` は `Dispatchers.IO` block内で実行、`status` / `screen` の状態更新のみ `withContext(Dispatchers.Main)` に退避。分岐semanticsは不変。zero-write・gate semanticsへの影響なし。

P3 (記録のみ、merge blockingとしない):

- **P3-1 (test gap) — 解消済み**: `74da17cf30` がBOM fixtureを `"\uFEFF" + lf` に修正、再実行で15 tests / 0 failures。
- **P3-2 (二重解析) — 解消済み**: `1a0da578cc` がpipelineをprepare/validateに分離し `echoedExportId` を撤去、parse/decodeを1回に一本化。
- **P3-3 (送信後取消) — 解消済み**: `1a0da578cc` の `ExchangeDisclosureState` により送信成功後のdisclosureは非cancelable化され、session失効は発生しない (PR review P1対応)。元の記述: 送信前確認screenはtransport成功後もCancelを残す。送信済みpackageの取消は当該sessionをinvalidateし、以降の当該宛replyは `EXPORT_MISMATCH` になる。fail-closed方向であり spec はこの場合を規定しないが、launcherは送信済否を判定できない (spec自身の前提と同一)。`cancelDisclosure` はflow固定sessionでなくstoreの現在active sessionを無効化するが、単一holder・単一processでは同等。
- **P3-4 (貼付経路の受領時上限) — 解消済み**: `1a0da578cc` が受領時 (`onImportTextChange`) に `acceptsExchangeImportEnvelope` を適用。元の記述: OutlinedTextFieldへの貼付はUI受領時にenvelope上限を前置検査しない (import実行時にpipelineが検査)。specは「可能な経路では全量保持の前にfail」と条件付けており、file経路 (limit+1 bounded read) は満たしている。
- **P3-5 (UX)**: Generating screenに取消affordanceがない (生成はbounded)。label-inclusive再選択にはcancel→再openが必要。
- **P3-6 (merge順序・gate機械検証の前提) — 解消済み**: merge `40639b4188` によりaccepted spec/plan + canonical docsがPR tree内。残る手続きは本監査記録をPRへdocs-only commitとして乗せること (head `6c73bbdb1d` のgate run [35003429988](https://github.com/nunu1733/NunuLauncher/actions/runs/35003429988) は「audit記録なし」でfail中であり、commitで解消する)。
- **P3-7 (数値整合の備考)**: PR本文は「1243 organizer tests」と記載しているが、同一filter setでの本audit実測はorganizer 1,249 (total 1,285、0 failures。3回のaudit roundすべて同数)。証跡は本記録の実測値を正とする。

対象外として記録: AC-9 (TalkBack / Switch Access / large font / clipboard・share target不在のUX evidence) とAC-10 (physical-device representative workflow evidence) はPR本文のとおり後続evidence PRで実施予定。AC-1/AC-3/AC-5/AC-6/AC-7/AC-11 (後半)/AC-12/AC-13 のうちUI/instrumentation・実機に属する半分も同様に後続対象 (本auditはunit/構造検証まで)。
