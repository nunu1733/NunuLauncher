# High-risk audit: PR #399 取り込み済み提案（pending intent）をdurable化しhub status cardへ接続する（D-08）

> Status: accepted
> Audit date: 2026-09-22

- Auditor: 独立audit session（ZCode orchestrator配下のgeneral-purpose subagent）。本PRの実装は別session/subagentが行っており、監査主体は実装に一切関与していない（AGENTS.md「高リスクPRの独立エビデンス」のsolo保守における独立session要件を満たす）。実装側の主張（PR本文・plan checklist）は位置確認にのみ参照し、本記録の判断は監査sessionが自ら実読したdiff・test本文・spec本文と、自ら実行した`git` / `gh`検証の結果のみによる。
- PR: https://github.com/nunu1733/NunuLauncher/pull/399 （base `main`、head `issue-374-spec-plan`）
- Head SHA: 34d405a333fbf84ab4ea35a13156cc61aea00fb9
- Head SHA検証: `git rev-parse origin/issue-374-spec-plan` とCI run 35628752080の`headSha`、`gh pr view`の`headRefOid`の3者が一致することを機械確認済み
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35628752080 （`pull_request` event・ci.yml。`gh run view 35628752080 -R nunu1733/NunuLauncher --json conclusion,headSha` で `conclusion=success`・`headSha=34d405a3…` を確認。`gh pr checks 399 -R nunu1733/NunuLauncher` で `final-status` pass、source job 3種（`organizer-unit-tests` / `check-style` / `build-debug-apk`）がskipなしでsuccess、全organizer instrumentation lane（api35 / db-migration / issue52 / issue53 / issue99 / issue155 / issue299 / issue332 / shared-writer）passを確認）
- Criteria: specs/374-durable-imported-intent/spec.md の受入条件 DI-AC-01, DI-AC-02, DI-AC-03, DI-AC-04, DI-AC-05, DI-AC-06, DI-AC-07, DI-AC-08, DI-AC-09, DI-AC-10, DI-AC-11, DI-AC-12, DI-AC-13 （FR-017）
- Criteria: specs/328-exchange-import-success-state/spec.md revision 2 の改訂6項目と維持条項 AC-2, AC-3, AC-4, AC-5, AC-7
- Criteria: specs/205-external-agent-exchange/spec.md pending保持規定改訂と不変条項 AC-11, AC-12, AC-13
- Criteria: specs/366-organizer-hub-shell/spec.md HUB-AC-03
- 前提文書（本文中で参照。処分の正本）: docs/product/organizer-disposition-migration.md §3.14（#328のAmend-Supersede所有境界）・§7.1（新store・読取時reconcile正本・必須oracle）

## Scope

対象diffは `git diff main..origin/issue-374-spec-plan` （head `34d405a333`時点）で **31 files、+5672 / -140**。production変更は次のとおり（specs/docs/testsを除く全列挙）:

| File | 変更 |
|---|---|
| `lawnchair/src/app/lawnchair/LawnchairApp.kt` | `ensureOrganizerStartupReconciliation()` のtrigger thread **先頭** へ `PendingImportStartupReconcile.reconcileAtStartup()` hook追加のみ（DI-AC-05 / Contract notes 8）。runCatchingで包まれ、model load待ち・readiness gateに依存しない。同fileは高リスクpath一覧に該当するためhigh-risk gateがlabelなしで適用されているが、変更はDB rename/migration書込を含まない上記hookのみであることをdiff実読で確認 |
| `lawnchair/src/app/lawnchair/organizer/personalization/PendingImportedIntentStore.kt` | 新規。純粋seam（`save` / `load` / `discard` / `delete` / `deleteIf`）+ record model `DurablePendingIntent`（identity: `exportId`・`intentIdentity` schemaVersion+digest・canonical decisions（export-scoped ref + `proposalLabel` text含むexactly-one-of groupSemantic）・planner-effective `minimizeMovement`・失効時刻echo・entryKind（IDLE/RUN_IN）・tombstone mark・作成時刻）。`rationale` / `confidence` / 対応表 / 人間可読item情報は**fieldとして存在しない**（型で保証） |
| `lawnchair/src/app/lawnchair/organizer/integration/AndroidPendingImportedIntentStore.kt` | 新規。production constructorは `context.noBackupFilesDir`（backup除外）+ `AtomicFile` + `SCHEMA_VERSION=1` 検査 + `synchronized(lock)`。欠損のみが「recordなし」、存在するfileの読取失敗・corruption・未知schema・構造不正はfail-closed清掃。`discard()` はtombstone atomic書換成功後のみbest-effort物理削除。`deleteIf` は全field一致のcompare-and-delete |
| `lawnchair/src/app/lawnchair/organizer/personalization/exchange/PendingIntentReconcile.kt` | 新規。読取時reconcile純粋関数（tombstone / session不在 / exportId不一致 / TTL（session値を正とする）/ ref集合不一致 / 未知desiredGroupRef / 未知categoryRef → 全てInvalid）+ `durableImportSummary`（record+sessionからの件数サマリ再構成。`exchangeImportSummary` と同一入力基準） |
| `lawnchair/src/app/lawnchair/organizer/integration/exchange/PendingImportStartupReconcile.kt` | 新規。起動時reconcileのstore完結アプリケーション点（Invalidのみ `delete()`、Valid/Absentは書込なし、never throws） |
| `lawnchair/src/app/lawnchair/organizer/integration/exchange/PendingImportedIntentModule.kt` | 新規。module-per-concern accessor（`@Volatile` singleton。`ExchangeSessionStoreModule` と同一pattern） |
| `lawnchair/src/app/lawnchair/organizer/integration/exchange/ExchangeFlowController.kt` | `generate()` の `store.save(newSession)` 成功直後に `pendingImportStore?.delete()`（書込順序「新session保存 → 旧pending無効化」の固定。DI-AC-03）。store注入はoptionalで既存fixture互換 |
| `lawnchair/src/app/lawnchair/organizer/integration/exchange/ExchangeFlowModule.kt` | production注入に `PendingImportedIntentModule.store()` 追加のみ |
| `lawnchair/src/app/lawnchair/organizer/ui/exchange/ExchangeFlowUi.kt` | import settleのdurable保存commit条件化（保存成功で初めて `ImportSuccess` 採用。失敗は `ImportPersistenceFailure` typed面 / DI-AC-01/13）、attempt-fenced書込（`pendingWriteMutex` + pre-write再照合 + settle時 `deleteIf` フェンス）、CTA settle全分岐でのrecord不変（DI-AC-07）、破棄のtombstone化（`discardImport` → `store.discard()`。commit成功で初めて面close。失敗は `IMPORT_DISCARD_FAILED` typed / DI-AC-08）、置換のin-process無効化（Generated・EncodeFailure両終端 / DI-AC-03）、`openPendingImportReview`（読取時reconcile → ImportReview面。継続CTAなし / DI-AC-01）、D-13両入口共通確認dialog、summary blockの成功面/再開面共用extract |
| `lawnchair/src/app/lawnchair/ui/preferences/destinations/OrganizerHubPreferences.kt` | status cardのsession-scoped 2行追加（依頼行・提案行。durable status行と開始CTAの間。compose順=読み順「状態→残期限→操作」。提案行は読取のたびにreconcile、fail-closed、失効時刻に1回再読取 / DI-AC-09/11） |
| `lawnchair/src/app/lawnchair/ui/preferences/destinations/ManualOrganizationPreferences.kt` | hostへのdurable store注入・`ExchangeOpen` 1回消費pre-open（REQUEST→T-15、PENDING_REVIEW→ImportReview）・import破棄確認dialogのhosting（両入口共通1 dialog / DI-AC-08） |
| `lawnchair/src/app/lawnchair/ui/preferences/navigation/PreferenceRoutes.kt` / `PreferenceNavigation.kt` | `ExchangeOpen` enum引数追加（既存callerは既定nullで不変） |
| `lawnchair/src/app/lawnchair/organizer/personalization/IntentModels.kt` | `GroupSemantic.proposalLabel` のKDoc「never persisted」規定を本契約へ改訂（layout DB / category store非永続の明示 + spec 374の例外記載。spec Change history通り） |
| `lawnchair/res/values/strings.xml` / `values-ja/strings.xml` | 置換確認文言拡張（取り込み済み提案の破棄追記）・CTA copyのT-18語彙・破棄確認本文のdurable意味論・persistence失敗3 string・`IMPORT_DISCARD_FAILED` / `IMPORT_REVIEW_UNAVAILABLE`・hub行3 string（EN/ja双方。DI-AC-12） |

**runtime書き込み経路の確認**: 本diffが追加する永続化の宛先は `AndroidPendingImportedIntentStore` の `noBackupFilesDir/organizer_pending_imported_intent_v1.json`（AtomicFile）1箇所のみ。`git diff` 全文へのgrepで `favorites` / `ModelWriter` / `DatabaseHelper` / `LauncherProvider` / recovery storeへの接触は0件（一致は文書・コメントの説明文のみ）。`LawnchairApp.kt` 変更はhook追加のみで既存のlayout適用reconcileには触れない。run state・layout DB・export session・diagnostics journalへのwriteはspec「Data and state」契約どおり存在しない（`settleContinue` 全分岐と `openPendingImportReview` の実読で確認）。

**migration対象**: なし（新規store追加のみ。既存storeのschema変更・既存dataの変換はdiff上存在しない）。backup/restoreは `noBackupFilesDir` により対象外。downgradeは旧版に読取経路がなく寛容無視、再upgrade後の最初の読取でreconcileがfail-closed清掃する。

## Criteria check

test名は監査sessionがtest本文を実読し、assert内容とspec受入条件の対応を確認したもの（名前のみの採用はしていない）。unit testは `tests/unit/app/lawnchair/organizer/` 配下、instrumentationは `tests/organizer-instrumentation/app/lawnchair/organizer/` 配下。

- **DI-AC-01（import成功時のdurable保存・保存成功が採用条件・status card行→ImportReview cold到達・run state/layout DB不変・再構成oracle）: PASS。** 保存はsettle内: `settleImport` → `persistPendingImport`（record構築 → `launchDurablePendingIntentSave`）→ 成功で `adoptImportSuccess`（pre-#374の採用pathを無変更extract）、失敗で `ImportPersistenceFailure` — 「保存失敗で成功状態を採用しない」が分岐構造で保証される。record内容は (a)〜(g) 全field（`durablePendingIntentFrom` 実読。`intentIdentity` はimport時算出済み正本の保存、`expiresAtEpochMs` はsession複製echo）。run state・layout DBへのwriteは保存pathに存在しない。status card提案行は `OrganizerHubPreferences` のreconcile付き読取、行の操作は `ExchangeOpen.PENDING_REVIEW` → `openPendingImportReview()`（IO上でrecord+active session読取 → reconcile → `ImportReview` 面 = 件数サマリ（成功面と同一の `ExchangeImportSummaryContent`）+ 残時間（session失効時刻が表示正本）+ 破棄。継続CTAは存在しない（disabled placeholder含む — `ExchangeImportReview` 実読）。再構成oracle: `DurableImportSummaryTest#plannerProjectionRebuiltFromTheDurableRecordEqualsTheImportTimeProjection` がrecord+session+`SessionExportReconstructor` 再構成から `IntentPlannerAdapter.project` を再実行しimport直後のprojectionと全field同一をassert（複数 `proposalLabel` ケース "Morning"/"Evening"、saved identityの再利用 — `assertNotEquals(record.intentIdentityDigest, 再導出digest)` で「再導出ではなく保存済み正本」を固定、`globalMinimizeMovement`、`assertEquals(original, rebuilt)`）。`durableImportSummaryReproducesEveryCountOfTheImportTimeSummary` が件数サマリ全count一致を固定。process死相当: `AndroidPendingImportedIntentStoreTest#recordSurvivesStoreRecreationAcrossProcessDeath`（store再生成 = process死遷移でrecordが残る）。instrumentation: `hubShowsTheProposalRowWhenAValidRecordExists` / `hubProposalRowOpensTheRunFaceWithTheImportReviewPreOpened` / `importReviewRendersSummaryRemainingAndDiscardWithNoCta`。保存の単一active: `singleActiveSecondSaveOverwritesTheFirst`。
- **DI-AC-02（TTL=依頼と同一・破棄/置換で無効・残時間の正本はsession失効時刻・pre-send cancel連動）: PASS。** 有効性のsession従属は `reconcilePendingIntent` の `(session == null | exportId不一致 | now >= session.expiresAtEpochMs) → Invalid` で実装され、`PendingIntentReconcileTest#reconcileTableIsFailClosedForEveryMismatch` が「no active session」「session expired (境界+1ms)」「exportId mismatch」をtable固定。pre-send cancel（`store.invalidate`）はsession不在として同一のInvalid経路で無効化される。残時間表示はsession失効時刻から `requestRemainingDisplay`（#372 T-15と同一語彙）で導出され、recordのecho値は表示に使われない（`openPendingImportReview` とhub提案行の実装がともに `session.expiresAtEpochMs` を渡す）。TTL境界の行消滅: `hubProposalRowDisappearsAtTheTtlBoundaryWithTheRecordCleaned`（clock相当の失効時刻schedule再読取）。起動時: `PendingImportStartupReconcileTest#recordWhoseSessionExpiredIsDeleted` / `recordWithoutAnySessionIsDeleted`。破棄はDI-AC-08、置換はDI-AC-03で個別に後述。
- **DI-AC-03（置換承認で提案破棄・書込順序固定・同一process内の成功状態/process-local pendingも破棄・確認文言拡張・辞退時不変）: PASS。** 書込順序は `ExchangeFlowController.generate()` 内で `store.save(built.session)` の直後に `pendingImportStore?.delete()` が1箇所固定（encode/composeの前）。同一process側は `invalidateImportedIntentForReplacement()` が `pendingValidated` + 表示中の `ImportSuccess` / `ImportPersistenceFailure` を破棄し、**Generated と EncodeFailure の両終端**で呼ばれる（encode failureはreplacement commit後にしか起きないため — review finding 4対応）。oracle: `ExchangeFlowStateHolderTest#replacementGenerationDeletesTheDurableRecordAndClearsTheSuccessState` / `replacementEncodeFailureStillInvalidatesTheInProcessProposal` / `scopedReplacementGenerationClearsTheRunInSuccessState`。文言: `exchange_replacement_warning` と `exchange_replacement_notice` に「取り込み済みの提案も破棄されます。」（ja正本）と "The imported proposal is also discarded."（en）を追記（両locale diff実読）。辞退時不変: 既存oracle `ExchangeGenerationGateTest#declinedReplacementAbortsAndKeepsTheExistingSessionUntouched`（無編集）がspec 205 AC-13の回帰としてgreen — 辞退pathは `store.save(newSession)` に到達しないためpending deleteも発火しない（分岐構造で確認）。
- **DI-AC-04（backup対象外・restore後は空・downgrade寛容無視・再upgrade reconcile清掃・migrationなし）: PASS。** backup除外: `AndroidPendingImportedIntentStore` production constructorが `context.noBackupFilesDir` を使用（export session storeと同一class。spec 374 DI-AC-10の「機械確認」は配置の実読 + 下記DI-AC-10のbyte-level test）。寛容読み/fail-closed: `unknownSchemaVersionDegradesToNoProposalFailClosed`（downgrade相当の未知schema）/ `corruptedOrTruncatedStorageDegradesToNoProposalFailClosed` / `readFailureOverAnExistingRecordIsCleanedFailClosed`（存在するfileの読取失敗は清掃して「なし」— 再出現経路の遮断）。restore後の空状態は `noBackupFilesDir` 構造（backupされない）で担保され、不在読取は `null`（`Absent`）。既存data migrationは存在しない（diff review: schema変更・変換codeは0件）。startup清掃側も `corruptLoadFailsClosedWithoutThrowing` / `unreadableResidueIsPhysicallyCleanedAndNeverShown` で二重に固定。
- **DI-AC-05（読取時reconcileの起動時+読取時適用・fail-closed無効化・置換途中process death/write failure oracle・startupのみoracle・tombstone commit直後death/削除失敗oracle）: PASS。** reconcile純粋関数はtombstone / session不在 / exportId不一致（置換途中）/ TTL / ref集合不一致（欠損・過剰）/ 未知desiredGroupRef / 未知categoryRefの全ケースをtable-drivenに無効化（`reconcileTableIsFailClosedForEveryMismatch` — 各ケースが `Invalid` を返すことを1テストで全件assert、Invalidは当該recordを持ち帰りcallerが清掃）。適用点は (1) 起動時（`LawnchairApp` trigger先頭。`PendingImportStartupReconcileTest` 8件が「hub未開封startup」を独立oracle化 — `recordWithAMismatchedExportIdIsDeleted`（置換途中process death相当）/ `recordWhoseSessionExpiredIsDeleted` / `tombstoneRemainingOnDiskIsDeleted` / `absentRecordWritesNothing` / `corruptLoadFailsClosedWithoutThrowing` / `unreadableResidueIsPhysicallyCleanedAndNeverShown`）、(2) status card読取時（`hubCleansAStaleRecordAndShowsNoProposalRow` — 読取のたびにreconcileしInvalidを清掃）、(3) ImportReview読取時（`openPendingImportReviewWithAStaleRecordCleansItAndStaysClosed` / `openPendingImportReviewWithoutAnyRecordSurfacesTheTypedStatus` — 発明した面を出さないtyped status）。process death oracle: `recordSurvivesStoreRecreationAcrossProcessDeath`（新session保存直後のprocess死 → exportId不一致 → 次読取で清掃）に加え、holder側のattempt-fenceが遅延settleを排除（`latePersistenceSettleAfterACancelNeverAdoptsAFaceOrARecord` / `aReleasedStaleSaveNeverOverwritesTheNewerAttemptsRecord` / `aStaleAttemptWhoseSaveNeverFiredNeverOverwritesTheNewerRecord` — mutex+`deleteIf` equalityでfence 1/pre-write再照合とfence 2/settle時清掃を固定）。write failure注入oracle: `tombstoneCommitFailureIsObservableAndKeepsTheRecordValid`（書込失敗 → `false` 返却 → record有効のまま）。tombstone commit直後death/物理削除失敗: `discardCommitsTombstoneThenPhysicallyDeletes`（commit成功後のdelete失敗は無視されmarkが残る）+ reconcileのtombstone検証 + `tombstoneRemainingOnDiskIsDeleted`（再起動後清掃）で「破棄済み提案の再表示経路なし」を3層で固定。
- **DI-AC-06（spec 328 revision 2の作成・6項目・AC-3/AC-2/AC-4/AC-7維持・owner受入）: PASS（技術内容）/ owner受入は本PR reviewでの確定を残す（Findings 1）。** rev.2が本PR diffに含まれる（`specs/328-exchange-import-success-state/spec.md` +41行）。rev.2 status注記に6項目すべてが明記されていることを実読: (1) process-local規定のdurable契約への置換（durable保存成功が採用条件・typed失敗）(2) 破棄のtombstone 2段commitへの置換 (3) D-2→D-13（両入口確認1回）(4) CTA copyのT-18語彙「この提案で続ける」統一 (5) freeze規則の消失保護面のstatus card+読取時reconcileへの移管（in-flight freeze維持・「画面離脱は破棄ではない」明文化）(6) AC-3 single-flight/attempt anchor・AC-2/AC-4/AC-7本体の維持。AC本体の維持はAC-1〜AC-10の条項diff（AC-5のみ「(revision 2)」としてD-13/tombstone契約へ更新、他は本体不変）とtest oracle表の対応更新で確認。
- **DI-AC-07（CTA従来挙動の維持・anchor/single-flight/gate拒否の既存oracle green・settleでのrecord不変）: PASS。** 既存oracleは無編集でgreen（CI unit lane。`ctaGateRefusalsKeepTheSuccessStateAndAllowRetry` / `discardIsRefusedWhileTheCtaIsContinuing` 等 — spec 328 AC-3/AC-5対応）。durable追加の影響分離: `settleContinue` のSuccess分岐は `pendingDurableRecord = null`（in-process参照の解放のみ）でstore呼出を含まず、Busy/NotAttachable/Failed分岐もrecordに触れない — `ctaSuccessSettleLeavesTheDurableRecordUntouched` が「成功settleでwrite・削除なし」を直接固定（「継続成功は提案を消費しない」Contract notes 6 / #375契約との接続）。anchor契約: `runInSettleAfterTheOwningRunIsGoneFencesTheCommittedRecordAway`（owning runId不一致はwrite後にfence）/ `inputEditDuringAnInFlightSaveNeverLeavesAStaleRecord` / `retrySettleIsAnchoredAndNeverLeavesARecordAfterTheAttemptIsCancelled`。保存がsettleに1 hop加えるが、anchor token比較とmutex serializationによりsingle-flight/ABAの既存契約は変化しない（`beginImportAttempt` が前attemptのfenced無効化を継承しない実装を実読）。
- **DI-AC-08（破棄tombstone 2段commit・commit成功前closeなし・commit失敗typed失敗で提案保持・session失効なし・再取り込み成立・両入口確認1回）: PASS。** `discardImport()` は成功面（attempt token anchor、`continuing` 中不受理維持）とImportReview面（record自身がanchor）の両方をカバーし、`store.discard()`（tombstone atomic書換 → 成功後のみbest-effort削除）の結果を `settleDiscardImport` で適用 — commit失敗は `IMPORT_DISCARD_FAILED` typed statusで面と提案を保持し、成功で初めて面close（`discardCommitFailureKeepsTheSuccessFaceAndThePendingProposal` / `discardCommitFailureKeepsTheImportReviewFaceAndTheRecord` / store側 `tombstoneCommitFailureIsObservableAndKeepsTheRecordValid`）。両入口（破棄して閉じるbutton・system Back）はhostの**1つの**確認dialogに収束（`ManualOrganizationPreferences` の `pendingImportDiscard` / `ExchangeImportSuccessBackHandler(onDiscardRequest)` — spec 328 rev.2 D-13）。session不生存・再取り込み: 既存oracle `discardClosesTheSuccessStateAndKeepsTheSession` / `discardedReplyCanBeImportedAgainWhileTheSessionLives`（無編集green）。instrumentation: `importReviewDiscardConfirmsOnceThenClosesTheFaceAndDeletesTheRecord` / `importReviewDiscardCancellationKeepsTheFaceAndTheRecord` / `systemBackOnTheImportReviewClosesZeroWriteKeepingTheRecord`（Back自体はzero-write closeで確認不要 — 破棄は明示操作のみというD-13契約の正しい適用。`ExchangeRequestFlowContractTest#backOnTheImportReviewFaceIsThePlainZeroWriteClose`）。
- **DI-AC-09（TalkBack読み順「状態→残期限→操作」・Switch Access/keyboard完結・200% font scale到達）: PASS（instrumentation）。** `OrganizerHubPreferencesInstrumentationTest`（新規・CI issue52 laneでgreen）: `hubExchangeRowsReadStateRemainingThenActionInOrder`（compose順=semantics順で状態→残期限→操作をassert）/ `hubRowsExposeNameRoleAndStateToAssistiveTechnology` / `hubStatusCardStaysReachableAtTwoHundredPercentFontScale` / `hubTraversalReachesStartDiagnosticsAndMaterialsInOrder`（keyboard traversal）/ `hubFocusRestoresToStartAfterBackFromRunSurface`。破棄と開封のkeyboard完結はflow面のfocusRequester明示復帰（dialog dismiss後の破棄slot復帰）+ 上記traversal oracleで構成。light/dark × jaのscreenshot evidenceは本監査では独立reviewしていない（Findings 4）。
- **DI-AC-10（field契約の型/contract test・不在fieldの存在しないこと・`noBackupFilesDir` 機械確認・summary同一入力基準）: PASS。** `PendingImportedIntentMappingTest`: `recordModelCarriesNoRationaleConfidenceOrHumanReadableItemField`（`DurablePendingIntent` が `rationale` / `confidence` / app label / folder title / category displayName / ref↔内部ID対応表のfieldを持たないことの型レベル固定）/ `persistedBytesContainNoRationaleConfidenceOrFreeText`（**wire byte level**で自由文が永続されないこと）/ `toDecisionsMapRoundTripsEveryDecisionVariant` / `minimizeMovementKeepsOnlyThePlannerEffectiveValue`。保存field（`intentIdentity` schemaVersion+digest・export-scoped ref・`proposalLabel` text・失効時刻・entryKind・tombstone・作成時刻・schema version）はrecord modelとwire recordの実読で全て存在。`noBackupFilesDir` 配置はproduction constructorの実読 + spec 204 `AndroidExportSessionStore` と同一patternの確認。summary導出の同一入力基準は `durableImportSummary`（`exchangeImportSummary` の直接再呼出、`categoryKindByRef` はsession `categoryRefs` の `CategoryIdentity` 判別、`scopeCount` は `session.scopeCandidates.size` — spec通り）と `DurableImportSummaryTest` で固定（projection等価oracleはDI-AC-01）。
- **DI-AC-11（spec 205/366改訂・依頼行/提案行のinstrumentation・既存regression green）: PASS。** spec 205: pending非保持規定（「typedな案内でzero-write終了し、validated intentを保持しない」）をdurable pending intent store保存への参照へ改訂、AC-11を#374 store所有へ改訂、AC-12/AC-13のgate構造は不変（diff実読。change historyに#374改訂を記録）。spec 366: HUB-AC-03の否定的観測から進行中AI依頼行・取り込み済み提案行を除外（復元CTA・run結果は維持）しtest oracle行も更新。行のinstrumentation: `hubShowsTheActiveRequestRowWithStateRemainingAndAction` / `hubHidesTheRequestRowWhenNoSessionExists`（active session不在時の行なし = fail-closed）/ `hubRequestRowOpensTheRunFaceWithTheRequestFlowPreOpened` / `hubShowsTheProposalRowWhenAValidRecordExists`（存在・残時間・T-15/ImportReview到達）。既存exchange系regression（`ExchangeFlowControllerTest` / `ExchangeImportPipelineTest` / 失敗表示・freeze対応test群）は `app.lawnchair.organizer.*` unit gateに含まれgreen（CI `organizer-unit-tests`）。
- **DI-AC-12（新規・改訂stringのEN/ja双方・format resource構成・既存語彙と非衝突）: PASS。** strings diff実読: 新規9 string（hub行3・persistence失敗3・discard失敗1・review unavailable 1…両localeで同name集合）+ 改訂4（replacement warning/notice・CTA copy 2種・discard confirm body）が `values/` と `values-ja/` の双方に存在。残時間は既存 `exchange_request_remaining_hours` plurals / `exchange_request_remaining_under_hour` を再利用（新規複合文の追加なし）。機械確認: `ExchangeFlowStateHolderTest#issue374StringRevisionsArePresentInBothLocales` / `issue374HubRowAndImportReviewStringsExistInBothLocales` が両locale XMLのname集合・内容をassert。`spotlessCheck` green（CI `check-style`）。
- **DI-AC-13（persistence失敗のtyped扱い・D-11様式・再試行で成功状態へ・中断で非保存・status card行なし）: PASS。** 面は `ExchangeImportPersistenceFailure`（primary remedy = もう一度保存する〔保存のみ再実行。validationは繰り返さない — `retryPendingIntentSave` がanchored recordの `store.save` だけを再実行〕+ 常設の中断する・診断を開く — #373 D-11投影様式）。oracle: `durableSaveFailureAdoptsTheTypedPersistenceFaceAndTheRetryAdoptsSuccess`（失敗 → ImportSuccess不採用 → retry成功で成功状態採用）/ `retrySaveFailureKeepsTheFaceAndSurfacesTheTypedNotice`（retry失敗でtyped statusが観測可能）/ `persistenceFailureInterruptClearsTheAttemptWithoutPersisting`（中断 = 保存されずclose）。中断後の提案非保存は「recordが存在しない → reconcile `Absent` → status card行なし」で構造的に保証（`Back` on persistence-failure face = zero-write close、`exchangeBackAction` の実読）。typed案内string（`exchange_import_persist_failed_title/body/retry`）EN/ja双方（DI-AC-12と同一確認）。

**共通gate**: 下記「Executed test surface」のとおりCI merge gate（`final-status`）が監査対象commit上でsuccess。`risk: layout-data` / `risk: migration` に該当する変更（layout DB / recovery DB / schema upgrader）は含まず、spec共通gate节の自己分類（`risk: privacy`。high-risk path `LawnchairApp.kt` へのhook追加によりlabelなしでもgate適用）と一致する。

## Executed test surface

監査sessionが実行した検証（checkout `/Users/nunu/Documents/work_idle/i_Nunulawncher`、branch `issue-374-spec-plan` @ `34d405a333fbf84ab4ea35a13156cc61aea00fb9`、`git status` clean）:

- `git rev-parse origin/issue-374-spec-plan` → `34d405a333fbf84ab4ea35a13156cc61aea00fb9`（PR headと一致）
- `git diff main..origin/issue-374-spec-plan --stat` → 31 files / +5672 -140（上記Scopeの範囲確認）
- `git diff main..origin/issue-374-spec-plan -- <production file>` 全production fileの実読（LawnchairApp.kt / store 2種 / reconcile / startup reconcile / module 2種 / controller / ExchangeFlowUi.kt / hub / host / navigation 2種 / IntentModels.kt / strings 2種）
- `git diff main..origin/issue-374-spec-plan -- CONTEXT.md specs/205-external-agent-exchange/spec.md specs/328-exchange-import-success-state/spec.md specs/366-organizer-hub-shell/spec.md` → 前提spec改訂が本PRに同梱されていることの確認。4 specのfrontmatter status（accepted / accepted / implemented / implemented）と要件ID（DI-AC-01..13・AC-2/3/4/5/7・AC-11/12/13・HUB-AC-03・FR-017）の存在をvalidator同一の正規表現ロジックで機械確認
- `gh run view 35628752080 -R nunu1733/NunuLauncher --json conclusion,headSha,status,event,url` → `{"conclusion":"success","headSha":"34d405a333fbf84ab4ea35a13156cc61aea00fb9","status":"completed","event":"pull_request"}`
- `gh pr checks 399 -R nunu1733/NunuLauncher` → `final-status` pass（merge gate）。`changes` / `validate-repo-contract` / `check-style` / `build-debug-apk` / `organizer-unit-tests` / instrumentation lane 9本すべてpass。`high-risk-evidence` のみfail（本audit記録のpush前であるため — gateの仕様どおり。docs-only commitで解消する）
- `gh api repos/nunu1733/NunuLauncher/issues/comments/5763681310` → 実装review Approved記録（plan checklist引用のcomment）の存在確認

**CI run 35628752080 が実行したcommand**（`.github/workflows/ci.yml` 実読 + run完了status）:

- `organizer-unit-tests`（pass 5m55s）: `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` — 本PRの新規unit test群（store / reconcile / startup reconcile / mapping / summary / holder / contract）はすべて `app.lawnchair.organizer.*` 配下でこのgateに包含される
- `check-style`（pass）: `./gradlew spotlessCheck`
- `build-debug-apk`（pass）: `./gradlew assembleLawnWithQuickstepGithubDebug`
- `organizer-instrumentation-issue52-tests`（pass 16m9s・API 36 emulator）: `./gradlew connectedLawnWithQuickstepGithubDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=…,app.lawnchair.organizer.ui.OrganizerHubPreferencesInstrumentationTest,…,app.lawnchair.organizer.ui.exchange.ExchangeImportSuccessInstrumentationTest,…` — 本PRの新規instrumentation test 2 classが明示的にfilterへ含まれ、実行・greenを確認。最終head `34d405a333` は3件のImportReview instrumentation test修復commitであり、その修復結果がこのrunで検証されている
- `organizer-instrumentation-{api35,db-migration,issue53,issue99,issue155,issue299,issue332,shared-writer}-tests`（全pass）— 既存regression surface
- `final-status`（pass 3s）— merge gate

監査session自身によるgradle再実行は行っていない（CI が監査対象commit上で同command setを完走・successしており、高リスクgateの「独立実行CI証拠」要件はCI runそのもので充足する。監査sessionはtest本文の実読によるassert内容確認を独立価値として実施した）。

## Findings

1. **（merge前の残条件・DI-AC-06）spec 328 rev.2・spec 205改訂・spec 366 HUB-AC-03縮小のowner受入は本PR reviewでの確定を待つ。** 3改訂は本PR（実装branch内docs commit）に同梱されており、plan.md checklistも「owner受入は最終PR reviewで確定」と宣言している。spec 374自体の受入（ChatGPT review Approved `5761252403` @ `c30f6f46d0`、owner指示によるPhase 2進行）と実装review Approved（comment `5763681310`、存在をAPI確認）は済んでいるが、rev.2の受入はmerge operatorのreview判断に属し、本auditはこれを代替しない。
2. **（高リスクgate）`high-risk-evidence` checkは本audit記録のpush前時点でfailしている。** 本記録の追加（docs-only delta、コード変更なし）によりgate要件（audit file存在・必須field・`final-status` success・source job実行）が揃う。Head SHAはPR headに一致するため「以後の変更がdocs/配下のみ」条件も充足する。
3. **（#375へのhandoff・契約どおりの範囲外）** recordはrebindに必要な最小集合（canonical decisions（ref・`proposalLabel`）+ `intentIdentity` + `minimizeMovement` + entryKind + 失効時刻echo）を保存するが、ImportReview面の継続CTAは提示しない（`importReviewRendersSummaryRemainingAndDiscardWithNoCta` の否定的観測付き）。継続済み提案の表示上の区別・消費後lifecycle・rebindは#375が所有する。#375実装時に追加fieldが必要になった場合はspec改訂（privacy再評価を含む）が要求される（plan「Explicitly unverified areas」の宣言どおり）。
4. **（残置・非blocking）cold-start emulator screenshot evidence（light/dark × ja）は本監査で独立reviewしていない。** spec DI-AC-01 oracleの「process死相当は冷起動emulator evidence」とDI-AC-09のscreenshot evidenceについて、本監査は (a) instrumentation laneのemulator実行（CI issue52 laneが`adb pull`付きでevidence収集・pass）と (b) test本文の実読を根拠とし、commit済みscreenshot arti­factの目視確認は行っていない。機械gate（instrumentation green・TalkBack順assert）は充足しており、PR本文へのevidence記載時に確認されるべき事項として残置する。
5. **（観察・non-blocking）物理device end-to-end（spec 205 AC-10のexport→外部agent→import→preview→confirm）は本PRの対象flow（CTA seam・既存lane）が既存証拠のまま不変であり、新規physical-device evidenceは要求されていない。** 新規に追加されたのはapp-private file storeとread-onlyなstatus card行のみで、transport・permission・外部送信経路の追加はない（diff実読）。
6. **（重大な不一致なし）** scope外fileへの接触、layout DB / `favorites` / recovery store / export session / diagnostics journalへのwrite経路の追加、anchor契約（attempt token・single-flight・`continuing` guard）の変更、home layout安全規約の適用対象（`favorites` 接触）— いずれも検出されなかった（diff全文実読 + grep機械確認）。実装はaccepted spec/planのChange setと一致し、plan.mdのExecution checklistで宣言された3件のreview修正（attempt-fence・TTL境界再読取・encode failure終端のin-process無効化）がいずれもdiff上に実装されていることを確認した。
