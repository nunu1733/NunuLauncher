# High-risk audit: PR #405 scope不一致の原因別remedyと取り込み済み提案のprocess死後再開（rebind）

> Status: accepted
> Audit date: 2026-09-22

- Auditor: 独立audit session（ZCode orchestrator配下のgeneral-purpose subagent）。本PRの実装（branch `issue-375-implementation` のcommit群）は別sessionが行っており、監査主体は実装・修正・revertに一切関与していない（読み取り + test実行 + 本記録の作成のみ。AGENTS.md「高リスクPRの独立エビデンス」のsolo保守における独立session要件）。実装側の主張（commit message・PR本文）は位置確認にのみ参照し、本記録の判断は監査sessionが自ら実読したdiff・test本文・spec本文と、自ら実行した`git` / `gh` / `./gradlew` / `python3`検証の結果のみによる。
- PR: https://github.com/nunu1733/NunuLauncher/pull/405 （base `main`、head `issue-375-implementation`、state OPEN、labels無し）
- Head SHA: f793de6b6ec27ba3327116841ee91e365465a7b0
  - Head SHA検証: `git rev-parse HEAD` == PR `headRefOid` == CI run `headSha` を機械確認済み（working tree差分は本記録の更新のみ）
- 再監査の経緯: 初回監査head `9cd3e7f8e128af15eec99dec285d1b34622602e2`（**Request changes** — 本PR導入のinstrumentation test compile error 4件により全9 instrumentation laneがbuild不能・`final-status` fail）の後、実装sessionによる3 commitがpushされたため新headで再監査した: `670ebff293`（compile error 4件の修正 — **Findings 1を解消**。CI run 35670206891でcompile通過・8/9 lane green・残failは旧「CTAなし」oracle 1件のみ）、`b7b2f538eb`（旧#374「CTAなし」oracleを#375契約へ更新＋本監査記録の取り込み — ただし更新oracleが **誤testTag** `exchange-import-continue` をassertしfail。**Findings 2新設**。CI run 35671906425で `The component with TestTag = 'exchange-import-continue' is not displayed!` により再fail）、`0e8d866832`（同一assertへの `performScrollTo` 追加 — 画面外という誤診であり **node不在は解消されず**、CI run 35673327714で `Action performScrollTo() failed.` により同一テストが3度目のfail）、`f793de6b6e`（**production変更**: ImportReview面Columnへ `verticalScroll` 追加〔SR-AC-10大フォント到達性〕＋本監査記録の取り込み — tag誤りは未修正のため同一テストが4度目のfailに加え **新規回帰3件** が発生。CI run 35675912121）。production codeの増分は上記 `verticalScroll` 追加のみでgate・store・anchor経路には接触なし（`git show f793de6b6e` 実読。初回監査の契約判定 (a)〜(d) は不変）
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35675912121 （`pull_request` event・ci.yml・headSha `f793de6b6e…` 一致を`gh run list --json`で確認。**conclusion=failure** — source job 5種（`organizer-unit-tests` / `check-style` / `build-debug-apk` / `validate-repo-contract` / `changes`）とinstrumentation lane 8本はすべてsuccessだが、`organizer-instrumentation-issue52-tests` が **4 test fail**（既存の誤testTag fail 1件＋`verticalScroll` 追加に起因する新規回帰3件。Findings 2）で、merge gate `final-status` もfail（本監査時点））
- 監査経過中のCI run（参照）: https://github.com/nunu1733/NunuLauncher/actions/runs/35669055469 （head `9cd3e7f8e1`・**failure** — compile error 4件で全9 instrumentation lane build不能）、https://github.com/nunu1733/NunuLauncher/actions/runs/35670206891 （head `670ebff293`・**failure** — compile修正後の初回実行。issue52 laneのみfail、失敗は旧oracle `importReviewRendersSummaryRemainingAndDiscardWithNoCta` 1件のみで133件中132件green）、https://github.com/nunu1733/NunuLauncher/actions/runs/35671906425 （head `b7b2f538eb`・**failure** — 更新oracleの誤tag assertで `The component with TestTag = 'exchange-import-continue' is not displayed!`）、https://github.com/nunu1733/NunuLauncher/actions/runs/35673327714 （head `0e8d866832`・**failure** — `performScrollTo` 追加後も同一テストが `Action performScrollTo() failed.` でfail）
- Criteria: specs/375-scope-remedy-rebind/spec.md の受入条件 SR-AC-01, SR-AC-02, SR-AC-03, SR-AC-04, SR-AC-05, SR-AC-06, SR-AC-07, SR-AC-08, SR-AC-09, SR-AC-10（FR-004, FR-006, FR-017）
- Criteria: specs/331-exchange-target-scope-coupling/spec.md の維持条項 AC-2, AC-6（本PR diff内のD-2 Amendment・§5改訂がgate規則・zero-write・fail-closedを1行も変えないことの証跡）
- Criteria: specs/228-organizer-missing-app-selection/spec.md AC-3（unchecked初期値契約が非rebind経路で維持されることの証跡）
- 前提文書（本文中で参照。accepted）: specs/375-scope-remedy-rebind/plan.md revision 8、docs/product/organizer-to-be-ux.md D-17、docs/product/organizer-disposition-migration.md §3.16/§7.1/§8/§11
- 監査対象diffの解像: 指示時のbase `5065ce3b10` は古いmainポイント（PR #402 merge）であり、headはその後のorigin/main `8272f5c0ee` をmerge済み（merge-base == `8272f5c0ee` == 現origin/mainを機械確認）。したがってPR diffの正は `git diff 8272f5c0ee...HEAD`（再監査head時点で28 files。初回監査時は26 files、+2197/−216。増分は本監査記録の取り込み・`ExchangeImportSuccessInstrumentationTest` のoracle修正・`ExchangeFlowUi.kt` への `verticalScroll` 追加〔production。+6行〕）であり、2-dotの `git diff 5065ce3b10..HEAD`（63 files超）はmain側の#376実装（PR #403）等を含む。本監査は前者を対象とし、`ManualOrganizationRunTest.kt` への+229行などがmain側由来で本PRの変更でないことを `git log 8272f5c0ee..78c87baee4 --name-only` で裏取りした。

## Scope

対象diffは `git diff 8272f5c0ee...HEAD`（head `9cd3e7f8e1`時点）で **26 files、+2197 / −216**。production変更の主要点（監査sessionが全文実読した範囲）:

| File | 変更 |
|---|---|
| `personalization/exchange/ExchangeMutationGate.kt`（新規） | process-wideなblocking gate（monitor `synchronized`、gate内suspension禁止をKDocで契約）。`heldCount` はdiagnostics用。`withGateOrNull` 拡張 |
| `personalization/exchange/RebindIntentRebuilder.kt`（新規） | 純粋rebuild seam。`reconcilePendingIntent` 再確認 → `sourceContextDigest` 等価（`CONTEXT_STALE`意味論）→ `SessionExportReconstructor.rebuild` → record decisionsから `PersonalizedIntentV1` 構築。identityはrecord保存値の **注入のみ**（再導出しない）。`Outcome.Rebuilt(sourceRecord)` がanchorのfull-equality参照 |
| `personalization/exchange/ScopeBindingGate.kt` | 純粋追加のみ: `ScopeBindingCauseDerivation`（`deriveConfirmMismatch` は解決不能優先の順序を `evaluate()` と共有、`deriveSelectionDiff`、`deriveRestoredSelection`）。既存 `evaluate()`・`ScopeMismatchCause` enumは無変更 |
| `personalization/exchange/PendingIntentReconcile.kt` | 純粋追加: identity shape検証（schema不一致・digest長≠64 → `Invalid`。例外化しない） |
| `personalization/PendingImportedIntentStore.kt` | `discardIf(expected): DiscardIfResult`（`Committed` / `NoMatch` / `WriteFailed`）をinterfaceへ追加。record model不変 |
| `integration/AndroidPendingImportedIntentStore.kt` | `discardIf`: store lock下のread-compare-tombstone（atomic）。tombstone書込成功後のみbest-effort物理削除。書込失敗は `WriteFailed`（record有効のまま・retryable） |
| `ui/ManualOrganizationRun.kt` | `start(trigger, intent, admissionAnchor, selectionRestore)`。`beginAdmission`: provisional RUN lease（gate外）→ `synchronized(lock)` 内でanchor呼出 → anchor内でgate取得・fresh読取・判定 → `Admit` ならcompletion action（operation生成・`State.Capturing`発行）をgate内で完結 → gate解放後に検出。`Refused` はlease解放のみで `AdmissionRefused`（typed）。`State.Selecting` に `intentScopeCandidates` / `restoredSelection` 追加。`confirmSelection` の早期gateをcause導出へ置換（availability考慮）。旧 `beginOperation` は削除 |
| `ui/exchange/ExchangeFlowUi.kt` | 再開CTA `continuePendingImport()`（single-flight、gate外で予備読取+rebuild、anchor closure組立、settleは純粋投影）、`requestAttemptInvalidation`（last-wins queuing・WriteFailed typed retry）、save fenceのgate内critical section化（`pendingWriteMutex` 廃止・UI settleをgate解放後の純粋投影へ分離）、`openPendingImportReview` / `discardImport` / `close` / `updateImportText` / `beginImportAttempt` のgate配下化、`ExchangeStatus.Kind` に `REBIND_CONTEXT_STALE` / `REBIND_ANCHOR_REFUSED` / `REBIND_START_FAILED`、`exchangeContractFailureText` のcause別mapping、`NoopPendingImportedIntentStore` に `discardIf` 実装 |
| `integration/exchange/ExchangeFlowController.kt` | `generate()` の置換commit（新session保存＋旧record削除）と `cancelDisclosure` をgate配下へ。`currentStructural()` 追加 |
| `integration/exchange/PendingImportStartupReconcile.kt` / `PendingImportedIntentModule.kt` / `ExchangeFlowModule.kt` / `LawnchairApp.kt` / `ManualOrganizationPreferences.kt` | process-wide gate singleton（`PendingImportedIntentModule.gate()`）の提供と、startup reconcile（load→reconcile→delete）・holder・controllerへの注入。`LawnchairApp.kt` 変更は `gate = ...gate()` 引数追加のみ |
| `ui/MissingAppSelectionScreen.kt` / `ManualOrganizationPreferences.kt` | 差分強調（`ScopeDiffRole` badge text + state description + testTag。色非依存）、復元初期値のseed（`remember(runId)` 初期値のみ） |
| `res/values/strings.xml` / `values-ja/strings.xml` | 新規8 string（cause別案内2・差分強調badge 2・CTA「この提案で続ける」・rebind typed案内3）。EN/ja同name集合 |
| `CONTEXT.md` / `specs/331-.../spec.md` / `specs/228-.../spec.md` | 3用語追加 / D-2 Amendment・§5 4-b・process death段落改訂 / D-1 Amendment |
| tests | `ScopeBindingCauseDerivationTest`（新規10件）・`RebindIntentRebuilderTest`（新規6件）・`ExchangeFlowStateHolderTest`（+9件のrebind/invalidation oracle + fence oracleの`discardIf`移行）・`PendingImportStartupReconcileTest`（fixture修正）・instrumentation 2 class（fixture修正のみ） |

**runtime書き込み経路の確認**: 本diffが追加するdurable書込は既存 `AndroidPendingImportedIntentStore`（#374 store・`noBackupFilesDir`・AtomicFile）への `discardIf` t‌ombstone 1経路のみで、record model・schema・宛先fileは不変。diff全文の実読で `favorites` / layout DB / `ModelWriter` / recovery store / export session store / diagnostics journal / run journalへの新規write経路は0件（CTA成功・失敗・拒否のいずれもstore非接触。`continuePendingImport` のsettle分岐実読）。復元初期値はprocess-local選択stateのseedのみで非永続。migration・backup影響なし（schema変更・変換code 0件）。ホームレイアウト安全規約はzero-write維持により適用対象外。

## Criteria check

test名は監査sessionがtest本文を実読し、assert内容とspec受入条件の対応を確認したもの（名前のみの採用はしていない）。unit test結果は監査session自身の強制再実行（下記「Executed test surface」）による。

- **SR-AC-01（SET_MISMATCH差分強調と同じ提案での続行・zero-write）: PASS（unit）/ 表示面のinstrumentationは未整備（Findings 3）。** cause導出は `ScopeBindingCauseDerivationTest` 10件（`missingAndExtraSelectionsAreSetMismatch` / `extraOnlySelectionIsSetMismatch` / `diffClassifiesMissingExtraAndUnresolvable` / `diffEmptyForExactSelection`）と `deriveSelectionDiff` の純粋table testで固定。差分強調は `MissingAppSelectionScreen` が `ScopeSelectionDiff` のみを消費しUI再導出しない（badge text + `stateDescription` 連結 + testTagで色非依存）。intent維持は既存 `ManualOrganizationRunTest#scopeBindingRejectsASelectionMissingTheExportedCandidate`（無編集green・CI+local）が回帰固定。「修正後confirm通過」は一致confirmのpass（`confirmingASelectionRunsTheScopeComposedComposeWithDeterministicOrder`）とreject（intent維持）の既存2面で構成され、1本の連結oracleは無い（軽微・Findings 7）。spec Test oracle表の「`ManualOrganizationRunTest`拡張」に対し、実装は既存回帰＋新規pure test classで充足（coordinator wiringは既存SET_MISMATCH testがtransitively固定 — 導出が `null`（pass）/非`null`（fail）を返すことは旧sort比較と同値）。
- **SR-AC-02（続行不能causeの優先・availability考慮・誤った救済の不提示）: PASS（unit）。** `unresolvableCandidateWinsOverSetMismatch`（検出cut不在＋集合差が同時存在時に `CANDIDATE_UNRESOLVED` が優先）・`nonAvailableCandidateIsUnresolvable`（DISABLED）が `deriveConfirmMismatch` の順序（解決不能→集合差、`evaluate()` と同一）を固定。`confirmSelection` の置換はdiff実読で確認（`current.detectedCandidates` を渡し、pass/fail結果は旧sort比較と同値でcause labelのみ変化）。`PROJECTION_MISMATCH` の案内区分は `exchangeContractFailureText` のcause別mapping（`CANDIDATE_UNRESOLVED` / `PROJECTION_MISMATCH` → `exchange_scope_unresolvable_mismatch`）。文案mapping自体のunit oracleは無くinstrumentationを要する（Findings 3）。既存 `ExchangeTargetScopeCouplingTest` はbranch側無編集でgreen。
- **SR-AC-03（process死後rebind: CTA→fresh run→完全一致→復元初期値→明示confirm1回→継続）: PASS（unit oracle）/ instrumentation統合test・冷起動emulator evidenceは本PRに存在しない（Findings 3 — SR-AC-03のinstrumentation側要件）。** projection全field等価oracle `RebindIntentRebuilderTest#rebindProjectionEqualsTheImportTimeProjectionFieldForField` が (a) identityがrecord保存値と同一、(b) `assertNotEquals(record.digest, 再導出digest)` で「注入であり再導出でない」ことを否定側でも固定、(c) `IntentPlannerAdapter.project` の結果がimport直後と全field同一、(d) decisions round-trip（`toDecisionsMap() == rebuilt.completed.decisions`）、(e) anchor参照が源recordそのもの、を1 testで固定。復元導出は `restoreIsScopeIntersectionWithResolvableCut` / `restoreIsDeterministic`（決定性・副作用なし）。idle由来の非復元はentryKind→`SelectionRestore` 写像（RUN_INのみ`PreviousExplicit`）＋非rebind seed空で構造保証。CTA→admission→提案非消費は `rebindSuccessStartsTheRunAndKeepsTheProposal`（record残存assert付き）。復元は初期値seedのみでconfirmなしの確定経路は存在しない（`ManualOrganizationPreferences` の `remember(runId)` seed実読 + `startAdmitted` の `restoredSelection` 設定実読）。
- **SR-AC-04（完全一致gate・zero-write・fail-closedの無変更）: PASS。** `ScopeBindingGate.evaluate` / `ScopeMismatchCause` enum / gate判定はdiff上1行も変更なし（実読）。既存scope gate系test（`ManualOrganizationRunTest` 87件・`ExchangeTargetScopeCouplingTest`）はbranch側無編集（`git log --name-only` で確認）でunit lane green（CI `organizer-unit-tests` pass + 監査sessionのlocal再実行1708件0 fail）。`ExchangeTargetScopeCouplingTest` の無編集greenも含む。
- **SR-AC-05（明示選択契約を弱めない）: PASS（構造）/ instrumentation残置（Findings 3）。** 復元は `State.Selecting.restoredSelection` → `remember(runId)` 初期値のみで、確定は `confirmSelection`（明示1回）のみ。非rebind経路（通常run・idle継続・attach）のseedはemptyのまま（`restoredSelection` は `PreviousExplicit` 時のみ設定）。通常run・idle継続のunchecked回帰は既存instrumentation（無編集・CI issue52 lane — ただし本runはcompile failureで未実行。過去runでのgreenと本diffがそのflowを変更しないことによる裏付け）。
- **SR-AC-06（run-in attach契約の回帰）: PASS。** `attachIntent` 回帰（`attachIntentBindsToTheSelectionSurfaceOnce` ほか `ManualOrganizationRunTest`）・選択凍結・single-shotはbranch側無編集でgreen。`ExchangeImportSuccessInstrumentationTest` のCTA契約test本体は無変更（fixtureのdigest 2行のみ）。attach経路にanchorは挿入されない（`connectRun` のattach分岐実読 — anchorはidle分岐の `run.start` にのみ渡る）。
- **SR-AC-07（CTA規律・race oracle群・entryKindフリップ・提案非消費）: PASS（unit oracle）/ 「処理中の破棄・Back不受理」に実装漏れ（Findings 4）。** single-flightは `ImportReview.continuing` の同時flip（`if (current.continuing) return`）+ 二重押下不受理。Busy typed拒否（`RUN_BUSY`）。成功時record残存・再継続可（`rebindSuccessStartsTheRunAndKeepsTheProposal`）。race oracle: `rebindRefusesWhenTheRealReplacementPathReplacesTheSessionMidFlow`（`preAdmissionBarrier` で **実際の `ExchangeFlowController.generate()` 置換commit** を別threadで完走させ `Future.get` でjoin — 置換が無ければ成立しない因果構造をtest自身がassert）・`rebindAdmissionIsRefusedWhenTheRecordVanishedAfterRebuild`（load queueで決定的TOCTOU再現）・`rebindAdmissionIsRefusedWhenTheRecordWasEntryKindFlippedAfterRebuild`（full-record equalityがentryKindフリップを検出）・いずれも「`State` はIdle・detection 0件」の否定的観測付き。否定的観測の分離（layout/journal 0件とcleanup件数）は「run journal新規eventなし（既存 `RUN_STARTED` 以降の相関のみ）」という設計（Non-goals）とzero-write分岐構造で充足。
- **SR-AC-08（admission anchor・exchange mutation gate・identity破損・write-failure・線形化）: 主要PASS（unit oracle）/ 3つのoracleが文言どおりには存在しない（Findings 5）。** (a) anchor table: `digestMismatchFailsTypedContextStaleAndKeepsTheProposal` / `rebindAdmissionIsRefusedWhenTheSessionExpiredAfterRebuild`（fake clockをload hookで決定的に進める — wall-clock非依存）/ `identityShapeCorruptionFailsClosedWithoutThrowing`（wrong schema・digest長不正のvalid JSON → 例外なし `Invalid` → 清掃・継続拒否。`PendingIntentReconcile` への純粋追加と一致）。(b) anchor拒否後の面の扱い: 無効化済み→gate内re-reconcile清掃・クローズ（`rebindAdmissionIsRefusedWhenTheRecordVanishedAfterRebuild` の `deleteCalls >= 1`）、置換済み→再読み取り（`InvalidProposal` 経路のre-adopt分岐実読）。(c) write-failure oracle: `invalidationWriteFailureKeepsTheProposalValidAndIsRetriedByTheNextInvalidation`（`WriteFailed` → recordは `discarded=false` で有効・typed `IMPORT_PERSIST_FAILED`・同commit再試行で解消）+ `writeFailedInvalidationSurvivesHolderRecreationWithoutFabricatingSuccess`（holder破棄→durableのみから再構築したholder Bで「成功扱いされた無効化」が存在しないこと・対照ケースとして正常discardで解消）。(d) `discardIf` 線形化: `Committed` / `NoMatch` のみ無効化成功（`settleInvalidation` 実読）、`WriteFailed` はtyped/retryable（store実装 + interface KDoc実読）。(e) gate配下化: session置換（`generate()` 内gate hold）・pre-send cancel・durable save（fence 1/2含む gate内critical section）・破棄tombstone・reconcile清掃（resume face / anchor拒否後 / startup）・anchor区間の全経路が同一singleton gate（`PendingImportedIntentModule.gate()`）を通ることをdiff実読で確認 — gate不在の競合経路は発見できなかった。`pendingWriteMutex` は完全廃止（grep 0件）。gate下のUI待機禁止は構造で充足（全gate blockが同期的store操作のみ・`withContext(uiDispatcher)` は必ずgate blockの外・`settleInvalidation` / `settlePendingIntentSave` はstoreを呼ばない純粋投影）。(f) lock順序: anchor経路「run lock → gate → store」、mutation経路「gate → store」の一方向のみで循環なし（実読）。
- **SR-AC-09（spec 331改訂・spec 228注記）: PASS（技術内容）/ owner受入は本PR reviewでの確定を残す（Findings 6）。** spec 331: D-2 Amendment（原因別remedy・gate規則・D-5単一class構造・zero-write・fail-closed不変を明記）・§5 4-b（attach生存範囲=同一processの選択面が開いている間）・process death段落の `Hub → ImportReview` 1経路＋rebind契約への置換・Change history追記 — Scope節と一致することをdiff実読で確認。spec 228: D-1へのAmendment注記（初期値のみ・明示confirm必須・非rebind不変）。`CONTEXT.md` に3用語（原因別remedy / rebind / 選択復元初期値）を追加。gate規則を変えないことはSR-AC-04の無編集greenと `evaluate()` diff 0行で機械裏取り。
- **SR-AC-10（strings双方・語彙・a11y）: strings機械確認PASS / a11y evidence未整備（Findings 3）。** 新規8 stringが `values/` と `values-ja/` の双方に存在することを監査sessionがXML走査で機械確認（name集合一致・format placeholder無し）。CTA labelはTO-BE T-18「この提案で続ける」、続行不能・構造変化案内は「依頼を作り直す」語彙（TO-BE §10のremedy語彙と整合）。差分強調はbadge text＋state description＋testTagで色非依存。**a11y assertion（TalkBack / Switch Access / keyboard / 200% font）とlight/dark × ja screenshot evidenceは本PR diffに存在しない。**

**契約4点の個別確認（監査指示項目 (a)〜(d)）**:

- **(a) anchorがgate内でfresh読取→判定→operation生成まで完結し検出はgate解放後: 確認（構造）。** `continuePendingImport` の予備読取・rebuildはgate外。anchor closureは `exchangeMutationGate.withGate { load / activeSession / reconcile(now) / 完全一致 / !isInvalidationPending → complete() }` で、`complete()`（operation生成・`State.Capturing`発行）をgate内で実行してreturn。`beginAdmission` はanchor return後に `startAdmitted` → 検出へ進むため、検出開始時点でgateは解放済み。gate下はrecord1件・session1件のAtomicFile読書き+判定のみ（処理時間界限充足）。RUN leaseはgate外・run lock前のprovisional取得で、拒否時 `lease.close()` のみ（`AdmissionRefused`・状態発行0件 — `rebindAdmissionIsRefusedWhenTheRecordVanishedAfterRebuild` のIdle断言とdetection 0件でoracle化）。ただし「検出seamへのgate非保持probe test」は存在しない（Findings 5-i）。
- **(b) 無効化commitのlinearize: 確認。** `discardIf` はstore lock下のatomic read-compare-tombstoneで、`Committed`（tombstone commit成功→best-effort物理削除）と `NoMatch`（不在・corrupt・置換済み）のみが無効化成功として処理される（`settleInvalidation`）。`WriteFailed` は `operation.failed = true` + typed statusで、queue済み遷移は実行されず同一commitが `retryPendingInvalidation` で再試行される。in-memory commit状態は導入されておらず、無効化の正本はdurable tombstone（7th review指摘2充足）。
- **(c) gate下のUI待機禁止: 確認（構造）。** 全gate block（save fence・discard・invalidation・resume face読取・startup reconcile・anchor）はIO上の同期的store操作と判定のみで、UI dispatcherへの切替・待機は全てgate block return後の `withContext(uiDispatcher)`。gate内にsuspension pointは無い（blockは `() -> T` の非suspend）。#374 save fence oracle（cancel/supersede中のstale record残存なし）は `discardIf` 化したうえで既存test群がgreen（CI unit lane + local）。ただしspec文言の「UI settle直前barrier→Main側rebind admission」の決定oracleそのものは無い（Findings 5-ii）。
- **(d) WriteFailed時のqueue済み遷移のlast-wins queuing: 確認。** outstanding操作中の後続無効化actionはcontinuationを **上書きqueue**（`continuations.clear() + +=` — 先着遷移は廃棄）し、`WriteFailed` ではqueueを実行せずface/stateを保持、`Committed` / `NoMatch` で最後の1件のみ実行。`outstandingInvalidationQueueIsLastWinsAndEditCSupersedesTheQueuedImportB`（Bのvalidation/save起動0件・最終face "C"）と `outstandingInvalidationQueueIsLastWinsAndCloseSuppressesTheQueuedImport`（closeが最後→Closed）の決定的oracleで固定。fence経由の `WriteFailed` も `InvalidationOperation` をretry anchorとして登録し、anchorは `isInvalidationPending` で当該recordのadmissionを拒否する。

## Executed test surface

監査sessionが実行した検証（checkout `/Users/nunu/Documents/work/NunuLauncher`、branch `issue-375-implementation` @ `9cd3e7f8e128af15eec99dec285d1b34622602e2`、`git status` → clean、JDK 21.0.12）:

- `git rev-parse HEAD` → `9cd3e7f8e128af15eec99dec285d1b34622602e2`（PR head / CI run headShaと一致）
- `git merge-base HEAD origin/main` → `8272f5c0ee…` == `git rev-parse origin/main`（headは現mainを包含。PR diff正は3-dot）
- `git diff 8272f5c0ee...HEAD --stat` → 26 files / +2197 −216（Scope表の範囲確認）
- `git diff 8272f5c0ee...HEAD -- <production file>` 全production file実読（上記Scope表）
- `git log 8272f5c0ee..78c87baee4 --name-only` → branch側10 commitの触れたfile一覧（`ManualOrganizationRunTest.kt` 等がmain側由来であることの裏取り）
- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL（exit 0）**
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --rerun --no-build-cache` → **BUILD SUCCESSFUL。1708 tests / 0 failures / 0 errors / 0 skipped**（初回2回はUP-TO-DATE / FROM-CACHE再利用だったため、監査として実体実行になるよう `--rerun --no-build-cache` で強制した。内訳: `ManualOrganizationRunTest` 87 / `ExchangeFlowStateHolderTest` 83 / `RebindIntentRebuilderTest` 6 / `ScopeBindingCauseDerivationTest` 10 / `PendingImportStartupReconcileTest` 8 を含む）
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin --no-build-cache` → **BUILD FAILED（CIと同一の4エラーをlocal再現）**:
  - `OrganizerHubPreferencesInstrumentationTest.kt:471:13` `HubFakePendingStore is not abstract and does not implement abstract member`（`discardIf` 未実装）
  - `OrganizerHubPreferencesInstrumentationTest.kt:1828:34` `Const 'val' initializer must be a constant value`（`private const val VALID_DIGEST = "a".repeat(64)` — 非定数初期化子）
  - `ExchangeImportSuccessInstrumentationTest.kt:109:13` `FakePendingStore is not abstract and does not implement abstract member`（同上）
  - `ExchangeImportSuccessInstrumentationTest.kt:693:34` 同一のconst val error
- `gh pr view 405 -R nunu1733/NunuLauncher --json ...` → OPEN / base main / headRefOid一致 / labels無し
- `gh repo view nunu1733/NunuLauncher --json nameWithOwner,defaultBranchRef` → `nunu1733/NunuLauncher` / `main`（AGENTS.mdの操作先確認。local gh v2.96.0は `gh repo view -R` 非対応のため位置引数で実行）
- `gh run list -R nunu1733/NunuLauncher --json databaseId,workflowName,status,conclusion,headSha,event` → run `35669055469`（CI・pull_request・headSha一致・**failure**）/ run `35669055395`（High-risk gate・**failure**）
- `gh run view 35669055469 --json jobs` → `changes` / `build-debug-apk` / `check-style` / `organizer-unit-tests` / `validate-repo-contract` = success。**instrumentation lane 9本（issue52 / issue155 / shared-writer / issue53 / db-migration / api35 / issue332 / issue99 / issue299）= 全failure**（`:compileLawnWithQuickstepGithubDebugAndroidTestKotlin FAILED`。api35 emulatorへのadb install失敗logはcompile失敗に起因するリトライ噪音）。**`final-status` = failure**
- `gh run view --job 106561254198 -R nunu1733/NunuLauncher --log` → compile error 4件の抜き出し（local再現と同一行番号）
- `gh pr checks 405 -R nunu1733/NunuLauncher` → `high-risk-evidence` fail（本audit記録のpush前のため — gate仕様どおり。ただしmergeには上記の通り `final-status` successが別途必須）、source job 3種pass、instrumentation 9本fail
- `python3` によるstrings走査 → 新規8 nameが `values/` / `values-ja/` 双方に存在・format placeholder無し
- `grep -rn pendingWriteMutex lawnchair/src tests/unit` → 0件（完全廃止の確認）

**CI run 35669055469 が実行したcommand**（`.github/workflows/ci.yml` のlane構成とjob logから確認）:

- `organizer-unit-tests`（pass 5m47s）: `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` — 本PRの新規unit test群はすべてこのgateに包含されgreen
- `check-style`（pass）: `./gradlew spotlessCheck`
- `build-debug-apk`（pass）: `./gradlew assembleLawnWithQuickstepGithubDebug`
- instrumentation lane 9本（全fail）: `:compileLawnWithQuickstepGithubDebugAndroidTestKotlin` が失敗し **test本体は1件も実行されず**（spec共通gateの「対象classのorganizer instrumentation lane」は本head上で不成立）。emulator起動前のcompile段階の失敗のため、emulator infrastructure起因ではない
- `final-status`（**fail**）— merge gate不成立

**再監査（増分 `9cd3e7f8e1..0e8d866832`。検証はhead `b7b2f538eb` → `0e8d866832` に跨って実行）で監査sessionが実行した追加検証**:

- `git log --oneline -5` / `git rev-parse HEAD` → `b7b2f538ebbc0bf29c90f51e2e979ded8782221d`（`gh pr view 405 --json headRefOid` と一致。working tree clean）
- `git diff 9cd3e7f8e1..HEAD --stat` → 3 files / +134 / −8（`670ebff293` compile修正 2 files + 本監査記録の取り込み。**production codeの増分はゼロ** — 初回監査のproduction実読・unit gate結果は新headにもそのまま有効）
- `git show 670ebff293` / `git show b7b2f538eb` → 両commitの全文実読（fake 2 classへの `discardIf` 実装・`validDigest` literal化・CTA oracle改名とassert反転・testTag確認）
- `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin --no-build-cache`（head `9cd3e7f8e1`）→ BUILD FAILED（初回Findings 1の4エラーを再現）/ 同（head `670ebff293`、`b7b2f538eb`）→ **BUILD SUCCESSFUL**
- `gh run view 35670206891 --json jobs`（head `670ebff293`）→ fail jobは `organizer-instrumentation-issue52-tests` と `final-status` のみ。`gh run view --job 106564827351 --log` → 失敗テストは `importReviewRendersSummaryRemainingAndDiscardWithNoCta` 1件のみ（`assertDoesNotExist` が Text 'Continue with this proposal' 1 node を検出 — `exchange_import_cta_idle` と `exchange_import_continue` が同一copyのため。tag `exchange-import-continue` の不存在assertは通過 = review面に当該tagなしの実行時証拠）
- `gh run view 35671906425 --json status,conclusion,jobs`（head `b7b2f538eb`）→ status=completed / **conclusion=failure**。fail jobは `organizer-instrumentation-issue52-tests` と `final-status` のみ（他8 instrumentation laneとsource job 5種はsuccess）。`gh run view --job 106570084675 --log` → 失敗テストは `importReviewRendersSummaryRemainingContinueAndDiscard` 1件: `java.lang.AssertionError: Assert failed: The component with TestTag = 'exchange-import-continue' is not displayed!`（Findings 2の実行時確定）
- strings走査（`grep` EN/ja）→ `exchange_import_cta_idle` と `exchange_import_continue` がEN・jaとも同text（"Continue with this proposal" /「この提案で続ける」— Findings 2の第2欠陥の根拠）

**追試commit `0e8d866832`（本記録の初版が実装sessionにcommitされた後の増分）の確認**:

- `git show 0e8d866832` → 本記録（監査session作成分）のcommit + `ExchangeImportSuccessInstrumentationTest` への `performScrollTo()` 追加のみ（testTag修正なし。production code増分ゼロ）
- `gh run view 35673327714 --json status,conclusion,jobs`（head `0e8d866832`）→ status=completed / **conclusion=failure**。fail jobは `organizer-instrumentation-issue52-tests` と `final-status` のみ。`gh run view --job <job> --log` → 失敗テストは同一 `importReviewRendersSummaryRemainingContinueAndDiscard` 1件: `java.lang.AssertionError: Action performScrollTo() failed.`（node不在の実行時確定。Findings 2の誤診判定を補強）

**追試commit `f793de6b6e`（production `verticalScroll` 追加）の確認**:

- `git show f793de6b6e` → `ExchangeFlowUi.kt` の `ExchangeImportReview` Columnへ `fillMaxWidth().verticalScroll(rememberScrollState())` 追加（+6行。testTag変更なし＝誤tag未修正。gate・store・anchorへの接触なし）＋本記録のcommit
- `gh run view 35675912121 --json status,conclusion,jobs`（head `f793de6b6e`）→ status=completed / **conclusion=failure**。fail jobは `organizer-instrumentation-issue52-tests`（4 test fail）と `final-status` のみ。失敗テスト: `hubProposalRowOpensTheRunFaceWithTheImportReviewPreOpened`（新規回帰）/ `importReviewDiscardCancellationKeepsTheFaceAndTheRecord`（新規回帰）/ `importReviewDiscardConfirmsOnceThenClosesTheFaceAndDeletesTheRecord`（新規回帰）/ `importReviewRendersSummaryRemainingContinueAndDiscard`（既知の誤tag）

## Findings

1. **（初回監査でBlocking → 【解消済み・`670ebff293`】）CI merge gate `final-status` が初回監査head上でfailした。原因は本PRが導入したinstrumentation test sourceのcompile error 4件。** `PendingImportedIntentStore` への `discardIf` 追加（本PR）によりinstrumentation側の2 fake store（`HubFakePendingStore` / `FakePendingStore`）がabstract memberを実装せず、本PRが追加した `private const val VALID_DIGEST = "a".repeat(64)` がKotlinとして不正（`const` 不可。`private val` か文字列literalへ）。unit lane・style・buildは通るがandroidTest compileはunit laneに含まれないため、実装sessionのlocal検証（unit 1674件）では検出されず、CI初回run 35669055469で顕在化した（全9 instrumentation lane build不能 → instrumentation lane 0件実行）。
   - **再監査での解消確認**: 修正commit `670ebff293`（2 fakeへの `discardIf` 実装・`validDigest` を64文字literalへ）により、監査sessionが `./gradlew compileLawnWithQuickstepGithubDebugAndroidTestKotlin --no-build-cache` → **BUILD SUCCESSFUL** を `9cd3e7f8e1` と `b7b2f538eb` の両headで再現確認。CI run 35670206891（head `670ebff293`）では9 laneすべてcompile・実行され、**8/9 laneがgreen**、失敗は旧「CTAなし」oracle 1件（次項）のみ — compile errorの解消をCI実行で実証。
2. **（Blocking — 現headで新たに発生。初回監査後のoracle更新 `b7b2f538eb` に内在）更新されたImportReview面CTA oracleが誤testTagをassertし、現headのCIでfailしている。** `importReviewRendersSummaryRemainingContinueAndDiscard` は継続CTAを `onNodeWithTag("exchange-import-continue")` で陽性assertするが、このtagは **取り込み成功面（ImportSuccess）のCTA**（`ExchangeFlowUi.kt` 3065行）のものであり、再開面（ImportReview）のrebind CTAの正は `exchange-import-review-continue`（同3260行）である。テストはreview面をcomposeしているため `exchange-import-continue` nodeは存在せず、実行時に必ず失敗する。
   - **CI実証（2重）**: (i) run 35670206891（head `670ebff293`）で旧oracleの `onNodeWithTag("exchange-import-continue").assertDoesNotExist()` は **通過** し、失敗したのは後続のtext不存在assertのみ（後述）— review面にtag `exchange-import-continue` が存在しないことの実行時証拠。(ii) run 35671906425（head `b7b2f538eb`）で更新oracleが `java.lang.AssertionError: Assert failed: The component with TestTag = 'exchange-import-continue' is not displayed!` でfail — `organizer-instrumentation-issue52-tests` lane fail → `final-status` fail。
   - **追試commit `0e8d866832`でも未解消**: 同commitは同一assertへ `performScrollTo()` を追加した（「discard focus復元スクロールでCTAが画面外になるため」= 画面外misdiag）。しかし原因はnode不在であり、CI run 35673327714で `java.lang.AssertionError: Action performScrollTo() failed.` により同一テストが3度目のfail → issue52 lane fail → `final-status` fail。node存在前提の修正では解消しない。
   - **追試commit `f793de6b6e`で悪化**: productionのImportReview面Columnへ `verticalScroll` を追加したが（大フォント到達性。SR-AC-10として妥当な改善）、tag誤りは未修正のため `importReviewRendersSummaryRemainingContinueAndDiscard` が4度目のfail。さらに **新規回帰3件** が発生した（CI run 35675912121）: `OrganizerHubPreferencesInstrumentationTest#hubProposalRowOpensTheRunFaceWithTheImportReviewPreOpened`、`ExchangeImportSuccessInstrumentationTest#importReviewDiscardCancellationKeepsTheFaceAndTheRecord`、`#importReviewDiscardConfirmsOnceThenClosesTheFaceAndDeletesTheRecord` — いずれも直前run（35673327714 @ `0e8d866832`）ではgreen。`verticalScroll` が D-13確認dialog周辺のfocus/表示挙動に影響している可能性が高い（推定。失敗stackはCI report参照）。失敗はissue52 lane計4件 → `final-status` fail。
   - **潜在の第2欠陥（tag修正後も残る）**: 更新oracleは `onNodeWithText(exchange_import_cta_idle).assertDoesNotExist()` を保持するが、`exchange_import_cta_idle` は `exchange_import_continue` と **同一copy**（EN "Continue with this proposal"、ja「この提案で続ける」— strings.xml EN 1488/1510行・ja 568/588行。T-18統一copyの帰結）であり、新CTAが表示されている限りrun 35670206891と同一理由（`Failed: assertDoesNotExist. Reason: Did not expect any node but found '1' node that satisfies: Text contains 'Continue with this proposal'`）でfailする。修正には (a) tagを `exchange-import-review-continue` へ、(b) idle copy不存在assertの削除（統一copy下で「旧copy不在」はtext照合では検証不能。区別が必要ならcopyの再設計は別判断）が必要。テスト群ヘッダコメント「with NO continuation CTA」も陳腐化している（低・文言のみ）。
3. **（Required — spec oracleに対するevidence欠落。test compile修正と同一または後続commitでの整備を要求）【再監査: 部分解消 — `b7b2f538eb` がImportReview面CTAの表示・有効・T-18 copy oracleを作成（旧「CTAなし」oracleの陳腐化も解消）。ただしそのoracle自体がFindings 2の誤tagで現headのCI fail中であり、実効的には未達。下記(i)〜(iv)のgapはすべて残存】** spec Test oracle表が要求する次のevidenceが本PR diffに存在しない: (i) SR-AC-03のrebind統合instrumentation（冷起動 → status card行 → 再開面 → CTA → 検出 → 復元選択面 → confirm → 確認面）と冷起動emulator evidence、(ii) SR-AC-05の「復元は編集可・confirm必須（confirmなしでplanに進まない）」instrumentation、(iii) SR-AC-10のa11y assertion（TalkBack / Switch Access / keyboard / 200%）とlight/dark × ja screenshot、(iv) SR-AC-01/02の差分強調表示・cause別案内のinstrumentation（`missing-app-selection-row-diff-missing` / `-extra` testTagは実装済みで、testから未使用）。unit oracleによる技術的裏付けは上記Criteria checkの通り厚いが、受入条件の文字通りの編集への到達は **部分的** であり、UI受入は未検証のままである。
4. **（Required — SR-AC-07の「処理中の破棄・Back不受理」がholder層で不受理になっていない）【再監査: 未対応 — 増分 `9cd3e7f8e1..b7b2f538eb` には含まれず、そのまま残置】** `discardImport()` は `ImportSuccess` 面では `if (current.continuing) return` でguardするが、`ImportReview` 面の分岐（`is ExchangeScreen.ImportReview -> null`）にcontinuing guardが無い（KDocは「Refused while the CTA is continuing」と宣言するが実装されていない — buttonの `enabled = !state.continuing` はaffordanceにすぎず、spec 328「UI disabledはaffordanceにすぎない」原則の継承に反する）。同様に `exchangeBackAction(ImportReview) = CLOSE` はcontinuingを確認せず、flow-level `ExchangeFlowBackHandler` → `holder.close()` がCTA処理中のBackで面を閉じる（`ExchangeImportSuccessBackHandler` はcontinuing中のBackをswallowするのに、ImportReview面には同等guardが無い）。データ整合への影響は限定的（closeはzero-write、discard成功はユーザー明示破棄という#374許容の消失系、admission後の遅延tombstoneは `discardIf` の完全一致により当該recordのみ）が、accepted specのscenario「再開CTAのsingle-flightとBusy」の「処理中は破棄・Backが不受理」を満たさない。unit testでも「continuing中のImportReview discard/Back拒否」は未cover（存在するのはImportSuccess面のguard testのみ）。
5. **（Required — SR-AC-08の3 oracleが文言どおりには存在しない）【再監査: 未対応 — 増分に含まれず、そのまま残置】** (i) 「検出seamへのprobeでgate非保持を構造的に確認する」testが無い（`ExchangeMutationGate.heldCount` はdiagnostics用に実装済みだが、どのtestも使用しない。検出がgate解放後に始まることはdiff実読による構造確認のみで、wall-clock非依存の自動証拠がない）。(ii) 「durable saveをUI settle直前でbarrier停止させMain側でrebind admissionを開始する」UI待機禁止の決定oracleが無い（`invalidationWriteFailure...` testがgate保持中にMain originの `onImportTextChange` がblockせずqueueすることを示す — 逆向きの進行可能性は実証するが、spec文言の構成そのものではない）。(iii) 6th reviewの「save commit → UI settle前に無効化 → 物理削除をbarrier停止 → 別holder / process recreationからrebind → `State.Capturing` 0件」の精密な線形化oracleは、cross-holder oracle（`writeFailedInvalidationSurvivesHolderRecreationWithoutFabricatingSuccess`）が主要部をcoversするが「物理削除のbarrier停止（tombstone commit後・削除前のcrash window）」ステップを明示的に再現していない（tombstone後crash window自体は `discardIf` fakeの `deleteGate` 機構とreconcileの破棄mark検証で閉じられている — 構造は確認）。
6. **（merge前の残条件 — SR-AC-09）spec 331改訂・spec 228 Amendment・CONTEXT.md 3用語のowner受入は本PR reviewでの確定を待つ。**【再監査: 残置】 spec/plan自体の受入（Phase 1最終review Approve `5767085523` @ `74e1fd0fae`）は済むが、実装PR diff内のdocs改訂の受入はmerge operatorのreview判断に属し、本auditはこれを代替しない。
7. **（観察・non-blocking）【再監査: 増分はtest/docsのみのため全項そのまま有効】** (a) 「修正後confirm通過」の1本に連結したcoordinator-level testは無い（reject（intent維持）と一致confirm passの既存2 testの組合せで、contract notes 5の既存構造確認は維持）。(b) anchor内の `isInvalidationPending(fresh)` はUI threadに属する `invalidationOperation` fieldをIO threadのgate区間から読む（非volatile）。JMM上は可視性が保証されず、最悪caseは「failed無効化が見えずadmitする」方向だが、当該recordはdurableにはvalid（WriteFailed=非tombstone）であるためadmissionはdurably正当であり、後続の同一commit再試行のみが作用しうる — fail-safe方向の理論的観察として記録する。(c) `manual_organization_missing_apps_state_*` とのstate description連結は「checked, 依頼では対象」の形で読まれ、既存契約（name/role/state）を維持する。
8. **（重大な不一致なし）** scope外fileへの接触（2-dot diffに現れる#376系fileはmain merge由来で本PRの変更でない — commit単位で裏取り済み）、layout DB / `favorites` / recovery store / export session / diagnostics journalへのwrite経路の追加、`ScopeBindingGate.evaluate` / `ScopeMismatchCause` enum / #374 record modelの変更、identity再導出 — いずれも検出されなかった。`ExchangeMutationGate`・admission anchor・`discardIf`・last-wins queuing・identity注入の設計はaccepted spec/planのDesign（Design 1〜7・Alternatives rejected）と一致し、7回のreview指摘（gate範囲・tombstone正本・UI待機禁止・provisional lease・block gate化）に対応する実装がdiff上に存在することを確認した。

**監査結論（再監査 @ `f793de6b6e`）: Request changes（維持）。** 初回監査のblocking項目であったinstrumentation compile error 4件は `670ebff293` により解消済みで、production codeへの増分はゼロ — accepted spec/planの契約（gate内anchor完結・`discardIf` 3値linearize・gate下UI待機禁止・last-wins queuing・identity注入・検出のgate解放後開始）について初回監査のunit oracle・diff実読に基づく判定は新headでも有効である。ただし現headでは、(1) `b7b2f538eb` が更新したCTA oracleの誤testTag（Findings 2）により `organizer-instrumentation-issue52-tests` が **3連続runでfail** し **merge gate `final-status` が赤**（CI run 35671906425 / 35673327714 / 35675912121。根本修正はtest 1行〔tagを `exchange-import-review-continue` へ〕と idle copy不存在assertの除去の2点。`performScrollTo`〔`0e8d866832`〕・`verticalScroll`〔`f793de6b6e`〕のいずれもnode不在を解消せず、後者は **新規回帰3件** を生んだ）、(2) spec oracleが要求するrebind instrumentation統合・冷起動emulator evidence・a11y/screenshot evidenceが未整備（Findings 3）、(3) continuing中の破棄・Back不受理のholder層guard欠落（Findings 4）とSR-AC-08の3 oracle不在（Findings 5）が残置、(4) spec 331/228改訂のowner受入がmerge review待ち（Findings 6）、の理由でmerge条件（AGENTS.md高リスクPR要件 + spec受入条件）を満たさない。test修正はcode変更であるため、 **修正push → CI green（`final-status` + 全instrumentation lane）を新headで確認したうえで、本記録のHead SHA・CI runを再度差し替える（再監査）こと** がmergeの前提である。
