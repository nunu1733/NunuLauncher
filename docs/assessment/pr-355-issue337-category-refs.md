# High-risk audit: PR #355 v4 category refs and run-scoped formation keys

> Status: accepted
> Audit date: 2026-09-18

- Auditor: independent general-purpose subagent session (separate from the implementation session; solo maintenance). 実装側の主張には依拠せず、diff本文・test本文・productionコードを監査session自らが実読し、検証コマンドを自ら再実行した。
- PR: https://github.com/nunu1733/NunuLauncher/pull/355
- Head SHA: 1b8868f7eca84abf4c218d7a31a812aeda0d1df7
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35326452186 （監査sessionが`gh run view`で独自照合: event=`pull_request`、headSha=`1b8868f7ec`、status=completed、conclusion=success、全15 job success。`gh pr checks`でも`final-status`=pass・source jobs（organizer-unit-tests/check-style/build-debug-apk）実行済みsuccessを09:06Z確認。唯一failの`high-risk-evidence`はheadにcommit済みの監査記録が旧pin（`917d093854`）であり、その後続commitがtest-onlyでdocs-onlyでないことによるもので、本記録の更新commitで解消する側面のもの。run 35326452186の`organizer-instrumentation-issue52-tests`はsuccess — 旧headで1回失敗した当該laneはemulator側systemui ANR型のinfra flakeだったことも併せて確認済み）
- Criteria: specs/337-exchange-category-group-proposals/spec.md AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-11, AC-12, AC-13

> Re-audit lineage: 監査は4回のpinで行った。1st: `4fcab060d2`（High 2件=production catalog wiring欠落・CI gate赤を発見）。2nd: `917d093854`（`f9d34576c2`+`917d093854`でwiring・carrier・disclosure copy・session store fail-closed・主要oracle群を解消、CI green確認）。3rd: `c46e1ab0fe`（`3fd39be240`+`c46e1ab0fe`のtest/docs-only増分を全hunk実読）。final（本記録）: `1b8868f7ec` — 監査実施中にtest-only commit 1本が追加されたため最終pin。監査対象範囲はbase `de77e280b7`..`1b8868f7ec`（1st/2ndラウンドでproduction面の全hunkを実読済み、3rd/finalラウンドの増分は`917d093854...1b8868f7ec`=test 5 file・docsのみでproduction変更ゼロを`git diff --stat`で機械確認済み）。

## Scope

対象diffは `git diff de77e280b7...1b8868f7ec`。監査sessionがdiff・production/test本文を実読して確認した変更面（本記録は全ラウンドの実読結果を統合）:

- **contract/codec面（`organizer/personalization/`）**: v4 schema同時bump、envelope `categories`（`ExportCategory(ref, kind, taxonomyId?, displayName: ExportCategoryName?)` — free-text-class carrierはspec D-1どおりに第2ラウンドで導入）、item-level `categoryRef`/`folderCategoryRef` のref一本化、`FreeTextClass.USER_CATEGORY_NAME`、`PersonalizationContextExportV1.init` のref namespace不変条件（ref一意・item/category disjoint・advertised所属・EXTERNAL_REDACTEDでname拒否。decodeも同一init経由でtampered文書を`SchemaMismatch`化）、`ExportSession.categoryRefs`、`ContextExportBuilder` のcanonical順catalog projection・同一allocator seam・3-namespace衝突検査、`SessionExportReconstructor` のsession∩catalog交差再構築、`IntentCodec`/`IntentModels.GroupSemantic` exactly-one-of + #336 name規則共有object（normalize→50 code point `Oversize`→`isValid` `SchemaMismatch`）、`IntentValidator` のcategory ref解決（`UNKNOWN_CATEGORY_REF`、KDoc順序記述更新済み）、`IntentPlannerAdapter` のidentity解決投影、`IntentIdentity` の正規化後authored内容基準、`IntentWireContract`/`ExchangePackageComposer` のdescriptor構造派生instruction、`ExchangeImportSummary` kind別count。
- **production wiring**: `ExchangeInputAdapter` が同一composition cutの`input.catalog`をexport/import両方向へthread（第2ラウンドの中心修正。実adapter+実`DefaultOrganizationInputComposer`を駆動する`OrganizationInputComposerTest.exchangeAdapterThreadsTheCompositionCatalogIntoBothDirections`でpin。`ExchangeFlowControllerTest` はinject lambdaで迂回するためこのpinが唯一の実seam検証であることも確認）。
- **durable面**: `AndroidExportSessionStore` のadditive `categoryRefs`（schema 2・file名不変、未知kindは`error()`→`runCatching→null`のsession無しfail-closed、strict record rejectを両testでpin）。**Launcher DB・favorites・recovery・`src/com/android/launcher3/**` への変更はゼロ**（`git diff --name-only`機械確認）。永続化増分はapp-private・backup対象外session recordのみ。
- **planner面（`risk: layout-data` 中核）**: `FormationKey`（Existing先→Proposed後・label UTF-8 byte順）、`formFolderGroups` のfallback排除・minGroupSize・capacity不変、canonical系executorのformation key化とordering用`effectiveCategory`のproposal明示無視、`executeGlobalCompact`（FullRunExecution.kt:586付近）とcandidate tail（PlanningPlacement.kt:263）のclassificationのみ消費（intent非参照）、`FolderNaming.FromProposalLabel` + title解決（`GeneratedFolderTitles`直提示・`withCompositionCatalog`経由・非blank契約）。第3/4ラウンドでplanner test corpus（mixed-semantic component分離の決定的非統合）を追加。
- **UI/privacy面**: summary kind別count（model 3種・UI行は既存category合算）、`exchange_failure_unknown_category_ref` en/ja、14-class/20種のexhaustive `when`、Pre-send Disclosure copyのD-2準拠更新+両locale copy test（第2ラウンド、1st監査後のreview指摘）。
- **docs正本**: spec 204/205/336/348のnormative更新+v4記述（205はData and state実名更新+Change history行。history行の並び順・番号はFindings F-2）、spec 337 accepted化+progress note、CONTEXT.md用語4件、DESIGN.md gate 12。

## Criteria check

`specs/337-exchange-category-group-proposals/spec.md`（accepted）の受入条件ごと。判定はhead `1b8868f7ec` でのcode/test実読と自run（1568 tests、0失敗）に基づく。

- **AC-1: PASS（残2 oracleはfollow-up宣言扱い、F-1a/b）**。envelope `categories` はexport-scoped random ref（item refと同一`RandomIdAllocator` seam）・kind・built-in `taxonomyId`（全tier）・tier制御`displayName`（label-inclusiveのみ）をcanonical identity順でprojection。順序は`Issue336ExchangeProjectionTest.advertisedRefsAreUniqueAcrossNamespacesAndInCanonicalOrder`が**session mapping経由のidentity列と`sorted()`一致**でpin（built-in/user-defined横断の全順序をkind分割ではなくidentity比較で検証）。3-namespace一意・disjointはplaced+candidate+categoryを載せたfixtureでpin、redacted tier名非露出は`redactedTierAdvertisesRefsWithoutUserDefinedNames`（文書bytes走査）+envelope init。raw `UserCategoryId`非出現・`taxonomyId` entry限定は既存corpusでpin済み。残（受入に際しblockでないと判断、根拠はF-1参照）: category refのcross-export unlinkability専用test、`categories`を載せた256 KiB容量経路test。
- **AC-2: PASS**。item-levelはadvertised refのみ（builder不変条件+`ExchangeTargetScopeCouplingTest`の`categoryRef != raw値`+session disjoint init）。codec round-tripはdecode=検証passthroughの構造を正確なcommentでpin（第3ラウンドで文言修正済み）。malformed `categories` decode fixtureは未追加だが、decodeはbuilderと同一model initを通るためadvertised所属・kind制約は強制される（構造実読確認、残nitsはF-1c扱い）。
- **AC-3: PASS**。exactly-one-of（codec+model）、表示名解決経路なし（"Commute"/`u:<uuid>`/捏造ref→`UNKNOWN_CATEGORY_REF` corpus）、proposal→identity非解決（`proposalLabelNeedsNoCategoryAndNeverResolvesToIdentity`）。
- **AC-4: PASS（candidate tail negativeのみ既知の残itemとして明示）**。positive: canonical系でproposalがformation key+titleになる（`runScopedProposalFormsAndNamesItsOwnFolder`）/existing refはidentity基準で従来どおり。negative matrix: `proposalsAreInertUnderTheGlobalCompactStrategies`（GLOBAL_COMPACT_V1/V2）、`runScopedProposalIsInertUnderANonFolderCreatingStrategy`が第3ラウンドで**STABLE_PAGE_TIDY_V1/V2+CATEGORY_CONTIGUOUS_V1にparameterizeされ、かつ「提案なしintent（preserveのみ）runと配置cellが完全一致」=ordering非参加を直接pin**。candidate tailはcode実読でclassificationのみ消費を確認済みだが専用negative testは不在（commit message自身も既知の残itemとして正直に記録、`proposalsDoNotChangeCandidatePlacement`相当はtree内に存在せず — test名grepで不在を確認。F-3a）。
- **AC-5: PASS**。`UserDefinedCategoryNameRules`の同一規則objectでnormalize→上限（`Oversize`）→`isValid`（`SchemaMismatch`）。`IntentCodecTest`が51 code point、`"   "`/`a|b`/`line\nbreak`、`"  Morning  "→"Morning"`正規化をpin。identityは正規化後内容。property形態のtestではない（代表例+規則object共有による構造保証。promotion pass-throughは`create()`経路のAC-9 testで最終pin予定）。
- **AC-6: PASS（apply stepは既存構造+後続pin前提、判定根拠はF-3b）**。第3/4ラウンドで実ファイルバック`UserDefinedCategoryStore`（#336 generation/digest付き）を交差する2つのoracle: `validatedProposalImportLeavesTheCategoryStoreUntouched`はexportをstore現catalogから構築→proposal import→**validation成功→`IntentPlannerAdapter.project`→planner.plan（`Planned`成立）まで走らせた後**、storeのidentity・entries・file bytesの不変をassert。`validatedImportNeverWritesTheDurableSessionRecord`はsession record bytes不変をpin。override store側とapply stepはcode実読でwriter不在（personalization/planning/apply seamに`UserDefinedCategoryStore`/`CategoryOverrideStore`参照なし、applyは#195 transactional suite配下）を確認 — 受入は可能と判断（F-3b参照）。
- **AC-7: PASS**。D-5表全行がpipeline実pathでpin: 未advertise/捏造→`UNKNOWN_CATEGORY_REF`、無割当delete（digest不変assert付）→`UNKNOWN_CATEGORY_REF`、**割当ありdelete→`assignedCategoryDeleteSettlesAsContextStale`（digest gate先行）**、rename→同一original ref解決継続+**reconstruction viewが現名をcarryすることを`SessionExportReconstructor.rebuild`直接assert（第3ラウンド追加）**。silent fallback経路なし（corpus+validator code実読）。
- **AC-8: PASS**。validator受理corpus（`desiredGroup` component内 existing-ref vs proposal / 同一component異label / 無関係同一labelの3 shape受理）と**planner投影側`mixedSemanticsInsideOneComponentSplitIntoDistinctFormationGroups`（mixed component・異label componentとも単一groupへ統合されないこと+plan決定的再現をassert。実runで失敗しない=感度あり: 統合が起きれば`FromCategory(GAMES)`/`FromProposalLabel(...)` folderが現れる構成）**。both-field/空label→`SchemaMismatch`、長さ→`Oversize`。
- **AC-11: PASS**。v4同時bump、v1-v3拒否、session additive `categoryRefs`（旧record→空mapping→fail-closed、未知kind→fail-closed、unknown property record→strict rejectの3方向をtest pin）。
- **AC-12: PASS**。descriptor→instruction構造派生+production path parity fixture（`UNKNOWN_CATEGORY_REF`/`SCHEMA_MISMATCH`/`OVERSIZE`）+policy matrix（canonical⊆accepted）、canonical template不変+`Issue327InterviewFirstContractTest`回帰緑、14-class/20種exhaustive `when`。You-mustにcategory規則明示（footer self-check専用bulletは依然You-must側で充足と解釈、注記F-2c）。
- **AC-13: PASS**。plan決定的再現（proposal/mixed corpus含む再plan一致）、`addingProposalsKeepsExistingCategoryFolderOrdinals`（既存group ordinal不変・Proposed末尾）、rename不変canonical bytes（#336 suite継続）、v3等価回帰（intent-less/golden corpus）はlane緑。

Criteria line対象外の宣言突合（再監査後・final）: **AC-9**（promotion UX — strings/surface不在をgrep確認、宣言どおり）、**AC-10**（model 3-count充足、UI行がbuilt-in+user合算のため3種区別は部分充足、preview側未実施 — 宣言どおり）、**AC-15/AC-16**（evidence pass、宣言どおり）。AC-14のnormative更新（204/205/336/348/CONTEXT/DESIGN）は本文更新として完了。

## Executed test surface

監査sessionがhead `1b8868f7eca84abf4c218d7a31a812aeda0d1df7` のworking tree（`git merge-base`/`gh pr view`で当該head一致・untracked局所file 1件以外のdirtyなしを確認）で自ら実行（macOS、JDK 21.0.12 / Gradle 9.3.0）。

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL**（2026-09-18T08:55:38Z実施、exit=0。同一tree入力につきup-to-date cache経由の成功判定）
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL**（08:55:40Z〜08:56:09Z、exit=0）。XML freshness（自runと時刻一致のmtime）と新規test名`mixedSemanticsInsideOneComponentSplitIntoDistinctFormationGroups`の実在をXMLから確認。**145 suites / tests=1568 / failures=0 / errors=0 / skipped=0**（集計: `build/test-results/testLawnWithQuickstepGithubDebugUnitTest/TEST-*.xml`。前round 1565→+3は`validatedProposalImportLeavesTheCategoryStoreUntouched`・`conflictingSemanticsInsideOneComponentAreAcceptedAndSplitByKey`・`mixedSemanticsInsideOneComponentSplitIntoDistinctFormationGroups`と一致。package内訳 organizer 139 suites・navigation 1・bugreport 3・backup 2）
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL**（08:56:09Z、exit=0）
- `./gradlew assembleLawnWithQuickstepGithubDebugAndroidTest` → **BUILD SUCCESSFUL**（08:56:16Z、exit=0。untracked局所file（#265調査用・本PR非対象・監査で非変更）込みでcompile成功）
- CI再検証（自前、09:06Z）: `gh run view 35326452186 -R nunu1733/NunuLauncher --json status,conclusion,headSha,event,jobs` → completed/success/`pull_request`/headSha=`1b8868f7ec…`、全15 job success。`gh pr checks 355 -R nunu1733/NunuLauncher` → **`final-status` pass**、source jobs（organizer-unit-tests・check-style・build-debug-apk）実行success、instrumentation 9 lane pass（`issue52`含む）、唯一`high-risk-evidence` fail=headにpin済みの旧記録（`917d093854`）に対する「docs-onlyでない後続変更あり・参照runが旧commit」という**本記録更新で解消予定の理由**であることをログから確認。`gh pr view` headRefOid=`1b8868f7ec…`（以後も移動なしを再poll時点で確認）
- static cross-check: `917d093854...1b8868f7ec`増分（`3fd39be240`→`c46e1ab0fe`→`1b8868f7ec`の全hunk実読: test 5 file・spec/docsのみ。`git diff --stat`でproduction変更ゼロを機械確認）、`ExchangeImportPipeline.kt:197`の"thirteen #204 contract classes"残存確認（1b8868f7ecでも未修正。199行の"20 kinds total"と非整合 — F-2a）、`specs/205`Change history挿入位置・番号確認、`proposalsDoNotChangeCandidatePlacement`のtree内不在をgrep確認（commit messageの記述どおりdraft除去済み、本記録はそのtestを主張に用いない）

## Findings

**総括: 1st audit時点のHigh/Medium（production wiring・CI gate・AC-6/7/8/13/14系oracle・carrier・disclosure copy・session fail-closed・AC-1/2/4/11系pin）はすべて解消を確認。残存はLow相当のtest/文書nitsと宣言済み後続スコープのみで、production挙動・privacy境界・layout-data安全面の障害は現head（`1b8868f7ec`）で未確認。**

1. **[Low, F-1] AC-1/AC-2の残oracle 3点を後続へ (blockせず)** — (a) category refのcross-export unlinkability専用test、(b) `categories`を載せた256 KiB容量/Oversize経路test、(c) malformed `categories`（未知kind・advertised外item ref・redacted tier name付き）decode→`SchemaMismatch` fixture。判断: **本PRのmergeを止める性質のものではない**。理由: (a)はcategory refがitem refと**同一seam・同一乱数生成経路**であり、unlinkabilityの正本pin（`UnlinkabilityAndIdentityContractTest`）とenvelope/sessionのdisjoint制約を共有して既に緑。専用testは感度の上乗せであり、現行コードではbare `String` ref生成経路がseam以外存在しない。(b)はencode側`MAX_EXPORT_BYTES`検査がfield非依存でbuilder gateを通過し、cap内件数（user-defined最大64+taxonomy）で構造有界。(c)はdecodeがbuilderと同一model initを走る経路を全testで通しており、init制約は強制される。→ **次のPR（AC-9/AC-10/AC-15/AC-16と同一トラック）に小さなfollow-upとして載せることを推奨**（Issue側で明示追跡すれば十分）。
2. **[Low, F-2] 文書級の不整合・軽微nits（blockではない）** — (a) `ExchangeImportPipeline.kt:197`は「thirteen #204 contract classes」のまま（正しくはfourteen。同KDoc 199行は"20 kinds total"更新済みで内部非整合。`c46e1ab0fe` commit messageはclass count修正を主張するが本ファイルは当該増分で変更されていない — 主張と実態の差として記録、修正は1語）。(b) spec 205 Change historyのv4行は**既存のchronological順（old→new）と`(7th)`番号（既存09-17の7th・09-18の8thと衝突）を無視して先頭へ挿入**されている（内容・链接は正しい。並び替えか`(9th)`採番が必要）。(c) footer self-check listへのcategory専用bulletは未追加（You-must行+「Every ref exists in the CONTEXT data」で実質充足、spec 337 D-8の「You must / self-check」解釈上は許容）。
3. **[Low→follow-upで解消要、宣言済みとして受理] F-3 planner残nits 2点の扱い** — (a) candidate tailのnegative oracle: 本PRは専用testを持たない（`PlanningPlacement.kt:263`がclassificationのみ消費する構造は実読確認済み。第4ラウンドのcommit message自身も既知の残itemとして明記し、draft→除去された`proposalsDoNotChangeCandidatePlacement`相当はtree内に存在しないことも確認。**本記録はそのtestを証拠として主張しない**）。regression防止ギャップとして後続PRでpinすべき。(b) AC-6のapply step・override store: validation→planまでは実store file-backedでpin済み。apply stepのstore不変は「新規writer構造的不存在」+既存#195 transactional suiteの既存pinに依拠（`organizer/application/`系・`CategoryOverrideStore`のpersonalization/planning/apply seam非参照を実読確認）。**block理由とはしない**が、AC-9 promotion pathのtest設計時に「apply経路のcatalog/override無変更」assertを1本足すことを推奨（spec AC-6文言の完全充足として）。
4. **[Info] 残存の未充足受入条件（宣言突合済み・追加の隠れ欠落なし）** — AC-9（promotion UX）、AC-10（summary model充足・UI 3種区別はbuilt-in/user合算行のため部分・preview側未実施）、AC-15/AC-16（evidence）。加えて上記F-1/F-2/F-3のtest・文書残item。**PRが未完として宣言した集合（AC-9/AC-10-preview/AC-15/AC-16）と実測の差分はF-1〜F-3のLow級に限定され、宣言にないMedium/Highの欠落は現headで未確認**。
5. **[Info] 環境** — working treeに非対象untracked file `tests/organizer-instrumentation/app/lawnchair/organizer/application/Issue265GateFailedRouteInstrumentationTest.kt`（#265局所作業物）が存続。androidTest laneは当該file込みで成功。監査で変更・削除していない。

### Machine gate充足性の明示（高リスク独立エビデンス契約）

- **green final-status on audited commit**: 満たす。`pull_request` run 35326452186（audited head `1b8868f7ec…`）で`final-status`=pass・source jobs実行済み（監査sessionが`gh run view`/`gh pr checks`で独自に再検証、09:06Z）。
- **独立audit record**: 本記録が該当。機械検証項目（Auditor/Audit date/Head SHA 40桁/CI run/Criteria lineのspec+AC ID/非空section/`./gradlew` command列）を充足するよう更新済み。**headにcommit済みの記録はまだ旧pin（`917d093854`）であるため、remoteの`high-risk-evidence` checkは本記録commit直後まで赤のまま**。coordinatorが本ファイル更新をdocs-only commitとして`1b8868f7ec`の上に積めば、pin整合（audit shaはheadのancestor+後続docs-only）とgreen run参照が揃い、当該checkもpassに転じる見込み（同手順はPR #341のre-pin precedentどおり）。すなわち**「緑のCI+独立記録の2条件は、本記録のcommitを以て同時に充足される」**。
