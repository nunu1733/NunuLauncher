# High-risk audit: PR #355 v4 category refs and run-scoped formation keys

> Status: accepted
> Audit date: 2026-09-18

- Auditor: independent general-purpose subagent session (separate from the implementation session; solo maintenance). 実装側の主張には依拠せず、diff本文・test本文・productionコードを監査session自らが実読し、検証コマンドを自ら再実行した。
- PR: https://github.com/nunu1733/NunuLauncher/pull/355
- Head SHA: 4fcab060d2a70a458c87ab071d9f09706175cea1
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35319007498 （監査時点でconclusion=failure。詳細はFindings F-2。green runの再取得・re-pinが必要）
- Criteria: specs/337-exchange-category-group-proposals/spec.md AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-11, AC-12, AC-13

## Scope

対象diffは `git diff de77e280b7...4fcab060d2`（41 files、+1454/−249。base `de77e280b7` = PR #354 merge後のmain、`gh pr view` でhead SHA・base一致を照合済み、label `risk: layout-data`、state OPEN）。監査はhead `4fcab060d2` のコードそのものに対して実施した（working treeは当該commitと一致、untrackedの局所file 1件のみ — Findings F-10）。

確認した変更面（diff実読に基づく）:

- **contract/codec面（`organizer/personalization/`）**: `ContextExportContract` のv4 bump（`personalization-context-v4` / `personalized-intent-v4`、`MAX_GROUP_SEMANTIC_FREE_TEXT_CHARS` 廃止）、envelope `categories`（`ExportCategory(ref, kind, taxonomyId?, displayName?)` + init不変条件）、`ExportItem.category/groupSemantic` → `categoryRef/folderCategoryRef` 置換、`FreeTextClass.USER_CATEGORY_NAME` 追加、`PersonalizationContextExportV1.init` のref namespace不変条件（category ref一意・item ref集合とdisjoint・item-level refはadvertised集合必須所属・EXTERNAL_REDACTEDでuser-defined displayName拒否）、`ExportSession.categoryRefs`（ref→`CategoryIdentity`）、`ContextExportBuilder` のcatalog projection（canonical identity順・`RandomIdAllocator` 同一seam・3-namespace衝突検査）、`exportPresentationValue()` 廃止、`SessionExportReconstructor` のsession mapping ∩ 現行catalog交差再構築、`IntentCodec`/`IntentModels.GroupSemantic` のexactly-one-of + #336 name規則（normalize→code point上限Oversize→isValid SchemaMismatch）、`IntentValidator` のcategory ref解決（`UNKNOWN_CATEGORY_REF`）、`IntentPlannerAdapter` のidentity解決投影（raw文字列をplannerへ渡さない）、`IntentIdentity` のauthored（正規化済み）内容基準digest、`IntentWireContract` descriptor改訂（exactly-one-of / proposalLabel lengthLimit / `refScope.groupSemanticCategoryRef` PRODUCTION_ENFORCED / `policy.categoryRefFromContext` AUTHORING_POLICY）、`ExchangePackageComposer` のinstruction派生反映、`ExchangeImportSummary` のkind別count追加。
- **durable面**: `AndroidExportSessionStore` のrecordへadditive `categoryRefs`（default空、record schema 2・file名不変、strict `Json` のまま）。永続化の増分はapp-private・backup対象外のexport session recordのみ。**Launcher DB schema・favorites・recovery・`src/com/android/launcher3/**` への変更はゼロ**（`git diff --name-only` で機械確認）。runtime書き込み経路の変更はplan形成経由の間接効果のみで、apply pathは既存transactional seamのまま。
- **planner面（`organizer/planning/`、`risk: layout-data` の実質中核）**: `FolderFormation` のkeyを `FormationKey`（Existing先→Proposed後、Proposedはlabel UTF-8 byte順）へ一般化、`formFolderGroups` のfallback排除・minGroupSize・capacity partitionはExisting経由で不変、`FullRunExecution.executeCanonicalPageCompact` のformation key解決 + `effectiveCategory`（ordering用）がproposalを明示的に無視、`executeGlobalCompact`（FullRunExecution.kt:586付近）とcandidate tail（PlanningPlacement.kt:263）はclassificationのみ（intent無視の不変を実読確認）、`FolderNaming.FromProposalLabel` + `folderNamingFor(FormationKey)`、`GeneratedFolderTitles` のtitle解決（labelをそのままtitle、非blankは値域により保証。production resolverは `GeneratedFolderTitles.resolver` ← `LawnchairApp` で注入され、`withCompositionCatalog` の `else ->` から到達することを実読確認）。
- **UI面**: import summaryのgrouping breakdown行（既存category参照 / 今回だけグループ提案 (保存されません)）、failure copy `exchange_failure_unknown_category_ref`（en/ja）、`exchangeContractFailureText` のexhaustive `when` へ1分岐追加（14 class / UI 20種のcompile時保証）。promotion UX（AC-9）は意図的に不在。
- **docs正本**: spec 204（groupSemantic定義・limits表・change history v4行）、spec 336（投影規律改訂・AC-14・status注記）、spec 337（accepted化・Phase2 progress note）、spec 348（parity表・scenario・change history）、CONTEXT.md 用語4件、DESIGN.md gate 12。**spec 205のnormative更新は不在**（Findings F-3）。

## Criteria check

`specs/337-exchange-category-group-proposals/spec.md`（accepted）の受入条件ごと。判定はcode実読+test本文実読+自ら実行したlaneの結果に基づく。

- **AC-1: PARTIAL (FAILとして扱う)**。契約面は実装・enforce済み: envelope initがref一意・item/category namespace disjoint・advertised所属・redacted tier name拒否を担保（decode tampered文書も同一initで `SchemaMismatch` 化を `ContextExportCodec.decode` runCatchingラップで確認）。`Issue336ExchangeProjectionTest.userDefinedClassificationsExportAsAdvertisedRefsWithoutRawIdOrNameAtItemLevel` が文書bytesからのraw `UserCategoryId` 非出現とsession無表示名をpin、`ExchangeTargetScopeCouplingTest` がredacted tierでのtaxonomyId保持・item-level ref化をpin。**しかし**: (a) production composition seamがcatalogを一切threadしておらず、実機・本番フローでは `categories` が常に空で、featureの前提である「active catalogのadvertise」が成立しない（**F-1、High**）。(b) spec Test Oracleが要求する `categories` のcanonical identity順テスト、ref乱数性/cross-export unlinkability（`UnlinkabilityAndIdentityContractTest` にcategory参照ゼロ）、3-namespace横断一意性のbuilderテスト、`MAX_EXPORT_BYTES` に対する `categories` 容量テスト（oversize testはsynthetic item ref leverへ置き換えられcategories側は未検証）が欠落（F-6〜F-8）。
- **AC-2: PASS (contract level)**。item-level fieldは `categoryRef`/`folderCategoryRef` のみ、raw built-in値はentryの `taxonomyId` にのみ現れる（`assertFalse(categoryRef == "ENTERTAINMENT")` を含むtestでpin）。builder不変条件はmodel init + 衝突時 `IllegalStateException`（3箇所）で実装。codec round-tripはv4 `categories` fixture込みの内容同一性assertionを持たない（round-trip testの `assertEquals(export, export)` はtautology・pre-existing。weaknessとしてF-8に記録）。
- **AC-3: PASS**。codec（both-set/neither-set → `SchemaMismatch`、`IntentCodecTest.groupSemanticSetsExactlyOneOfCategoryRefAndProposalLabel`）+ model init `require((categoryRef == null) != (proposalLabel == null))`。validatorはadvertised集合+session mappingのみで解決し、`IntentValidator` の解決ループに対応表示名解決経路なし（`unadvertisedCategoryRefIsRejectedFailClosed` が "Commute" 文字列・`u:<uuid>`・捏造idを `UNKNOWN_CATEGORY_REF` でpin）。adapterは `session.categoryRefs` 経由でidentity化し、`proposalLabelNeedsNoCategoryAndNeverResolvesToIdentity` がproposal→identity非解決をpin。
- **AC-4: PARTIAL**。positive: `runScopedProposalFormsAndNamesItsOwnFolder`（異分類2itemが同一labelで1 folder・naming=`FromProposalLabel`・determinism再plan一致）と `existingCategoryReferenceConsumesThroughFolderPlacementSemantics`（`FromCategory(GAMES)` 不変）。negative: page-localは `runScopedProposalIsInertUnderANonFolderCreatingStrategy`（STABLE_PAGE_TIDY_V1）でpin。**GLOBAL_COMPACT_V1/V2・CATEGORY_CONTIGUOUS_V1・candidate tailのmatrix negativeと、「ordering keyがproposalを消費しない」直接testは不在**（codeは `FullRunExecution.kt:586` / `PlanningPlacement.kt:263` / `effectiveCategory` の実読でmatrix一致を確認したが、spec Test Oracleのstrategy別negative固定を満たさない。F-5）。
- **AC-5: PASS**。codecが `UserDefinedCategoryNameRules.normalize`→code point 50上限（超過 `Oversize`）→`isValid`（空/`|`/改行 → `SchemaMismatch`）を正順で適用し、同一規則objectを#336 storeと共有（promotion pass-throughは構造保証。境界test: 51 code points → Oversize、`"   "`/`a|b`/`line\nbreak` → SchemaMismatch、`"  Morning  "` → 正規化 `"Morning"`）。property test形態ではなく代表例test（注記）。identityは正規化後authored内容（`IntentIdentity.canonicalRow`）。
- **AC-6: FAIL (oracle不在)**。validation/plan/apply経路でcatalog・override storeが無変更であるproperty testは本PRに追加されておらず、既存にもproposal intentを通したstore不変testはない。構造的不存在（import〜apply pathにcategory store writerがないこと）は `ExchangeImportPipeline`/`IntentValidator`/apply seam実読で確認済みだが、spec AC-6/D-7が要求するtestで固定されていない（F-4）。PR本文は「既存のzero-write規律test」で足りると主張しており、主張が過大。
- **AC-7: PASS**。D-5表の各行: 未advertise/捏造ref → `UNKNOWN_CATEGORY_REF`（validator corpus test）; 無割当delete → digest不変assert付きでpipeline実path（`categoryDeletedWithoutAssignmentIsRejectedAsUnknownRef` が `ExchangeImportPipeline.import` 経由で `Contract(UnknownCategoryRef)` をpin）; 割当ありdelete → resolved identity変化でdigest変わる（#336 from `Issue336ExchangeProjectionTest.digestTracksIdentityNotDisplayName` 系統）+ pipeline順序がdigest gateをreconstruction/validationより前に置くことを `ExchangeImportPipeline.validate`（:125-127→rebuild→validator）と `ExchangeImportPipelineTest.structuralChangeAfterExportConvergesOnContextStale` で確認; rename → identity存続で受理・現名 `Transport` 表示（`renamedCategoryKeepsTheReferenceValid`）。reconstruction intersection（`SessionExportReconstructor` のsession∩catalog）はparity test（`reconstructionViewAdvertisesOnlyIdentitiesThatStillExistAndShowsNoRawId`）でpin。細注: export→import間renameをreconstruction経由で通すstale testはbuild-time rename相当であり、renameでのview name更新はreconstruction path直接testなし（Low、本文注記）。
- **AC-8: PARTIAL**。both-field/空label/長さのclassはcodec+validator両面でpin済み。planner corpus: 同一label・非connected統合 ✓、別label分離 ✓、同一categoryRef・非connected統合 ✓。**同一 `desiredGroup` component内の異semantic分岐と「同一label+別componentの異semantic」のcorpus testが不在**（2nd review R-2'で要求された3ケースのうち2ケース。F-5）。reject不发生（受理されること）自体はrule objectの不在=構造保証で弱い。
- **AC-11: PASS (一部注記)**。`personalization-context-v4`/`personalized-intent-v4` bump、v1/v2/v3文書は `SCHEMA_MISMATCH` fail-closed（`IntentCodecTest.savedV1ToV3DocumentsAreRejectedFailClosed`、`ExchangeTargetScopeCouplingTest` のcontext v1拒否）。`categoryRefs` additive永続化 + pre-v4 recordが空mappingでdecodeされfail-closedへ落ちることを `AndroidExportSessionStoreTest.categoryRefMappingIsDurableAndLegacyRecordsFailClosed` がpin。**逆方向（v4 recordを旧binaryが読む=unknown keyでdecode失敗→session無し扱い）はstrict `Json`（`ignoreUnknownKeys=false` 既定）+ `runCatching→null` 構造で成立するが、unknown keyを直接pinするtestはない**（D-8「両方向の挙動をtestでpin」に対し片側のみ。F-9）。
- **AC-12: PASS**。descriptor claim（EXACTLY_ONE / proposalLabel lengthLimit=50 / `refScope.groupSemanticCategoryRef` PRODUCTION_ENFORCED / `policy.categoryRefFromContext` AUTHORING_POLICY）からinstructionが構造派生され、`Issue348AiFacingContractSyncTest` がparity fixtureをproduction path（`ExchangeImportPipeline.import`）経由でaccept/reject両pin、policy matrix（canonical ⊆ accepted）と `CategoryRefFromContextArray` の受理fixtureを追加。canonical templateは不変（judgment-bearing optional fieldを掲載せず、`CANONICAL_INTENT_TEMPLATE` 実読）し、`Issue327InterviewFirstContractTest` は本PRで無変更・自ランのlaneで緑。細注: 「捏造禁止」の明示はYou-must行のみで、footer self-check listへのcategory固有bulletは未追加（Low、F-9注）。
- **AC-13: PARTIAL**。determinism（同一入力でplan一致、proposal/既存ref双方でassert済み）とrename非変更canonical bytes（`CategoryIdentityCatalogTest.renameDoesNotChangeCanonicalPlanBytes`、無変更で緑）は確認。**「proposalの有無だけが異なるrunで既存group ordinal不変」の再確認testは不在**（FormationKey順序はExisting先なので論理的に成立するが、spec Test Oracle「ordinal安定性」のtestが欠落。`IntentPreferenceConsumptionTest` にordinal assertionなしをgrepで確認。F-5。PR本文の「ordinal安定」主張はtestで裏付けられていない）。

Criteria lineの対象外で本PRが明示的に未完了と宣言したもの: **AC-9**（promotion UX — strings/surface不在を実grepで確認、宣言どおり）、**AC-10のpreview側**、**AC-15 / AC-16 evidence**。加えて宣言にない未充足がAC-6（上掲）、AC-14のspec 205分（F-3）、AC-10 summary側のUI区分（F-4注: summary modelは3種countだがUI表示行は「既存category参照（built-in+user合算）」と「今回だけのグループ提案」の2行で、AC-10の3種区別表示は合算行のため部分充足。`exchange_import_summary_proposed_group` の「保存されません」copyはen/ja実装済み）。

## Executed test surface

監査sessionが自ら実行（macOS、JDK 21.0.12 / Gradle 9.3.0、working tree = head `4fcab060d2`）。

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL in 2s**（2026-09-18T07:34:45Z実施。5 actionable tasks up-to-date=同一treeに対するcache再利用、taskは成功判定）
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL in 30s**（07:34:47Z実施）。結果XMLのfreshness（mtime 07:35、17 tasks executed）を監査sessionが機械確認。**145 suites / tests=1554 / failures=0 / errors=0 / skipped=0**（`build/test-results/testLawnWithQuickstepGithubDebugUnitTest/TEST-*.xml` を集計。内訳: organizer 139 suites、ui.preferences.navigation 1、bugreport 3、backup 2）。実施したcommandのfilter幅はPR packetの「1553 tests」claimと同系統で整合。
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL in 9s**（07:35:18Z。同一tree入力のup-to-date中心）
- `./gradlew assembleLawnWithQuickstepGithubDebugAndroidTest` → **BUILD SUCCESSFUL in 6s**（07:35:28Z。untracked局所file `tests/organizer-instrumentation/.../Issue265GateFailedRouteInstrumentationTest.kt` もcompile対象に含まれるため同file込みで成功。当該fileは本PRの成果物ではなく監査で削除・変更していない）
- CI evidence再確認（read-only、AGENTS.md preflight含む）:
  - `gh repo view nunu1733/NunuLauncher --json nameWithOwner,defaultBranchRef` → `nunu1733/NunuLauncher` / `main` を照合
  - `gh pr view 355 -R nunu1733/NunuLauncher` → head=`4fcab060d2a70a458c87ab071d9f09706175cea1`、base=main、label `risk: layout-data`、state OPEN
  - `gh pr checks 355 -R nunu1733/NunuLauncher`（2026-09-18T07:38Z頃）→ **final-status=fail、organizer-instrumentation-issue52-tests=fail、high-risk-evidence=fail**、以下pass: organizer-unit-tests、check-style、build-debug-apk、validate-repo-contract、instrumentation他11 lanes
  - `gh run view 35319007498 ...` → event=`pull_request`、headSha=`4fcab060d2`、conclusion=**failure**。失敗testは `InjectedInputEnvironment.ensureWindowFocused` の15s window-focus timeout（focused window=`Application Not Responding: com.android.systemui`）で、code assertion失敗ではなくemulator側のANR/infra flake signature。同一SHAに再run・attempt>1は `gh run list --branch issue-337-impl` / `actions/runs?head_sha=...` で0件（唯一のrunがfailureのまま）
- static cross-check（抜粋、監査session自らのgrep/読解）: `git diff de77e280b7...4fcab060d2 --name-only` に `ExchangeInputAdapter.kt` 不在および `composeFrom`/`currentStructural` の `catalog` 未thread（ExchangeInputAdapter.kt:77、:100-105）→ F-1; digest定義が `CanonicalStructuralInputs.catalog` を消費しない確認（`SourceContextIdentity.canonicalRepresentation`）; pipeline順序 session→expiry→digest→reconstruct→validate 確認（ExchangeImportPipeline.kt:113-152）; `planFolderGroups` のcallerがFullRunExecution×2 + PlanningPlacement×1でmatrix範囲と一致; codec round-trip testの `assertEquals(export, export)` tautology確認。

## Findings

重大度順。**本監査はmerge可否の最終判断を代置しないが、F-1・F-2が存在するかぎり、このPRは高リスク独立エビデンス契約（docs/project/github-workflow.md）が要求する「検証対象commit上でのfinal-status成功」を満たさず、現headでのmergeは許可されない**。

1. **[High] production seamでcatalogがthreadされず、featureが本番で到達不能（AC-1/AC-3の製品面不達）**。`ExchangeInputAdapter.composeFrom`（ExchangeInputAdapter.kt:77）は `ExportInputs` を `catalog` 省略（既定null）で構築し、`currentStructural()`（:100-105）も `CanonicalStructuralInputs` にcatalogを渡さない。結果、production exportは `categories=[]`、item `categoryRef` は常にnull、import再構築はsession∩null catalog=空で、`groupSemantic.categoryRef` を含んだあらゆる応答が `UNKNOWN_CATEGORY_REF` として落ちる（fail-closed方向なのでprivacy・データ安全性の破損はないが、spec 337 outcome 1「既存category参照」と正常系Scenarioが実機で成立しない）。`plan.md` Expected filesは「`integration/exchange/ExchangeInputAdapter.kt` (catalog引き回し)」を明示的に含み、本PRの全testは `ExportInputs(catalog=…)` を手動注入するharness pathのみを通るため、この欠落をunit lane・androidTest・CIのいずれも検出できない（`ExchangeInputAdapter` を実構成で駆動するtestはrepo内に存在せず、`ExchangeFlowControllerTest` はinject lambdaでadapterを迂回）。修正は1行×2（`catalog = input.catalog` / `catalog = result.inputs.catalog`）+ 実adapterを通すwiring test。**spec 337の「Implementation progress: contract core landed」およびPR bodyの完了宣言はこの欠落を未完了として宣言しておらず、主張が不正確**。
2. **[High (evidence)] 検証対象commitのCI merge gateがred**。run 35319007498（ci.yml pull_request、head 4fcab060d2）は `organizer-instrumentation-issue52-tests` のemulator ANR型timeout（systemui not responding、`InjectedInputEnvironment.kt:183`）と `final-status` が失敗。失敗laneは本PR変更面（personalization/planningのみ、instrumentation無変更）と無関係のinfra flake signature（他11 instrumentation lanes・organizer-unit・check-style・build-debug-apkは同SHAでsuccess）。ただし高リスク契約は「agent報告ではなく実runの成功」を要求するため、**当該laneのre-run（または再push後の新run）でfinal-status緑を確認し、本記録のCI run lineをre-pinするまでmerge要件は未達**。`high-risk-evidence` run 35319012773の失敗は本audit記録がheadに存在しないことによるもので、記録導入+green runで解消する見込み。
3. **[Medium] AC-14のnormative更新が不完全 — spec 205が未更新**。spec 337 D-8/AC-14は「spec 205（Data and stateの #204由来制約の実名更新）」を実装PR必須として列挙するが、本PRは205に触れていない。205:249の「#204 accepted契約由来の制約 (実名)」はref生成seam（`RandomIdAllocator`）等の実名記述であり、v4のcategory ref / `categories` projectionを反映していない（205 AC-5のclass数記述も旧い）。204/336/348/CONTEXT/DESIGN更新はpresentかつ内容正確。progress note・PR bodyは205を未完として挙げてもいないため、**PRが未完として宣言した範囲（AC-9、AC-10 preview、AC-15/16）以外にAC-14の一部とAC-6（F-4）が未充足**。
4. **[Medium] AC-6のproperty testが不在**。proposal intentを通したvalidation/plan/apply経路で `UserDefinedCategoryStore`/`CategoryOverrideStore` が無変更であることを固定するtestがない（構造的writer不在はcode実読で確認済み）。specは「全経路で無変更であることのproperty test」を要求。加えてAC-10 summary側はmodelの3種countが揃う一方、UI breakdownはbuilt-in+user-definedを1行に合算しており（ExchangeFlowUi.kt:1572-1584、「既存カテゴリを参照する項目」）、AC-10の3種区別表示は部分充足。「保存済みに見えない」主張をpinするUI testも未追加（deferred宣言はpreview側のみ）。
5. **[Medium] AC-4/AC-8/AC-13のplanner/validator corpusがPR本文の主張に対して過大**。本文・spec progress noteは「strategy別negative・ordinal安定・新規assertion」を挙げるが、実testは (a) GLOBAL_COMPACT_V1/V2 negative、(b) ordering非参加の直接test、(c) 同一component内異semantic・同一label+別component異semanticのbranch corpus、(d) proposal有無ordinal不変の再確認を欠く（既存suiteにcategory intent関連のGLOBAL_COMPACT testなしをgrepで確認）。matrix自体はcode実読で正しく、回帰safe側（無intent run不変・determinism・FromProposalLabel naming・fallback/capacity不変）はpin済み。証跡としての上記4点の追加を推奨（production変更は不要の見込み）。
6. **[Low] AC-1 builder oracleの欠落**: `categories` のcanonical identity順test、ref生成ごとに新鮮/cross-export非共有のunlinkability test（category面。`UnlinkabilityAndIdentityContractTest` はitem/exportIdのみ）、3-namespace横断一意性のbuilderテスト、`categories` をleverにした `MAX_EXPORT_BYTES`/Oversize容量testがない（capacity testはsynthetic item ref leverに代替され、encoder境界自体はpinのまま）。
7. **[Low] `ExportCategory.displayName` が spec D-1/plan Interface 2の `ExportCategoryName(freeTextClass, value)` carrierでなく裸 `String?`**。`FreeTextClass.USER_CATEGORY_NAME` は宣言されるがどの値にも添付されず（KDoc言及のみ）、tier gateはbuilder/reconstructor呼び出し点とenvelope initで動作上是正（decode破損文書もinitで弾かれる）だが、single-point classキャリアという契約の形がspec表記とズレている。表記を寄せるか、ADR/spec側で「carrier実質化」として明示的に更新すべき。
8. **[Low] codec round-tripのoracleが弱い**: `ContextExportCodecTest` のround-tripは `assertEquals(export, export)`（tautology、pre-existing）で内容比較がなく、`categories` を含んだv4文書のencode→decode対称性、malformed `categories`（kind不明・item refと衝突・redacted tierでname付き）decode拒否のfixtureが未追加（init経由でfailはする）。`ExchangeTargetScopeCouplingTest` のround-tripも成功判定のみ。
9. **[Low] 文書・計数・commentのstale/不正確**: `ExchangeImportPipeline.kt:199` KDocの「19 kinds total」が20に未更新（`exchangeContractFailureText` 側は14-classへ更新済み）; `ContextExportBuilder.kt:224-226`「category refs are drawn last」はcode順（build()冒頭で先にdrawし、item/candidate/exportIdのallocator列がcatalog存在時にずれる）と矛盾する誤記——refはopaque・per-generation生成なので契約影響はないが、注記の意味が逆; `IntentValidator.validate` KDocの順序記述にcategory ref解決が不記載; instruction footer self-check listにcategory固有bulletなし（You-must側でカバー）。`AndroidExportSessionStore` の新record→旧binary方向はstrict Json＋runCatchingでfail-closed（既定で`ignoreUnknownKeys=false`）だがunknown key直接testなし（F-9 Lowに含む）。
10. **[Info] 監査環境の注記**: working treeにuntracked file `tests/organizer-instrumentation/app/lawnchair/organizer/application/Issue265GateFailedRouteInstrumentationTest.kt`（#265調査の局所作業物、本PRと無関係）が存在。androidTest laneは当該file込みでBUILD SUCCESSFULで、本PRの判定・数値（1554 tests等）には影響しない。監査では削除・変更していない。

**このPRが充足していない受入条件（明示）**: AC-6（test oracle不在）、AC-14（spec 205未更新分。204/336/348/CONTEXT/DESIGN分は充足）、AC-9（宣言済み未完）、AC-10（summary model充足・summary UI表示は合算行のため部分・preview側は宣言済み未完）、AC-15 / AC-16（宣言済みevidence pass）。宣言済みの未完に加え、宣言にない未充足はAC-6・AC-14/205・AC-1（production wiring・builder oracle群）・AC-4/8/13（matrix negative・ordinal・component corpus）であり、F-1・F-2は現headでのmergeを許可しない。
