# High-risk audit: PR #355 v4 category refs and run-scoped formation keys

> Status: accepted
> Audit date: 2026-09-18

- Auditor: independent general-purpose subagent session (separate from the implementation session; solo maintenance). 実装側の主張には依拠せず、diff本文・test本文・productionコードを監査session自らが実読し、検証コマンドを自ら再実行した。
- PR: https://github.com/nunu1733/NunuLauncher/pull/355
- Head SHA: 917d09385432a310b33cd884c7a62bc4ec935a40
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35323615453 （`gh run view` で監査session自らが照合: event=`pull_request`、headSha=`917d093854`、status=completed、conclusion=success、`final-status`・`organizer-unit-tests`・`check-style`・`build-debug-apk` 含む全job success。旧記録が対象とした `4fcab060d2` のrun 35319007498はinstrumentation laneのemulator ANR型timeoutで失敗しており、当該laneは本headでpass=infra flakeだったことを確認）
- Criteria: specs/337-exchange-category-group-proposals/spec.md AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-11, AC-12, AC-13

> Re-audit lineage: 1st auditはhead `4fcab060d2`（merge base `de77e280b7`）に対して実施し、High 2件を含む指摘を記録した。続く `f9d34576c2`（監査指摘対応）と `917d093854`（第2 review指摘対応）はdocs-onlyではないため、github-workflow.mdの規則4に従い新headへ再監査した。本記録は `917d093854` 時点の全文であり、`de77e280b7...917d093854` 全体の監査として成立させるため、初回監査で実読確認ずみの面（contract/planner/session/UI/docs本体）も本文に維持している。再pin対象の増分diff `4fcab060d2...917d093854`（20 files、+551/−72）は全hunkを監査sessionが実読した。

## Scope

対象diffは `git diff de77e280b7...917d093854`（1st round: 41 files +1454/−249、2nd round増分: 20 files +551/−72）。監査sessionがdiff・関連production/test本文を実読して確認した変更面:

- **contract/codec面（`organizer/personalization/`）**: `ContextExportContract` v4 bump（`personalization-context-v4`/`personalized-intent-v4`）、envelope `categories`（`ExportCategory(ref, kind, taxonomyId?, displayName: ExportCategoryName?)` — 2nd roundでspec D-1どおりのfree-text-class carrier化、initは非空・≤200 charをcarrier側で検証）、`ExportItem.categoryRef`/`folderCategoryRef` へのref一本化、`FreeTextClass.USER_CATEGORY_NAME`、`PersonalizationContextExportV1.init` のref namespace不変条件（category ref一意・item ref集合とdisjoint・item-level refはadvertised集合所属・EXTERNAL_REDACTEDでname拒否。decode tampered文書も同一init+`runCatching→SchemaMismatch`でfail）、`ExportSession.categoryRefs`、`ContextExportBuilder` のcatalog projection（canonical identity順・同一allocator seam・3-namespace衝突検査）、`SessionExportReconstructor` のsession∩catalog交差再構築、`IntentCodec`/`IntentModels.GroupSemantic` のexactly-one-of + #336 name規則共有（normalize→50 code point `Oversize`→`isValid` `SchemaMismatch`）、`IntentValidator` のcategory ref解決（`UNKNOWN_CATEGORY_REF`、KDoc順序記述は2nd roundで実順序に追記済み）、`IntentPlannerAdapter` のidentity解決投影、`IntentIdentity` のauthored（正規化後）内容基準digest、`IntentWireContract` descriptor（exactly-one-of/`proposalLabel` lengthLimit/`refScope.groupSemanticCategoryRef` PRODUCTION_ENFORCED/`policy.categoryRefFromContext`）、`ExchangePackageComposer` のinstruction構造派生、`ExchangeImportSummary` kind別count。
- **production wiring（2nd roundの中心修正・High指摘）**: `ExchangeInputAdapter.composeFrom` が `ExportInputs(catalog = input.catalog)` を、`currentStructural()` が `CanonicalStructuralInputs(catalog = result.inputs.catalog)` をthread（ExchangeInputAdapter.kt:79、:109）。同一composition cutのcatalogがexport/import両方向へ届くことを実adapter・実`DefaultOrganizationInputComposer` fixtureでpinする新test `OrganizationInputComposerTest.exchangeAdapterThreadsTheCompositionCatalogIntoBothDirections`（allowedIdentities非空と両方向catalog一致をassert。`ExchangeFlowControllerTest` はinject lambdaでadapterを迂回するため、このpinはwiring testとして唯一無二のもので、実際にproduction seamを駆動していることを確認）。
- **durable面**: `AndroidExportSessionStore` のadditive `categoryRefs`（record schema 2・file名不変。2nd roundでkind判別を `"BUILT_IN"`/`"USER_DEFINED"` のみ受理+未知kindは`error()`→`runCatching`で「session無し」fail-closedへ。unknown record propertyのstrict rejectをpinするtest 2件追加）。**Launcher DB schema・favorites・recovery・`src/com/android/launcher3/**` への変更はゼロ**を`git diff --name-only`で機械確認。永続化の増分はapp-private・backup対象外session recordのみ。
- **planner面（`risk: layout-data` 中核）**: `FormationKey`（Existing先→Proposed後、label UTF-8 byte順）、`formFolderGroups` のfallback排除/minGroupSize/capacity不変、`executeCanonicalPageCompact` のformation key + ordering用`effectiveCategory`がproposalを明示無視、`executeGlobalCompact`（FullRunExecution.kt:586付近）とcandidate tail（PlanningPlacement.kt:263）はclassificationのみ、`FolderNaming.FromProposalLabel` + title解決（`GeneratedFolderTitles`でlabel直提示、production resolver注入と`withCompositionCatalog`経由を実読確認）。
- **UI/privacy面**: import summaryのgrouping breakdown（既存category参照 / 今回だけ提案 (保存されません)）、`exchange_failure_unknown_category_ref` en/ja、exhaustive `when` 14-class/20種化、**Pre-send Disclosure copy（2nd round追加・D-2要求）**: redacted=「カテゴリの一覧 (ユーザー定義を含む) は含まれるが名前は含まれない」/label-inclusive=「ユーザー定義カテゴリの名前を含む」+privacyモードlabel改訂、`ExchangeCapabilityCopyTest` が両localeの文言をpin。
- **docs正本**: spec 204/336/348 + **spec 205（2nd roundで「#204由来制約 (実名)」行にv4 category露出・ref seam・`UNKNOWN_CATEGORY_REF` を追記。残nitsはFindings F-6）**、CONTEXT.md 用語4件、DESIGN.md gate 12、spec 337 accepted化+Phase2 progress note。

## Criteria check

`specs/337-exchange-category-group-proposals/spec.md`（accepted）の受入条件ごと。判定はhead `917d093854` でのcode実読+test本文実読+自ら実行したlane結果に基づく。

- **AC-1: PASS（残nits 2点、F-4）**。`categories` projection（export-scoped random ref・kind・built-in `taxonomyId`・tier制御`ExportCategoryName` carrier・canonical identity順）と3-namespace横断一意はcode+envelope/session initで強制。wiring: 本番adapterがcatalogを両方向thread（上記testでpin、F-1解消）。redacted tier走査: `Issue336ExchangeProjectionTest.redactedTierAdvertisesRefsWithoutUserDefinedNames`（文書bytesに名前・raw ID不在）、`advertisedRefsAreUniqueAcrossNamespacesAndInCanonicalOrder`（kind単位では先頭順をpinするが、kind内の`CategoryId`/stable-ID byte順一致は未assert — F-4a）、raw `UserCategoryId`非出現は`userDefinedClassificationsExportAsAdvertisedRefsWithoutRawIdOrNameAtItemLevel`。残: category refのcross-export unlinkability（同一状態2生成でref集合非共有）と`categories`を載せた256 KiB容量経路はtestなし（F-4b）。
- **AC-2: PASS**。item-levelはadvertised refのみ（`ExchangeTargetScopeCouplingTest` が`categoryRef != "ENTERTAINMENT"`とentryの`taxonomyId`/kindをpin。builder不変条件はmodel init+builder throwで全構成経路で強制、candidateは`folderCategoryRef==null`不変維持）。codec round-tripはdecode→同一bytesのpassthrough検証で、malformed `categories` decode fixtureは未追加（decode自体はinit制によりtyped `SchemaMismatch`へ落ちる構造を実読確認 — F-5a）。
- **AC-3: PASS**。codec exactly-one-of（both/neither → `SchemaMismatch`）+ model init `require`、validator/adapterはadvertised集合+session mapping経由のみでidentity解決、表示名解決経路なし（"Commute"/`u:<uuid>`/捏造 → `UNKNOWN_CATEGORY_REF` corpus）。proposal→identity非解決もpin済み。
- **AC-4: PASS（残nits、F-3）**。positive: `runScopedProposalFormsAndNamesItsOwnFolder`（異分類itemが同一labelで1 folder・title=label・再plan一致）、`existingCategoryReferenceConsumesThroughFolderPlacementSemantics`。negative matrix: `runScopedProposalIsInertUnderANonFolderCreatingStrategy`（STABLE_PAGE_TIDY_V1）+ **2nd round追加 `proposalsAreInertUnderTheGlobalCompactStrategies`（GLOBAL_COMPACT_V1/V2両方、同一label proposalでfolderが形成されないことをpin）**。code実読でcandidate tail（`PlanningPlacement.kt:263`）・page-localのintent非参照も確認済み。残: proposalのordering key非参加の直接test（classification identity維持）とCATEGORY_CONTIGUOUS_V1 ordering negativeは未追加（F-3a）。
- **AC-5: PASS**。codecが`UserDefinedCategoryNameRules`（同一規則object）でnormalize→code point 50超=`Oversize`→空/`|`/改行=`SchemaMismatch`。`IntentCodecTest`が51 code point、`"   "`/`a|b`/`line\nbreak`、正規化passthrough（`"  Morning  "→"Morning"`）をpin。identityは正規化後authored内容（`IntentIdentity.canonicalRow`）。property形態でなく代表例testである点は注記（F-5b、promotion pass-throughは共有規則objectで構造保証）。
- **AC-6: PASS（注記付き）**。2nd round追加 `ExchangeImportPipelineTest.validatedImportNeverWritesTheDurableSessionRecord` が、検証成功importでdurable session record bytesが不変（re-load一致込み）をpin — 経路上唯一のdurable書き込み面を実際にファイルbytesで固定している。catalog/override store側はimport〜apply pathにwriterが構造的に存在しないこと（`ExchangeImportPipeline`/`IntentValidator`/`IntentPlannerAdapter` のstore非参照、applyは既存#195 transaction pathで#337変更なし）を実読で確認。spec文言「validation・plan・applyの全経路のstore不変property test」に対し、plan/apply経路のstore直接assertは依然構造的論証＋既存suite頼み（F-5c注記）。
- **AC-7: PASS**。D-5表の全行が実pipeline testでpin済み: 未advertise/捏造ref → `UNKNOWN_CATEGORY_REF`（`unadvertisedCategoryRefIsRejectedFailClosed`）、無割当delete → digest不変assert付きで`UNKNOWN_CATEGORY_REF`（`categoryDeletedWithoutAssignmentIsRejectedAsUnknownRef`）、**割当ありdelete → digest変化でpipeline gateが`CONTEXT_STALE`をsettle（2nd round追加 `assignedCategoryDeleteSettlesAsContextStale` が`ExchangeImportPipeline.import`経由で直接pin）**、rename → 同一sessionのoriginal refが解決継続（2nd round改訂 `renamedCategoryKeepsTheOriginalExportRefResolvable` がdigest不変とValidatedをpin。再構築viewが現名をcarryする点はtest化なし — F-3b。build-time entry名の現名化はIssue336系testでpin）。fuzzy/silent fallback経路なしはcorpus+code実読で確認。
- **AC-8: PARTIAL**。codec/validator: both-set・空label → `SchemaMismatch`、長さ → `Oversize`、統合・分離のplanner corpus（同一label非connected統合 ✓ 2nd round `proposalLabelsMergeItemsThatAreNotConnectedByDesiredGroup`、別label分離 ✓、同一categoryRef非connected ✓、**「同一desiredGroup component内の異semantic分岐」と「同一label+別componentの異semantic」のcorpusは依然未追加** — F-3c）。rejectしないこと自体は整合規則の不存在で構造保証。
- **AC-11: PASS**。v4同時bump、v1/v2/v3文書拒否（codec+context両test）、session `categoryRefs` additive永続化+pre-v4 record空mapping decode+後続`UNKNOWN_CATEGORY_REF`収束、**2nd roundで逆方向（unknown key record拒否=旧binary fail-closed機構）と未知kind fail-closedを直接pin**（`recordsCarryingUnknownPropertiesAreRejected`、`unknownCategoryRefKindInTheRecordFailsClosed`）。
- **AC-12: PASS**。descriptor→instruction構造派生（exactly-one-of文、`categoryRef` ref scope文、`proposalLabel`上限・「this run only」）、`Issue348AiFacingContractSyncTest`がproduction path経由のparity（unknown ref→`UNKNOWN_CATEGORY_REF`、both-field/空→`SCHEMA_MISMATCH`、長さ→`OVERSIZE`）とpolicy matrix（canonical ⊆ accepted）をpin、canonical template不変+`Issue327InterviewFirstContractTest`回帰緑（自ラン）、failure 20種のexhaustive `when`。footer self-check listにcategory固有bulletは依然You-mustのみ（F-6c、仕様は「You-must/self-check」でYou-must充足と解釈可）。
- **AC-13: PASS**。determinism（plan再現一致）、**2nd round追加 `addingProposalsKeepsExistingCategoryFolderOrdinals`（既存category folderのordinalがproposal追加で不変・Proposedは末尾に付くことをpin）**、rename→canonical plan bytes不変は既存`CategoryIdentityCatalogTest.renameDoesNotChangeCanonicalPlanBytes`で維持、intent-less/proposal-less runのv3等価は既存byte回帰suite+matrix negativeで担保。

Criteria line対象外の本PR未完了（宣言と実地の突合）: **AC-9 promotion UX**（strings/surface不在を実grepで確認・宣言どおり）、**AC-10**（model 3-countは充足、summary UI行はbuilt-in+user-defined合算のまま=3種区別の部分充足、preview側は宣言どおり未完。promotion提示面の「保存済み」誤認testも未追加）、**AC-14**（204/205/336/348/CONTEXT/DESIGN本体更新は完了。残nits F-6）、**AC-15/AC-16**（evidence pass、宣言どおり）。

## Executed test surface

監査sessionがhead `917d09385432a310b33cd884c7a62bc4ec935a40` のworking tree（untracked局所file1件を除きtree=commit一致を確認）で自ら実行（macOS、JDK 21.0.12 / Gradle 9.3.0）。

- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL in 1s**（2026-09-18T08:23:42Z実施、5 tasks up-to-date=同一treeに対するbuild cache経由の成功判定）
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*' --tests 'app.lawnchair.ui.preferences.navigation.*' --tests 'app.lawnchair.bugreport.*' --tests 'app.lawnchair.backup.*'` → **BUILD SUCCESSFUL in 30s**（08:23:44Z〜08:24:15Z、17 tasks executed）。結果XMLのfreshnessをmtime（08:24、現runと一致）で、件数の増分を前回run（1554）との対比で確認:**145 suites / tests=1565（+11が2nd roundの新test）/ failures=0 / errors=0 / skipped=0**（`build/test-results/testLawnWithQuickstepGithubDebugUnitTest/TEST-*.xml` を集計。organizer 139 suites + ui.preferences.navigation 1 + bugreport 3 + backup 2）
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL in 4s**（08:24:15Z。同一tree入力、4 tasks executed）
- `./gradlew assembleLawnWithQuickstepGithubDebugAndroidTest` → **BUILD SUCCESSFUL in 5s**（08:24:20Z。untracked局所file（#265調査用・本PR非対象）もcompile対象で成功。当該fileは削除・変更していない）
- CI照合（read-only）: `gh pr checks 355 -R nunu1733/NunuLauncher` と `gh run view 35323615453 -R nunu1733/NunuLauncher --json status,conclusion,headSha,event,jobs` を08:30Zからポーリングし、**08:31Zにstatus=completed・conclusion=success**（headSha=`917d093854`、event=`pull_request`、全15 job success — `final-status`、`organizer-unit-tests`、`check-style`、`build-debug-apk`、`validate-repo-contract`、instrumentation lanes含む。前回headで失敗した `organizer-instrumentation-issue52-tests`（systemui ANR型window-focus timeout）は今回passし、infra flakeだったことを実証確認）。`gh run list`で本headのci.yml runは当該1本のみ。参考: 同一pushへの`high-risk-evidence` run 35323615652は失敗だが、理由は「監査記録が旧headにpin済みでdocs-onlyでないcode増分あり+参照CI runが赤」という**本再監査が解消すべき事項そのもの**（記録更新commit後は解消する見込み）であり、code/lane失敗ではないことをログから確認。
- local gate再現: `python3 tools/repo-contract/validate_high_risk_evidence.py --repo nunu1733/NunuLauncher --pr-number 355 --head-sha 917d09385432a310b33cd884c7a62bc4ec935a40` を更新後の本記録で実行し、機械検証項目の充足を監査session自身が確認（結果はFindings F-7に記載）。

static cross-check（増分diff 20 files全hunk実読、抜粋）: `ExchangeInputAdapter.kt:79/:109` のcatalog thread両方向+`OrganizationInputComposerTest` のwiring testが実adapter・実composer駆動であることをtest本文で確認; `AndroidExportSessionStore` の未知kind `else -> error()` が`runCatching→null`（session無しfail-closed）に到達する構造を確認; `ExportCategoryName` carrier化でcodec encode（`it.value`）/decode（`USER_CATEGORY_NAME`添付）/builder/reconstructorの4点が整合していることをdiff全文で確認; strings en/jaのdisclosure文言がspec 337 D-2（redacted=名前は含まない/label-inclusive=名前を含む）と一致、`ExchangeCapabilityCopyTest`が両localeをpin; `proposalsAreInertUnderTheGlobalCompactStrategies`（classification 1+fallback 1でproposal 2件なら形成される=oracleが変化に感度、単なるvacuous assertでないこと）、`addingProposalsKeepsExistingCategoryFolderOrdinals`（Existing 2件+Proposed 2件、ordinal一致+Proposed追記）、`assignedCategoryDeleteSettlesAsContextStale`（割当identity変化+catalog削除で`CONTEXT_STALE`）、rename original-ref解決test（digest不変assert含む）の各assert内容を監査sessionが直接読んだ。

## Findings

**総括: 1st auditのHigh 2件（F-1 production wiring・F-2 CI red）は解消。以下F-1〜F-2（解消済み）と残存nits（F-3〜F-7）を順に記録する。残存はすべてtest/文書級で、production挙動・privacy境界の障害は現時点で確認されていない。AC-9・AC-10（UI 3種区別・preview側）・AC-15・AC-16が未完了である点はPR/specの宣言どおり**。

1. **[Closed] F-1 (High, 1st round): production catalog wiring** — `ExchangeInputAdapter` がexport/import両方向でcomposition catalogをthreadするよう修正され（code: 2行+comment）、実adapter経由wiring testでpin済み。1st roundで指摘した「本番で`categories=[]`・全`categoryRef`がfail-closed」の欠陥は解消。wiring testが`ExchangeFlowControllerTest`のinject lambdaを回避して実`DefaultOrganizationInputComposer`を駆動していることも確認。
2. **[Closed] F-2 (High evidence, 1st round): CI merge gate** — 監査対象commit `917d093854` のpull_request run 35323615453は`final-status`含む全job success（前回の失敗laneはflakeと再確認）。高リスク証拠のCI run lineは当該green runへ更新済み。**運用上の注意: 本更新記録をPR branchへcommitするまで`high-risk-evidence` checkは赤のまま（run 35323615652のFAIL理由は旧pin）**。
3. **[Low] 残存test oracleギャップ（AC-8/AC-4/AC-7）** — (a) AC-8の2nd review要求corpusのうち「同一`desiredGroup` component内で異semantic宣言→formation key単位分岐（rejectされない）」と「同一labelだが別component+異semantic」の2ケースが依然未test（`proposalLabelsMergeItemsThatAreNotConnectedByDesiredGroup`は非connected同士の統合のみ）。(b) proposalがordering keyにならないこと（classification identity維持）の直接testと、`CATEGORY_CONTIGUOUS_V1`のordering入力negativeが未追加（formationのmatrix negativeはGLOBAL/page-localで充足）。(c) rename再構築viewが現名をcarryするassertは未追加（commit messageの"carries the current name"はtest化された主張としては過大）。いずれもcode実読では挙動が正しいことを確認済みの回帰防止ギャップ。
4. **[Low] AC-1残nits** — `advertisedRefsAreUniqueAcrossNamespacesAndInCanonicalOrder` の順序検証はkind単位（built-in先）のみで、`CategoryId` byte順・stable ID byte順のkind内一致は未assert。category refのcross-export unlinkability（同一状態2生成でref集合非共有）と、`categories`実載りでの256 KiB容量/Oversize経路はtestなし（前者は乱数seam共有、後者はbuilder既存size gateで構造的に担保される方向）。
5. **[Low] codec/identity oracleの弱さ（据え置き）** — (a) codec round-trip検証はdecodeが同一bytesを返すpassthroughで、`categories` non-empty文書のdecode対称性・malformed `categories`（未知kind・advertised集合外item ref・redacted tier name付き）decode→`SchemaMismatch`のfixtureは未追加（制約自体は`ExportCategory`/envelope initで全decode経路に適用されることを実読確認）。(b) `ExportCategoryName` carrierは導入されたが`ContextExportCodecTest`のround-trip fixtureはcatalogなし（`categories=[]`）のまま。(c) AC-6のplan/apply経路store不変は構造的論証（writer不在の実読）+session record bytes不変testまで。promotion実装時にAC-9側のcreate()経路testを含めて固定されるべき。
6. **[Low] 文書の微整合** — `ExchangeImportPipeline.kt` KDocは「20 kinds total」へ更新済みだが直前の"the thirteen #204 contract classes"がfourteen未更新で内部非整合（14+4+2=20）。spec 205はData and state本文（v4実名）を更新したが**Change history行の追記がない**（337所産の実装PR必須更新の扱い。先例: 7th/8thはいずれも本文改訂と同数のhistory行を持つ）。`ContextExportCodecTest`新commentの"re-encodes to the identical bytes"はpassthrough挙動の説明として不正確。footer self-check listへcategory専用bulletなし（You-mustで規定充足と読むことは可能、D-8文言は「You must / self-check」）。
7. **[Info] gate再検証結果** — 本記録更新後、`python3 tools/repo-contract/validate_high_risk_evidence.py --repo nunu1733/NunuLauncher --pr-number 355 --head-sha 917d093854...` を自環境で再実行する。期待結果: 旧記録の3失敗理由（非docs-only増分・赤CI run参照・未充足pin）は解消されるはずで、その実行結果と残った指摘（あれば）はこの節の末尾に追記する（実施結果: **PASS — 35323615453 green照合済み、audit fields/sections/commands全充足。本項目は更新commit後のremote再評価でも最終確認される**）。
8. **[Info] 環境** — working treeに1st roundと同一のuntracked file `tests/organizer-instrumentation/app/lawnchair/organizer/application/Issue265GateFailedRouteInstrumentationTest.kt`（#265局所作業物・本PR非対象）が存在。androidTest laneは当該file込みで成功。監査で変更・削除していない。

**本PRが充足していない受入条件（再監査後・明示）**: AC-9（promotion UX、宣言済み）、AC-10（summary modelは3-count充足だがUI表示行がbuilt-in+user-defined合算のため3種区別は部分充足。preview側と「保存済み」誤認防止のUI testは宣言済み/未追加）、AC-14の残nit=spec 205 change history行（F-6）、AC-15/AC-16（evidence pass、宣言済み）。F-3〜F-6は上記に紐づく受入条件部分充足・testギャップとして扱う（production挙動の欠陥は現headで未確認。ただしF-3a/F-4b/F-5aが埋まらない限り、対応ACの「testで検証される」要求文言は完全には満たされない）。
