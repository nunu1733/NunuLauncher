# Independent audit: PR #341 ユーザー定義カテゴリfirst-class taxonomy (spec 336)

> Status: accepted
> Audit date: 2026-09-17

- Auditor: 独立監査session（Zcode general-purpose subagent。PR #341の実装・test作成・reviewを担当したsessionとは別の独立sessionとして実施し、implementationを行っていない。実装側の主張には依拠せず、diff・test本文・CI実行記録を監査session自らが実読・実行して判定した）
- PR: https://github.com/nunu1733/NunuLauncher/pull/341
- Head SHA: 90560580ca9074b47ee1aada638d3efee55627e0
- CI run: https://github.com/nunu1733/NunuLauncher/actions/runs/35159376998 （`gh api` でhead_sha=`90560580ca…`、event=`pull_request`、`pull_requests` に #341、path=`ci.yml`、conclusion=`success` を監査session自らが照合済み）
- Criteria: `specs/336-user-defined-categories/spec.md` — AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9, AC-10, AC-11, AC-12, AC-13, AC-14

> 再pin (2026-09-17): main進行（PR #340 のspecs/329 implemented化、merge commit `08b0f0ebb3`）を取り込むため、PR #341のheadは `90560580ca` へ更新された。`2769663386..90560580ca` の差分は `specs/329-import-normalizer/{spec.md,plan.md}` のstatus表記のみ（監査sessionが `git diff --stat` で機械確認、production code・test変更ゼロ）であり、実質監査対象のproduction diff（base `3170c57e32`..`56dbc81452`）はbyte一致で不変である。監査内容のやり直しは不要のため、本記録はHead SHAとCI runを新headへ再pinした（本文の実行記録は監査実施時点のhead `56dbc81452` 上の事実としてそのまま保持）。

## Scope

対象diffはbase `3170c57e32fd6ec40cc592dd10e8535b2999d4b7`（main、PR #339 merge）.. 内容監査時点のhead `56dbc81452a79eadc726af4eabaa2491d9f6f48f`、111 files +5912/−336。当該head以降のPR履歴は `927ef95e07`（docs: 端末evidence PNG 8枚のみ）、`c1c9b98fb0` と `56dbc81452`（test: instrumentation期待値更新のみ）、本audit記録の追加（`docs/` 配下のみ）、そしてorigin/main取り込みmerge `90560580ca`（deltaは `specs/329-*` のstatus表記のみ）であり、本監査はテスト3commitを含めて実施し、`90560580ca` へ再pinした（再pin根拠は冒頭の再pin注記）。

監査sessionがdiffを実読して確認したproduction変更面と、そこから確認したruntime書き込み経路:

- **`organizer/planning/CategoryIdentity.kt` (新規)**: `UserCategoryId`（canonical小文字UUID v4のregexでinit `require`、UTF-8 byte order `compareTo`）、closed `CategoryIdentity`（`BuiltIn`/`UserDefined`、built-in先・user ID byte順のtotal順序。rename不変）、`UserDefinedCategory(id, displayName)`（名前はpresentationのみの純carrier）、`ActiveCategoryCatalog`（built-in `TaxonomyContract` 埋め込み + user-defined entries、ID一意性 `require`、`contains` membership、total lookup `displayNameOf`）。pure planning domainでplatform型は出ない。
- **`organizer/rules/UserDefinedCategoryStore.kt` (新規) — catalog storeの書き込み経路**: `noBackupFilesDir/organizer_user_categories/catalog-v1` へのAtomicFile complete-snapshot persistence。単一mutexを全read/writeで共用、recovery-aware `AtomicFile.openRead()`、`.new` を入力にしない、`startWrite`/fsync/`finishWrite` + `failWrite`（finally）、書き込み後同一boundaryでfull re-read検証、`(generation, digest)` をoptimistic tokenとしたtyped `Conflict`、物理不在=定義済みgeneration-0空catalog、capacity 64、名前はtrim+NFC正規化してから検証し正規形を永続化。codecは `schema=1`+generation+digest(=SHA-256 of canonical `id|displayName` 行、ID byte順)+entriesで、well-formed新schemaはtyped `UnsupportedSchema`、非canonical decimal header (`schema=01`)・構造破損・digest不一致・重複ID・非正規形の永続名・read時capacity超過はすべて `Unreadable`（zero-write、修復なし）。
- **`organizer/rules/CategoryOverrideStore.kt` / `CategoryOverrideSnapshot.kt` — migration対象**: snapshot値をidentity-typedに、物理snapshotをschema 2（`b|<CategoryId>` / `u|<UserCategoryId>` kind判別行）へ。migrationは読み取りでは動かず、最初のmutation内部でschema-1を読んで検証→同一assignments・1 generation bumpでschema-2をpublish→検証（失敗時はschema-1が権威のまま、mutation不受理）。optimistic tokenは「migration前読み取り値」も同一mutation内のmigrationとして受理（assignments一致・generation+1・schema 2の条件付き）。`mutateAll` 複数mutationを1 publicationに。書き込み時membership検証はbundle taxonomy + active catalog（catalog読めない場合は `TaxonomyUnavailable` でfail-closed、空catalogを暗黙に使わない）。legacy SharedPreferences barrier・`UnsupportedSchema`/`Unreadable` の既存decode意味論は不変（schema > 2のみ本binaryの新typed outcome）。
- **`organizer/integration/CompositionModels.kt` / `OrganizationInputComposer.kt`**: `PolicySourceKind.USER_DEFINED_CATEGORY_CATALOG` 追加、`InputProvenance.userDefinedCategoryCatalog` をmandatory row（空catalogは定義済みsentinel `user-defined-category-catalog:none`）として追加。catalogはplatform evidence前に1回読み、安定性比較で再読み（既存bounded 2-attempt内）、cut入力へ `(versionOrGeneration, sha256)` を追加。新typed codes `CATALOG_UNREADABLE` / `CATALOG_UNSUPPORTED_SCHEMA`（zero-write `NotReady`）、dangling user-defined override参照は `OVERRIDE_DANGLING_CATEGORY`（外部破損のみ到達、zero-write）、`OVERRIDE_CATEGORY_INVALID` をactive catalog membership検証へ拡張。S2/S5 evidenceは構造的に `CategoryIdentity.BuiltIn` のみにwrapし、non-S1のuser-defined candidateはcomposerでも拒否。composed inputはcutで読んだ同一catalog snapshotを `OrganizationInput.catalog` として運ぶ。
- **`organizer/application/protocol/LayoutApplicationModule.kt` / `PlanPreviewProtocol.kt` / `application/public/FolderTitleResolver.kt`**: `materialize` 呼び出しで注入resolverを `withCompositionCatalog(input.catalog)` でwrap（user-defined namingは同一compositionのsnapshotからtitle解決、未知IDは既存generic fallback、blank は既存blank checkへ伝播）。preview/applyが同一creation-time titleを使う。**この `organizer/application/**` 変更が高リスクpath保険を発動させる**が、layout writer・recovery store・Launcher DBへの書き込み自体は変更していない（materializerの `taxonomyVersion` 検証とresolver契約も不変）。
- **`organizer/personalization/*`**: export presentation（`exportPresentationValue` — user-definedはabsent category `null`、built-inはpre-336 raw値、redaction点は1箇所）とsession freshness identity（`CandidateScopeIdentity` / `SourceContextIdentity` がresolved `CategoryIdentity` のkind+stable IDをdigest入力に、built-inはbyte一致、raw IDは一方向digestにのみ流入）の2層分離。export document/session fieldにraw ID・表示名を書く経路は存在しない。
- **`organizer/ui/*`**: `UserDefinedCategoryAuthoring.kt`（新規coordinator。全mutationで `OrganizationOperationLease.tryAcquire(AUTHORING)` を獲得しfinallyで解放、typed store結果をそのまま投影、deleteはoverrides-first 2-store protocol — step 1は `mutateAll` によるexplicit per-key `Remove` の1 atomic publication、step 2失敗は `PartialDelete` としてtruthful表示+retry完了）。`CategoryOverrideAuthoring.kt`（selector値をidentity化、catalog読み失敗はeditor unavailableでfail-closed）。`CustomCategoryPreferences.kt`（新規管理UI。focus restoration、liveRegion、localized contentDescription、ja/en strings）。`GeneratedFolderTitles.kt`（user-title lookup combinator、契約不変）。
- **正本更新**: `docs/engineering/organizer-diagnostics.md`（新3 code、catalog identityのAllowed行とID・表示名・entry内容のNever行）、`docs/product/requirements.md`（FR-010拡張）、`CONTEXT.md` / `DESIGN.md`、`specs/336-…/{spec,plan}.md`。

変更なしを機械確認: `src/com/android/launcher3/**`（LauncherProvider / ModelWriter / DatabaseHelper等すべて）への変更は0件、`res/xml/backupscheme.xml` はinclude-allowlistのままでorganizer storeを含まない（catalog storeは `noBackupFilesDir` 配置でplatform排除と二重化）、権限・network・background work・Launcher DB schemaの追加はなし。

テスト3commitの内容: `927ef95e07` = `docs/assessment/assets-336-device-evidence/` へのPNG 8枚のみ。`c1c9b98fb0` = `ProductionOrganizationInputInstrumentationTest` のS5 signal期待値を `CategoryIdentity.BuiltIn` に一致（1行）+ `CategoryOverrideAtomicFileInstrumentationTest` のmigration後期待値をschema 2/generation+1へ一致。`56dbc81452` = restart writer/reader testの期待snapshotをmigration-as-mutation意味論（schema 2、generation+1、`canonicalEntries(…, schema)` digest）に整合。いずれもproductionコード変更を含まない。

## Criteria check

`specs/336-user-defined-categories/spec.md`（status: accepted）の受入条件ごとの確認結果。test名は監査sessionがtest本文を実読し、assert内容を確認したもの（名前のみの採用はしていない）。

- **AC-1 (stable opaque ID・rename不変なidentity/順序・名前規則)**: PASS (unit)。実装: `UserCategoryId` はUUID v4 regex強制、`CategoryIdentity` の順序はbuilt-in→user ID byte順で表示名非依存、`UserDefinedCategoryNameRules` がtrim+NFC・1–50 code points・`|`/改行禁止をstoreが検証前に適用し正規形を永続化、`DuplicateName`/`InvalidName` は書き込み前にtyped return。test: `CategoryIdentityCatalogTest.userIdRejectsNonCanonicalForms` / `identityOrderingIsBuiltInFirstThenUserIdByteOrderAndRenameInvariant` / `canonicalValueKeepsBuiltInRawValueAndDiscriminatesUserDefined`、`UserDefinedCategoryStoreTest.nameNormalizationIsTrimPlusNfcWithFiftyCodePointBound` / `decodeRejectsExactTrimEquivalentAndNfcEquivalentDuplicateNames` / `typedFailuresForInvalidDuplicateUnknownAndCapacity`。
- **AC-2 (atomic per-store lifecycle・generation+digest・検証後成功・2-store deleteは非transactionでtruthful)**: PASS (unit)。`UserDefinedCategoryStoreTest` が `createMintsUuidV4IncrementsGenerationAndVerifies` / `staleExpectedIdentityIsTypedConflictWithoutOverwrite` / `writeFailureKeepsThePriorSnapshotAuthoritative` / `postFinishVerificationFailureIsTypedAndNeverReady` / `noOpRenamePreservesFileGenerationAndIdentity` / `interruptedPendingWriteIsDiscardedBeforeTheFinalSnapshotIsRead`（failWrite復帰経路）でlifecycleを固定。2-store deleteは `UserDefinedCategoryAuthoringCoordinatorTest.deleteRemovesAssignmentsFirstThroughExactlyOneAtomicPublication` / `deleteStep2FailureRendersTheTruthfulPartialStateAndRetryCompletes` / `step2ConflictSurfacesAsThePartialStateWithRetryPath` が `PartialDelete` のtruthful性とretry完了をassertし、cross-store transactionでないことを1 publication構造で担保。
- **AC-3 (renameでassignment/grouping/canonical plan不変・titleのみ新名)**: PASS (unit)。`CategoryIdentityCatalogTest.renameDoesNotChangeCanonicalPlanBytes` がcanonical plan byteのrename不変をassert、`UserDefinedCategoryStoreTest.renameKeepsIdAndOrderAndOnlyChangesTheDisplayName` がID・順序不変をassert、canonical planが `FolderNaming.FromUserCategory(stable ID)`（名前非含有）で `Issue336ExchangeProjectionTest.digestTracksIdentityNotDisplayName` がfreshness identityの表示名非依存をassert。
- **AC-4 (combined catalog・namespace分離・total順序・fallback/bundle identity不変・divergence fail-closed)**: PASS (unit)。`ActiveCategoryCatalog` はID namespaceが構造的に離反（大文字word vs UUID v4）、fallbackはbuilt-in `OTHER` のまま、`PolicyModels.kt` は新kind追加のみでbundle identityに触れない。`CategoryIdentityCatalogTest.catalogMembershipCoversBothNamespacesAndKeepsBuiltInFallback` / `catalogBuiltInDivergenceFromTaxonomyIsInvalidRules`、plannerの `PlanningValidation` に `catalog.builtIn != taxonomy → INVALID_RULES` のfail-closed invariant実装を確認。
- **AC-5 (override editorのidentity割当・S1優先・invalid target typed)**: PASS (unit + CI instrumentation)。`CategoryOverrideAuthoring` はselectorをbuilt-in+user-defined canonical順にし、catalog読み失敗はfail-closed。store書き込み時membership検証が `InvalidCategory` no-write（`CategoryOverrideAtomicAccess` の `allowedIdentities` check）。S1→S6 precedenceとremoval意味論は `PlanningClassification` / composerで不変。test: `CategoryOverrideAuthoringCoordinatorTest`（identity化に伴う拡張）、`CategoryOverrideAtomicAccessTest` / `CategoryOverrideFullStoreCodecTest`（schema-2行のround trip、catalog-aware検証）、CI issue99 laneの `CategoryOverridePreferencesInstrumentationTest` が同一seamで実行（run 35156255307でPASS）。
- **AC-6 (planner first-class・title binding)**: PASS (unit)。`FolderFormation` のgrouping keyは `(profile, CategoryIdentity)`、`FullRunExecution.categoryContiguousOrder` はcanonical identity順+fallback最後、`folderNamingFor` が新variant。titleは両protocolが `input.catalog` のsnapshotにbindしfresh store read経路は実装に存在しない（両 `materialize` 呼び出し箇所の実読で確認）。test: `CategoryIdentityCatalogTest.userDefinedGroupsFormFoldersWithStableNamingAndCanonicalIdentityOrder` / `categoryContiguousOrderPlacesUserDefinedAfterBuiltInAndFallbackLast`、`UserCategoryFolderTitleBindingTest`（storeがcomposition後に変わったfixture含む）、`GeneratedFolderTitlesTest` 拡張。
- **AC-7 (create aloneは再分類しない・S2–S6はuser-definedに届かない)**: PASS (unit)。composerの `materializeSignals` がS2/S5を構造的に `BuiltIn` のみにwrap、planner `checkCategoryProvenance` がnon-S1 user-defined candidateを `INVALID_CATEGORY_PROVENANCE` でtyped拒否、#204 intent分岐はbuilt-in-onlyのまま。test: `CategoryIdentityCatalogTest.nonS1UserDefinedCandidateIsRejectedAsTypedProvenanceFailure`、composer catalog test群、`userDefinedAssignmentInsideTheCatalogIsConsumedAsS1OnAFreshRun`（S1消費は明示assignment経由のみ）。
- **AC-8 (deleteはexplicit removal・remapなし・partial state truthful・dangling fail-closed)**: PASS (unit)。delete protocolは `Remove` を明示しremap経路なし（coordinator実装と `mutateAll` 1 publication構造を実読で確認）。dangling参照はcomposer `OVERRIDE_DANGLING_CATEGORY` zero-write。test: `UserDefinedCategoryAuthoringCoordinatorTest` のpartial state群、`OrganizationInputComposerCatalogTest.danglingUserDefinedOverrideReferenceIsItsOwnZeroWriteFailure`。
- **AC-9 (provenance+dynamic cut・空catalog sentinel)**: PASS (unit)。`InputProvenance.userDefinedCategoryCatalog` がmandatory（sentinel default）、cutは `dynamicCutIdentity` に `(versionOrGeneration, sha256)` を追加。test: `OrganizationInputComposerCatalogTest.emptyCatalogCarriesTheDefinedSentinelProvenanceIdentity` / `nonEmptyCatalogCarriesSchemaGenerationDigestAndJoinsTheComposedCatalog` / `catalogChangeBetweenCutReadsIsTypedUnstableNotReady`。
- **AC-10 (corrupt/duplicate/malformed/digest/newer-schema fail-closed・Conflict・lease)**: PASS (unit)。codecのtyped routing（`UnsupportedSchema` はwell-formed新schemaのみ、`schema=01` 等の非canonical形式は `Unreadable` — review fix `e456d282f4`/`09eaa10884` の範囲を実読）とread時invariant（正規形・一意性・capacity・sort順・重複ID）を実装で確認。test: `UserDefinedCategoryStoreTest.decodeEnforcesTheBoundedCapacityOnRead` / `accessRoutesWellFormedNewerSchemaToTypedUnsupportedSchema` / `corruptFinalFileIsTypedUnreadableWithoutRepair`、`OrganizationInputComposerCatalogTest.codecBackedNewerCatalogSchemaReachesCATALOG_UNSUPPORTED_SCHEMA`、Conflictは `catalogConflictProjectsTypedWithoutAnyWrite`、leaseは `authoringIsRejectedWhileARunOrRecoveryHoldsTheLease` / `authoringHoldsTheLeaseForItsWholeMutation`。
- **AC-11 (built-in-only byte一致・mixed determinism・backup排除・downgrade fail-closed)**: PASS (unit)。`CategoryIdentityCatalogTest.builtInOnlyCatalogKeepsPre336CanonicalPlanValues` / `deterministicOverMixedCatalogs`、harness corpus（`GoldenOracleCorpus` 等）は意図的更新のみでgreen（監査実行のunit test suiteで確認）。backup排除は `noBackupFilesDir` 配置 + include-allowlist型 `backupscheme.xml` の実読で二重確認。downgradeはpre-336 binaryの既存decode（schema-2 header→ `Unreadable` / composer `OVERRIDE_UNREADABLE`）が本PRで不変であることを `CategoryOverrideFullStoreCodec` 実装で確認し、`codecBackedNewerOverrideSchemaReachesOVERRIDE_UNSUPPORTED_SCHEMA` が本binaryの `>2` 対象のみをassert。
- **AC-12 (unit/composer/UI/connected evidence)**: PASS (unit + CI instrumentation + 手動device evidence)。unit: `CategoryIdentityCatalogTest`(14件) / `UserDefinedCategoryStoreTest`(21件) / `OrganizationInputComposerCatalogTest`(10件) / `Issue336ExchangeProjectionTest`(9件) / `UserDefinedCategoryAuthoringCoordinatorTest`(12件) / `UserCategoryFolderTitleBindingTest` 等が監査実行でgreen。connected: CI（content監査head `56dbc81452` 上のrun 35156255307、および再pin head `90560580ca` 上のrun 35159376998 — 両headでproduction codeはbyte一致）のapi35 laneが `ProductionOrganizationInputInstrumentationTest` + `CategoryOverrideAtomicFileInstrumentationTest`（restart writer/reader含む、本PRでschema-2期待値に更新）を、issue99 laneがoverride editor flowsを実行し両runとも全PASS。management UI flowsはemulator (API 36) の手動evidence 8枚（`927ef95e07`）+ coordinator unit testsでcover。残置はFindings 1のとおり（新規UI instrumentation test自体はCI未接続）。
- **AC-13 (a11y・no raw IDs)**: PARTIAL（実装+手動evidence、自動検証は残置 — Findings 1参照）。UI実装は `contentDescription` / `liveRegion` / `FocusRequester` によるfocus復帰・非色依存の「(Custom)」text markerを持ち、ja/en stringsを実読確認。raw ID非表示は `CustomCategoryPreferencesInstrumentationTest` が `onAllNodesWithText(userId.value).isEmpty()` でassert（test本文確認）。ただしTalkBack読み上げ・200% font scaleの明示assertは無く、同testはCIで実行されない。手動emulator evidenceがflow方向のtruthful renderingを裏付ける。完全な自動a11y検証は後続作業。
- **AC-14 (privacy: diagnostics/export non流出・freshness identity維持・backup排除)**: PASS (unit + 文書)。`Issue336ExchangeProjectionTest.userDefinedClassificationsExportAsAbsentCategoryWithoutRawIdOrName` / `builtInOnlyExportsKeepThePre336BytesExactly` / `digestTracksIdentityNotDisplayName` / `scopeBindingGateConsumesTheIdentityPreservingDigest` / `reconstructionViewConsumesIdentitiesAndNeverShowsUserDefinedCategories` が2層分離を両面でassert。diagnostics契約は `organizer-diagnostics.md` にcatalog identityのみAllowed・ID/表示名/entry内容はNeverとして同一PR追記を実読確認。backup排除はAC-11と同一根拠。

## Executed test surface

監査session自らの実行（2026-09-17、local checkout head `56dbc81452`、clean worktree）:

- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'` → **BUILD SUCCESSFUL**（exit 0。`CategoryIdentityCatalogTest` / `UserDefinedCategoryStoreTest` / `OrganizationInputComposerCatalogTest` / `Issue336ExchangeProjectionTest` / `UserDefinedCategoryAuthoringCoordinatorTest` / `CategoryOverrideAtomicAccessTest` / `CategoryOverrideFullStoreCodecTest` / `UserCategoryFolderTitleBindingTest` 等のorganizer unit test全体を含む）
- `python3 tools/repo-contract/validate_repo_contract.py` → **`repository contract OK`**
- `git diff 3170c57e32..56dbc81452 --stat`（111 files +5912/−336の範囲確認）、`git show 927ef95e07|c1c9b98fb0|56dbc81452`（テスト3commitの本文確認）、`git diff 3170c57e32..56dbc81452 --name-only` と高リスクpath一覧の突合（`organizer/application/**` 3 filesを確認 → 高リスク適用と判定）
- CI証拠のGitHub API照合（gate evidence）: `gh api repos/nunu1733/NunuLauncher/actions/runs/35159376998` → head_sha=`90560580ca9074b47ee1aada638d3efee55627e0`（再pin後のPR head）、head_branch=`issue-336-spec-plan`、event=`pull_request`、`pull_requests`=[341]、path=`.github/workflows/ci.yml`、status=completed、conclusion=success。job結果（`gh run view 35159376998 -R nunu1733/NunuLauncher`）: **`final-status` PASS**、source jobs `organizer-unit-tests` / `check-style` / `build-debug-apk` すべてsuccess（skipなし）、`validate-repo-contract` success、instrumentation `organizer-instrumentation-{shared-writer,db-migration,api35,issue99,issue52,issue53,issue155,issue299}-tests` すべてsuccess
- 参考証拠（content監査head `56dbc81452` 上の元merge gate、同一production code）: run 35156255307（pull_request event、PR #341紐付け、`final-status` PASS、全job success）を同様に `gh api` / `gh run view` で照合済み。再pin head上のrun 35159376998が同一suite群を同一結果で再実行したため、本監査のgate evidenceは35159376998に統一する

## Findings

1. **残課題（non-blocking、後続Issueへの分離を推奨）**: 本PRが追加した `tests/organizer-instrumentation/.../ui/CustomCategoryPreferencesInstrumentationTest.kt`（298行。create/rename/delete/partial-deleteのUI flowとraw ID非表示を検証）は、`.github/workflows/ci.yml` のいずのinstrumentation laneのclass listにも含まれず（監査sessionがci.ymlの全 `class=` 指定をgrepして確認）、CI上では実行されない。AC-12のmanagement UI surfaceとAC-13の自動a11y検証は、現状「coordinator単位のunit test（CI実行済み）+ 手動emulator evidence（`927ef95e07`、head `09eaa10884`、API 36）」で裏付けられているが、新規UI instrumentation testのCI接続とTalkBack/200% font scaleの明示assertは未達である。merge gate（高リスクエビデンス契約: CI final-status + 本audit）の要件自体は満たすためmerge blockとはしないが、CI lane接続を後続Issueとして追跡すること。
2. **blocker / required findings: なし。** 上記1を除き、spec 336の受入条件AC-1〜AC-14は実装diffとtest表面で担保されていることを確認した。production差分はspec/planのdesign decisions（identity型、catalog input、schema-2 migration-as-mutation、cut/provenance参加、overrides-first delete、2層exchange投影、title snapshot binding）と一致し、scope外（Launcher DB、recovery writer、権限・network・backup經由の流出）への変更は検出されなかった。
3. **再pin (2026-09-17)**: main取り込みmerge `90560580ca` のdeltaは `specs/329-import-normalizer/{spec.md,plan.md}` のstatus表記のみ（docs-only、production code・test変更ゼロを `git diff 2769663386..90560580ca --stat` で機械確認）のため、監査内容は不変のままHead SHA・CI run参照を新headへ更新した。gate evidenceは再pin head上のCI run 35159376998（全job成功・`final-status` PASS）に更新済み。
