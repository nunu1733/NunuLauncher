# Independent audit: PR #384 整理方針pickerを材料面T-05へ移設しrun中変更特例（確認なし破棄+再start）を廃止する (spec 368)

> Status: accepted
> Audit date: 2026-09-20

- Auditor: Independent audit session (ZCode subagent、GLM。implementation/spec作業を担当したsessionとは別の作業として実施。実装側の主張 (PR本文・Issue packet・evidence README) は参照したが依拠していない — 記載内容は監査sessionが自ら読んだdiff/test本文/実行結果のみによる)
- PR: https://github.com/nunu1733/NunuLauncher/pull/384
- Head SHA: `da665de7e02e0c15dc1dbc2e7dfc9e19def6cabf` (base `main` = `ca9c171e91c222d11b2c32d3201b16571b98bf2d`)。`gh pr view 384 --json headRefOid,baseRefOid` で取得したheadと監査用checkoutのHEADが一致すること、worktreeがcleanであることを確認
- CI run (audit時点): https://github.com/nunu1733/NunuLauncher/actions/runs/35477032170 (head `da665de7e0`)。**audit実行時点で `build-debug-apk` / `check-style` / `organizer-unit-tests` / `changes` / `high-risk-evidence` / `validate-repo-contract` の6 checkがpass、organizer instrumentation 9 laneはpending** (下記Findings 1)。`final-status` は未確定
- Criteria: `specs/368-strategy-picker-material-relocation/spec.md` AC-1〜AC-9 (+ `plan.md` の検証計画・Risk・Explicitly unverified areas)、Issue #368受入条件1〜5。spec/planはaccepted (2026-09-19、re-review [Approve相当](https://github.com/nunu1733/NunuLauncher/issues/368#issuecomment-5742960028) @ head `5d5b61ee4e`、acceptance commit `734f37a86f`。Contract notes 1〜3はowner指示により受入 — 本監査はIssue comments APIで受入経緯を確認)
- Owner決定 (2026-09-20): two-pane full-shell runtime evidenceは環境制約3点 (実測、evidence README記録) により未確認範囲として残し、**現evidence package (実`PreferenceNavigation` level runtime oracle＋synthetic defensive oracle＋code-level citation) を受入** して独立監査へ進む旨が[Issue コメント](https://github.com/nunu1733/NunuLauncher/issues/368#issuecomment-5746197068)で記録されている。本監査はこれを前提に、その文書化の正確性を検証した

## Scope

本監査は、accepted spec 368とplanを正本として、PR #384のhead `da665de7e02e0c15dc1dbc2e7dfc9e19def6cabf` の全diff (30 files, +2594/−710、base `ca9c171e91c2`...head) を自ら読んで確認したものである。監査用checkout (`/Users/nunu/Documents/work/NunuLauncher`、branch `issue-368-strategy-picker-material-relocation`、HEAD `da665de7e0`、clean) でspec/plan/実装/test本文を実読し、automated evidenceは監査session自らの実行結果のみを用いた (下記Executed test surface)。emulator instrumentationは監査sessionでは再実行していない (監査指示どおり。実装sessionの実行結果は[Issue packet コメント](https://github.com/nunu1733/NunuLauncher/issues/368#issuecomment-5743576316)とその後の対応コメント群として確認した)。

確認した変更面:

- **StrategyWriteArbiter.kt**: restart path (`RESTART_RESERVED`/`RESTARTING`状態、`restartRun`/`restartSuppressed`/`restartNeeded` seam、`onStateObserved` test seam) を削除し状態機械を`IDLE → WRITING → IDLE`へ縮小。`OrganizationOperationGate`注入と`tryAcquire(AUTHORING)`を`Idle → Writing`と同一直列化点で原子に実行。`onStrategySelected`をtyped開始outcome (`Started`/`RefusedRunOrRecoveryActive`/`RefusedAuthoringBusy`/`RefusedWriteBusy`) 返却へ変更し、全拒否がstore呼出なし・`Writing`不遷移であることを本文で確認。書込coroutineは`CoroutineStart.UNDISPATCHED`開始 (pre-start cancellationでもbodyがtry blockへ入る) で、releaseは`NonCancellable + mainDispatcher`の`finally`相当内で`token.close()` + `IDLE`復帰 — 実装review「高」指摘 (pre-start cancelでtoken漏れ) の修正を実読確認
- **ManualOrganizationRun.kt**: `operationActive: StateFlow<Boolean>`新設。`activeOperation`/`recoveryLease`の遷移site 12箇所 (beginOperation/finish/stale/cancel/dismiss/recovery開始・終了・放棄、confirm終端を含む) に同一lock内の`updateOperationActiveLocked()`が対応していることを、assignment site列挙 (grep 12箇所) とupdate呼出 (12箇所) の突き合わせで機械確認
- **OrganizerStrategyPreferences.kt (新規, 263行)**: T-05 destination。picker本体 (`strategyPickerItems`)、`readSelectedStrategy`、catalog読取 (fail-closed/default-as-effective不変)、arbiter構築 (`runOrRecoveryActive = { coordinator.operationActive.value }`)、typed outcome handling (`RefusedAuthoringBusy`/`RefusedWriteBusy`時のretry文言live region通知、frozen理由とは別testTag `strategy-picker-retry-notice`)。frozen表示はoperation lifetime projectionから導出 (表示State列挙を読まない)
- **ManualOrganizationPreferences.kt**: run面からpicker呼出・arbiter構築・catalog読取・freeze計算・`strategyPickerItems`定義・`readSelectedStrategy`を削除 (diff実読)。`strategyDisplayName`/`strategyDescription`はT-05側再利用のためinternal化。run面にはstrategy UI (行・section表題・読み取り専用表示) が一切残らない
- **OrganizerHubPreferences.kt / PreferenceRoutes.kt / PreferenceNavigation.kt**: 材料`PreferenceGroup`へ`organizer_strategy_title/summary`の`NavigationActionPreference`追加、引数なしroute `HomeScreenOrganizerStrategy`追加、destination登録。`runOverride` test seamはdestinations既存の`run` paramと同型のmirrorで、production呼出は省略 (挙動変更なし。diff実読)
- **ExchangeFlowUi.kt**: `strategyWriteStartBlockedFor`/`strategyRestartSuppressedFor` truth table 2関数、`strategyArbiterBusy` callback、import/CTA開始時のstrategy busy拒否、`ExchangeStatus.Kind.IMPORT_STRATEGY_BUSY`/`CTA_STRATEGY_BUSY`とstatus→string対応を削除 (diff実読)。idle start row freeze等の非strategy契約は不変
- **strings (EN/ja)**: 新規4種ずつ同一key set (`organizer_strategy_title`/`_summary`/`organizer_strategy_frozen_operation_active`/`organizer_strategy_retry_when_busy`)、削除4種ずつ (`exchange_import_strategy_busy`/`exchange_import_cta_strategy_busy`/`exchange_strategy_frozen_import`/`exchange_strategy_frozen_continuing`)。ja正本・en対訳とも実読。ja側に削除箇所由来の字下げ乱れ2行あり (下記Findings 4)
- **正本改訂**: spec 182 (§Preview integration第1bullet、write-authority step 4、2 scenario、Change history)、spec 283 (Non-goals凍結解除2箇所、Change history)、spec 328 (status行、Outcome、競合affordance scenario、AC-3/AC-5 oracle行、Change history)、`docs/product/organizer-disposition-migration.md` §3.9/§3.14 (spec 328実装済みの事実修正＋#368境界追記)。改訂境界の一致は下記Criteria check AC-7
- **tests**: `StrategyWriteArbiterTest` (restart系oracle 7種削除→typed outcome/lease/single-flight/全終端解放/pre-start cancel oracleへ全面改訂)、`ManualOrganizationRunTest` (AC-9(a)(b)(d) oracle新設)、`ExchangeFlowStateHolderTest` (strategy gate 4 test削除)、`ExchangeImportSuccessInstrumentationTest` (`receiptRefusedByTheStrategyArbiterStaysRetryableFromTheHeldText`削除)、`StrategyPickerInstrumentationTest` (T-05 rehost＋run面否定的観測追加)、`StrategyPickerFreezeInstrumentationTest` (frozen理由をoperation active用へ更新、retry文言区別testへ置換)、`OrganizerHubPreferencesInstrumentationTest` (T-05 entry navigation追加)、`StrategyT05ProductionNavigationTest`/`StrategyT05VisualEvidenceTest` (新規evidence tooling)、`docs/assessment/assets-368-strategy-picker-t05/` (PNG 4枚＋README)

`organizer/application/**`、`organizer/planning/**`、`organizer/rules/**` (`LayoutStrategySelectionModule`含む)、`OrganizationOperationLease.kt`自体、DB、Launcher3 bridge、selection store formatへの変更はない (diff --stat 30 fileが上記範囲に収まることをname-onlyで機械確認)。依存・権限・通信・DB migrationの追加はなし (manifest/gradleはdiff 0件)。`risk: layout-data`/`risk: migration`非該当 (spec共通gateの自己分類と一致)。

## Criteria check

spec 368の受入条件ごとの確認結果。unit testの実行結果は監査session自らの実行 (下記Executed test surface) による。instrumentation oracleはtest本文の実読 (assert内容の確認) と実装sessionの実行記録 (Issue packet) に基づく。

- **AC-1 (pickerはT-05のみ、run面に存在しない)**: PASS (構造 + unit/instrumentation oracle実読)。構造: `strategyPickerItems`の呼出と定義がrun面から削除され、T-05 destinationが唯一のhostであること (両file実読)。oracle: `StrategyPickerInstrumentationTest.theRunSurfaceShowsNoStrategyPicker` (testTag・section表題の`assertDoesNotExist`。Idle代表状態。Selecting/Previewはrun面suite自体がpicker-freeで行使する旨をtestコメントに記録 — plan Open question 3が実装に委ねた集合の確定) + 同suite全testのT-05 rehost + `OrganizerHubPreferencesInstrumentationTest.hubStrategyEntryOpensTheMaterialsStrategySurface` (材料group entry→T-05 navigation)。実装sessionのemulator実行 (picker 13/13、hub 16/16 PASS) はIssue packetで確認 (再実行はしていない)
- **AC-2 (run中にcommitする経路が存在しない)**: PASS (構造 + unit)。UI affordance: `OrganizerStrategyPreferences`のenabled/frozen理由が`operationActive` (operation lifetime projection) のみから導出されることを実読。構造gate: `ManualOrganizationRunTest.anActiveRunRefusesTheStrategyWriteWithoutAStoreCall` — 実run開始後 (`operationActive == true` asserted) の選択が`RefusedRunOrRecoveryActive`を返し、`writes.get() == 0` (store非呼出) かつarbiterが`IDLE`のまま (Writing不遷移) であることを監査実行でgreen確認。run面上の選択UI経路はsource上に存在しない (AC-1と同一の構造確認)
- **AC-3 (operation不在時の検証付き書込・次回run反映)**: PASS (構造 + unit + instrumentation oracle実読)。書込経路は既存の検証付きcommand (`writeStrategy = LayoutStrategySelectionModule.store(context).select(id) is Committed`) のまま (実読)。選択は次回runのcomposer readを通じて初めてplanningへ到達する (spec 182改訂step 4と一致)。既存`LayoutStrategySelectionStoreTest`・composer/provenance系unit testは無編集で監査実行green (1530 tests / 0 failures中に含まれる)。instrumentation書込公開assert (`StrategyT05ProductionNavigationTest`終端のstore公開assert含む) も実読確認
- **AC-4 (旧oracle削除＋obsolete理由のPR記録＋restart経路不存在)**: PASS (grep + diff + PR本文)。機械確認: `RESTART_RESERVED`/`RESTARTING`/`restartRun`/`restartSuppressed`/`restartNeeded`/`strategyArbiterBusy`/`IMPORT_STRATEGY_BUSY`/`CTA_STRATEGY_BUSY`/`exchange_strategy_frozen`/`exchange_import_strategy_busy`/`exchange_import_cta_strategy_busy`/`onStateObserved`の12 symbolについて`lawnchair/src` + `tests/`のgrep残存 **0件** (監査session実施)。strategy commitから`dismiss()`+`start(trigger)`への呼出経路は`StrategyWriteArbiter`/T-05 destinationに存在しない (grep 0件)。PR本文のobsolete理由記録と削除diffの一致を各4系統で突合: (1) `StrategyWriteArbiterTest` restart系oracle 7種 (commit+restart/restartNeeded=false/restartSuppressed/restartThrows/cancel-no-restart/RESERVED-RESTARTING window/non-idle busy — 削除diff実読) → E-7解消によるrestart特例廃止、(2) `ExchangeFlowStateHolderTest` strategy gate 4 test (`strategyGatePredicatesFollowTheAcceptedEntryPolicy`/`arbiterBusyRefusesImportStartsAndCtaStarts`/`ctaIsRefusedWhileTheStrategyArbiterIsWriting`/`idleStrategyCommitKeepsTheRunIdleAndTheCtaThenStartsTheRun` + status string一覧strategy 2種) → gate自体の削除、single-flightは新oracleが引継ぎ、(3) `ExchangeImportSuccessInstrumentationTest.receiptRefused...` → `IMPORT_STRATEGY_BUSY` status削除、(4) `StrategyPickerFreezeInstrumentationTest.continuingFreezeUsesItsOwnReasonCopy` → exchange系frozen理由削除、retry文言区別testへの置換。すべて一致
- **AC-5 (spec 283 accessibility契約のT-05適用)**: PASS (oracle実読 + evidence存在確認)。`StrategyPickerInstrumentationTest`のselectableGroup/parent row単一truth/200% font/fail-closed/default-as-effective oracle群はassert不変のままhostのみT-05へ付け替え (diff実読)。visual evidence: `t05-light-selected-unselected.png`/`t05-dark-selected-unselected.png`/`t05-200percent-font.png` (bounds assert付き。`StrategyT05VisualEvidenceTest.captureTwoHundredPercentFontReflow`本文で「行がwindow幅を超えない」assertを確認) + light/dark README記載。PNG本文のピクセル再検証は監査scope外 (未確認範囲)。two-pane operation-active frozenはsynthetic defensive oracle (`captureTwoPaneOperationActiveFrozen`) として位置づけられ、READMEが「production two-pane evidenceの代替ではない」と明記 — owner決定 (A) の前提どおり
- **AC-6 (single-flight・検証付き書込失敗時挙動・全終端Idle復帰)**: PASS (unit)。`aSecondSelectionWhileWritingIsRefusedAsWriteBusyWithoutAStoreCall` (in-flight中の2本目が`RefusedWriteBusy`・store非呼出)、`everyTerminalPathReleasesTheArbiterAndTheAdmissionDomain` (commit/non-commit/storage failure/throw failureのtable-driven終端別に`IDLE`復帰・domain解放・再書込可をassert)、`cancellingTheScopeDuringAWriteStillReleasesTheArbiterAndTheDomain`、`aScopeCancelledBeforeTheBodyRunsStillReleasesTheArbiterAndTheDomain` (manual queue dispatcherでbody起動前cancelを決定的に再現し、`IDLE`復帰・domain解放・再admission成功まで固定 — 実装review「高」のreproduction test)。すべて監査実行でgreen
- **AC-7 (exchange非strategy契約の回帰なし＋正本改訂が境界内)**: PASS (diff review + unit)。strategy非依存の既存exchange test (`ExchangeFlowStateHolderTest`残test・`ExchangeImportSuccessInstrumentationTest`残test) は無編集で監査実行green。正本改訂: spec 182は§Preview integration・write-authority step 4・2 scenarioのみ (diff実読)、spec 283はNon-goals凍結解除2箇所＋Change historyのみ、spec 328はstrategy固有条項のみ (run内entry picker freeze・idle picker continuation中無効化・commit時gate・exchange側書込開始gate・restart状態機械・`IMPORT_STRATEGY_BUSY`/`CTA_STRATEGY_BUSY`、AC-3/AC-5のstrategy oracle項目) をlease基準へ置換し、idle start row freeze・CTA single-flight・attempt anchor・owning runId再照合・Back interception等の非strategy条項は不変 — Contract notes 2 (option A) の境界と一致することをdiff全文で確認。処分文書§3.9/§3.14は「spec 328未実装」の事実修正＋#368境界追記で、#374のrev.2所有を明記 (判断本文の変更なし)
- **AC-8 (frozen理由・retry文言の新規EN/ja資源・live region・削除string排除)**: PASS (strings diff + instrumentation oracle実読 + grep)。新規4種が`values/`と`values-ja/`の双方に同一key setで存在 (diff実読)。frozen理由とretry文言は別stringで、UIも別testTagの別行 (`strategy-picker-frozen-reason`/`strategy-picker-retry-notice`) とし、双方`liveRegion = LiveRegionMode.Polite` (実装実読)。`StrategyPickerFreezeInstrumentationTest.aTypedRefusalAnnouncesItsOwnRetryCopyDistinctFromTheFrozenReason`がretry文言のlive region属性とfrozen文言非表示・行有効をassert (実読)。削除4種は`values/`/`values-ja/`双方から削除済み、reference grep 0件 (監査session実施)
- **AC-9 (lease基準の相互排他oracle)**: PASS (unit、すべて監査実行でgreen)。(a) `aPausedStrategyWriteBlocksRunAdmissionUntilItsTerminal` — 実`OrganizationOperationLease`＋実`ManualOrganizationRun`で、書込をpublication前で停止中に`start()`が`Busy`・run不開始 (`State.Idle`) をassert、書込終端後にrun開始成功。(b) AC-2のoracleと同一。(b2) `anOccupiedDomainWithoutRunOrRecoveryIsRefusedAsAuthoringBusy` — gate占有＋projection falseで`RefusedAuthoringBusy`・store非呼出。(c) `everyTerminalPath...` (上記AC-6) + cancel系2 test。(d) `operationActiveResetsAfterEveryTerminalState` (5終端のprojection=false) と`aStrategyWriteIsPossibleAfterEveryTerminalState` (5終端 = InputUnavailable/NoChanges/Applied/Stale/Cancelledのtable-drivenで、各終端後にprojection=false **と** 実leaseを使ったstrategy書込の`Started`→`IDLE`完了を別々にassert) — 実装review「中」指摘 (Appliedのみ実write) の修正を実読・実行確認

### Issue受入条件1〜5との対応

1→AC-1、2→AC-2、3→AC-3、4→AC-4、5→AC-5。すべて上記のとおり確認済み。受入4の「旧oracleがなぜobsoleteかのPR記録」はPR本文に系統別に記載されており、削除diffとの一致を監査sessionが突合した。

## Executed test surface

監査session自らが実行 (head `da665de7e02e0c15dc1dbc2e7dfc9e19def6cabf`、worktree clean、source未変更):

- `git rev-parse HEAD` → `da665de7e02e0c15dc1dbc2e7dfc9e19def6cabf`、`git status` → clean。`gh pr view 384 -R nunu1733/NunuLauncher --json headRefOid,baseRefOid` → 監査対象head/baseと一致
- `gh repo view nunu1733/NunuLauncher --json nameWithOwner,defaultBranchRef` → `nunu1733/NunuLauncher` / `main` を確認
- `./gradlew spotlessCheck` → **BUILD SUCCESSFUL (exit 0)**。初回はup-to-date判定だったため、`--rerun-tasks`で強制再実行 (**BUILD SUCCESSFUL in 13s、5 tasks executed**) — 実際の実行による検証
- `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'` → **BUILD SUCCESSFUL (exit 0)**。test taskは実際に実行され、結果XML集計 (監査session自ら): **139 class / 1530 tests / 0 failures / 0 errors / 0 skipped**。実装packet記載の「1529 tests」は中間head `68d1c38eea`時点の数であり、最終headではpre-start cancel test等の追加分を含み1530 — 整合
- `./gradlew assembleLawnWithQuickstepGithubDebug` → **BUILD SUCCESSFUL (exit 0)**
- instrumentation (emulator): 監査sessionでは再実行していない (監査指示)。実装sessionの実行記録: `StrategyPickerInstrumentationTest` 13/13、`StrategyPickerFreezeInstrumentationTest` 3/3、`OrganizerHubPreferencesInstrumentationTest` 16/16、`ExchangeImportSuccessInstrumentationTest`/`ManualOrganizationPreferencesInstrumentationTest` PASS、`StrategyT05VisualEvidenceTest` 4/4、`StrategyT05ProductionNavigationTest` 3 AVD PASS ([Issue packet](https://github.com/nunu1733/NunuLauncher/issues/368#issuecomment-5743576316)以降の対応コメント群)。本監査はこれを実装session evidenceとして記録するだけであり、独立再実行済みとは扱わない

## Findings

1. **CI状態 (正直記録)**: audit実行時点でrun 35477032170 (head `da665de7e0`) は `build-debug-apk` / `check-style` / `organizer-unit-tests` / `changes` / `high-risk-evidence` / `validate-repo-contract` がpass、organizer instrumentation 9 lane (api35 / db-migration / issue155 / issue299 / issue332 / issue52 / issue53 / issue99 / shared-writer) が **pending**。`final-status` 未確定。本PRは`risk: layout-data`/`risk: migration`を付けないspec分類でありhigh-risk evidence gateの対象外だが、merge者は最終head上で`final-status`が緑であることを確認すること (下記6)。
2. **two-pane full-shell runtime evidenceは未確認範囲として残置 (owner決定済み、non-blocking)**: instrumentation環境の実測制約3点 — (1) production `ManualOrganizationModule` singleton経由の実runがinstrumentation process内で`InputUnavailable(ReconciliationPending)`になりactive operationに到達不可、(2) 実`Preferences` two-pane shellがcompose-test activity上で`No compose hierarchies`でcomposition開始不可 (3 AVDで再現)、(3) 実Back発火経路のoracleは本体assert全通過後のactivity teardownでnavigation-compose entry-lifecycle artifactによりcrash (Back注入3方式×3 AVDで再現) — が `docs/assessment/assets-368-strategy-picker-t05/README.md` に記録されていることを確認した。READMEの記載 (`PreferenceNavigation` level runtime oracleがsupported path (run面→hub→T-05) の順序を実測: hop 1遷移中はrun面とhubが共存しoperation alive、hop 1完了でdismiss→operation終了、hop 2でT-05はunfrozen compose・書込可) は`StrategyT05ProductionNavigationTest`本文のassert内容と一致することを実読で確認した。`Preferences.kt`のtwo-pane構造 (first=`PreferencesDashboard`／second=単一movable NavHost) のcode-level citationも実装実読で一致。本監査はこのowner決定を前提に文書化の正確性を検証したものであり、これを欠陥とは扱わない
3. **D-1 (低): `StrategyT05ProductionNavigationTest`のKDocとtest名が最終runtime truthに未同期**。class KDoc (56〜62行目) は「outgoing run surfaceとincoming **T-05** がtransiently coexistし、operation active中はT-05がfrozen affordanceをrenderする」と記載し、test名 `productionTransitionShowsFrozenT05OnlyWhileTheOperationOutlivesIt` もfrozen T-05を示唆するが、test本体はsupported path (run面→hub→T-05) を駆動し、共存するのはrun面+hub、T-05はoperation終了後のunfrozen composeしかassertしていない。spec Change history・plan Risk 4・READMEは最終truthへ同期済みである一方、このtest KDoc/名称のみ旧説明が残っており、Re-review 5が要求した「文書単一truth化」がこの1 fileで未完了。evidence toolingのKDocであり契約文書ではないため非ブロッキングだが、#369等の後続作業時に修正を推奨する
4. **D-2 (低・外観): `values-ja/strings.xml`に削除箇所由来の字下げ乱れ**。`exchange_import_cta_failed`と`exchange_import_summary_recognized`の2行に余分な字下げ (`            <string ...>`) が残る。機能影響なし (`spotlessCheck` PASS)。次回当該file編集時に整容する程度の指摘
5. **D-3 (低・観察): `specs/328-exchange-import-success-state/plan.md` (歴史的実装plan) は旧arbiter状態機械の記述のまま**。本PRはspec.mdのみを改訂し、同plan.md (Status: implementation-ready、2026-09-18付の#328実装plan) は未更新のため、`RestartReserved`/`Restarting`/`strategyArbiterBusy`の記述が残る。spec.mdのChange historyが改訂事実と移管先 (#368) を明記しており、plan.mdを日付付き実行記録と捉えれば矛盾ではないが、spec 328 rev.2 (#374) の際にplan.mdも含めて整材する境界として認識することを推奨する
6. **本記録pushによるhead移動**: 本audit記録はPR branch上のdocs-only commitとして追加されるため、push後のPR headは `da665de7e0` から移動する。deltaは本file追加のみを想定し、merge者は最終head上でCI (`final-status` 含む) が緑であることを確認すること。本監査のverdictは `da665de7e0` に対するものであり、docs-only deltaを許容する。code変更時は本SHAに対する再監査を要する
7. **独立verdict: 合格 (Approve)** (head `da665de7e02e0c15dc1dbc2e7dfc9e19def6cabf` に対する)。AC-1〜AC-9とIssue受入1〜5はすべて、構造確認 (diff実読・grep機械確認) と監査session自らのunit test実行 (1530 tests / 0 failures) / spotlessCheck (強制再実行) / assemble (exit 0) で確認した。spec 182/283/328改訂と処分文書§3.9/§3.14更新はContract notes 2境界内。旧oracle廃止理由はPR本文に記録され削除diffと一致。two-pane full-shell runtime evidenceの残置はowner決定 (2026-09-20) どおりのdocumented limitationであり、evidence READMEの記録は実測内容と正確に一致している。上記D-1〜D-3は非ブロッキング。merge条件は最終head上でのCI `final-status` green (Findings 1/6)

## 未確認範囲 (明示)

- organizer instrumentation testの監査sessionによる再実行 (emulator起動なし。実装sessionの実行記録をIssue packetとして記録したのみ)
- two-pane full-shell (実`Preferences` shell) のruntime実画面evidence — 環境制約3点により未取得 (owner決定 (A) で受入済み。README記録の正確性は検証済み)
- evidence PNG 4枚のピクセル内容の再検証 (file存在・取得test本文のassert・README記載の一致確認まで。screencapの目視再評価はしていない)
- 実装sessionの Gradle / emulator 実行log自体の取得 (Issue packet記載を申告として確認。独立再実行可能なunit/spotless/assemble面は監査sessionが自ら実行して covering)
- 監査時点でpendingのCI instrumentation 9 laneの結果 (Findings 1。merge前に最終head上で確認されるべき)
