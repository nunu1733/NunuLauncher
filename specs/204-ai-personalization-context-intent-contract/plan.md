# Implementation Plan: AI personalization用 Context / PersonalizedIntent exchange contract

> Issue: #204
> Spec: [spec.md](./spec.md)
> Status: accepted (spec受入済み — 2026-09-15。実装は下記Execution checklistに従う)
> Baseline: `origin/main` = `9ea2ba0eb4d9ef61bd96ef2b480bbd20ed055edd` (2026-09-15時点、#203実装を含む)。5th review (ChatGPT review "Request changes", 2026-09-15, snapshot `2501a1fb` 基準) 対応を含むrevision。過去の再入場検証履歴は「Current evidence」節を参照。

## Current evidence

確認済みの現行状態 (2026-09-15, `origin/main` @ `9ea2ba0eb4d9ef61bd96ef2b480bbd20ed055edd`)。実装開始時に再検証する。

- `lawnchair/src/app/lawnchair/organizer/planning/OrganizationInput.kt` — `OrganizationInput(snapshot, rules, taxonomy, signals, targets, runMode)`。`RunMode` は `FullOrganization` / `IncrementalPlacement` / `ScopeComposedOrganization` (#228、`TargetSet.additions` が非空になり得る唯一のmode)。`CapturedItem` は `ItemKind` として `APPLICATION`/`DEEP_SHORTCUT`/`SHORTCUT_LEGACY`/`FOLDER`/`APPWIDGET`/`CUSTOM_APPWIDGET`/`APP_PAIR`/`Unknown` を持つ (#235でwidgetが第一級計画対象)。`LayoutSnapshot.reservedWorkspaceRegions` (`ReservedWorkspaceRegion`) はitem外のplatform占有領域constraint。`ItemId`/`ProfileId`/`CategoryId` 等は `planning/Identity.kt` のopaque value class。本契約のadapterはこのdata classへ **optional field `intentPreferences` をadditive追加する** (spec「Planner接続」節、受入gate Q1の固定内容)。
- **intent addressable性とper-item mobilityの現行真実 (3rd review対応で再確認)**: `planning/PlanningPlacement.kt` の `determinePreservation` は次の優先順位 (先勝ち) で保持を決める: (1) workspace配置のauthoritative reservation重複 → `RESERVED_REGION` (ADR-0010、最優先)、(2) `locked` → `LOCKED`、(3) `availability != Availability.AVAILABLE` → `UNAVAILABLE_TARGET` (`Availability` は `AVAILABLE`/`DISABLED`/`QUIET`/`LOCKED_PRIVATE_SPACE`/`UNAVAILABLE`)、(4) Dock配置 → `DOCK`、(5) widget kind → `relocateWidgets` 時のみnull (移動可)、それ以外 `WIDGET`、(6) `APP_PAIR` kind または AppPairMember配置 → `APP_PAIR`、(7) `SHORTCUT_LEGACY` kind → `LEGACY_SHORTCUT`、(8) role == `Preserved` → `NON_TARGET`、(9) FolderMember配置 → `STRUCTURAL`、それ以外移動可。**`integration/FullTargetSetMaterializer.kt` はfolder memberを `ExistingRole.Preserved` に割当てるため (L42確認済み)、full target compositionでfolder memberがrun時に受け取る最終理由は `NON_TARGET` が先で `STRUCTURAL` は到達しない**。すなわちexportの `fixReason` はrun時 `PreserveReason` のコピーではなく **export時点で確定するunderlying fixed cause projection** である (spec「per-item mobility projection」節。1:1対応testは存在しない)。`ExistingRole.Movable` になるのは「top-level workspace配置の `APPLICATION`/`DEEP_SHORTCUT`/`FOLDER`」のみ。kindだけでは移動可能性を記述できず (dock app、folder member、reservation重複、利用不可itemはkind上addressableでも固定されている)、V1契約はkind matrix (addressable性) に加えてper-item mobility matrix (移動可能性) を投影する。`NON_TARGET`/`STRATEGY_PRESERVED` はrun composition/strategy依存でexport時点に確定しないためprojection対象外とし、plannerを正本のまま残す。
- `lawnchair/src/app/lawnchair/organizer/planning/OrganizationPlanner.kt` — 唯一の外部planning seam `plan(OrganizationInput): PlanningResult` (spec 182 AC-4)。前回baselineから未変更。
- `lawnchair/src/app/lawnchair/organizer/planning/LayoutStrategyRegistry.kt` — #182内部strategy catalog。#235によりwidget placement role/stream/band (`PlanningPlacement.kt` / `PlacementAllocator.kt`、`PlacementCode.WIDGET_UNIT`) が追加済み。`PlanningResult` は `organizationStrategy: StrategyId` と #228 の `unplaced` を持つ。`PreserveReason` enumは `planning/PlanningResult.kt`。
- `lawnchair/src/app/lawnchair/organizer/integration/OrganizationInputComposer.kt` / `CompositionModels.kt` — `InputProvenance` を所有。**policy input identityは現行6つ**: `rules`、`taxonomy`、`signals`、`targets`、第5 `layoutStrategySelection`、第6 `personalization` (#203。**常に存在し**、signal snapshot不在時は `PersonalizationSignalSnapshot.unavailable().policyIdentity()` のsentinel identity。U-4 2026-09-15 reviewで固定)。`PolicySourceKind` は現時点で7値 (`ORGANIZER_POLICY_BUNDLE`, `CATEGORY_OVERRIDE_SNAPSHOT`, `LAYOUT_STRATEGY_SELECTION`, `PLATFORM_CLASSIFICATION_EVIDENCE`, `MATERIALIZED_CLASSIFICATION_SIGNALS`, `MATERIALIZED_FULL_TARGET_SET`, `PERSONALIZATION_SIGNAL_SNAPSHOT`)。本契約のintent identityは **第7 policy input** (`PolicySourceKind.PERSONALIZED_INTENT`) として追加し、#203と同じ「optional source・常に存在するidentity」規律に従う。#228の `StaleCandidateSelection` 等、composition failure型も拡張済み。
- **#203実装済み (3rd review対応でre-anchor)**: `organizer/personalization/` に `PersonalizationSignalModels.kt`、`PersonalizationSignalSnapshot.kt`、`PersonalizationSignalSnapshotSource.kt`、`SystemUsageAggregator.kt`、`PersonalizationBuckets.kt` が存在する (`main` = `9ea2ba0e`)。`PersonalizationSignalSnapshot` はcompositionごとに再構築されるdynamic inputで、`schemaVersion` (`personalization-signals-v1`) と `contentDigest` (`sha256Canonical(canonicalRepresentation())`) でcontent-addressedに識別される。bucketは `ForegroundBucket` (0–4)、`RecencyClass` (0–3)、`ActiveDaysClass` (0–4)、`LauncherCountClass` (0–4)。**signalはdynamicであるためstructural `sourceContextDigest` の入力から除外し**、export時のsignal identityはsessionにsignal provenanceとして記録するのみ (spec「source context identity」節)。なお `organizer/personalization/` という名前の純粋packageが既に #203 で存在する — 本契約の新規moduleは同名packageへの追加 (同一purity guard対象) であり、既存 #203 fileと衝突しない名前で置く。
- `lawnchair/src/app/lawnchair/organizer/rules/` — `BuiltInOrganizerPolicyBundleSource.kt` (ADR-0007のpolicy authority)、`LayoutStrategySelectionStore.kt`、`CategoryOverrideStore.kt` (schema version + generation + digest のatomic store族の先例。`context.noBackupFilesDir` + `AtomicFile` を使用することを確認済み。export session storeのAndroid実装の様式参照先)。ADR-0007へは #228 のscope-composed target identity拡張が追記済み (本契約と矛盾なし)。
- **Android境界packageの先例 (re-review対応で確認)**: `organizer/integration/` には既存のAndroid実装adapter (`AndroidCandidatePorts.kt`、`AndroidClassificationSignalSnapshotSource.kt`) が置かれており、`organizer/rules/`・`organizer/integration/` はpurity guard (`tests/unit/app/lawnchair/organizer/planning/PurityGuardTest.kt`。対象pathは `organizer/planning` のみ) の対象外である。session storeのAndroid実装はこの慣例に従い `organizer/integration/` に置く。
- `lawnchair/src/app/lawnchair/organizer/application/preview/` — spec 194 plan preview (`inspectPlan`)。spec 195 confirmation UIは `organizer/ui/`。#271 durable status / #288 diagnostics export filename は本契約と直交するadditive変更。
- unit test置き場は `tests/unit/app/lawnchair/organizer/` (planning/integration/rules/ui 等)。既存guard: `planning/PurityGuardTest.kt`、property test基盤 `planning/PlannerGeneratedPropertyTest.kt`、widget系 `planning/WidgetPlacementStrategyTest.kt`、scope-composed系 `planning/ScopeComposedPlannerTest.kt` / `integration/ScopeComposedCompositionTest.kt`、#203系 `personalization/PersonalizationSignalSnapshotTest.kt` 等。実行commandはbuilding guideの `./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'`。
- `docs/product/requirements.md` は FR-016 まで (FR-017は未割当)。owner review (2026-09-13) の通りFR-017を確定扱いにできる。
- #205/#206 (外部agent exchange / managed AI) は OPEN。consumer不在でも本契約 (codec + validator + adapter + session store) は単体でtest可能な純粋module + 境界1つで成立する。
- 推測 (未確認): `InputProvenance` への第7 input追加と `OrganizationInput` への `intentPreferences` 追加が `CompositionModels.kt` / `OrganizationInputComposer.kt` のsealed構造 (`SourceUnavailable` 等の全網羅箇所) へ与える影響の詳細。実装child issueで確認する。

**再入場検証 (2026-09-13, 第2回)**: 前回snapshot baseline `f9afd8bfde121932c0c8ed965225d52a84d86ab4` から現baseline `c5274b5d0d1a4cd3a5cf55ef8dcadb84283a3cda` への差分を確認した。変更は #304 (API 36 window focus: CI evidence capture、UI lane系test)、#283 (strategy picker選択表示: strings/preferences UI)、#287 (grid変更時のunknown lock回収: `LockAuthoring.kt` のfolder child rank validation緩和とlocks系test)、#300/#308 (UI注入前提/Compose focus) であり、planning seam・`InputProvenance`・preview path・本契約の接続面への変更はない。#287の `LockAuthoring` 変更はlock review capacityの話であり、export契約のlock制約projectionの意味は不変。**owner review "Request changes" (2026-09-13, snapshot `65b9fc859d` 基準) を処理し**、P1×4 (process death耐性、missing ref fail-closed、export決定性の自己矛盾、redacted tierの弱いprivacy契約) / P2×2 (source context binding不在、APP_PAIR扱い未定義) を本planとspecの該当節へ反映した。コード確認により、旧specの「plannerはwidgetとapp pairを第一級計画対象」という記述が誤り (app pairは無条件preserved) であることを検証済み。

**再入場検証 (2026-09-15, 第3回)**: 前回snapshot baseline `c5274b5d0d1a4cd3a5cf55ef8dcadb84283a3cda` から現baseline `397d3fd95764878366e7c9e5ce41ab65e6f3f9ca` への差分を確認した。変更は #298 (Nova restore reload thread affinityのspec/plan)、#299 (Nova restore capture invalidの実装: `CaptureInvariant.kt` 新設、`RowManifestCodec`/`LayoutApplicationModule`/`OrganizationInputComposer`/`LauncherModel`/`LoaderTask` 変更)、#315 (bounded failure evidence capture: CI capture tool、`DiagnosticsLogger`) である。`OrganizationInputComposer.kt` の変更は `CaptureFailureObserver` のsignature拡張 (capture不変条件category追加、diagnostics目的) のみで、`InputProvenance`・`PolicySourceKind`・composition input構造は不変。`docs/engineering/organizer-diagnostics.md` の変更は #299 のcapture例外行への `invariant=` field追加のみで、digest/識別子のredaction分類は不変。planning seam (`OrganizationPlanner`、`determinePreservation`、`FullTargetSetMaterializer`、preview path) への変更はなく、本契約の接続面は不変である。**owner re-review "Request changes" (2026-09-13, snapshot `324e6182` 基準) を処理し**、P1×3 (digest定義の自己参照、cross-export追跡識別子、kind-onlyのmobility意味論) / P2×1 (ExportSessionStoreのpurity境界矛盾) を本planとspecへ反映した: digestをexport文書のfieldから外してsession-localな `sourceContextDigest` (internal canonical source projectionに対するdigest) に再定義、`ref` を乱数割当に変更してunlinkability契約化、per-item `mobility` projection (`determinePreservation` の優先順位と同型。上記の通り優先順位を実code確認済み) と `MOBILITY_CONTRADICTION` を導入、`ExportSessionStore` をinterface (純粋package) とAndroid実装 (`organizer/integration/`) に分離。

**再入場検証 (2026-09-15, 第4回) と3rd/4th review対応**: 前回snapshot baseline `397d3fd957` から現baseline `9ea2ba0eb4` への差分の主体は #203実装 (PR #321 merge。`organizer/personalization/` package、`PolicySourceKind.PERSONALIZATION_SIGNAL_SNAPSHOT`、`InputProvenance.personalization` 第6 input追加) である。**ChatGPT 3rd review "Request changes" (2026-09-15、snapshot `12f773ad` 基準、Issueコメント `5676896908`) を処理し**: (1) `sourceContextDigest` の入力から#203 signal snapshotを除外 (structural state限定) し、export時signal identityはsessionのsignal provenance記録のみ (import照合要件なし) に変更 — 正常な外部往復中のsignal再構築が `CONTEXT_STALE` を起こす問題を解消、(2) cross-export unlinkability主張を「stable identifierを提供しない」に縮小しsemantic linkage残存を脅威modelへ明記、ref割当はentropy契約に修正、(3) `fixReason` をunderlying fixed cause projectionとして再定義し、`FullTargetSetMaterializer.kt:42` でfolder memberが `ExistingRole.Preserved` になること (run時reasonは `NON_TARGET` が先) を実codeで確認の上、FIXED/MOVABLE判定とfixReason cause precedenceのtestを分離、(4) `FORBIDDEN_CONTENT`/`MOBILITY_CONTRADICTION` をschema外authority表現 / schema内semantic field矛盾として排他分離、(5) 受入gate Q1 (`OrganizationInput.intentPreferences` optional field)、Q3 (capability set 6項目)、Q4 (content limits + TTL 24h)、Q6 (per-ref usage projection) をspecへ固定。 **ChatGPT 4th review "Request changes" (2026-09-15、snapshot `296b7123` 基準、Issueコメント `5677179868`) を処理し**: (1) no-intent sentinel identityを `PolicyInputIdentity` 型不変条件 (`rules/PolicyModels.kt` の `require(versionOrGeneration.isNotBlank())` / `require(SHA_256.matches(sha256))` を実code確認済み) を満たす有効content-addressed値 (`versionOrGeneration="none"`、`sha256 = sha256Canonical("personalized-intent:none")`) として固定し、空文字digest表現を禁止、AC-6へsentinel testを追加、(2) `INVALID_ENUM`/`CAPABILITY_UNSUPPORTED` をschema定義enum外値 / schema有効機能のcapability宣言不対応として排他分離し、V1のexportは固定6capability set全体を常に宣言 (subset禁止)、V1での `CAPABILITY_UNSUPPORTED` 到達条件を明記、(3) `exportId` を `ref` と同一の乱数seam (seam名を `RandomIdAllocator` に一般化) ・同一entropy契約として固定 (counter/timestamp/決定的導出禁止) し、AC-14へ生成規則testを追加。 **ChatGPT PR review "Request changes" (2026-09-15、PR #322 @ `b4e37a2784`、P1×3/P2×1) を処理し**: (1) planner preference consumption (`OrganizationInput.intentPreferences` → `FullRunContext.preferences` → canonical executor のimportance ordering rankと`pageAffinity` preferred page bias。既存run (intentなし) はbyte-identicalのまま維持し、consumption testでpreference差が決定的にplacementへ反映されることとlock/preservationが弱まらないことを検証)、(2) `ExportSessionStore` seamのtyped expiry (`load` は期限切れのmatching recordも返し、validatorが `SESSION_EXPIRED`/`EXPORT_MISMATCH` を区別。`save` は成否Booleanを返す)、(3) Context export codec (新規 `ContextExportCodec` がContract 1のfield名/`schemaVersion`/optional omissionを一意に固定し、encode時に256 KiB上限を実測してfail-closed)、(4) binding test fixture修正 (MOVABLE/CONDITIONAL分岐を実到達させる)、(5) `pageAffinity` のexport grid page範囲検証 (`0 <= pageAffinity < grid.pageCount`、範囲外は `INVALID_ENUM`)。 **ChatGPT PR 2nd review "Request changes" (2026-09-15、PR #322 @ `c9a1793d15`、P1×1/P2×1) を処理し**: (1) 残りcapabilityのplanner消費を実効化 — `PRESERVE`/`GLOBAL_PREFERENCE` (globalMinimizeMovement + item preserve → ordering先頭rank)、`GROUPING` (desiredGroup members のcohesion co-ordering)、`REGION_AFFINITY` (TOP first band rank) をcanonical executorのpreference key chainに追加 (priority: preserve > group > importance > region、いずれもpreference未指定時は定数rankで既存orderingと等価)。`IntentPreferenceConsumptionTest` にREGION_AFFINITY/GROUPING/PRESERVEの決定的反映testと決定的再現testを追加、(2) `PlannerMobilityBindingTest` のreservation fixture修正 (`reservedOverlap` をrow 0に残し、`FIXED/RESERVED_REGION` ↔ `PreserveReason.RESERVED_REGION` のbindingを明示assertする `reservedRegionCauseBindsToThePlannerReasonExplicitly` を追加)。 **ChatGPT PR 3rd review "Request changes" (2026-09-15、PR #322 @ `2eab59ceb9`、P1×2) を処理し**: (1) `PRESERVE`/`GLOBAL_PREFERENCE` の消費を契約どおりのmovement-minimization意味論へ再実装 — global `minimizeMovement`、または任意のmovable itemへの `preserve` 指定時にsingleton orderingをcaptured visual order (page順・row-major cell順) へ切替え、allocationが既存layoutを可能な限り再現する (preserve対象がcaptured cellから遠ざからない。移動量が増えない)。旧「preserve対象を先頭cellへ送る」実装とそのtestは撤去、契約整合test (`preserveAndMinimizeMovementPreferencesReduceDisplacement`) はplain planが両itemをswapするfixtureで「preferenceによって移動量が増えない」ことを固定、(2) `GROUPING` のgroup identityを `{self} ∪ desiredGroup` の正規化keyへ変更 (相互group `A<->C` が同一keyになり複数groupでもcohesive)。`groupSemantic` 単独形の消費を実装 — `groupSemantic.category` がtaxonomy allowed内ならsingletonのsortCategoryとfolder formation categoryのoverlayとして効く、free-textのみの場合はsemantic tag groupとしてcohesion orderingに効く。2group fixtureと `groupSemantic` 含むconsumption testを追加、(3) REGION_AFFINITYのTOP/MIDDLE/BOTTOM band ordering testを追加。 **ChatGPT PR 4th review "Request changes" (2026-09-15、PR #322 @ `2aad982502`、P1×3/P2×1) を処理し**: (1) per-item `preserve` をallocation hint (captured cell) として `Allocator.allocateWithCellHint` / `allocateOnPageOnlyWithCellHint` へ接続 — hint cellが空きなら正確に使用し、そうでなければcanonical first fitへ劣化するため **preserve対象のdisplacementが悪化することはない**。global `minimizeMovement` は独立にsingleton orderingのcaptured visual order切替として機能 (sparse layout・preserve単独・global単独・両方のcontract testを追加)、(2) `GROUPING` identityをdesiredGroup relation graphの **connected components** へ正規化 (`intentComponentRanks`、union-find。片方向宣言でも参照member側がcohere、複数groupはdisjoint rank)。片方向group＋別group fixtureを追加、(3) preference消費を全executorへ接続 — canonical executor (canonical ordering + cell/band hint allocation)、page-local lift-then-place (preference layered ordering `preferenceLayeredOrder` + page-local hint allocation)、global compact (layered ordering + hint)、widget stream (pageAffinity → stream page regroup、regionAffinity → band window soft-first with strategy fallback)。strategy横断matrix test (`IntentPreferenceStrategyMatrixTest`、8 registered strategies) でpreferenceが全familyでplanへ決定的に反映されることとdeterminismを固定、(4) REGION_AFFINITY band testを実target band検証へ変更 (TOP/MIDDLE/BOTTOM各band行への配置assert)。 **ChatGPT PR 5th review "Request changes" (2026-09-15、PR #322 @ `c8c868f80b`、P1×1) を処理し**: widget streamのpreference接続を#235のpage-local widget policy権限の内側へ戻した — (1) `pageAffinity` によるstream page regroupを撤去 (現行の `PageLocalBand`/`PageLocalTopAnchored` は両者ともpage-local = "widgets stay on their captured page"。cross-page widget affinityは、それを明示的に許す将来policyでのみ消費)、(2) `regionAffinity` band hintはstrategy宣言window (`PageLocalBand` のcaptured band) とのintersectionがある場合のみsoft-first適用し、`PageLocalTopAnchored` (window宣言なし) はintentでre-anchorされない、(3) `WidgetIntentAuthorityTest` を新設 — V2両strategyで「別pageへのpageAffinityでもwidgetはcaptured page残留」「TopAnchoredはBOTTOM hintでre-anchorされない」「Band交集内のhintはcaptured band内で効く」を固定。 **ChatGPT 5th review "Request changes" (2026-09-15、snapshot `2501a1fb` 基準、Issueコメント `5677330910`) を処理し**: (1) `CAPABILITY_UNSUPPORTED` をV1でreserved/unreachableに固定 (V1固定full-set契約との整合。V1必須failure test対象から除外、schema外機能は `SCHEMA_MISMATCH`。AC-5更新。subset advertisementを許す将来schema versionで有効化)、(2) plan AC-4のtest wordingをspecの排他分類へ統一 (`FORBIDDEN_CONTENT` はauthority表現に限定。locked refへのsemantic移動希望はAC-13/`MOBILITY_CONTRADICTION` 側のみ)、(3) `RandomIdAllocator` 旧名称 (`RefAllocator`) の残存箇所を統一。

## Design

### Modules and interfaces

新規package `lawnchair/src/app/lawnchair/organizer/personalization/` (DESIGN.md §9の論理構成に従う追加。純粋Kotlin、Android型・DB・network・I/Oなし)。**purity境界は例外なし** であり、Android/file-backedな実装は置かない (re-review P2対応)。

```text
organizer/personalization/                    # 純粋package (purity guard対象、例外なし。#203の既存fileと同居)
├── ContextExportModels.kt    # PersonalizationContextExportV1 typed model + mobility/session/usage projection model (pure)
├── ContextExportBuilder.kt   # canonical inputs + RandomIdAllocator -> export (pure, tier-aware, mobility投影, usageSignals投影)
├── RandomIdAllocator.kt      # 新鮮な乱数identifier (exportIdとitems[].refの両方) を供給するseam interface (乱数源を引数注入する純粋境界)
├── SourceContextIdentity.kt  # internal canonical STRUCTURAL source projection + sourceContextDigest計算 (pure。signalを含まない)
├── IntentModels.kt           # PersonalizedIntentV1 typed model (pure)
├── IntentCodec.kt            # JSON <-> typed model (closed schema, limits)
├── IntentValidator.kt        # strict validation -> typed failure (pure; coverage/session/stale/mobility検証含む)
├── IntentIdentity.kt         # accepted intentのcontent digest / schema identity (pure)
├── ExportSessionStore.kt     # export session (durable・期限付き) のseam interface のみ
└── IntentPlannerAdapter.kt   # validated intent -> OrganizationInput.intentPreferences (pure)

organizer/integration/
└── AndroidExportSessionStore.kt  # ExportSessionStoreのAndroid/file-backed実装 (purity対象外)
```

- **Seam原則**: 呼び出し側もtestも同じpublic seam (builder / codec+validator / adapter / session store) を使う。internal実装 (乱数ref生成、digest計算、session書込み) を直接検証しない。
- **ContextExportBuilder** の入力は既存canonical入力 (`LayoutSnapshot` 相当のcaptured data、解決済み分類、lock/availability/placement状態、#203 `PersonalizationSignalSnapshot`) と注入された **`RandomIdAllocator`** (新鮮な乱数identifier源。`exportId` と `items[].ref` の **両方** を同一seam・同一entropy要件で供給する。4th review P2対応。testでは決定的sourceを注入し、production実体はintegration境界で暗号論的強度の乱数を提供する) である。tierに応じるfield除外 (自由文class一括制御) はbuilder内の単一点で行う。`exportId`/`ref` はcanonical入力から決定的に導出せず生成ごとの乱数とし (counter/timestamp/決定的導出禁止。entropy契約。spec「生成規則」節)、ref↔`ItemId` 対応はsessionのみが保持する。**export文書にstate fingerprint (`sourceContextDigest`) を含めない** — digestは `SourceContextIdentity` がexport本文とは独立に計算する (内部`ItemId` key、envelope/ref/tier/capability非依存、**#203 signal snapshotを入力に含まない**)。`usageSignals` はbuilderがsignal snapshotをexport-scoped `ref` に紐付けたbucket projectionとして生成する (package名・profile identityはbuilder内部でのみ解決しexport文書へ出さない)。sessionにはsignal provenance (`PersonalizationSignalSnapshot.schemaVersion` + `contentDigest` またはabsent) をbuilderが渡す。
- **ExportSessionStore (interface)** は純粋packageに置くseamのみを定義する (session model型を含む。session modelは `exportId`、ref↔`ItemId` map、tier、structural `sourceContextDigest`、signal provenance、生成・失効時刻を保持)。**実装 `AndroidExportSessionStore`** は `organizer/integration/` に置き、export生成時にsessionをapp-private・backup対象外でdurableに保存する唯一の境界module。実装様式は `rules/CategoryOverrideStore.kt` 族 (`noBackupFilesDir` + `AtomicFile` + schema versionのatomic書込み。確認済み) に従い、Launcher favorites DBとは独立である。V1はsingle-active-session (新規export生成が既存sessionを無効化。TTL 24時間) であり、seamの `load` は **期限切れのmatching recordも返す** (validatorが `SESSION_EXPIRED` と未知sessionの `EXPORT_MISMATCH` をtypedに区別する。process death後にtyped失効が復元できること)。`save` はdurable書込みの成否をBooleanで返し、失敗時にはexport発行をfail-closedで抑止できる。process death耐性はこの実装が担う。purity guardは純粋package全体を例外なしでcoverし、実装は純粋packageの外にある。
- **IntentValidator** は spec の失敗class (`SCHEMA_MISMATCH`/`EXPORT_MISMATCH`/`SESSION_EXPIRED`/`CONTEXT_STALE`/`OVERSIZE`/`UNKNOWN_REF`/`DUPLICATE_REF`/`INCOMPLETE_COVERAGE`/`INVALID_ENUM`/`FORBIDDEN_CONTENT`/`MOBILITY_CONTRADICTION`/`CAPABILITY_UNSUPPORTED`) をtyped sealed resultで返す。fail-closed、zero-write。coverage不変条件 (`itemIntents` refs ∪ `unresolvedRefs` == 全exported refs、互いに素)、per-ref mobility検証 (`FIXED`/`CONDITIONAL` refへの意味矛盾を `MOBILITY_CONTRADICTION` でreject)、source binding (structural `sourceContextDigest` 再計算照合。session経由で取得。**signal照合は行わない**) はvalidatorの検証項目である。`FORBIDDEN_CONTENT` (schema外authority表現) と `MOBILITY_CONTRADICTION` (schema内semantic fieldのmobility矛盾) は排他分離である。
- **IntentPlannerAdapter** はvalidated intentを `OrganizationInput` の新optional field **`intentPreferences`** (受入gate Q1で固定したpure typed projection) へ変換する。plannerはこれをordering/preference bias (importance順序、group cohesion、page/region親和、minimize-movement) としてのみ消費し、`determinePreservation` 等の保持判断・constraints・run mode・`TargetSet` 意味論を変更しない。widget親和の実効化は `widgetPolicy` 持ちstrategyを選択したrunでのみplanner側で行われる。
- **Provenance統合**: accept済みintentは `PolicySourceKind.PERSONALIZED_INTENT` として `InputProvenance` へ **第7 policy input** (`personalizedIntent`) を追加する (第5 `layoutStrategySelection`、第6 `personalization` の次。`LayoutStrategySelectionSnapshot` と同じgeneration/digest契約族)。**identity fieldは常に存在し**、intent未使用runではsentinel identityを持つ (#203 `personalization` inputと同じ「optional source・常に存在するidentity」規律。既存runへの影響なし)。**sentinel identityは `PolicyInputIdentity` の型不変条件 (`versionOrGeneration` 非空、`sha256` 64hex。`rules/PolicyModels.kt` で確認済み) を満たす有効content-addressed値**: `versionOrGeneration = "none"`、`sha256 = sha256Canonical("personalized-intent:none")` (4th review P1対応。#203の `unavailable().policyIdentity()` と同じ様式)。文字どおりの空文字digestは型不変条件に違反するため禁止。
- **Diagnostics**: `PlanningResult` echoへ intent identity (digest) を追加する。個人情報 (label、package、座標) はdiagnosticsへ出さない (organizer-diagnostics.mdの規則拡張を同PRで)。signal provenanceはdiagnosticsに出力しない (session-local)。

### Data flow

```text
canonical inputs --(ContextExportBuilder + RandomIdAllocator, tier)--> PersonalizationContextExportV1 (state fingerprintを含むusageSignals投影付き。signal identityは文書に現れない) + export session (durable作成; structural sourceContextDigestはSourceContextIdentityが計算し、signal provenanceとともにsessionのみへ)
    --> [AI/agent: #205/#206またはtest double] --> PersonalizedIntentV1 (exportIdのみecho)
    --(IntentCodec + IntentValidator; session照会・structural sourceContextDigest再計算照合 (signal非照合)・per-ref mobility検証)--> ValidatedPersonalizedIntent (intent identity digest付き) | typed failure
    --(IntentPlannerAdapter)--> OrganizationInput.intentPreferences
    --(OrganizationPlanner.plan: 既存唯一seam)--> PlanningResult (intent identity echo)
    --(spec 194/195/13: 既存preview/confirm/apply)--> 適用
```

- network・providerとの入出力は本moduleの外 (#205/#206)。codecはtransport非依存のcanonical JSON表現のみ定義する。
- regenerationは新規exportId/intent identity。既存previewの流用はspecどおり無効化する。同一canonical状態の再生成はsessionに同じstructural `sourceContextDigest` を記録し、`exportId` と `ref` 集合は新規である (export文書自体のbyte一致は要求せず・観測もしない)。signal provenanceは生成時点のsnapshotが同一なら同一記録値になるが、照合要件ではない。

### Alternatives rejected

- **AIへ `OrganizationInput` を直接渡す**: lock/bounds安全性、privacy (raw ID/package)、schema移行性で不適切 (Issue本文が明示)。採用しない。
- **intentを `RuleSemantics` へ直書き**: intentはrun-scopedのdynamic inputでありimmutable bundle policyではない。ADR-0007のauthority model (bundle ≠ dynamic state) に反する。
- **#182 catalogへの「AI strategy」追加**: intentはsemantic preferenceであり組み込みstrategyとは別物。catalogはcuratedでなければならない (spec 182 Non-goals)。
- **`ref` の決定的割当 (canonical入力からの導出)**: 同一canonical状態から同一 `ref` 集合が得られるため、外部providerが複数export間で同一itemを照合・追跡できる (re-review P1)。乱数性の有無に関わらず安定なexported仮名はunlinkabilityを損なうため、生成ごとの乱数割当を採用した。
- **`sourceContextDigest` をexport文書のfieldに含める**: digest定義が自己参照になり (re-review P1)、かつcanonical状態のfingerprintを外部に露出させて同一状態の再export照合を可能にする。session-localに分離した (internal canonical structural projection)。
- **`sourceContextDigest` に#203 signal snapshotを含める (stale digest)**: signal snapshotはcompositionごとに再構築されるdynamic inputであり (#203、spec 203「usage変化はplanをstaleにしない」)、digestに含めると正常な外部AI往復中のsignal再取得だけで `CONTEXT_STALE` が発生する (3rd review P1)。structural状態のみをdigest入力とし、signal identityはsessionにprovenance記録するのみにした。
- **plannerがexport時のsignal実体を再利用する**: stale signalのreplayはplannerの現行入力規律 (composition時点のsnapshot) と矛盾し、signal provenance照合 (P1-1の解消と矛盾する要求) を生む。plannerは常に現行signal snapshotを使い、export時signal identityは記録のみにする。
- **`fixReason` をrun時 `PreserveReason` の1:1コピーとする**: full target compositionではfolder memberのrun時reasonが `NON_TARGET` (role Preserved) になることを `FullTargetSetMaterializer.kt:42` で確認済みであり、1:1主張は成立しない (3rd review P2)。export時点で確定するunderlying cause projectionに再定義した。

## Change set

| Area | Intended change | Why here |
|---|---|---|
| `organizer/personalization/` (追加) | Context/Intent model、builder、RandomIdAllocator seam、SourceContextIdentity (structural)、codec、validator、identity、adapter、ExportSessionStore **interface** | 契約本体。純粋package (例外なしのpurity対象。#203既存fileと同居) |
| `organizer/integration/AndroidExportSessionStore.kt` (新規) | ExportSessionStoreのAndroid/file-backed実装 (`noBackupFilesDir` + `AtomicFile` + schema version) | 唯一の状態境界。purity packageの外のAndroid境界 (`integration/` の既存 `Android*` adapter慣例) |
| `organizer/integration/CompositionModels.kt` | `PolicySourceKind.PERSONALIZED_INTENT` 追加、`InputProvenance.personalizedIntent` 第7 input追加 (sentinel default) | determinism/provenance保証 |
| `organizer/integration/OrganizationInputComposer.kt` | accepted intentの読み取りとstable cutへの組入れ (intent利用runのみ) | spec 183系のconsistent-cut規律を維持 |
| `organizer/planning/OrganizationInput.kt` | optional field `intentPreferences` 追加 (additive、既存runはnull) | 受入gate Q1の投影先 |
| `organizer/planning/PlanningResult.kt` | intent identity echo (optional) | diagnostics/previewでの追跡 |
| `docs/engineering/organizer-diagnostics.md` | intent identity/digest のallowed field追加 (session内容・自由文は引き続き不許可) | privacy規則の正本更新 |
| tests (下記Verification) | contract/property/security tests | AC対応 |
| `specs/204-.../spec.md`, `plan.md` | status更新、Open questions解消の記録 | 正本管理 |

実装はchild issueへ分割する (下記Execution order)。本Issueの実装縦切りは「純粋codec/validator + session store (interface + Android実装) + provenance統合」までとし、#205/#206接続を含まない (Issue本文のImplementation orderどおり)。ExportSessionStoreはLauncher favorites DBと独立なapp-private storageであり、`risk: layout-data` 判定の根拠 (home layout正本を触らない) を実装PRの説明に明記する。

## Migration and recovery

- 新規純粋module追加 + export session store (app-private・backup対象外・Launcher favorites DBと独立) のみ。Launcher DB schema変更なし → Launcher DB migrationは不要。
- session store自体はschema version付きの新規storageであり、version不一致・破損sessionはfail-closedで無効扱いする (session喪失の影響は「再exportが必要」のみで、既存layout・planning入力への影響はない)。
- intent未使用runは全て従来どおり (互換性: 既存testが無修正で通ることが回帰基準)。
- rollback: 実装のrevertで完了。session storeはrevertで不要になる一時recordのみを保持する。validation失敗時のzero-writeにより、失敗からの復旧も「何も起きていない」状態である。
- backup/restore: session storeはbackup対象外とし、backup/restore互換に影響しない (restore後は既存session無効 → 再export)。

## Verification

| Acceptance criterion | Automated/manual evidence | Command or environment |
|---|---|---|
| AC-1 (FR-017) | requirements.md更新 (受入PR) | review |
| AC-2 (versioned schema) | model contract test: schemaVersion固定、unknown version拒否 | unit test (`./gradlew testLawnWithQuickstepGithubDebugUnitTest --tests 'app.lawnchair.organizer.*'`) |
| AC-3 (provider非依存) | 同一intentをtest doubleと将来providerが同じseamで取り込める契約test | unit test |
| AC-4 (planner authority) | `FORBIDDEN_CONTENT` 検証test (**座標/span/reservation/DB mutation/script等のschema外authority表現を含むintentのreject。locked refへのsemantic移動希望はAC-13/`MOBILITY_CONTRADICTION` 側のみ**。spec 5th review対応) + purity guard拡張 | unit test |
| AC-5 (fail-closed zero-write) | 各typed failureのtable-driven test。malformed/oversize/duplicate/unknown/**coverage違反 (`INCOMPLETE_COVERAGE`)/session失効 (`SESSION_EXPIRED`)/source不一致 (`CONTEXT_STALE`)/enum外値 (`INVALID_ENUM`)**。`INVALID_ENUM` と `CAPABILITY_UNSUPPORTED` の排他分離test。**`CAPABILITY_UNSUPPORTED` はV1でreserved/unreachable (V1必須failure test対象外。schema外機能は `SCHEMA_MISMATCH` で検証)** | unit test |
| AC-6 (intent identity/determinism) | 同一accepted intent + 同一inputs → byte-equal plan。regenerate → 別identity。preview無効化。**no-intent sentinel identityが `PolicyInputIdentity` の型不変条件 (非空version、64hex digest) を満たすことのtest** | unit + property test |
| AC-7 (prompt injection) | injection label fixtureを含むexport/intentのsecurity test群 | unit test |
| AC-8 (privacy tier) | tier別export field集合の契約test (package/profile/raw usage不混入の全field走査 + **自由文classのtier別走査: `EXTERNAL_REDACTED` にlabel/folder title/自由文・surrogate fieldが一切現れないこと**) | unit test |
| AC-9 (preview path再利用) | planが`PlanningResult`→`inspectPlan`以外の経路を取らないことのコードレビュー + 既存preview testの無修正通過 | review + unit test |
| AC-10 (test計画) | 本表 | 本plan |
| AC-11 (process death耐性) | export生成 → **store instanceを破棄・再生成してprocess deathを模擬** → 失効前intent取り込み成功。失効後は `SESSION_EXPIRED`。不明sessionは `EXPORT_MISMATCH` | unit test (ExportSessionStore seam経由) |
| AC-12 (source context binding) | export後に **構造的** 入力canonical状態を変化させ、session-localなstructural `sourceContextDigest` 再計算照合が `CONTEXT_STALE` でrejectすること。**#203 signal snapshotのみを変化させた場合はimport成功 (signal変化はstale判定対象外)**。同一状態での再生成が同一structural `sourceContextDigest` をsessionに記録し、signal provenanceが記録され、`exportId`/`ref` が新規になることの分離test。export文書 (envelope) にdigest・signal identityが現れないことの走査 | unit + property test |
| AC-13 (kind + mobility projection matrix) | kind matrix: `APP_OR_SHORTCUT`/`FOLDER`/`WIDGET` のみ `items` に現れること。`APP_PAIR`/`SHORTCUT_LEGACY` は `preservedConstraints` 集計のみ。`Unknown` 除外。これらへの `ref` 参照が `UNKNOWN_REF` になること。mobility matrix: reservation重複/lock/利用不可/dock/folder member/app pair member配置が対応する `fixReason` 付き `FIXED` に投影され、widgetが `CONDITIONAL` になること。**FIXED/MOVABLE/CONDITIONAL判定がplannerの移動可否と一致するproperty test** と、**`fixReason` の固定原因precedenceの個別test** を分離して実施 (`fixReason` はrun時 `PreserveReason` のコピーではなくunderlying cause。1:1対応testは存在しない)。`FIXED` refへの移動系fieldとwidget refへの `desiredGroup` が `MOBILITY_CONTRADICTION` でrejectされること | unit test (fixture: 各固定原因を持つcaptured item) |
| AC-14 (cross-export identifier unlinkability) | 同一canonical状態から別々に生成した2つのexportが `exportId`・`ref` 集合を共有しないこと (collision確率negligibleのentropy契約のtest)。**`exportId` も `ref` と同一の乱数seam (`RandomIdAllocator`)・entropy契約で生成されることの生成規則test (counter/timestamp/決定的導出でないこと)**。export field全体の走査で `sourceContextDigest` 相当の状態fingerprint・signal identityが現れないこと。ref↔`ItemId` mapがsession storeの外に現れないこと。**保証範囲の明記: semantic contentからの確率的linkageは対象外** | unit + property test |

追加観点:
- **Property test**: validatorは全fail classでtotal functionである (入力任意byte列に対しrejectまたはvalidのいずれか、例外・hangなし)。`SourceContextIdentity` は同一canonical **structural** 状態 → 同一digest、構造的状態変化 → digest変化、**signal snapshotのみの変化 → digest不変** (serialization安定性を含む)。builderは同一inputs + 同一RandomIdAllocator → 同一export bytes (testability上の決定性であり契約上のunlinkability要求と直交する)。生成refはexport内で一意かつ決定的導出でないこと (乱数源に依存しない上限回数の重複検出)。coverage不変条件のpartition性 (集合演算のproperty test)。mobility projectionの **FIXED/MOVABLE/CONDITIONAL判定** は `determinePreservation` (run時role入力を除いたexport時点projectable入力) と同一入力で同一移動可否判定になること (plannerとの整合property test)。`fixReason` のcause precedenceはexport側契約として独立に検証する (plannerとの1:1対応testは存在しない)。既存のproperty基盤 (`tests/unit/app/lawnchair/organizer/planning/PlannerGeneratedPropertyTest.kt` 族) に整合させる。
- **Planner binding test (audit P2-1対応)**: `PlannerMobilityBindingTest` がexport側mobility判定 (FIXED/MOVABLE/CONDITIONAL) とplanner `determinePreservation` の移動可否を全projection caseで束縛する (全保持fixture case + role Preserved (full-target NON_TARGET) case + builder出力との一致)。planner将来の保持semantics変化時に乖離を検出する。
- **Purity test**: `organizer/personalization/` のAndroid依存・I/O依存ゼロ (`PurityGuardTest.kt` 族のwalk対象personalization版を新設。**package全体を例外なしでcover** し、session store実装は純粋packageに置かないことで例外を構造的に不要にする)。ExportSessionStoreはseam interface経由でのみ触り、`AndroidExportSessionStore` 実装はintegration側testで検証する。
- **Widget projection test**: widget itemを含むexport/intentでspan不変が保たれること (`FORBIDDEN_CONTENT` のspan指定reject、adapter投影後も `PlacementCode.WIDGET_UNIT` 系のstrategy宣言semanticsを弱めない) を #235 の `WidgetPlacementStrategyTest` と同水準のfixtureで検証する。widget親和intentが `widgetPolicy` なしstrategy下で移動を強制しないことの確認を含む。
- **高リスク分類**: 本契約自体はpure追加だが、provenance/planner統合PRは `risk: layout-data` 払いとし、high-risk gate (CI `final-status` + `docs/assessment/pr-<n>-*.md`) を満たす。

## Documentation updates

- [ ] spec status/history (受入時)
- [ ] `CONTEXT.md`: 「Personalization Context Export」「Personalized Intent」「export-scoped ID」追加 (受入時)
- [ ] `DESIGN.md`: §4へpersonalization module行追加、§11 gate表へ本契約の正本を行追加 (受入時)
- [ ] `docs/product/requirements.md`: FR-017追加、FR-014との境界備考、D-011言及 (受入PR)
- [ ] `docs/engineering/organizer-diagnostics.md`: intent identity許容 (最初の実装PR)
- [ ] ADR: なし (契約自体はspecで十分。将来provider接続 (#205) でprivacy/threat model判断時に検討)

## Execution checklist / implementation order

1. (前提) spec受入 — **完了 (2026-09-15、6th ChatGPT review Approve)**。受入gate (Q1 planner投影、Q3 capability set、Q4 content limits + session TTL) とQ2 (FR-017)、Q5 (自由文/surrogate方針)、Q6 (usage signal projection) は **すべて解決済み** (spec「Open questions」節)。
2. child A: `personalization/` 純粋package — models + builder (RandomIdAllocator注入、usageSignals投影、signal provenance) + SourceContextIdentity (structural) + codec + validator + identity、purity guard、全contract test (AC-2,3,4,5,7,8,12,13,14)。**実装済み (本PR)** — 既存 `PurityGuardTest` が `organizer/personalization/` 全体を例外なしでcoverし、session store実装は純粋packageの外 (`integration/`) に置くことで例外なし構造を維持した。
3. child B: provenance統合 (`PERSONALIZED_INTENT` 第7 input、sentinel default。**sentinelは `PolicyInputIdentity` 型不変条件を満たす有効値**、AC-6のsentinel test) + `OrganizationInput.intentPreferences` 追加 + adapter + `PlanningResult.personalizedIntentDigest` echo + composer stable cut 組入れ (AC-6,9)。`risk: layout-data` + high-risk gate。**本PRでの実装範囲**: 第7input (sentinel default)、`intentPreferences` field、adapter、echo は実装済み。composerのaccepted intent読取りは **#205/#206のproducer接続まで実施しない** — accepted intentを生産する経路が本契約の実装時点では存在せず (spec 204はprovider実装を含まない)、composerのpublic seamに仮想的なparameterを追加することは「production用とtest用の実体が必要になるまで仮想的なinterfaceを増やさない」設計規約に反するため、実装child PRで意図的に見送った (AGENTS.md)。composer統合は #205 (外部agent exchange) の最初の実装PRで、このseamに接続する。
4. child C: `usageSignals` projectionの実装test拡充はchild Aに含まれる (#203は実装済みのため追加契約作業は不要)。
5. #205/#206: provider接続 (本Issueの範囲外)。

ExportSessionStoreはchild Aに含める (AC-11): seam interfaceは純粋package、durable実装 `AndroidExportSessionStore` は `organizer/integration/` に置き、既存store族の様式 (`noBackupFilesDir` + `AtomicFile` + schema version) に従いbackup対象外設定を含める。production RandomIdAllocatorの乱数源 (暗号論的強度) もchild Aで確定する。

## Dependencies / blockers

- 受入gate (Q1/Q3/Q4) とQ6はspecで解決済み。child A/Bのblockerはspecのacceptanceのみである (残る確定事項は実装詳細)。
- #203 は実装済み (`main` = `9ea2ba0e`)。`usageSignals` projectionとsignal provenanceはchild Aで実装する。
- #205/#206 は本契約のconsumerとして本Issue実装後 (Issue本文: 「Provider integrationより先に本contractをacceptedにする」)。

## Risks

- intent→planner投影が既存determinism/idempotence (INV-7/8) を壊す恐れ → child Bで既存harness/property suiteの無修正通過を必須化し、intentあり/なしのcross-run testを追加する。
- widget/span投影の漏れ: intentのpageAffinity等が #235 のwidget stream/band・span不変semanticsを迂回する形で実装される恐れ → `FORBIDDEN_CONTENT` のspan指定rejectと投影後planのwidget意味論testで固定する (Verification参照)。
- privacy tier判定の漏れ (将来field追加時に意図せず外部送信) → user作成自由文をclosed classとして宣言制御し、新field追加時にclass宣言を強制するtype設計。tier別field集合走査test (AC-8) で固定。
- **mobility projectionとplanner保持semanticsの乖離**: planner側の `determinePreservation` が将来変化した場合、export側のmobility述語が旧実装のままになると誤った `MOVABLE` 投影 (または過剰な `FIXED` 投影) が起きる → mobility投影を `determinePreservation` と同一入力で同一判定になるproperty testでbindし (Verification参照)、乖離を既存testが検出する構造にする。なおplannerが最終正本である契約 (intentはpreference) により、乖離時の最悪caseはvalidatorの誤reject/誤許可であり、layout破壊にはならない。
- **export session storeの漏えい**: ref↔内部`ItemId` mapは内部identityを含むため、app-private設定・backup対象外・diagnostics非出力をchild Aのtestで固定する (漏えい経路: backup、log、export)。session本文 (自由文) を保存しない設計の契約test。
- **RandomIdAllocatorの乱数品質**: production乱数源が弱い (予測可能) と `exportId`/`ref` のunlinkability・逆算耐性が損なわれる → child Aで暗号論的強度の乱数源を確定し、integration境界で注入する設計により純粋moduleのtestabilityと分離する。`exportId` と `ref` が同一seam・同一entropy要件であることもchild Aのtestで固定する (4th review P2)。
- **`CONTEXT_STALE` の誤検知**: `sourceContextDigest` 再計算が非決定的 (順序非安定等) だと、状態不変でもimportが失敗する → `SourceContextIdentity` 決定性property test (同一状態→同一digest) と、状態不変でのimport成功testで固定する。
- export/intent本文の意図しない永続化 → session store以外の永続化経路が存在しないことをpurity/persistence testで固定。
- session失効と外部AI応答待ちのUX不整合 (長文生成中に失効) → `SESSION_EXPIRED` の再export導線は #205 のUI課題として記録 (本契約はfail-closedのみ定義)。

## Explicitly unverified areas

- internal canonical **structural** source projectionの具体的serialization形式 (`sourceContextDigest` の対象。specは契約条件 — 内部`ItemId` key、envelope/ref/tier/signal非依存、process跨ぎ安定 — を固定し、具体形式はchild Aで確定してproperty testで検証する)。
- RandomIdAllocatorのproduction乱数源の具体実装 (暗号論的強度の選定) とidentifier形式 (長さ・alphabet)。child Aで確定。契約条件 (生成ごとの新鮮さ・export内一意性・entropy要件・ItemId逆算不可・exportId/ref同一seam) はtestで検証する。
- `AndroidExportSessionStore` の具体storage実装 (既存store族のどの様式を採るかの最終選定、backup除外の適用方法の詳細)。置き場所 (`organizer/integration/`) と様式参照先 (`rules/CategoryOverrideStore.kt` 族) は本planで確定済み。child Aで実装する。
- `usageSignals` の内部 `ItemId` ↔ `PersonalizationEntryKey` 対応解決の具体実装 (builder内部。child Aで確定)。
- `InputProvenance` への第7 input追加と `OrganizationInput.intentPreferences` 追加のsealed構造・全網羅箇所への影響の詳細 (child Bで確認)。
- #205/#206 UI・transport経路での実際の取り込み、session失効時の再export導線UX (範囲外)。
